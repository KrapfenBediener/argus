---
phase: 04.9-governance-oberflaeche
plan: 2
type: execute
wave: 2
depends_on: ["04.9-01"]
files_modified:
  - docs/leitung-<suffix>.html   # konkreter Name entsteht ERST zur Ausführungszeit (D-06) — steht bewusst nicht im Plan
  - .gitignore
  - LEITUNG-URL.md               # untracked (gitignored) — einzige Fundstelle des konkreten Dateinamens
autonomous: true
requirements: [D-01, D-02, D-03, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "Die Leitungs-Seite ist unter einem nicht erratbaren Dateinamen (Muster docs/leitung-<suffix>.html, Suffix = 10 Hex-Zeichen, erst zur Ausführungszeit erzeugt) erreichbar, trägt noindex/nofollow und wird von keiner getrackten Datei des Repos referenziert (D-06)"
    - "Der konkrete Dateiname steht AUSSCHLIESSLICH in der untracked, gitignorierten Datei LEITUNG-URL.md — in keiner committeten Datei und keinem Commit-Text (D-06)"
    - "Login nur mit MasterToken: Code ohne is_master=true wird abgewiesen; Session liegt in sessionStorage und ist nach Abmelden/Tab-Schließen weg (D-07)"
    - "Nach Login landet die Leitung DIREKT im Bereich Zugänge — keine Start-/Übersichtsseite (D-10)"
    - "Die Seitennavigation zeigt eine sichtbar gesperrte, ausgegraute Sektion 'Phase 7 · nach DSB' (Identitäten, Audit-Log) — nicht klickbar (D-09)"
    - "Codes lassen sich je Präsidium erstellen (dauerhaft / 24 h / einmalig) und sperren/entsperren (revoked-Flag); MasterToken-Zeilen sind nicht sperrbar (D-11)"
    - "Der Aufruf eines Einsatzprotokolls zeigt einen Protokollierungs-Hinweis und erscheint anschließend selbst im Protokoll-Bereich (D-01)"
    - "Bei den Foto-Fristen steht der Hinweis: keine Verlängerung möglich — Beweisbedarf heißt Sicherung durch die zuständige Stelle innerhalb 72 h (D-02)"
    - "Die Protokoll-Frist ist nur mit Pflicht-Begründung verlängerbar; abgelaufene/<24-h-Fristen erscheinen als hervorgehobener Wiedervorlage-Block oben (D-03, D-09)"
  artifacts:
    - path: "docs/leitung-<suffix>.html"
      provides: "Desktop-Governance-Seite: Login, Zugänge, Einsatzprotokolle, Protokoll, Phase-7-Sperrsektion (Pfad-Muster — konkreter Name nur in LEITUNG-URL.md)"
      min_lines: 400
      contains: "noindex"
    - path: "LEITUNG-URL.md"
      provides: "konkreter Dateiname + GitHub-Pages-URL der Leitungs-Seite — untracked, gitignored, einzige Fundstelle"
  key_links:
    - from: "docs/leitung-<suffix>.html"
      to: "vendor/supabase.js"
      via: "script src ../vendor/supabase.js"
      pattern: "\\.\\./vendor/supabase\\.js"
    - from: "docs/leitung-<suffix>.html"
      to: "rpc argus_exchange_code"
      via: "fetch beim Login"
      pattern: "argus_exchange_code"
    - from: "docs/leitung-<suffix>.html"
      to: "rpc argus_governance_protokoll"
      via: "Protokoll-Aufruf mit p_kuerzel"
      pattern: "argus_governance_protokoll"
    - from: "docs/leitung-<suffix>.html"
      to: "access_tokens.revoked"
      via: "update beim Sperren/Entsperren"
      pattern: "revoked"
---

<objective>
Eigene desktop-taugliche Governance-/Leitungs-Webseite im ARGUS-Design
(D-06–D-13): MasterToken-Login, drei Bereiche (Zugänge, Einsatzprotokolle,
Protokoll), sichtbar gesperrte Phase-7-Zone. Bündelt die Leitungs-Funktionen an
EINEM Ort — die Feld-App verliert ihre Governance-Insel parallel in Plan 03.

Purpose: Konsolidierte Leitungs-Oberfläche, getrennt von der Feld-PWA;
Sicherheitsgewinn Code-Sperre; Einsatzprotokoll-Modell bedienbar machen.
Output: `docs/leitung-<suffix>.html` (Single-File, Diskretions-Entscheid:
ein File mit Inline-CSS/JS, nur vendor/Fonts als externe Assets) +
untracked `LEITUNG-URL.md` mit dem konkreten Namen.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04.9-governance-oberflaeche/04.9-CONTEXT.md
@.planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md
@docs/UI-AUSBLICK.html (NUR lesen — Referenz-Optik, NICHT ändern)

**VERBINDLICHE GEHEIMHALTUNGS-REGEL (D-06, gilt für diesen gesamten Plan):**
Der konkrete Dateiname der Leitungs-Seite (docs/leitung-<suffix>.html mit
echtem Suffix) darf in KEINER committeten Datei auftauchen — nicht in der
SUMMARY dieses Plans, nicht in CHANGELOG/WHATS_NEW/UPDATE-Sheets, nicht in
STATE/ROADMAP oder anderen .planning-Dokumenten, nicht in Commit-Messages.
Das Repo ist public; ein committeter Name macht „unguessable" wertlos. In
committeten Texten ausschließlich „Leitungs-Oberfläche"/„Leitungs-Seite"
schreiben. Einzige Fundstelle des Namens: die untracked Datei LEITUNG-URL.md
im Projektroot — von dort erhält der Owner die URL.

Verbindliche Optik (aus docs/UI-AUSBLICK.html, :root Zeilen 10–16): IBM Plex
Sans/Mono, hell, Variablen --bg #fff, --surf #f6f7f8, --surf2 #eef0f2,
--line #d9dde1, --tx #16181a, --mut #5d6368, --t1 #C62828, --t2 #F2B600,
--t3 #2E7D32, --t5 #1b1d20, --prio #E65100, --ready #1565C0, --acc #1f2530
(Schiefer = Chrome). Triage-Farben nur funktional, kein Apple-/Royal-Stil.

Die Seite wird via GitHub Pages PUBLIC ausgeliefert: Schutz liegt VOLLSTÄNDIG
in JWT/RLS. Im Quelltext keine internen Begriffe (kein „PolG", „DSFA",
„Polizei BW"-Strategie, keine Verweise auf interne Docs). Deutsche UI, knapp.
Kein node/npm; Logik-Tests via `osascript -l JavaScript` (JSC).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grundgerüst — Login, Layout, Navigation, Phase-7-Sperrsektion</name>
  <files>docs/leitung-<suffix>.html, .gitignore, LEITUNG-URL.md</files>
  <read_first>
    - docs/UI-AUSBLICK.html Zeilen 1–60 (Variablen, Grundtypografie — Referenz)
    - docs/demo-6e12b5ebde.html Zeilen 1–15 (noindex-Meta-Muster, Auslieferungs-Konvention)
    - index.html Zeilen 220–221 (SUPA_URL/SUPA_KEY), 453–500 (exchangeCode-Fetch-Muster gegen rpc/argus_exchange_code), 100–115 (Versions-/Header-Konvention)
    - docs/fonts/ Verzeichnisinhalt (vorhandene IBM-Plex-woff2-Gewichte für @font-face)
  </read_first>
  <action>
    **Dateinamen ERST JETZT erzeugen (D-06):** Suffix = 10 Hex-Zeichen aus
    /dev/urandom (z. B. `head -c5 /dev/urandom | xxd -p`). Der konkrete Name
    steht bewusst NICHT in diesem Plan (Repo ist public). Den vollständigen
    Dateinamen und die spätere GitHub-Pages-URL AUSSCHLIESSLICH in eine neue
    Datei `LEITUNG-URL.md` im Projektroot schreiben; in `.gitignore` den
    Eintrag `/LEITUNG-URL.md` ergänzen (beides gehört zu DIESEM Task —
    .gitignore wird committet, LEITUNG-URL.md NIE). In allen folgenden
    Befehlen/Greps die Datei über das Glob `docs/leitung-*.html` ansprechen.

    Neue Datei `docs/leitung-<suffix>.html` (Muster docs/demo-*.html, D-06).
    Single-File: Inline-CSS + ein Inline-Script, extern nur
    `<script src="../vendor/supabase.js">` (D-08) und @font-face auf
    `fonts/ibm-plex-*.woff2` (relativ — die Datei liegt selbst in docs/).

    Kopf: Copyright-Kommentar wie in index.html, `<meta name="robots"
    content="noindex, nofollow, noarchive">` + googlebot-Meta (Muster demo-Seite),
    Titel neutral „ARGUS · Leitung". KEIN manifest, KEIN Service-Worker-Register
    (keine PWA — bewusst flüchtig), Seite NICHT in sw.js/PRECACHE aufnehmen,
    NIRGENDS verlinken (D-06).

    CSS: :root-Variablen 1:1 aus UI-AUSBLICK übernehmen (D-09). Layout:
    Schiefer-Topbar (var(--acc)) mit Wortmarke „ARGUS · Leitung" links und
    Abmelden-Knopf rechts; darunter zweispaltig — linke Seitennavigation
    (Bereiche: Zugänge / Einsatzprotokolle / Protokoll; darunter optisch
    abgesetzte Sektion „Phase 7 · nach DSB" mit ausgegrauten, NICHT klickbaren
    Einträgen „Identitäten" und „Audit-Log" + Schloss-Zeichen — sichtbare
    Grenze, D-09); rechts Inhaltsbereich (max-width ~1100px). Desktop-first,
    aber bei schmalem Viewport nicht kaputt (Nav oben statt links genügt).

    JS-Grundgerüst: Konstanten SUPA_URL/SUPA_KEY aus index.html übernehmen.
    Login-View: Eingabefeld MasterToken (Format-tolerant wie die Feld-App:
    Whitespace/Bindestriche egal) + „Einfügen"-Knopf (navigator.clipboard) +
    Anmelden. Login ruft `rpc/argus_exchange_code` per fetch (Muster
    index.html Z. 461: apikey-Header = SUPA_KEY); Antwort MUSS `is_master ===
    true` haben, sonst Abweisung „Nur mit MasterToken" OHNE Speicherung (D-07).
    Erfolg: jwt + exp*1000 in **sessionStorage** (Schlüssel `argusl_jwt`,
    `argusl_jwt_exp`) — bewusst flüchtig, KEIN localStorage (D-07). Supabase-
    Client via `supabase.createClient` mit globalem Bearer-Header (Muster sb()
    in index.html). Abgelaufene Session (exp <= now) → zurück zum Login.
    Abmelden: sessionStorage.clear() + Reload.

    Router: einfacher State (aktiver Bereich), nach Login DIREKT Bereich
    „Zugänge" rendern (D-10 — keine Übersichtsseite, kein Dashboard). Eigene
    leichte In-Page-Modals für confirm/prompt (keine nativen Dialoge —
    Konsistenz zur Projektkonvention). Hilfsfunktionen esc() (HTML-Escaping
    aller DB-Werte — XSS-Schutz, Daten kommen aus gemeinsam beschreibbaren
    Tabellen) und fmtMs() (de-DE-Datumsformat für bigint-ms, Muster govFmt).
    Die drei Bereiche als Platzhalter-Funktionen, Inhalte in Task 2/3.
  </action>
  <acceptance_criteria>
    - Dateiname matcht das Muster: `ls docs/leitung-*.html | grep -cE "leitung-[0-9a-f]{10}\.html"` = 1
    - LEITUNG-URL.md existiert und enthält den konkreten Dateinamen; `git check-ignore LEITUNG-URL.md` → ignoriert; `.gitignore` enthält `/LEITUNG-URL.md`
    - `grep -c "noindex" docs/leitung-*.html` ≥ 2 (robots + googlebot)
    - `grep -c "sessionStorage" docs/leitung-*.html` ≥ 3 und `grep -c "localStorage" docs/leitung-*.html` = 0
    - `grep -c "is_master" docs/leitung-*.html` ≥ 1 (Login-Gate) und Source-Assertion: Abweisungspfad ohne Speicherung vorhanden
    - `grep -c "\.\./vendor/supabase\.js" docs/leitung-*.html` = 1; `grep -c "jsdelivr\|googleapis\|gstatic" docs/leitung-*.html` = 0 (T8-Linie: keine externen Hosts)
    - **Nirgends-referenziert-Gate (D-06):** `git grep -E "leitung-[0-9a-f]{10}" -- ':!docs/leitung-*'` → 0 Treffer — über ALLE getrackten Dateien, ausdrücklich inkl. committeter .planning-Dokumente; nur die Leitungs-Datei selbst ist ausgenommen (LEITUNG-URL.md ist untracked und damit für git grep unsichtbar)
    - `grep -c "Phase 7" docs/leitung-*.html` ≥ 1 und die Sektion hat kein click-Handling (Source-Assertion: keine data-action/onclick an den gesperrten Einträgen)
    - `grep -ci "alert(\|confirm(\|prompt(" docs/leitung-*.html` = 0 für native Aufrufe (eigene Modal-Namen sind erlaubt — Grep auf `window.alert\|window.confirm\|window.prompt` und nackte Aufrufe prüfen)
    - `grep -ci "polg\|dsfa\|auftragsverarbeit" docs/leitung-*.html` = 0 (keine internen Begriffe)
    - JSC-Syntax-Check des Inline-Scripts (osascript -l JavaScript, Script-Block extrahieren und per new Function parsen): OK
  </acceptance_criteria>
  <done>Seite lädt, Login-Gate steht, Navigation mit gesperrter Phase-7-Sektion rendert, drei Bereichs-Platzhalter erreichbar; konkreter Name nur in der untracked LEITUNG-URL.md.</done>
</task>

<task type="auto">
  <name>Task 2: Bereich „Zugänge" — Codes erstellen + Code-Sperre</name>
  <files>docs/leitung-<suffix>.html</files>
  <read_first>
    - index.html Zeilen 630–656 (vAdmin — Portierungs-Quelle UI-Logik), 2692–2715 (adminlink-Handler: Insert-Felder für Einmal-Codes, Code-Auswahl-Logik, Link-Bau), Definitionen von genToken()/genShortCode() (per grep -n "function genToken\|function genShortCode" index.html lokalisieren)
    - .planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md (revoked-Spalte, argus_check_code-Verhalten)
  </read_first>
  <action>
    Bereich „Zugänge" (D-11) implementieren — Portierung der Admin-Panel-Logik
    aus index.html, erweitert um 24-h-Codes und Code-Sperre:

    1. **Laden:** `from('praesidien').select('id,name').order('name')` und
       `from('access_tokens').select('short_code,label,is_master,temporary,ttl_hours,single_use,used_at,revoked,praesidium_id')`
       (Master-RLS erlaubt alles). Gruppierung je Präsidium.

    2. **Erstellen je Präsidium — drei Arten (D-11):**
       Dauerhaft / 24 h / Einmalig. genToken() + genShortCode() aus index.html
       portieren. Einmal-Link: Insert wie der adminlink-Handler (temporary:true,
       ttl_hours:24, single_use:true, used_at:null, Label „<Präsidium> Einmal-Link",
       bestätigten short_code aus dem Insert-Select verwenden). Dauerhaft/24 h:
       vorhandenen passenden, NICHT gesperrten Code anbieten (Auswahl-Logik des
       Handlers: temporary-Flag, kein single_use); existiert keiner → neuen
       anlegen (Dauerhaft: temporary:false, ttl_hours:null; 24 h: temporary:true,
       ttl_hours:24; beide single_use:false, is_master:false). Link-Format:
       Feld-App-Wurzel + '#code=' + short_code; Wurzel-URL aus location ableiten
       (Pfadsegment „docs/<datei>" entfernen). Kopieren in die Zwischenablage
       mit Fallback-Anzeige im Modal (Clipboard kann scheitern).

    3. **Code-Tabelle je Präsidium:** short_code (mono), Art (Dauerhaft / 24 h /
       Einmalig), Status (aktiv / verbraucht [used_at gesetzt bei single_use] /
       GESPERRT rot), Aktion „Sperren" bzw. „Entsperren" → nach Bestätigungs-Modal
       `from('access_tokens').update({revoked:true|false}).eq('short_code',…)`,
       danach Reload des Bereichs. **MasterToken-Zeilen (is_master=true): kein
       Sperren-Knopf** — Selbstaussperrung verhindern; stattdessen Hinweistext
       „MasterToken — Sperrung nur direkt in der Datenbank". Hinweis unter der
       Tabelle: „Gesperrte Codes wirken am Feldgerät beim nächsten Start bzw.
       bei Wiederverbindung (ab App-Version 0.23)." — der Re-Check kommt erst
       mit Plan 03 / v0.23.0; Geräte auf v0.22.0 verlieren den Zugang erst,
       wenn ihr JWT abläuft.
  </action>
  <acceptance_criteria>
    - `grep -c "argus_exchange_code\|access_tokens" docs/leitung-*.html` ≥ 3; Insert-Felder für Einmal-Link identisch zur Feld-App (Source-Vergleich: temporary, ttl_hours, single_use, used_at)
    - `grep -c "revoked" docs/leitung-*.html` ≥ 3 (Laden, Update, Status-Anzeige)
    - `grep -c "ab App-Version 0.23" docs/leitung-*.html` ≥ 1 (Wirksamkeits-Hinweis am Sperr-Bereich)
    - Source-Assertion: der Sperren-Pfad ist hinter `is_master`-Prüfung der Token-Zeile ausgeschlossen (kein Sperren-Knopf bei is_master=true)
    - Source-Assertion: Link-Bau entfernt das docs/-Segment aus location (Wurzel-URL), '#code='-Format wie index.html
    - JSC-Test (osascript): genShortCode()/genToken() portiert und liefern Format wie die Feld-App (8 Zeichen → XXXX-XXXX bzw. Token-Länge); JSC-Syntax-Check der Gesamtdatei OK
  </acceptance_criteria>
  <done>Leitung kann je Präsidium alle drei Code-Arten erstellen/kopieren und Codes sperren/entsperren; MasterToken nicht sperrbar.</done>
</task>

<task type="auto">
  <name>Task 3: Bereiche „Einsatzprotokolle" + „Protokoll"</name>
  <files>docs/leitung-<suffix>.html</files>
  <read_first>
    - .planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md (Rückgabeformate argus_governance_einsaetze/protokoll/photo, argus_extend_protokoll_frist, governance_log-Action-Werte)
    - index.html Zeilen 657–810 der Version v0.22.0 (vGovernance/govGroupCard/govLogSection/fristStatus/govFmt — Portierungs-Quelle für Wiedervorlage-Logik, Frist-Einstufung, Protokoll-Rendering). Da Plan 03 in derselben Wave den Rückbau fahren kann, deterministisch aus dem Release-Commit v0.22.0 lesen: `git show bc74c8d:index.html` (Hash bc74c8d = Release-Commit v0.22.0)
  </read_first>
  <action>
    **Bereich „Einsatzprotokolle" (D-12, D-01, D-02, D-03):**

    1. Laden via `rpc('argus_governance_einsaetze')`; client-seitig nach
       merge_group_id gruppieren (ein Verbund = ein Einsatz). **Wiedervorlage-
       Block ganz oben** (D-09, rot var(--t1)): Einsätze mit purge_after
       abgelaufen oder < 24 h — fristStatus() aus der Feld-App portieren
       (Einstufung abgelaufen/bald/ok/null, identische Grenzwerte). Hinweis im
       Block: „Frist nur mit Begründung verlängern oder Löschung geschehen
       lassen — der Übergabe-Export beim Abschluss ist der dauerhafte Weg."

    2. Einsatz-Karte: Kennung(en), Typ (Übung/Einsatz — Merkmal einsatz_typ),
       abgeschlossen am, Patientenzahl, Protokoll-Frist (purge_after, fmtMs) und
       Foto-Frist (photo_purge_after) mit festem Hinweis: „Fotos werden 72 Std
       nach Abschluss endgültig gelöscht — KEINE Verlängerung. Beweisbedarf:
       Sicherung durch die zuständige Stelle innerhalb der 72 Std." (D-02).

    3. **„Protokoll öffnen":** Kürzel-Pflicht — einmal je Session per Modal
       abfragen, in sessionStorage `argusl_kuerzel` halten; davor Hinweis-Modal
       „Jeder Aufruf eines Einsatzprotokolls wird protokolliert (Kürzel +
       Zeitpunkt). Fortfahren?" (D-01). Dann `rpc('argus_governance_protokoll',
       {p_ccp_id, p_kuerzel})`. Anzeige: Einsatz-Kopf (ccps-Daten) +
       Patiententabelle (Desktop-Tabelle: Nr., Kategorie, Status, Name falls
       erfasst, Sichtungszeit, Kern-Vitalwerte/Verletzungen kompakt — Felder
       defensiv rendern, alles esc()) + Checklisten-Abschnitt. Je Patient mit
       has_photo=true: Knopf „Foto anzeigen" → `rpc('argus_governance_photo',
       {p_patient_id, p_kuerzel})` (loggt foto_view) → Overlay mit Bild +
       Protokoll-Hinweis (Muster Feld-App-Overlay). has_photo=false bzw.
       Foto-Frist abgelaufen → kein Knopf.

    4. **„Protokoll-Frist +72 h verlängern":** Pflicht-Begründung mit
       Leer-Abweisung (Schleife wie govextend), Bestätigungs-Modal mit neuer
       Frist-Vorschau (greatest(now, purge_after) + 72 h) und dem Export-Hinweis
       „Export an die zuständige Stelle statt wiederholter Verlängerung" →
       `rpc('argus_extend_protokoll_frist', {p_ccp_id, p_kuerzel, p_grund,
       p_stunden:72})` → Bereich neu laden (D-03).

    **Bereich „Protokoll" (D-13):** zwei Abschnitte:
    governance_log (`from('governance_log')`, order at desc, limit 200) mit
    Action-Mapping foto_view → „Foto-Abruf", protokoll_view →
    „Einsatzprotokoll-Aufruf", frist_verlaengert → „Frist verlängert"
    (+ Begründung/neue Frist); purge_log (`from('purge_log')`, order at desc,
    limit 100) mit Zeitpunkt, Kennung, Grund (frist/inaktiv/foto_72h) und
    Zählern — inhaltsfrei, reine Anzeige. Leere Zustände mit Hinweistext.

    **SUMMARY-Regel (D-06):** In der SUMMARY dieses Plans (und in den
    Commit-Messages) den konkreten Dateinamen NICHT nennen — nur das Muster
    `docs/leitung-<suffix>.html` und den Verweis „konkreter Name siehe
    LEITUNG-URL.md (untracked)".
  </action>
  <acceptance_criteria>
    - `grep -c "argus_governance_einsaetze\|argus_governance_protokoll\|argus_governance_photo\|argus_extend_protokoll_frist" docs/leitung-*.html` ≥ 4 (alle vier RPCs verdrahtet)
    - `grep -c "purge_log" docs/leitung-*.html` ≥ 1 und `grep -c "governance_log" docs/leitung-*.html` ≥ 1
    - Source-Assertion: vor dem argus_extend_protokoll_frist-Aufruf steht eine Leer-Abweisungs-Schleife für die Begründung (Pflichtfeld)
    - Source-Assertion: KEINE Verlängerungs-UI für photo_purge_after; der 72-h-/Sicherungs-Hinweis (D-02) ist als Text vorhanden (`grep -c "zuständige Stelle" docs/leitung-*.html` ≥ 1)
    - Source-Assertion: Protokollierungs-Hinweis VOR dem Protokoll-RPC-Aufruf (D-01) und Kürzel-Pflicht (kein RPC-Aufruf mit leerem p_kuerzel erreichbar)
    - JSC-Test (osascript): portierte fristStatus() mit den 10 Fällen aus 04.8-05 (abgelaufen/bald/ok/null + Grenzwerte) → 10/10 PASS
    - JSC-Syntax-Check der Gesamtdatei: OK
    - Abschluss-Gate (D-06): `git grep -E "leitung-[0-9a-f]{10}" -- ':!docs/leitung-*'` → 0 Treffer (nach `git add` der Plan-Artefakte, vor dem Commit prüfen — auch Commit-Messages ohne konkreten Namen)
    - Commit(s) nach Konvention `feat(04.9-02): …` mit Co-Authored-By-Zeile; NICHT pushen
  </acceptance_criteria>
  <done>Alle drei Bereiche funktionsfähig; Wiedervorlage oben; Foto-Härtung und Protokollierung in der UI korrekt kommuniziert; konkreter Dateiname in keiner committeten Datei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| öffentliches Internet → statische Seite | Datei ist public abrufbar; KEINE Daten ohne JWT |
| Browser-Session → Supabase REST | is_master-JWT als einzige Autorität; RLS/RPC-Gates serverseitig (Plan 01) |
| Arbeitsverzeichnis → public Repo | konkreter Dateiname nur in untracked LEITUNG-URL.md — nie in getrackten Dateien |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigation |
|-----------|-----------|-----------|-------------|-----------------|
| T-4.9-07 | Information Disclosure | Seiten-Quelltext (public) | mitigate | keine internen Begriffe/Strategie im Source; alle Daten erst nach JWT-Login; unguessable Name + noindex; Name nur in untracked LEITUNG-URL.md, git-grep-Gate über getrackte Dateien |
| T-4.9-08 | Spoofing | Login | mitigate | is_master===true-Pflicht clientseitig + serverseitige Master-Gates jedes RPCs (Plan 01) — Client-Gate ist nur UX |
| T-4.9-09 | Tampering (XSS) | Rendering von DB-Werten (kennung, label, begruendung von Feld-Geräten beschreibbar) | mitigate | konsequentes esc() auf JEDEN dynamischen Wert |
| T-4.9-10 | Information Disclosure | JWT im Browser | mitigate | sessionStorage (flüchtig), Abmelden-Knopf, kein localStorage, keine SW-Cache-Aufnahme |
| T-4.9-11 | Elevation | Code-Sperre auf MasterToken (Selbstaussperrung) | mitigate | UI verhindert Sperren von is_master-Zeilen |
</threat_model>

<verification>
- Datei existiert (Muster leitung-[0-9a-f]{10}.html), ≥ 400 Zeilen, alle Grep-/Source-Assertions der Tasks grün.
- JSC: Syntax OK, fristStatus 10/10.
- `git grep -E "leitung-[0-9a-f]{10}" -- ':!docs/leitung-*'` → 0 Treffer (Name in keiner getrackten Datei; LEITUNG-URL.md untracked + gitignored).
</verification>

<success_criteria>
- D-06 bis D-13 vollständig umgesetzt; D-01/D-02/D-03 UI-seitig korrekt kommuniziert.
- Konkreter Dateiname existiert ausschließlich in LEITUNG-URL.md (untracked).
- Live-Verifikation im Browser bleibt als offener Punkt für den Owner (Umgebung hat keinen Browser) — in SUMMARY als „offen" führen.
</success_criteria>

<output>
Create `.planning/phases/04.9-governance-oberflaeche/04.9-02-SUMMARY.md` when done.
GEHEIMHALTUNGS-REGEL beachten: Die SUMMARY nennt den konkreten Dateinamen NICHT —
nur das Muster `docs/leitung-<suffix>.html` + Verweis auf LEITUNG-URL.md.
</output>

## Artifacts this phase plan produces

- Datei `docs/leitung-<suffix>.html` (Single-File-Leitungsseite, public, noindex, unverlinkt; konkreter Name nur in LEITUNG-URL.md)
- Datei `LEITUNG-URL.md` im Projektroot (untracked, gitignored — konkreter Name + URL für den Owner)
- .gitignore-Eintrag `/LEITUNG-URL.md`
- sessionStorage-Keys: `argusl_jwt`, `argusl_jwt_exp`, `argusl_kuerzel`
- Portierte Funktionen (Kopien in der Seite): genToken, genShortCode, fristStatus, fmtMs (govFmt-Äquivalent), esc
