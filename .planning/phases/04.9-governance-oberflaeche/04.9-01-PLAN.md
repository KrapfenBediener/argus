---
phase: 04.9-governance-oberflaeche
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0003_phase49_einsatzprotokoll.sql
autonomous: false
requirements: [D-01, D-02, D-03, D-17, D-18, D-19, D-20, D-21]
user_setup:
  - service: supabase
    why: "DDL nur über Management API mit ephemerem Personal Access Token (PAT) — kein CLI, kein Dashboard-Zugang für den Executor"
    env_vars:
      - name: SUPABASE_PAT
        source: "Supabase Dashboard → Account → Access Tokens → neuen ephemeren PAT erzeugen; nach Plan-Ende widerrufen"
must_haves:
  truths:
    - "Ein gesperrter Code (revoked=true) kann nicht mehr eingetauscht werden (argus_exchange_code lehnt ab) und wird von argus_check_code als gesperrt gemeldet"
    - "Einsatz-Abschluss setzt purge_after UND photo_purge_after einheitlich auf Abschluss+72 h — auch wenn ein Altgerät (v0.22.0) noch p_frist_tage übergibt (D-03, D-15-Kompatibilität)"
    - "Jeder Abruf eines Einsatzprotokolls erzeugt serverseitig zwingend einen governance_log-Eintrag mit Kürzel + Zeitstempel, VOR der Datenrückgabe (D-01, D-17)"
    - "Die Protokoll-Frist (purge_after) ist per RPC nur mit Pflicht-Begründung verlängerbar; die Foto-Frist (photo_purge_after) ist durch KEINEN RPC mehr verlängerbar (D-02, D-03, D-18)"
    - "Nicht-Master-Geräte können Patienten und Checklisten abgeschlossener Einsätze nicht mehr lesen; ccps-Tombstone-Zeilen bleiben lesbar (Abschluss-Erkennung der Feld-App, D-19)"
    - "Keine toten RPCs: argus_governance_list und argus_extend_photo_frist existieren nach der Migration nicht mehr"
  artifacts:
    - path: "supabase/migrations/0003_phase49_einsatzprotokoll.sql"
      provides: "revoked-Flag, argus_check_code, argus_governance_einsaetze, argus_governance_protokoll, argus_extend_protokoll_frist, 72-h-Abschluss, RLS-Härtung"
      contains: "argus_governance_protokoll"
  key_links:
    - from: "argus_governance_protokoll"
      to: "governance_log"
      via: "insert mit action 'protokoll_view' VOR der Rückgabe"
      pattern: "protokoll_view"
    - from: "argus_exchange_code"
      to: "access_tokens.revoked"
      via: "Ablehnung bei revoked=true"
      pattern: "revoked"
    - from: "argus_patients_select"
      to: "ccps.closed_at"
      via: "closed_at is null in der USING-Klausel für Nicht-Master"
      pattern: "closed_at is null"
---

<objective>
Migration 0003 baut den serverseitigen Unterbau des Einsatzprotokoll-Modells
(Owner-Entscheide 2026-06-10): Einsatzprotokoll-Abruf mit Zwangs-Log (D-01, D-17),
einheitliche 72-h-Grundfrist beim Abschluss (D-03), Fotos hart ohne Verlängerung
(D-02), Frist-Verlängerung umgewidmet auf purge_after (D-18), RLS-Härtung
geschlossener Einsätze (D-19), revoked-Flag mit Exchange-/Check-Prüfung (D-20),
Purge-Modell unverändert auf pg_cron (D-21).

Purpose: Ohne diesen Unterbau kann weder die Desktop-Governance-Seite (Plan 02)
noch der Feld-App-Umbau (Plan 03) gebaut werden.
Output: `supabase/migrations/0003_phase49_einsatzprotokoll.sql`, live angewendet
und per Management API + REST-Negativtests verifiziert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04.9-governance-oberflaeche/04.9-CONTEXT.md
@supabase/migrations/0002_phase48_datenschutz.sql
@supabase/migrations/0001_phase4_jwt_rls.sql
@.planning/phases/04.8-datenschutz-haertung/04.8-02-SUMMARY.md

Betriebsbedingungen: KEIN node/npm. DDL ausschließlich über die Supabase
Management API (`POST https://api.supabase.com/v1/projects/sehuosjyjmrpzcqrelej/database/query`,
Header `Authorization: Bearer <PAT>`, Body `{"query": "<SQL>"}` via curl).
Muster: Anwendung von 0002 (siehe 04.8-02-SUMMARY). Migration idempotent halten
(zweifaches Anwenden gefahrlos). Zeitstempel-Konvention: bigint in JS-Millisekunden.
PAT erscheint NIE in Dateien oder Commits.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0003 schreiben (Einsatzprotokoll-Modell, revoked, RLS)</name>
  <files>supabase/migrations/0003_phase49_einsatzprotokoll.sql</files>
  <read_first>
    - supabase/migrations/0002_phase48_datenschutz.sql (komplett — Stil, Idempotenz-Muster, Kommentar-Konvention, bestehende RPC-Definitionen)
    - supabase/migrations/0001_phase4_jwt_rls.sql Zeilen 14–110 (argus_exchange_code: exakter Aufbau, Code-Normalisierung, Vault-Zugriff — wird in 0003 mit revoked-Prüfung neu erstellt)
  </read_first>
  <action>
    Neue Datei `supabase/migrations/0003_phase49_einsatzprotokoll.sql` im Stil von 0002
    (Copyright-Header, Abschnitts-Kommentare, idempotent, Begründungs-Kommentare an
    jeder nicht offensichtlichen Stelle). Inhalt in dieser Reihenfolge:

    1. **revoked-Flag (D-20):** `alter table public.access_tokens add column if not exists
       revoked boolean not null default false`.

    2. **argus_exchange_code neu erstellen (D-20):** identisch zur Fassung aus 0001,
       plus: `revoked` mit in den select aufnehmen; direkt nach der not-found-Prüfung
       bei `coalesce(revoked,false)` mit jsonb-Error `'Code wurde gesperrt'` abbrechen
       (eigener Wortlaut, unterscheidbar vom generischen „Ungültiger oder verbrauchter
       Code" — die Feld-App zeigt ihn dem Nutzer an). Kommentar: Wiederholung der
       used_at-bigint-Begründung aus 0001 beibehalten.

    3. **Neuer RPC `argus_check_code(code text) returns jsonb` (D-20):** SECURITY
       DEFINER, search_path public. Normalisiert den Code wie argus_exchange_code
       (upper, Whitespace/Bindestriche entfernen, short_code-Format XXXX-XXXX),
       liest access_tokens und gibt `{"found": bool, "revoked": bool}` zurück —
       KEIN JWT, KEINE Seiteneffekte (kein used_at-Update), kein Präsidiums-Leak.
       Zweck-Kommentar: Re-Check der Feld-App beim Start/Reconnect; gibt bewusst
       nur das Minimum zurück. Grant execute to anon.

    4. **argus_close_einsatz auf 72-h-Grundfrist (D-03):** `create or replace` mit
       UNVERÄNDERTER Signatur `(p_ccp_id uuid, p_typ text, p_frist_tage int default null)`
       — p_frist_tage wird ab jetzt IGNORIERT (Kompatibilitäts-Kommentar: v0.22.0-Geräte
       übergeben noch 14/30; die Frist-Validierung entfällt ersatzlos). Beide Fristen
       einheitlich: `purge_after = coalesce(closed_at, v_now) + 72h` und
       `photo_purge_after = coalesce(closed_at, v_now) + 72h` (72h = 72*3600000 ms).
       Typ-Validierung ('uebung'/'einsatz'), Berechtigungs- und merge_group-Logik
       unverändert aus 0002 übernehmen. Rückgabe-jsonb wie bisher (closed_at,
       purge_after, photo_purge_after, ccp_count).

    5. **Neuer RPC `argus_governance_einsaetze() returns jsonb` (D-12):** Master-only
       (raise exception 'Nur mit MasterUser-Token'), KEIN Log-Eintrag (reine Liste,
       analog argus_governance_list aus 0002). Liefert jsonb-Array mit einem Element
       je abgeschlossener ccps-Row, die noch NICHT gepurged ist. **„Noch nicht
       gepurged" WÖRTLICH an die Purge-Bedingung (b) aus argus_run_purge (0002)
       angleichen:** `closed_at is not null` UND (exists patients ODER exists
       checklists ODER `coalesce(ort,'') <> ''` ODER `coalesce(master_medic,'') <> ''`
       ODER `join_token is not null`). Begründung als Kommentar in die Migration:
       Liste und Purge teilen EINE Definition von „noch nicht gepurged" — ein
       bloßer (exists patients ODER exists checklists)-Filter würde abgeschlossene
       Einsätze ohne Patienten und Checklisten (aber mit noch laufender purge_after
       und vorhandenen Kopfdaten) unsichtbar machen, obwohl ihre Löschfrist läuft
       und über die Leitungs-Seite sichtbar/verlängerbar sein muss (Wiedervorlage).
       Felder: ccp_id, kennung, merge_group_id, einsatz_typ, closed_at, purge_after,
       photo_purge_after, patient_count, photo_count (count(photo)), checklist_count.
       Sortierung closed_at absteigend. Kommentar: vollständig gepurgte Einsätze
       (Tombstones) erscheinen nur noch im purge_log (Bereich „Protokoll" der
       Leitungs-Seite). Grant execute to anon.

    6. **Neuer RPC `argus_governance_protokoll(p_ccp_id uuid, p_kuerzel text)
       returns jsonb` (D-01, D-17, Muster argus_governance_photo):** SECURITY DEFINER,
       Master-Pflicht, Kürzel-Pflicht (btrim, leere Abweisung per exception
       'Kürzel erforderlich'). Lädt die ccps-Row; nicht gefunden → exception;
       `closed_at is null` → exception 'Einsatz noch offen — Einsatzprotokolle gibt
       es nur für abgeschlossene Einsätze'. Verbund: bei gesetzter merge_group_id
       alle Rows der Gruppe einbeziehen. **Zwangs-Log VOR der Rückgabe:** insert in
       governance_log (at = v_now, ccp_id = p_ccp_id, kuerzel = btrim(p_kuerzel),
       action = 'protokoll_view'). Rückgabe-jsonb mit drei Schlüsseln:
       `ccps` (Array: id, kennung, einsatz_typ, closed_at, purge_after,
       photo_purge_after, ort), `patients` (Array: je Patient `to_jsonb(p)` MINUS
       die Schlüssel 'photo', 'locked_by', 'locked_at', PLUS `has_photo` =
       photo is not null — Foto-Daten kommen NIE über diesen RPC, nur über den
       separat protokollierten argus_governance_photo), `checklists` (Array der
       Rows). Grant execute to anon.

    7. **argus_governance_photo bleibt bestehen (Diskretions-Entscheid, dokumentieren):**
       Kommentarblock in der Migration: der Foto-Einzelabruf mit foto_view-Log wird
       von der Leitungs-Seite weiterverwendet (Lazy-Load — Protokoll-RPC liefert
       bewusst keine Base64-Fotos). Keine Code-Änderung an der Funktion.

    8. **argus_governance_list entfernen (Diskretions-Entscheid „keine toten RPCs"):**
       `drop function if exists public.argus_governance_list();`. Kommentar: ersetzt
       durch argus_governance_einsaetze; das Feld-App-Governance-Panel (v0.21.1–v0.22.0,
       einziger Konsument) wird in Plan 04.9-03 zurückgebaut — bis zum App-Update
       zeigt das alte Panel auf Master-Geräten einen Fehlertext (bewusst akzeptiert,
       reine MasterUser-Funktion).

    9. **argus_extend_photo_frist → argus_extend_protokoll_frist (D-02, D-03, D-18):**
       `drop function if exists public.argus_extend_photo_frist(uuid, text, text, int);`
       Neue Funktion `argus_extend_protokoll_frist(p_ccp_id uuid, p_kuerzel text,
       p_grund text, p_stunden int default 72) returns jsonb`: Master-Pflicht,
       Kürzel-Pflicht, Begründungs-Pflicht (btrim, leer → exception), p_stunden
       1–720. Wirkt auf **purge_after** (NICHT photo_purge_after):
       neue Frist = greatest(v_now, coalesce(purge_after, v_now)) + p_stunden*3600000;
       Update auf die ccps-Row UND alle Rows derselben merge_group (der Einsatz =
       der Verbund, konsistent zu argus_close_einsatz). Nur für abgeschlossene
       Einsätze (closed_at is null → exception 'Einsatz noch offen'). Log-Eintrag
       in governance_log (action 'frist_verlaengert', begruendung, neue_frist).
       Rückgabe `{"purge_after": <ms>}`. Kommentarblock: Owner-Entscheid 2026-06-10 —
       Fotos sind HART (72 h, keine Verlängerung); verlängerbar ist nur noch das
       Einsatzprotokoll (Daten ohne Fotos). Grant execute to anon.

    10. **RLS-Härtung (D-19):** (a) `argus_patients_select` per drop+create neu:
        USING-Klausel für Nicht-Master zusätzlich an `closed_at is null` des
        zugehörigen CCP binden (Subselect wie in 0002, plus closed_at-Bedingung).
        (b) Checklisten nachziehen: `drop policy if exists argus_checklists_rw`;
        neu `argus_checklists_select` (select; Master ODER ccp im eigenen Präsidium
        UND closed_at is null) und `argus_checklists_write` (for all; gleiche
        Bedingung in USING und WITH CHECK). (c) `argus_ccps_select` UNVERÄNDERT
        lassen — Kommentar mit Begründung: die Feld-App erkennt den Abschluss über
        die lesbare Tombstone-Row (checkClosedEinsatz/applyMeta); nur die INHALTE
        (patients/checklists) sind für Nicht-Master nach Abschluss gesperrt.

    11. **pg_cron/Purge (D-21):** Kommentarblock (keine Code-Änderung):
        argus_run_purge und der Job argus_purge bleiben unverändert — die
        72-h-Grundfrist entsteht ausschließlich beim Abschluss (Punkt 4) bzw. per
        Verlängerung (Punkt 9); die Purge-Funktion liest nur purge_after/
        photo_purge_after und braucht keine Anpassung. governance_log-Spalten-
        Kommentar zu action um 'protokoll_view' ergänzen (comment on column).
  </action>
  <acceptance_criteria>
    - Datei existiert; `grep -c "create or replace function" supabase/migrations/0003_phase49_einsatzprotokoll.sql` ≥ 5 (exchange_code, check_code, close_einsatz, governance_einsaetze, governance_protokoll, extend_protokoll_frist)
    - `grep -c "drop function if exists public.argus_governance_list" supabase/migrations/0003_*.sql` = 1 und `grep -c "drop function if exists public.argus_extend_photo_frist" supabase/migrations/0003_*.sql` = 1
    - `grep -c "protokoll_view" supabase/migrations/0003_*.sql` ≥ 2 (Log-Insert + Spalten-Kommentar)
    - `grep -n "p_frist_tage" supabase/migrations/0003_*.sql` zeigt den Parameter nur noch in Signatur + Ignorier-Kommentar, in keiner Frist-Berechnung
    - Source-Assertion: der Filter von argus_governance_einsaetze enthält die volle Purge-(b)-Bedingung (`grep -c "join_token is not null" supabase/migrations/0003_*.sql` ≥ 1 im Funktionsbereich) samt Begründungs-Kommentar
    - Source-Assertion: im Body von argus_governance_protokoll steht der governance_log-Insert VOR dem return (Zeilennummern-Vergleich per grep -n)
    - Source-Assertion: in argus_extend_protokoll_frist kommt `photo_purge_after` NICHT als Update-Ziel vor (`grep "set photo_purge_after" auf den Funktionsbereich` = 0 Treffer)
    - Jede neue/ersetzte Funktion hat `security definer` und `set search_path` (grep-Zählung deckungsgleich mit Funktionszahl)
  </acceptance_criteria>
  <done>Migration 0003 liegt vollständig, idempotent und kommentiert vor; alle 11 Punkte abgedeckt.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Ephemerer Supabase-PAT (Checkpoint)</name>
  <what-built>Migration 0003 ist geschrieben; zum Anwenden braucht die Management API einen Personal Access Token.</what-built>
  <how-to-verify>
    1. Supabase Dashboard → Account → Access Tokens → neuen Token erzeugen (ephemer, nur für diese Migration).
    2. Token im Chat bereitstellen (er landet in keiner Datei, keinem Log, keinem Commit).
    3. Nach Abschluss von Task 3 den Token im Dashboard widerrufen (zusammen mit ggf. noch offenen Alt-PATs, siehe STATE.md).
  </how-to-verify>
  <resume-signal>PAT im Chat bereitgestellt (oder bereits im Execution-Context enthalten — dann gilt der Checkpoint als vorab erfüllt, wie bei 04.8-02)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Migration anwenden, verifizieren, committen</name>
  <files>supabase/migrations/0003_phase49_einsatzprotokoll.sql</files>
  <read_first>
    - .planning/phases/04.8-datenschutz-haertung/04.8-02-SUMMARY.md (Verifikations-Muster: Management-API-Queries, REST-Negativtests, Seed-Daten mit Bereinigung)
    - index.html Zeilen 220–221 (SUPA_URL + anon SUPA_KEY für die REST-Negativtests)
  </read_first>
  <action>
    1. Migration per Management API anwenden, danach ein ZWEITES Mal (Idempotenz-Beleg).
    2. Strukturprüfungen per Management API (information_schema/pg_catalog):
       revoked-Spalte existiert (boolean, default false); die 6 Funktionen existieren
       mit prosecdef=true; argus_governance_list und argus_extend_photo_frist
       existieren NICHT mehr; Policies argus_patients_select (mit closed_at-Bedingung
       im qual), argus_checklists_select/write vorhanden, argus_checklists_rw weg;
       cron.job argus_purge unverändert vorhanden.
    3. Funktionstests serverseitig (Management-API-Session, Claims via
       `set_config('request.jwt.claims', …, true)` faken; Seed-Daten anlegen und
       danach restlos löschen):
       a) Temp-Token mit revoked=true → `select argus_exchange_code(code)` liefert
          jsonb-Error 'Code wurde gesperrt'; `argus_check_code(code)` liefert
          found=true, revoked=true; nach revoked=false liefert check revoked=false.
       b) Temp-CCP → `argus_close_einsatz(id,'einsatz', 30)` → purge_after =
          photo_purge_after = closed_at + 259200000 (72 h, p_frist_tage ignoriert).
       c) Mit Master-Claims: argus_governance_einsaetze listet den Temp-CCP;
          zusätzlich Filter-Test: zweiter Temp-CCP OHNE Patienten/Checklisten,
          aber mit gesetztem ort, abgeschlossen → erscheint in der Liste
          (Purge-(b)-Angleichung); argus_governance_protokoll(temp_ccp, 'TEST')
          liefert ccps/patients/checklists-Schlüssel, patients OHNE photo-Schlüssel,
          und governance_log enthält danach genau einen protokoll_view-Eintrag mit
          Kürzel 'TEST'; Aufruf auf OFFENEN CCP → exception.
       d) argus_extend_protokoll_frist(temp_ccp,'TEST','Testgrund',72) → purge_after
          +72 h verschoben, photo_purge_after UNVERÄNDERT, frist_verlaengert-Eintrag
          im governance_log.
       e) Mit Präsidiums-Claims (praesidium_id des Temp-CCP, is_master=false):
          select auf patients des GESCHLOSSENEN Temp-CCP → 0 Rows; select auf die
          ccps-Row → 1 Row (Tombstone-Lesbarkeit bleibt).
    4. REST-Negativtests per curl gegen SUPA_URL mit anon-Key OHNE JWT:
       rpc/argus_governance_einsaetze → 400 'Nur mit MasterUser-Token';
       rpc/argus_governance_protokoll → 400; rpc/argus_extend_protokoll_frist → 400;
       rpc/argus_governance_list → 404 (existiert nicht mehr);
       rpc/argus_extend_photo_frist → 404;
       rpc/argus_check_code mit Fantasie-Code → 200 mit found=false.
    5. Commit NUR der Migrationsdatei: `git add supabase/migrations/0003_phase49_einsatzprotokoll.sql`
       + Commit `feat(04.9-01): Migration 0003 — Einsatzprotokoll-Modell, revoked-Flag, RLS-Härtung`
       mit Co-Authored-By-Zeile. NICHT pushen. PAT-Widerruf-Erinnerung in die SUMMARY.
  </action>
  <acceptance_criteria>
    - Zweifaches Anwenden ohne Fehler (Idempotenz)
    - Alle Strukturprüfungen aus Schritt 2 grün (Ausgaben in der SUMMARY dokumentiert)
    - Funktionstests a–e grün; Seed-Daten nachweislich entfernt (Abschluss-Query: 0 Test-Rows)
    - Alle 6 REST-Negativtests liefern exakt die erwarteten HTTP-Codes/Fehlertexte
    - Commit existiert und enthält ausschließlich die Migrationsdatei; PAT in keiner Datei (`git grep -i "sbp_" -- .` ohne Treffer im Commit)
  </acceptance_criteria>
  <done>Live-DB entspricht Migration 0003, alle Verifikationen dokumentiert, Commit lokal auf main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| anon REST → SECURITY-DEFINER-RPCs | öffentlicher anon-Key, JWT optional — jede Funktion prüft Claims selbst |
| Feld-Gerät (Präsidiums-JWT) → Daten geschlossener Einsätze | nach Abschluss nur noch Governance-Pfad |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigation |
|-----------|-----------|-----------|-------------|-----------------|
| T-4.9-01 | Spoofing | argus_governance_* RPCs | mitigate | argus_is_master()-Pflicht in jeder Funktion; REST-Negativtests ohne JWT |
| T-4.9-02 | Repudiation | Einsatzprotokoll-Abruf | mitigate | Zwangs-Log (protokoll_view/foto_view) VOR Rückgabe, Kürzel-Pflicht |
| T-4.9-03 | Information Disclosure | argus_governance_protokoll | mitigate | keine Foto-Daten im Protokoll-RPC (has_photo-Flag), Master-only, geloggt |
| T-4.9-04 | Tampering | purge_after/photo_purge_after via REST | mitigate | RLS: geschlossene ccps-Rows für Nicht-Master schreibgesperrt (0002), Fristen nur via RPC |
| T-4.9-05 | Elevation | gesperrter Code mit noch gültigem Alt-JWT | accept (Rest-Risiko) | revoked greift bei Exchange + Re-Check (Plan 03); Alt-JWT bleibt bis exp gültig — bewusst akzeptiert, echte Identitäten erst Phase 7 |
| T-4.9-06 | DoS / Enumeration | argus_check_code (anon aufrufbar) | accept | gibt nur found/revoked zurück, gleiche Angriffsfläche wie bestehender Exchange; Codes 8-stellig |
</threat_model>

<verification>
- Migration 2× angewendet, Struktur- und Funktionstests grün, Negativtests grün.
- Kein PAT in Dateien/Commits.
- `git log -1 --stat` zeigt nur die Migrationsdatei.
</verification>

<success_criteria>
- Einsatzprotokoll-Modell serverseitig vollständig (D-01, D-02, D-03, D-17, D-18, D-21).
- revoked-Flag wirksam in Exchange + Check (D-20).
- RLS verhindert Lesen von patients/checklists geschlossener Einsätze für Nicht-Master (D-19).
- Keine toten RPCs zurückgelassen.
</success_criteria>

<output>
Create `.planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md` when done.
Wichtig für Folgepläne dokumentieren: exakte Rückgabeformate der neuen RPCs,
governance_log-Action-Werte, PAT-Widerruf-Status.

ZUSÄTZLICH als bekannte, AKZEPTIERTE Übergangs-Abweichung in die SUMMARY
aufnehmen: v0.22.0-Altgeräte zeigen beim Einsatz-Abschluss weiterhin „Löschung
nach 14/30 Tagen", der Server setzt ab dieser Migration aber einheitlich 72 h —
die Daten werden also FRÜHER gelöscht als am Altgerät angezeigt. Gilt bis zum
Geräte-Update auf v0.23.0 (Plan 03); Plan 04 führt den Punkt in den
STATE-Owner-Punkten weiter, das UPDATE-Sheet (Plan 03) bittet ums App-Update.
</output>

## Artifacts this phase plan produces

- Spalte `access_tokens.revoked` (boolean, default false)
- RPC `argus_check_code(code text)` → `{"found": bool, "revoked": bool}`
- RPC `argus_close_einsatz(uuid, text, int default null)` — 72-h-Fristen, p_frist_tage ignoriert
- RPC `argus_governance_einsaetze()` → jsonb-Array abgeschlossener, ungepurgter Einsätze (Filter = Purge-Bedingung (b) aus 0002)
- RPC `argus_governance_protokoll(uuid, text)` → `{ccps, patients (ohne photo, mit has_photo), checklists}` + Zwangs-Log `protokoll_view`
- RPC `argus_extend_protokoll_frist(uuid, text, text, int default 72)` → `{"purge_after": ms}` + Log `frist_verlaengert`
- ENTFERNT: `argus_governance_list()`, `argus_extend_photo_frist(uuid,text,text,int)`
- Policies: `argus_patients_select` (neu, closed_at-Bedingung), `argus_checklists_select`, `argus_checklists_write` (ersetzen `argus_checklists_rw`)
- governance_log.action neuer Wert: `protokoll_view`
