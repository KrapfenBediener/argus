---
phase: 04.9-governance-oberflaeche
plan: 3
type: execute
wave: 2
depends_on: ["04.9-01"]
files_modified:
  - index.html
  - sw.js
  - version.json
  - CHANGELOG.md
  - docs/UPDATE_v0.23.html
autonomous: true
requirements: [D-02, D-03, D-04, D-14, D-15, D-16, D-20]
must_haves:
  truths:
    - "Die Feld-App enthält keine Governance-View, keinen Governance-Einstieg und keine Governance-Handler mehr (D-14)"
    - "Der Abschluss-Workflow bietet keine 14/30-Tage-Wahl mehr; er kommuniziert die feste 72-h-Löschfrist; die Typ-Wahl Übung/Einsatz bleibt als Merkmal (D-15, D-03)"
    - "Der Übergabe-Export ist im Abschluss-Workflow prominent als der einzige dauerhafte Weg in die Trägerdoku ausgewiesen (D-04)"
    - "Ein gesperrter Code (revoked) sperrt das Gerät beim nächsten Start bzw. Reconnect; offline blockiert der Re-Check NIEMALS (D-20, D-11)"
    - "Das Trainings-Drehbuch nennt keine 14/30-Tage-Fristen mehr (D-16)"
    - "Release v0.23.0 ist vollständig (APP_VERSION, version.json, SW-Cache v41, CHANGELOG, WHATS_NEW, UPDATE-Sheet — die 72-h-Umstellung ist nutzerrelevant)"
  artifacts:
    - path: "index.html"
      provides: "Feld-App ohne Governance, 72-h-Abschluss, checkRevokedCode()"
      contains: "argus_check_code"
    - path: "docs/UPDATE_v0.23.html"
      provides: "visuelles Update-Sheet v0.23"
    - path: "sw.js"
      provides: "Cache-Bump"
      contains: "ccp-shell-v41"
  key_links:
    - from: "index.html init-Block + online-Handler"
      to: "rpc argus_check_code"
      via: "checkRevokedCode() fire-and-forget"
      pattern: "argus_check_code"
    - from: "closeEinsatzWorkflow"
      to: "rpc argus_close_einsatz"
      via: "Aufruf ohne p_frist_tage"
      pattern: "argus_close_einsatz"
---

<objective>
Feld-App auf das Einsatzprotokoll-Modell umstellen: Governance-Insel entfernen
(D-14 — „keine Foto-Insel in der App"), Abschluss-Workflow auf die feste
72-h-Grundfrist (D-15, D-03) mit prominentem Export-Hinweis (D-04),
offline-sicherer revoked-Re-Check (D-20), Drehbuch nachziehen (D-16),
Release v0.23.0 mit UPDATE-Sheet.

Purpose: Die Feld-PWA bleibt schlank und einsatzfokussiert; Leitungs-Funktionen
leben ab jetzt ausschließlich auf der Desktop-Seite (Plan 02).
Output: index.html v0.23.0, sw.js v41, version.json, CHANGELOG, docs/UPDATE_v0.23.html.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04.9-governance-oberflaeche/04.9-CONTEXT.md
@.planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md
@.planning/phases/04.8-datenschutz-haertung/04.8-03-SUMMARY.md
@.planning/phases/04.8-datenschutz-haertung/04.8-05-SUMMARY.md

**VERBINDLICHE GEHEIMHALTUNGS-REGEL (D-06, gilt für diesen gesamten Plan):**
Der konkrete Dateiname der Leitungs-Seite (Muster docs/leitung-<suffix>.html,
entsteht in Plan 02) darf in KEINER committeten Datei auftauchen — nicht in
index.html/sw.js, nicht in CHANGELOG/WHATS_NEW/UPDATE-Sheet, nicht in der
SUMMARY dieses Plans, nicht in Commit-Messages. In allen Nutzer- und
Doku-Texten nur „Leitungs-Oberfläche"/„Leitungs-Seite" schreiben — der Owner
erhält die URL aus der untracked Datei LEITUNG-URL.md (Plan 02).

index.html ist sehr groß — NICHT komplett lesen; die read_first-Angaben nennen
Zeilenbereiche (Stand v0.22.0) und Grep-Anker. Konventionen: deutsche UI,
In-App-Modals (confirmModal/promptModal, NIE native Dialoge), Optik beibehalten,
Local-first nicht brechen. Kein node — JSC-Tests via `osascript -l JavaScript`
(Mock-Harness-Muster aus 04.8-03: Mikrotask-Drain über File-Output).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rückbau Governance-View + Abschluss-Workflow auf 72 h</name>
  <files>index.html</files>
  <read_first>
    - index.html Zeilen 243–248 (Zustandsvariablen _closeTyp/_closeFrist/_govList/_govErr/_govLog/_govLogErr), 650–656 (vAdmin-Ende mit opengovernance-Knopf), 657–810 (kompletter Governance-Block: govFmt, fristStatus, loadGovernanceList, vGovernance, govGroupCard, govLogSection), 1314–1442 (closeTypModal, exportChoiceModal, closeEinsatzWorkflow, finalizeCloseEinsatz, pendingCloseRead, flushPendingClose), 1605–1620 (titles + render-Dispatcher), 2015–2045 (Export-Kommentarblock + exportRowFromPatient — „Governance-Panel"-Kommentare), 2140–2155 (einsatzexport: closeeinsatznow-Knopf), 2716–2776 (Handler opengovernance/govreload/govphoto/govextend), 2980–2990 (closeeinsatz/exportback/closeeinsatznow-Handler), 3175–3200 (back()-Zweige + 3,5-s-Refresh-Ausschlussliste)
  </read_first>
  <action>
    **A) Rückbau (D-14)** — vollständig entfernen (Quality-Gate-Checkliste):
    Zustandsvariablen _govList/_govErr/_govLog/_govLogErr (Z. 246–247);
    opengovernance-Knopf in vAdmin (Z. 654); den gesamten Block govFmt,
    fristStatus, loadGovernanceList, vGovernance, govGroupCard, govLogSection
    (Z. 657–810); titles-Eintrag `governance:"Foto-Governance"`;
    Dispatcher-Zweig `else if(v==="governance")`; die vier Handler
    opengovernance, govreload, govphoto, govextend; den back()-Zweig
    `state.view==="governance"`; „governance" aus der 3,5-s-Refresh-
    Ausschlussliste. An der vAdmin-Stelle einen Einzeiler-Kommentar
    hinterlassen: Leitungs-Funktionen (Codes, Einsatzprotokolle, Protokoll)
    liegen auf der separaten Leitungs-Seite — OHNE deren Dateinamen zu nennen
    (Quelltext ist öffentlich, D-06).

    ZUSÄTZLICH in die Rückbau-Checkliste: die Export-Kommentare in Z. 2025
    („… Das Governance-Panel bleibt der einzige Foto-Export-Pfad.") und
    Z. 2041 („KEIN row.photo — … Governance-Panel = einziger Foto-Export-Pfad")
    werden nach dem Rückbau FALSCH — es gibt kein Governance-Panel in der App
    mehr. Beide Kommentare umformulieren auf die „Leitungs-Oberfläche" als
    einzigen Foto-Abruf-Pfad (ohne Dateinamen, D-06); die inhaltliche
    Begründung (Foto gehört nicht in die dauerhafte Trägerdoku) beibehalten.

    **B) 72-h-Umstellung (D-15, D-03, D-04):**
    1. closeTypModal: nur noch Typ-Wahl Übung/Einsatz; die Frist-Hinweise
       „Löschung nach 14/30 Tagen" ersetzen durch einen gemeinsamen Hinweis
       „Löschfrist einheitlich: 72 Std nach Abschluss — der Typ ist nur ein
       Merkmal fürs Protokoll"; den mc-custom-Knopf („Frist anpassen…") und
       die custom-Logik ersatzlos entfernen.
    2. closeEinsatzWorkflow: confirmModal-Text anpassen — „Daten werden 72 Std
       nach Abschluss automatisch gelöscht (Verlängerung nur über die Leitung).
       Fotos werden 72 Std nach Abschluss ENDGÜLTIG gelöscht — keine
       Verlängerung."; die freie Frist-Eingabe (promptModal 1–365) und die
       frist-Variable entfernen; nur noch _closeTyp setzen.
    3. exportChoiceModal-Text schärfen (D-04): „Der Übergabe-Export ist der
       EINZIGE dauerhafte Weg in die Einsatzdokumentation — nach 72 Std werden
       die Daten automatisch gelöscht." + bisherige Inhaltsbeschreibung.
    4. _closeFrist-Zustandsvariable komplett entfernen (Deklaration Z. 243,
       alle Zuweisungen/Resets in closeEinsatzWorkflow, finalizeCloseEinsatz,
       exportback-Handler, back()-Zweig einsatzexport).
    5. finalizeCloseEinsatz: RPC-Aufruf `argus_close_einsatz` nur noch mit
       p_ccp_id + p_typ (Migration 0003 hat p_frist_tage default null);
       Offline-Queue-Payload ohne fristTage ({ccpId, typ, ts}); Erfolgs-Toast
       „Einsatz abgeschlossen — Löschung in 72 Std".
    6. pendingCloseRead: Validierung auf ccpId + typ reduzieren (fristTage
       NICHT mehr fordern — Legacy-Queue-Einträge von v0.22.0 mit fristTage
       bleiben gültig, das Feld wird ignoriert; Kommentar dazu).
       flushPendingClose: RPC ohne p_frist_tage.
    7. Weitere 14/30-Reste prüfen: `grep -n "14 Tage\|30 Tage\|14/30" index.html`
       — UI-Texte und Kommentare auf 72-h-Modell umstellen (CHANGELOG-Historie
       und WHATS_NEW-Alteinträge UNVERÄNDERT lassen — historische Texte).

    **C) JSC-Tests aktualisieren:** pendingCloseRead/flushPendingClose-Suite
    (Muster /tmp/argus_t2_test.js aus 04.8-03) an die neue Payload anpassen:
    Legacy-Eintrag MIT fristTage → gültig, RPC ohne p_frist_tage; neuer Eintrag
    ohne fristTage → gültig; kaputtes JSON → drop. Syntax-Check der ganzen
    Datei (new Function über beide Inline-Blöcke).
  </action>
  <acceptance_criteria>
    - `grep -c "vGovernance\|govGroupCard\|govLogSection\|loadGovernanceList\|opengovernance\|govphoto\|govextend\|govreload\|_govList\|_govErr\|_govLog\|fristStatus\|govFmt" index.html` = 0
    - `grep -c "Governance-Panel" index.html` = 0 (Export-Kommentare Z. 2025/2041 auf „Leitungs-Oberfläche" umformuliert, ohne Dateinamen)
    - `grep -c "argus_governance_list\|argus_extend_photo_frist" index.html` = 0 (entfernte RPCs werden nicht mehr referenziert)
    - `grep -c "_closeFrist" index.html` = 0 und `grep -c "p_frist_tage" index.html` = 0
    - `grep -n "14 Tage\|30 Tage" index.html` → Treffer nur in historischen WHATS_NEW-Einträgen (kein Treffer in TRAIN_LESSONS, Modal-Texten, Kommentaren des Abschluss-Workflows)
    - `grep -c "72 Std" index.html` ≥ 3 (Abschluss-Confirm, closeTypModal-Hinweis, Toast)
    - Source-Assertion: exportChoiceModal enthält „dauerhafte" + „gelöscht" (D-04-Wortlaut)
    - JSC: angepasste pendingClose-Suite PASS (inkl. Legacy-fristTage-Fall); Syntax-Check OK
    - Kein natives alert/confirm/prompt (Datei-Grep wie in 04.8 sauber)
  </acceptance_criteria>
  <done>Governance restlos entfernt (inkl. veralteter Export-Kommentare), Abschluss läuft auf festen 72 h mit prominentem Export-Hinweis, Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: revoked-Re-Check (offline-sicher)</name>
  <files>index.html</files>
  <read_first>
    - index.html Zeilen 453–500 (exchangeCode + refreshJwtIfNeeded — Fetch-Muster, localStorage-Keys argus_jwt/argus_jwt_exp/argus_code), 390–425 (argus_unlocked-Verwaltung getUnlocked/setUnlocked), 3340–3360 (init-Block: refreshJwtIfNeeded-Aufruf, hasAccess-Logik), 3394–3400 (online-Handler)
    - .planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md (argus_check_code-Rückgabeformat {found, revoked})
  </read_first>
  <action>
    Re-Check des revoked-Flags (D-20, Diskretions-Entscheid Taktung: App-Start
    + online-Event, fire-and-forget):

    1. **Reine Entscheidungsfunktion `revokedDecision(resp)`** (JSC-testbar,
       direkt über checkRevokedCode platzieren): Eingabe = geparste RPC-Antwort
       oder null. Rückgabe 'lock' NUR bei eindeutigem `revoked === true`;
       'ok' bei revoked === false; 'ignore' bei null/Transportfehler/
       unerwartetem Format UND bei `found === false` (Kommentar: ein gelöschter
       Code ist KEIN Sperr-Signal — konservativ, um Fehlaussperrungen zu
       vermeiden; Sperren läuft ausschließlich über revoked=true).

    2. **`checkRevokedCode()`**: liest `argus_code` aus localStorage; kein Code
       oder `navigator.onLine === false` oder _training → sofort return (NIE
       blockieren, NIE offline raten). POST auf `rpc/argus_check_code` per
       fetch (Muster exchangeCode: apikey-Header, kurzes AbortSignal-Timeout
       ~6 s, damit ein hängender Request nichts aufhält). Antwort durch
       revokedDecision(); bei 'lock': localStorage-Keys argus_jwt,
       argus_jwt_exp, argus_code entfernen, argus_unlocked leeren,
       Realtime-Verbindung nicht weiter nutzen, Hinweis-Modal „Der Zugangscode
       wurde von der Leitung gesperrt. Bitte neuen Zugangscode anfordern."
       (confirmModal mit nur OK), danach in den gesperrten Zustand wechseln
       (gleicher Pfad wie fehlender Zugang im init — go auf den
       locked/Freischalt-Screen + render). Bei 'ok'/'ignore': nichts.

    3. **Verdrahtung:** im init-Block nach `await refreshJwtIfNeeded()` als
       fire-and-forget (`checkRevokedCode();` OHNE await — der Start darf
       darauf nicht warten); im online-Handler (Z. 3394 ff.) zusätzlich
       aufrufen. Kein Aufruf im Trainings-Sandbox.

    4. **exchangeCode-Fehlerpfad:** der neue Server-Fehlertext „Code wurde
       gesperrt" (Migration 0003) läuft durch die bestehende Fehleranzeige —
       prüfen, dass er beim Freischalten/Refresh als Klartext beim Nutzer
       ankommt (kein generisches Schlucken); falls refreshJwtIfNeeded den
       Fehler still ignoriert, dort beim Sperr-Text dieselbe Lock-Wirkung wie
       in Schritt 2 auslösen.

    5. **JSC-Test:** revokedDecision mit den Fällen null / Netzwerkfehler-Objekt /
       {found:true,revoked:true} / {found:true,revoked:false} / {found:false} /
       leeres Objekt / String-Müll → erwartete Entscheidungen; Syntax-Check.
  </action>
  <acceptance_criteria>
    - `grep -c "argus_check_code" index.html` ≥ 1 und `grep -c "checkRevokedCode" index.html` ≥ 3 (Definition + init + online-Handler)
    - Source-Assertion: in checkRevokedCode steht die offline-Guard (`navigator.onLine === false` → return) VOR jedem fetch; der init-Aufruf hat KEIN await
    - Source-Assertion: 'lock' entfernt argus_jwt, argus_jwt_exp, argus_code und leert argus_unlocked; 'ignore' bei found:false (Kommentar mit Begründung vorhanden)
    - JSC: revokedDecision-Suite (≥ 7 Fälle) PASS; Syntax-Check OK
    - Kein neues natives Dialog-Element; Modal-Text deutsch und knapp
  </acceptance_criteria>
  <done>Gesperrte Codes sperren das Gerät bei Start/Reconnect; offline und bei Transportfehlern passiert exakt nichts.</done>
</task>

<task type="auto">
  <name>Task 3: Drehbuch + Release v0.23.0 (UPDATE-Sheet)</name>
  <files>index.html, sw.js, version.json, CHANGELOG.md, docs/UPDATE_v0.23.html</files>
  <read_first>
    - index.html Zeilen 2281–2350 (TRAIN_LESSONS: Lektionen „Einsatz abschließen" ~Z. 2303 und ~Z. 2344, Foto-Lektion ~Z. 2320), 106 (APP_VERSION), 110–135 (UPDATE_SHEETS/WHATS_NEW-Struktur)
    - docs/UPDATE_v0.20.html ODER docs/UPDATE_v0.21.html (Sheet-Aufbau/Optik als Vorlage)
    - sw.js Zeilen 1–30 (CACHE_NAME, PRECACHE_URLS)
    - CHANGELOG.md Kopf (Eintrags-Format)
  </read_first>
  <action>
    **Drehbuch (D-16, DAUERREGEL):**
    1. Lektion „Einsatz abschließen" (~Z. 2344): „(Übung 14 Tage, Einsatz 30
       Tage)" ersetzen — neue Aussage: Daten löschen sich 72 Std nach
       Abschluss (Fotos ebenso, endgültig); der Übergabe-Export beim Abschluss
       ist der einzige dauerhafte Weg in die Einsatzdokumentation; Typ
       Übung/Einsatz bleibt als Merkmal.
    2. Lektion ~Z. 2303 („nach einer Frist automatisch gelöscht") → „72 Std
       nach Abschluss automatisch gelöscht".
    3. Foto-Lektion ~Z. 2320 prüfen: „72 Std nach Abschluss" stimmt weiterhin —
       unverändert lassen (Beleg in SUMMARY).
    4. Gesamtes TRAIN_LESSONS auf weitere Governance-/Fristen-Bezüge greppen
       (governance, 14, 30, Frist) und konsistent machen.

    **Release v0.23.0 (Release-Konvention):**
    5. APP_VERSION '0.23.0' (Z. 106) = version.json; sw.js CACHE_NAME
       'ccp-shell-v40' → 'ccp-shell-v41'. Die Leitungs-Seite NICHT in
       PRECACHE_URLS aufnehmen.
    6. CHANGELOG.md-Eintrag v0.23.0: 72-h-Löschfrist (einheitlich, Wahl
       entfällt), Übergabe-Export als dauerhafter Weg, Governance aus der
       Feld-App entfernt (jetzt separate Leitungs-Oberfläche), Code-Sperre
       wirkt am Gerät (Re-Check). Keine internen Begriffe, Dateiname der
       Leitungs-Seite NICHT nennen (nur „Leitungs-Oberfläche", D-06).
    7. WHATS_NEW['0.23.0']: 2–3 knappe, nutzerorientierte Punkte (72-h-Frist +
       Export-Bedeutung; Foto-Governance aus der App in die Leitungs-Oberfläche
       umgezogen; gesperrte Codes melden sich beim Start).
    8. docs/UPDATE_v0.23.html im Stil der bisherigen Sheets (ARGUS-Optik,
       gleiche Struktur wie UPDATE_v0.20/21): die 72-h-Umstellung IST
       nutzerrelevant. Inhalte: einheitliche 72-h-Löschfrist, Export =
       einziger dauerhafter Weg, Hinweis Fotos endgültig nach 72 Std.
       ZUSÄTZLICH ein deutlicher „Bitte App aktualisieren"-Hinweis: Geräte
       mit v0.22.0 zeigen beim Abschluss noch die alten Fristen („Löschung
       nach 14/30 Tagen"), gelöscht wird aber bereits einheitlich nach
       72 Std — die Anzeige stimmt erst ab v0.23. KEIN Link/Dateiname der
       Leitungs-Seite (Sheet ist öffentlich, D-06); Stichproben-Grep ohne
       „PolG"/„DSFA"/„Auftragsverarbeit".
       UPDATE_SHEETS['0.23.0'] = 'docs/UPDATE_v0.23.html' eintragen.
    9. Syntax-Check (JSC, beide Inline-Blöcke) nach letztem Edit. Commits nach
       Konvention (`feat(04.9-03): …` je Task bzw. Release-Commit
       `feat(v0.23.0): …`) mit Co-Authored-By-Zeile; NICHT pushen.
       Commit-Messages und SUMMARY OHNE konkreten Leitungs-Seiten-Dateinamen.
  </action>
  <acceptance_criteria>
    - `grep -n "14 Tage\|30 Tage" index.html` → 0 Treffer in TRAIN_LESSONS (Bereich Z. 2281 ff.)
    - `grep -c "APP_VERSION = '0.23.0'" index.html` = 1 und version.json enthält "0.23.0"; `grep -c "ccp-shell-v41" sw.js` ≥ 1
    - `grep -Ec "leitung-[0-9a-f]{10}" index.html sw.js docs/UPDATE_v0.23.html CHANGELOG.md` → 0 Treffer je Datei (Leitungs-Seite nirgends genannt/verlinkt — Muster-Grep, der konkrete Name steht in keinem committeten Text)
    - docs/UPDATE_v0.23.html enthält den „Bitte App aktualisieren"-Hinweis zur Fristen-Anzeige von v0.22.0 (`grep -ci "aktualisieren" docs/UPDATE_v0.23.html` ≥ 1)
    - WHATS_NEW['0.23.0'] und UPDATE_SHEETS['0.23.0'] existieren; docs/UPDATE_v0.23.html existiert und enthält noindex-Meta wie die bisherigen Sheets (Vorlage prüfen) sowie 0 Treffer für interne Begriffe
    - CHANGELOG.md hat einen `## v0.23.0`-Eintrag
    - JSC-Syntax-Check OK
  </acceptance_criteria>
  <done>Drehbuch konsistent mit 72-h-Modell, Release v0.23.0 vollständig nach Konvention inkl. UPDATE-Sheet mit Update-Aufforderung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Feld-Gerät → argus_check_code | anon-aufrufbarer Check; Gerät vertraut nur eindeutigem revoked=true |
| Gerät offline → lokale Daten | Re-Check darf Local-first nie brechen |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigation |
|-----------|-----------|-----------|-------------|-----------------|
| T-4.9-12 | DoS (Selbst-Aussperrung) | checkRevokedCode | mitigate | 'lock' nur bei explizitem revoked===true; found:false/Fehler → 'ignore'; JSC-getestet |
| T-4.9-13 | Elevation | Gerät mit gültigem JWT nach Sperrung | accept (Rest-Risiko) | Re-Check bei Start/Reconnect verkürzt das Fenster; vollständige Lösung erst Phase 7 (Identitäten) |
| T-4.9-14 | Information Disclosure | UPDATE-Sheet/CHANGELOG (öffentlich) | mitigate | keine internen Begriffe, kein Leitungs-Seiten-Dateiname (Muster-Grep leitung-[0-9a-f]{10} = 0) |
| T-4.9-15 | Tampering | Offline-Abschluss-Queue (Legacy-Payload) | mitigate | pendingCloseRead toleriert fristTage, Server ignoriert p_frist_tage — kein abweichender Fristen-Pfad mehr |
</threat_model>

<verification>
- Alle Grep-Assertions der drei Tasks grün; JSC-Suiten PASS.
- App-Start ohne Netz im Code-Pfad nachvollziehbar nicht blockiert (offline-Guards vor jedem fetch des Re-Checks).
- Release-Artefakte konsistent (Version, Cache, Sheet, WHATS_NEW).
- Konkreter Leitungs-Seiten-Dateiname in keiner geänderten/committeten Datei (Muster-Grep).
</verification>

<success_criteria>
- D-14, D-15, D-16, D-04, D-20 in der Feld-App umgesetzt; D-02/D-03 in allen Nutzertexten konsistent (72 h).
- Live-Verifikation am echten iPhone (Update-Banner, Abschluss-Strecke, Sperr-Test) bleibt offener Owner-Punkt — in SUMMARY führen.
</success_criteria>

<output>
Create `.planning/phases/04.9-governance-oberflaeche/04.9-03-SUMMARY.md` when done.
GEHEIMHALTUNGS-REGEL beachten: SUMMARY ohne konkreten Leitungs-Seiten-Dateinamen
(nur „Leitungs-Oberfläche" bzw. Muster docs/leitung-<suffix>.html).
</output>

## Artifacts this phase plan produces

- Funktionen: `checkRevokedCode()`, `revokedDecision(resp)` (index.html)
- ENTFERNT: View `governance`, `vGovernance`, `govGroupCard`, `govLogSection`, `loadGovernanceList`, `govFmt`, `fristStatus`, Handler opengovernance/govreload/govphoto/govextend, Variablen `_govList/_govErr/_govLog/_govLogErr/_closeFrist`; veraltete „Governance-Panel"-Export-Kommentare umformuliert auf „Leitungs-Oberfläche"
- Geänderte Workflows: `closeEinsatzWorkflow`/`closeTypModal`/`exportChoiceModal`/`finalizeCloseEinsatz`/`pendingCloseRead`/`flushPendingClose` (72-h-Modell, Queue-Payload {ccpId, typ, ts})
- Release: APP_VERSION 0.23.0, SW-Cache ccp-shell-v41, `WHATS_NEW['0.23.0']`, `UPDATE_SHEETS['0.23.0']`, Datei docs/UPDATE_v0.23.html (inkl. „Bitte App aktualisieren"-Hinweis wegen v0.22.0-Fristen-Anzeige)
