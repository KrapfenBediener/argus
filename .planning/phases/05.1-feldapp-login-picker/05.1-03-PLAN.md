---
phase: 05.1-feldapp-login-picker
plan: 03
type: execute
wave: 2
depends_on:
  - 05.1-01
files_modified:
  - index.html
autonomous: false
requirements: []
must_haves:
  truths:
    - "Ein einzelnes Freischalt-Feld erkennt automatisch: erst argus_exchange_code (Sammel/Gast/Master), bei Misserfolg argus_exchange_person_code — wie die Leitungs-Seiten-doLogin"
    - "Ein gültiger Person-Code schaltet die Sitzung frei und trägt USBNK + Rolle + Präsidiums-Scope; alle nachfolgenden patients/ccps-Schreibvorgänge werden serverseitig personenscharf protokolliert"
    - "Bestehende Sammel-/Gast-/Master-Codes und bereits freigeschaltete Geräte verhalten sich unverändert (Koexistenz)"
    - "Der Picker zeigt zwei Top-Level-Reiter Echtbetrieb/Schulung mit Default Echtbetrieb; ein Person-Token sieht sein eigenes Präsidium (Echt-Reiter) und dessen schulungs_zwilling_id-Twin (Schulungs-Reiter)"
    - "Im Schulungsmodus ist ein unverkennbarer, permanenter SCHULUNG-Banner sichtbar; ein Wechsel Echt<->Schulung erfordert eine bewusste Bestätigung (kein versehentliches Mischen)"
    - "Offline/local-first bleibt intakt: JWT lokal + stille Verlängerung; der zuletzt gewählte Reiter/Scope wird offline gemerkt; tacSTART und die Triage-Logik bleiben unangetastet"
  artifacts:
    - path: "index.html"
      provides: "Person-Login-Auto-Erkennung, Echt/Schulung-Zwei-Reiter-Picker, USBNK-Sitzung, verstärkter SCHULUNG-Banner, Scope-Persistenz"
      contains: "argus_exchange_person_code"
  key_links:
    - from: "doUnlock()/exchangeCode()"
      to: "argus_exchange_person_code"
      via: "Auto-Erkennung: Fallback nach argus_exchange_code"
      pattern: "argus_exchange_person_code"
    - from: "Picker-Reiter Schulung"
      to: "schulungs_zwilling_id-Twin"
      via: "Person-Token-Scope deckt Präsidium + Twin"
      pattern: "schulungs_zwilling"
    - from: "Person-Login"
      to: "audit_log (server)"
      via: "USBNK im JWT → Trigger aus Plan 01 schreiben personenscharf"
      pattern: "usbnk"
---

<objective>
Die Feld-App (`index.html`) auf das Stufe-1-Identitätsmodell umstellen: (1) Pro-Person-Login per EINEM Freischalt-Feld mit Auto-Erkennung — erst `argus_exchange_code`, dann `argus_exchange_person_code` (Muster der Leitungs-Seiten-`doLogin`, D5.1-02); die Sitzung trägt USBNK + Rolle + Scope. (2) Echt/Schulung-Picker als Top-Level Zwei-Reiter (Default Echtbetrieb, unverkennbarer SCHULUNG-Banner; Person-Token = eigenes Präsidium + `schulungs_zwilling_id`-Twin; Sammelcodes behalten das heutige Modell; D5.1-03). Bestehende Geräte/Codes bleiben unverändert; Offline/local-first und tacSTART bleiben unangetastet.

Purpose: Erst mit der USBNK-tragenden Feld-Sitzung wird das in Plan 01 verdrahtete T4-Protokoll für Feld-Aktionen voll personenscharf (D-08 aus Phase 5). Der Picker verhindert, dass Trainingsdaten je in einen echten MANV geraten — bei maximaler Feldtauglichkeit (großflächig, einhändig, eindeutig).

Output: aktualisierte `index.html` (KEIN Release in diesem Plan — Versions-Bump/SW/Drehbuch macht Plan 04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-feldapp-login-picker/05.1-CONTEXT.md
@.planning/PHASE7-ROLLENMODELL-DRAFT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Person-Login-Auto-Erkennung im Freischalt-Pfad (USBNK-Sitzung)</name>
  <files>index.html</files>
  <read_first>
    - index.html — exchangeCode() (~Z.601–630: ruft argus_exchange_code, speichert argus_jwt/argus_jwt_exp/argus_code, gibt {praesidium_id,is_master,...} zurück), doUnlock() (~Z.721–742), refreshJwtIfNeeded() (~Z.635–650: stille Verlängerung mit argus_code), applyRevokedLock()/checkRevokedCode() (~Z.677–718: löscht argus_jwt/argus_code etc.), getUnlocked()/addUnlock()/isUnlocked() (~Z.538–562: argus_unlocked-Liste), die init-Pfade (~Z.3420–3517: _isMaster/_praesidiumId-Restore, hasAccess-Gate).
    - docs/leitung-*.html — doLogin() Abschnitt 3 (argus_exchange_person_code: liest data3.argus_role/usbnk/praesidium_id; speichert Sitzung; Rollen-Mapping master/admin/flz-normal) als zu spiegelndes Muster (sessionStorage dort, localStorage hier).
    - supabase/migrations/0015_phase5_identitaeten.sql — argus_exchange_person_code-Rückgabe ({jwt,exp,usbnk,argus_role,praesidium_id,praesidium_name,ttl_seconds}) und der CR-02-Guard in argus_exchange_code (Person-Codes werden dort abgelehnt → der erste Versuch scheitert für Person-Codes „sauber").
  </read_first>
  <action>
    Erweitere den Freischalt-Pfad um eine Auto-Erkennung, ohne die bestehende Sammel-/Gast-/Master-Strecke zu verändern (D5.1-02, Koexistenz). Konkret: lege eine neue Funktion `exchangePersonCode(clean)` an, die argus_exchange_person_code per fetch aufruft (gleiches Header-Muster wie exchangeCode), bei Erfolg `argus_jwt`/`argus_jwt_exp`/`argus_code` setzt UND zusätzlich die Stufe-1-Sitzungsfelder lokal persistiert: neue localStorage-Keys `argus_usbnk`, `argus_role`, `argus_person_praesidium` (Echt-Präsidium des Tokens). Setze entsprechende globale Variablen (z. B. `_usbnk`, `_argusRole`). Gib ein Ergebnis-Objekt {usbnk, argus_role, praesidium_id, praesidium_name} zurück; bei Fehler null (Klartext in _lastExchangeError).

    Passe doUnlock() an: nach einem fehlgeschlagenen exchangeCode() (kein JWT) — und NUR wenn der Fehler kein „Code wurde gesperrt" ist (gesperrt bleibt gesperrt) — einen zweiten Versuch mit exchangePersonCode() unternehmen. Bei Person-Erfolg: argus_verified setzen; das Präsidium des Person-Tokens via addUnlock(praesidium_id) freischalten (dauerhaft, kein ttl) und _praesidiumId/_praesidiumName setzen sowie refreshSchulungFlag() aufrufen; Toast „Angemeldet als USBNK <usbnk>" (kein Klarname — D-02). Person-Tokens mit role='master'/'flz' (praesidium_id=null) NICHT als Geräte-Master behandeln: für 'normal'/'admin' steht praesidium_id, für 'master'/'flz' zeige einen Hinweis-Toast „Dieser Zugang ist für die Leitungs-Seite" und brich ab (die Feld-App ist für normale Feld-Erfassung; präsidienübergreifende FLZ-Lage ist Phase 6). Die `checkinvite`/init-#code-Pfade entsprechend NICHT verändern (sie bleiben Sammelcode-only — Person-Codes kommen über das Freischalt-Feld).

    Stelle die stille Verlängerung sicher: refreshJwtIfNeeded() nutzt argus_code — bei einem Person-Code würde der erste exchangeCode()-Versuch scheitern (CR-02). Erweitere refreshJwtIfNeeded() so, dass bei gesetztem argus_role (Person-Sitzung) direkt exchangePersonCode(argus_code) für die Verlängerung genutzt wird; sonst wie bisher exchangeCode(). applyRevokedLock() zusätzlich um das Entfernen von `argus_usbnk`/`argus_role`/`argus_person_praesidium` und das Rücksetzen von `_usbnk`/`_argusRole` erweitern. Offline: keine Verlängerung erzwingen (navigator.onLine-Guard bleibt) — local-first unberührt.
  </action>
  <verify>
    <automated>grep -q "argus_exchange_person_code" index.html && grep -q "function exchangePersonCode" index.html && grep -q "argus_usbnk" index.html && grep -q "argus_role" index.html && node --check index.html 2>/dev/null; if [ $? -ne 0 ]; then BODY=$(awk '/<script>/{f=1;next}/<\/script>/{f=0}f' index.html); echo "$BODY" > /tmp/argus_app.js && node --check /tmp/argus_app.js; fi && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - exchangePersonCode() ruft argus_exchange_person_code, persistiert JWT + argus_usbnk/argus_role/argus_person_praesidium, setzt _usbnk/_argusRole.
    - doUnlock() macht den Person-Fallback NUR nach gescheitertem Sammel-Exchange und nicht bei „gesperrt"; master/flz-Person-Tokens werden im Feld höflich abgewiesen (Leitungs-Seiten-Hinweis), normal/admin schalten ihr Präsidium frei.
    - refreshJwtIfNeeded() verlängert Person-Sitzungen über exchangePersonCode(); applyRevokedLock() räumt die neuen Keys mit.
    - Bestehende Sammel-/Gast-/Master-/#code-/#inv-Strecken unverändert; tacSTART/Triage nicht berührt; JS-Syntax valide.
  </acceptance_criteria>
  <done>Auto-Erkennung funktioniert (Sammel zuerst, Person als Fallback); USBNK-Sitzung wird persistiert + still verlängert; Syntax-Check grün.</done>
</task>

<task type="auto">
  <name>Task 2: Echt/Schulung-Zwei-Reiter-Picker + verstärkter SCHULUNG-Banner + Scope-Persistenz</name>
  <files>index.html</files>
  <read_first>
    - index.html — vPraesidium() (~Z.809–841: Präsidiumsliste, isSchul-Icon 🎓/🏗, unlock-input-Card), der Banner in der Topbar (~Z.1741: `🎓 SCHULUNGSUMGEBUNG`-Markierung, abhängig von _praesidiumSchulung && !_training), refreshSchulungFlag() (~Z.417–429: _praesidiumSchulung + localStorage argus_praesidium_schulung), selectpraesidium/changepraesidium-Handler (~Z.2902–2920), die Start-View-Schulungs-Buttons (~Z.2366–2374).
    - supabase/migrations/0015_phase5_identitaeten.sql — Abschnitt 2 (praesidien.schulungs_zwilling_id: Echt-Präsidium → sein Schulungs-Pendant).
    - .planning/PHASE7-ROLLENMODELL-DRAFT.md — Rollen-Tabelle (normaler Token = Echt + Schulungs-Präsidium; Picker zwei Reiter, MANV: schnelle Auswahl, aktiver Modus immer unverkennbar).
  </read_first>
  <action>
    Baue in vPraesidium() einen Top-Level Zwei-Reiter „Echtbetrieb / Schulung" oberhalb der Präsidiumsliste (großflächige, einhändig bedienbare Tabs; hoher Kontrast; Default Echtbetrieb). Persistiere den aktiven Reiter in `argus_picker_mode` (localStorage, offline gemerkt; Default 'echt'). Lade die Präsidienliste so, dass das schulungs_zwilling_id-Feld mitgelesen wird (`select id,name,schulung,schulungs_zwilling_id`).

    Reiter-Logik:
    - Echtbetrieb-Reiter: zeigt die nicht-Schulungs-Präsidien (schulung=false) im Scope des Geräts/Tokens. Für eine Person-Sitzung (argus_person_praesidium gesetzt): nur das eigene Echt-Präsidium. Für Sammelcode-/Master-Geräte: das heutige Modell (alle freigeschalteten bzw. alle für Master).
    - Schulungs-Reiter: zeigt die Schulungs-Präsidien. Für eine Person-Sitzung: das schulungs_zwilling_id-Twin des eigenen Echt-Präsidiums (über die geladene Liste auflösen). Sammelcodes behalten ihr heutiges Modell (ein Schulungs-Code → Schulungs-Präsidium direkt) — nicht umbauen.
    Wenn für einen Reiter im aktuellen Scope nichts vorhanden ist, einen knappen Hinweis statt einer leeren Liste zeigen.

    Verstärke den SCHULUNG-Banner: wenn das aktive Präsidium ein Schulungs-Präsidium ist (_praesidiumSchulung && !_training), muss der Banner permanent, vollbreit und unverkennbar sein (kräftige Warn-/Prio-Farbe, großer Text „🎓 SCHULUNGSUMGEBUNG — keine echten Patientendaten"). Baue außerdem in den Wechsel zwischen den Modi eine bewusste Bestätigung ein: Wechselt der Nutzer von einem aktiven Echt-Präsidium in den Schulungs-Reiter (oder umgekehrt) und wählt dort ein Präsidium, das den Modus ändert, zeige vorher confirmModal mit klarer Warnung („Du wechselst von ECHTBETRIEB zu SCHULUNG …" bzw. umgekehrt) — bestätigt der Nutzer nicht, kein Wechsel. Das verhindert das versehentliche Mischen (D5.1-03 / CONTEXT-Specifics-Punkt 2). Im laufenden MANV bleibt der Wechsel schnell (ein Tab-Tap + eine Bestätigung), aber der aktive Modus ist IMMER eindeutig.

    Picker und Banner dürfen die unter Stress bewährte Einfachheit nicht verkomplizieren (CONTEXT-Specifics-Punkt 1): der häufige Fall (ein einzelner Echt-Einsatz, Echt-Reiter Default) bleibt so schlicht wie heute — der Schulungs-Reiter ist die sichtbare Ausnahme. Nur In-App-Modals (confirmModal), keine nativen Dialoge. tacSTART/Triage NICHT anfassen. Offline: Reiter-Auswahl + zuletzt bekannter Scope aus localStorage; bei fehlendem Netz keine Block-Zustände.
  </action>
  <verify>
    <automated>grep -q "argus_picker_mode" index.html && grep -q "schulungs_zwilling_id" index.html && grep -qi "SCHULUNGSUMGEBUNG" index.html && grep -q "confirmModal" index.html && node --check index.html 2>/dev/null; if [ $? -ne 0 ]; then BODY=$(awk '/<script>/{f=1;next}/<\/script>/{f=0}f' index.html); echo "$BODY" > /tmp/argus_app2.js && node --check /tmp/argus_app2.js; fi && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Zwei-Reiter Echt/Schulung, Default Echt, in argus_picker_mode persistiert; Präsidienliste lädt schulungs_zwilling_id.
    - Person-Sitzung: Echt-Reiter = eigenes Präsidium, Schulungs-Reiter = dessen Twin; Sammelcodes/Master unverändert.
    - SCHULUNG-Banner permanent, vollbreit, unverkennbar; Modus-Wechsel mit bewusster confirmModal-Bestätigung (kein Mischen).
    - Großflächig/einhändig/hoher Kontrast; nur In-App-Modals; offline gemerkt; tacSTART/Triage unberührt; JS-Syntax valide.
  </acceptance_criteria>
  <done>Picker mit zwei Reitern + verstärktem Banner + Wechsel-Bestätigung steht; Scope offline gemerkt; Syntax-Check grün.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Browser-Roundtrip Person-Login + Echt/Schulung-Picker + personenscharfes Audit</name>
  <action>Checkpoint — siehe <what-built> / <how-to-verify> / <resume-signal>. Pausiert für menschliche Verifikation; nicht auto-advance.</action>
  <what-built>
    Die Feld-App hat jetzt Person-Login-Auto-Erkennung (ein Freischalt-Feld) und einen Echt/Schulung-Zwei-Reiter-Picker mit verstärktem SCHULUNG-Banner und Wechsel-Bestätigung. Die Sitzung trägt USBNK; Feld-Aktionen werden über die Plan-01-Trigger personenscharf protokolliert. (Kein Release in diesem Plan — Version/SW/Drehbuch kommt in Plan 04.)
  </what-built>
  <how-to-verify>
    Die App lokal/auf einem Testgerät gegen das Live-Backend öffnen (statischer Server reicht; auf dem iPhone via HTTPS-Adresse). Voraussetzung: ein 'normal'-Person-Code aus der Leitungs-Seite (Plan 02) oder per RPC (Plan 01) für ein Präsidium, das ein schulungs_zwilling_id-Twin hat.
    1. Sammelcode-Koexistenz: einen bestehenden Sammel-/Gast-/Master-Code im Freischalt-Feld eingeben → verhält sich exakt wie bisher (kein Regress).
    2. Person-Login: den 'normal'-Person-Code im selben Feld eingeben → Auto-Erkennung schaltet das Präsidium frei, Toast nennt die USBNK (kein Klarname).
    3. Picker: zwei Reiter Echtbetrieb/Schulung, Default Echtbetrieb; Echt-Reiter zeigt das eigene Präsidium, Schulungs-Reiter den Twin. Wechsel Echt→Schulung erfordert eine bewusste Bestätigung; im Schulungsmodus ist der SCHULUNG-Banner permanent und unübersehbar.
    4. Personenscharfes Audit: im Echt-Modus eine Patienten-Aktion ausführen (anlegen, Kategorie wechseln, transportfertig, auschecken, Foto) → in der Leitungs-Seite (Audit-Log) bzw. per REST je ein audit_log-Eintrag mit der USBNK und der korrekten action.
    5. Offline: Netz trennen → App arbeitet weiter (lokaler Modus), zuletzt gewählter Reiter/Scope bleibt; Netz wieder an → stille JWT-Verlängerung der Person-Sitzung, kein Re-Login nötig.
    6. tacSTART unverändert: eine Erfassung über den geführten tacSTART-Walk durchspielen → identisch zu heute.
  </how-to-verify>
  <resume-signal>Schreibe „approved" (mit kurzer Bestätigung: Koexistenz ok, Person-Login + USBNK-Audit ok, Picker/Banner/Wechsel-Bestätigung ok, offline ok, tacSTART unverändert) oder beschreibe die Abweichungen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Freischalt-Feld → Exchange-RPC | Code-Eingabe entscheidet Sammel- vs. Person-Strecke; CR-02 muss jede Verwechslung abweisen |
| Echt- vs. Schulungs-Scope | Trainingsdaten dürfen nie in einen echten MANV gelangen |
| Lokale Sitzung (JWT/USBNK) → Server-Audit | USBNK kommt aus dem signierten JWT, nicht aus Client-Eingaben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-053-01 | Elevation of Privilege | Person-Code über Feld-Sammel-Exchange (CR-02) | mitigate | Reihenfolge: argus_exchange_code zuerst — der CR-02-Guard (0015) lehnt is_person-Codes dort ab; der Person-Fallback nutzt argus_exchange_person_code, der seinerseits Nicht-Person-Codes abweist; beide Pfade in 0015 unverändert, dieser Plan ändert KEINE RPC |
| T-053-02 | Tampering | versehentliche Echt/Schulung-Vermischung | mitigate | Harte Scope-Trennung über schulungs_zwilling_id; bewusste confirmModal-Bestätigung beim Moduswechsel; permanenter, unverkennbarer SCHULUNG-Banner; Default Echtbetrieb |
| T-053-03 | Spoofing | USBNK fälschen für falsche Protokoll-Zuordnung | mitigate | USBNK steht im signierten JWT-Claim (vom Server ausgestellt); die Trigger (Plan 01) lesen argus_usbnk() server-seitig — Client kann den Wert nicht setzen |
| T-053-04 | Elevation of Privilege | master/flz-Person-Token im Feld als Geräte-Master | mitigate | Feld weist master/flz-Person-Tokens (praesidium_id=null) höflich ab (Leitungs-Seiten-Hinweis); nur normal/admin schalten Feld-Scope frei; kein _isMaster-Setzen aus Person-Tokens |
| T-053-05 | Denial of Service | Offline blockiert Arbeiten | mitigate | local-first bleibt: navigator.onLine-Guards in Verlängerung/Re-Check; Reiter/Scope lokal gemerkt; keine neuen blockierenden Netz-Pfade |
| T-053-SC | — | keine Package-Installs in diesem Plan | accept | Reine index.html-Edits, keine npm/pip/cargo-Installs |
</threat_model>

<verification>
- Sammelcode-Koexistenz unverändert; Person-Login per Auto-Erkennung; USBNK in den audit_log-Einträgen.
- Picker zwei Reiter (Default Echt) + Twin-Auflösung; unverkennbarer Banner + Wechsel-Bestätigung.
- Offline/local-first + stille Verlängerung intakt; tacSTART unverändert.
</verification>

<success_criteria>
- Feld-App nutzt die Stufe-1-Identität (Ein-Feld-Auto-Login + USBNK-Sitzung), ohne Sammelcode-Strecken oder tacSTART zu verändern.
- Echt/Schulung-Picker mit harter Trennung, unverkennbarem Schulungsmodus und bewusstem Wechsel.
- Offline-Weiterarbeit + stille JWT-Verlängerung bleiben; nur In-App-Modals; D-02 (kein Klarname) gewahrt.
- KEIN Release in diesem Plan (index.html-Funktion; Version/SW/Drehbuch macht Plan 04).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03)
- `index.html` — exchangePersonCode() + doUnlock-Auto-Erkennung + USBNK-Sitzung (argus_usbnk/argus_role/argus_person_praesidium), Echt/Schulung-Zwei-Reiter-Picker (argus_picker_mode), schulungs_zwilling_id-Twin-Auflösung, verstärkter SCHULUNG-Banner, bewusste Moduswechsel-Bestätigung, stille Verlängerung für Person-Sitzungen.
</artifacts>

<output>
Create `.planning/phases/05.1-feldapp-login-picker/05.1-03-SUMMARY.md` when done
</output>
