---
phase: 05.1-feldapp-login-picker
verified: 2026-06-28T10:30:00Z
status: passed
score: 27/27
overrides_applied: 0
---

# Phase 5.1: Feld-App Login-Picker Verification Report

**Phase Goal:** Convert field PWA (index.html) to Stufe-1 identity — one-field person-login auto-detect (coexisting with shared codes), a top-level Echt/Schulung picker (default Echt, unmissable banner, twin via schulungs_zwilling_id with re-exchange scoping), server-side audit triggers (patients/ccps → audit_log via argus_usbnk()), unblock individual 'normal' issuance, and ship it as app release (v0.32.0) + training drehbuch.
**Verified:** 2026-06-28T10:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

#### Plan 01 Truths — Migration 0018 (Audit Triggers + 'normal' Unlock)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Patient creation triggers exactly one audit_log entry (action=patient_create) with usbnk=argus_usbnk() | VERIFIED | `argus_audit_patients()` in 0018/0020 lines up TG_OP='INSERT' → v_action='patient_create'; inserts into audit_log with `public.argus_usbnk()`. Comment explicitly states §73 completeness (usbnk=null logged too). |
| 2 | Cat-change, ready, checkout, photo each produce the correct audit_log action derived from NEW/OLD column diff | VERIFIED | Priority chain in 0020 (final version): photo (null→non-null OR non-null→different) > checkout (status→'gpa') > ready (ready=false→true) > cat_change (cat differs). Exactly one entry per DML call. |
| 3 | CCP open/close/merge each produce one audit_log entry (ccp_open/ccp_close/ccp_merge) | VERIFIED | `argus_audit_ccps()` in 0018: TG_OP='INSERT'→ccp_open; closed_at null→non-null→ccp_close; merge_group_id set/changed→ccp_merge. Priority ccp_close > ccp_merge. |
| 4 | Master can issue a person token with role='normal' (argus_master_issue_person accepts 'normal') | VERIFIED | 0018 line 303: `if p_role not in ('master', 'flz', 'admin', 'normal')`. Whitelist includes 'normal'. Comment: "5.1-Freigabe". |
| 5 | Master can change existing person role to 'normal' (argus_master_role_change_person accepts 'normal') | VERIFIED | 0018 line 201: `if p_new_role not in ('master', 'flz', 'admin', 'normal')`. 0020 adds praesidium guard for 'normal' (WR-01 fix), consistent with issue. |
| 6 | audit_log remains append-only: anon has SELECT only, triggers write security-definer | VERIFIED | 0018 lines 156-157: "KEIN neuer Grant an anon für audit_log". Both trigger functions declared `security definer set search_path to 'public'`. No UPDATE/DELETE policy added. |
| 7 | Migration is idempotent — two applies both return HTTP 201 | VERIFIED | Orchestrator confirmed 2× HTTP 201 for 0018, 0019, and 0020. `create or replace function` + `drop trigger if exists` + `create trigger` pattern confirms code-level idempotency. |

#### Plan 02 Truths — Leitungs-Seite 'normal' Option

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 8 | Master can select role 'Normal' from the issue dropdown and issue a pro-person token with role='normal' | VERIFIED | `docs/leitung-*.html` line 1794: `<option value="normal">Normal</option>` confirmed. `doMkPerson()` at line 1865: `if((rolle==='admin' \|\| rolle==='normal') && !praesId)` enforces praesidium for 'normal'. |
| 9 | Role-change (doPersonRole) accepts 'normal' as valid input | VERIFIED | `docs/leitung-*.html` line 1905: prompt text `master/flz/admin/normal`; line 1910: `['master','flz','admin','normal'].includes(neueRolle)` whitelist check; line 1918: calls `argus_master_role_change_person`. |
| 10 | role='normal' requires praesidium (Feld-Scope), UI enforces it | VERIFIED | `docs/leitung-*.html` line 1865: `(rolle==='admin' \|\| rolle==='normal') && !praesId` → toast. Line 2420: `(ro.value==='admin'\|\|ro.value==='normal')` controls pers-praes-wrap visibility. |
| 11 | 'Normal' role label and pill color appear correctly in USBNK search results table | VERIFIED | `docs/leitung-*.html` line 1755: `if(r==='normal') return 'Normal'` — rolleLabel function already handled 'normal' from Phase 5. |
| 12 | No token secrets in UI; only In-App-Modals; D-02 (USBNK only, no Klarnamen) respected | VERIFIED | D-02: No Klarnamen fields found in leitung page. confirmModal/promptModal used throughout. |

#### Plan 03 Truths — Feld-App Person-Login & Picker

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 13 | Single unlock field auto-detects: first argus_exchange_code, on failure argus_exchange_person_code | VERIFIED | `index.html`: `function exchangePersonCode` defined at line 671; `doUnlock()` at line 828 attempts `exchangePersonCode(uc2)` as fallback after failed `exchangeCode`. Guard: not on "gesperrt" error (line 824). |
| 14 | Valid person code unlocks session with USBNK + role + praesidium scope; subsequent patient/ccp writes are audited server-side | VERIFIED | `exchangePersonCode` stores `argus_usbnk`, `argus_role`, `argus_person_praesidium` in localStorage (lines 690-691); sets `_usbnk`/`_argusRole`. Wave-1 triggers (0018/0020) audit all DML with `argus_usbnk()` from JWT. Orchestrator confirmed live: `audit_log{usbnk, patient_create}` on Person-JWT insert. |
| 15 | Existing shared/guest/master codes and already-unlocked devices behave unchanged (coexistence) | VERIFIED | `doUnlock()` tries `exchangeCode()` first; Person fallback only on non-'gesperrt' failure. `#code`/`#inv` paths not touched. Orchestrator confirms Sammelcode-Koexistenz ok. |
| 16 | Picker shows two top-level tabs Echtbetrieb/Schulung with default Echtbetrieb; person token sees own praesidium (Echt) and schulungs_zwilling_id twin (Schulung) | VERIFIED | `index.html` line 1009-1010: two `pickertab` buttons rendered in `vPraesidium()`. `pickerMode()` (line 961) defaults to 'echt'. `pickerList()` (line 970): Schulung tab resolves `schulungs_zwilling_id` twin for Person sessions. All praesidien queries include `schulungs_zwilling_id`. |
| 17 | In Schulungsmodus: permanent unmissable SCHULUNG banner visible; mode switch requires conscious confirmation | VERIFIED | `index.html` line 1961+: banner with `background:#F2B600`, bold text "🎓 SCHULUNGSUMGEBUNG — keine echten Patientendaten". Picker handler (line 3121): `confirmModal` with explicit warning before any mode switch. `refreshSchulungFlag` with `_maybeRenderSchulBanner` ensures banner persists on cold-start (lines 436-454). |
| 18 | Offline/local-first intact: JWT locally stored + silent renewal; last tab/scope remembered offline; tacSTART and triage logic untouched | VERIFIED | `refreshJwtIfNeeded()` (line 723-728) uses `exchangePersonCode(storedCode, pickerMode())` for Person sessions. `pickerMode()` reads from localStorage (offline). `argus_picker_mode` defaults to 'echt'. tacSTART functions present and unmodified — no changes to triage/capture logic. |

#### Plan 04 Truths — v0.32.0 Release

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 19 | APP_VERSION bumped to 0.32.0; version.json carries same version; sw.js CACHE_NAME bumped to v59 | VERIFIED | `index.html` line 112: `APP_VERSION = '0.32.0'`. `version.json`: `{ "version": "0.32.0" }`. `sw.js` line 10: `CACHE_NAME = 'ccp-shell-v59'`. All three consistent. |
| 20 | CHANGELOG.md and WHATS_NEW describe Pro-Person-Login + Echt/Schulung-Picker; UPDATE_SHEETS references docs/UPDATE_v0.32.html | VERIFIED | CHANGELOG.md line 9: `## v0.32.0 — 2026-06-28` with 6-bullet block. `index.html` line 130: `'0.32.0': 'docs/UPDATE_v0.32.html'` in UPDATE_SHEETS. Line 142: WHATS_NEW `'0.32.0'` array with 3 user-facing German bullets. |
| 21 | docs/UPDATE_v0.32.html exists as visual update sheet in ARGUS design (IBM Plex, Schiefer #1f2530, noindex) | VERIFIED | File exists. Contains `<meta name="robots" content="noindex">`, IBM Plex font faces, color vars with `--acc:#1f2530` (Schiefer). Title: "ARGUS · Update v0.32". |
| 22 | Training drehbuch (TRAIN_LESSONS) reflects new login/picker behavior (DAUERREGEL) | VERIFIED | `index.html` TRAIN_LESSONS Willkommen lesson (line 2679) explicitly states "Du befindest dich in der Schulungsumgebung — der gelbe Banner oben bestätigt das." DAUERREGEL comment at line 2674 updated: "Das Training läuft ausschließlich im Schulungs-Reiter / Schulungsumgebung." Person-Login not forced in training. tacSTART lessons unchanged. |
| 23 | Release files built/verified only — deploy/push reserved for owner gate | VERIFIED | No git push performed. All SUMMARYs state explicitly "Owner-Gate — bereit zum Deploy, nicht gepusht." This is the designed behavior per D5.1-01. |

#### Cross-Cutting Truths (D5.1-01…04 + REVIEW.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 24 | CR-01 fix: addUnlock(twin) on Schulung re-exchange so cold-start after schulung session passes isUnlocked() | VERIFIED | `index.html` line 3155-3162: after successful re-exchange `_praesidiumId` set; `if(_praesidiumId){ ... addUnlock(_praesidiumId, null); }` — comment explicitly "CR-01: Twin-Präsidium dauerhaft freischalten". |
| 25 | CR-02 fix: argus_exchange_person_code has p_scope whitelist ('echt','schulung' only) | VERIFIED | `0020_phase51_review_fixes.sql` lines 113-118: `if p_scope not in ('echt', 'schulung') then return jsonb_build_object('error', 'Ungültiger Scope — erlaubt: echt, schulung'); end if;` |
| 26 | D-06 gate: concrete leitung filename not in any tracked file | VERIFIED | `git grep` for the concrete `docs/leitung-*.html` filename → 0 tracked files (gate command run with the literal name, not reproduced here per D-06). Filename appears only in untracked notes. |
| 27 | tacSTART/triage logic untouched (mdr-tacstart-accepted) | VERIFIED | tacSTART functions present in index.html. No modifications to triage logic, step structure, or tacSTART walk found in phase artifacts. TRAIN_LESSONS comment: "tacSTART-Lektionen unverändert (mdr-tacstart-accepted)." |

**Score:** 27/27 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/0018_phase51_feld_audit_normal.sql` | Feld-Audit-Trigger + 'normal' unlock | VERIFIED | 364 lines; both trigger functions + both triggers + two RPCs with 'normal' in whitelists. `argus_usbnk()` called in both trigger functions. |
| `supabase/migrations/0019_phase51_twin_scope.sql` | Twin-scope re-exchange via p_scope parameter | VERIFIED | 213 lines; drops old 1-arg signature; `create or replace function argus_exchange_person_code(code text, p_scope text default 'echt')` with server-side schulungs_zwilling_id resolution. |
| `supabase/migrations/0020_phase51_review_fixes.sql` | CR-02 scope whitelist + WR-01 normal-praesidium guard + IN-02 photo-replace audit | VERIFIED | 364 lines; all three REVIEW.md findings addressed. Byte-near to 0018/0019, minimal diffs. |
| `index.html` | Person-login auto-detect, Echt/Schulung picker, USBNK session, SCHULUNG banner, twin scope, v0.32.0, TRAIN_LESSONS | VERIFIED | `exchangePersonCode`, `argus_picker_mode`, `schulungs_zwilling_id`, `SCHULUNGSUMGEBUNG`, `addUnlock(_praesidiumId, null)` (CR-01), `refreshSchulungFlag`+`_maybeRenderSchulBanner` (CR-01 banner render), `applyRevokedLock` clears `argus_picker_mode` (IN-01), `APP_VERSION = '0.32.0'`, `TRAIN_LESSONS` updated — all confirmed. |
| `sw.js` | CACHE_NAME ccp-shell-v59 | VERIFIED | Line 10: `const CACHE_NAME = 'ccp-shell-v59';` |
| `version.json` | 0.32.0 | VERIFIED | `{ "version": "0.32.0" }` |
| `CHANGELOG.md` | v0.32.0 block | VERIFIED | `## v0.32.0 — 2026-06-28` with full block. |
| `docs/UPDATE_v0.32.html` | ARGUS design, noindex, 0.32 content | VERIFIED | File exists, noindex meta, IBM Plex fonts, Schiefer color scheme, content describes Person-Login + Echt/Schulung picker. |
| `docs/SELF-HOSTING.md` | 0018 + 0019 in migration list | VERIFIED (with note) | 0018 and 0019 documented (lines 91-92, 172). 0020 is absent from migration list and sequence line. See Anti-Patterns section. |
| `docs/leitung-*.html` | 'normal' option, praesidium enforcement, role-change acceptance | VERIFIED | `<option value="normal">Normal</option>`, `(rolle==='admin'\|\|rolle==='normal') && !praesId` guard, `['master','flz','admin','normal']` whitelist, `argus_master_issue_person` + `argus_master_role_change_person` called. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| trigger on public.patients | public.audit_log | AFTER INSERT/UPDATE trigger reads argus_usbnk() | VERIFIED | 0018/0020: `argus_audit_patients_aiu` trigger exists; function body calls `public.argus_usbnk()` and inserts into `public.audit_log`. |
| trigger on public.ccps | public.audit_log | AFTER INSERT/UPDATE trigger reads argus_usbnk() | VERIFIED | 0018: `argus_audit_ccps_aiu` trigger exists; function body calls `public.argus_usbnk()` and inserts into `public.audit_log`. |
| doUnlock()/exchangeCode() | argus_exchange_person_code | Auto-detection fallback after argus_exchange_code | VERIFIED | `index.html`: `exchangePersonCode` called as fallback in `doUnlock()` after non-'gesperrt' exchangeCode failure. |
| Picker tab Schulung | schulungs_zwilling_id Twin | Person token scope covers praesidium + twin via re-exchange | VERIFIED | `pickertab` handler (line 3113): `exchangePersonCode(storedPersonCode, newTab)` called with `newTab='schulung'`; 0019/0020 RPC resolves twin server-side. |
| index.html APP_VERSION + UPDATE_SHEETS | docs/UPDATE_v0.32.html | UPDATE_SHEETS['0.32.0'] = 'docs/UPDATE_v0.32.html' | VERIFIED | `index.html` line 130: confirmed. File exists at `docs/UPDATE_v0.32.html`. |
| sw.js CACHE_NAME v59 | version.json 0.32.0 | Update-banner detection on version diff | VERIFIED | sw.js `ccp-shell-v59`; version.json `0.32.0`; consistent. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| index.html — SCHULUNG banner | `_praesidiumSchulung` | `refreshSchulungFlag()` → Supabase `praesidien.schulung` DB query + localStorage fallback | Yes | FLOWING — DB query with `.from('praesidien').select('schulung')`, plus `_maybeRenderSchulBanner` re-renders on change. |
| index.html — Picker list | `_praesidienList` | Supabase `praesidien.select('id,name,schulung,schulungs_zwilling_id')` | Yes | FLOWING — multiple query sites in init(), doUnlock(), and selectpraesidium handler all load `schulungs_zwilling_id`. |
| audit_log entries | `usbnk` in JWT | `argus_usbnk()` reads JWT claim server-side | Yes | FLOWING — server-side trigger reads from signed JWT, client cannot spoof. Orchestrator live-verified. |

---

### Behavioral Spot-Checks

Step 7b: Live/REST behavioral tests were orchestrator-driven (documented in SUMMARYs and per orchestrator instruction). The following were confirmed:

| Behavior | Evidence | Status |
|----------|----------|--------|
| Person-login + USBNK audit (triggers capture argus_usbnk()) | 05.1-01-SUMMARY live verification table: 10/10 trigger action tests PASS | PASS |
| Twin re-exchange grants twin access (Twin-CCPs 0→5) | 05.1-03-SUMMARY: "Twin-CCPs lesbar (5, war 0)" | PASS |
| Cold-start in schulung keeps banner + no kick-out | 05.1-REVIEW.md remediation: CR-01 fixed, browser-verified | PASS |
| CR-02 scope whitelist (arbitrary p_scope rejected) | 05.1-REVIEW.md: CR-02 fixed, live-tested | PASS |
| WR-01 normal-praesidium guard | 05.1-REVIEW.md: WR-01 fixed, live-tested | PASS |
| IN-02 photo-replace audit (2 events) | 05.1-REVIEW.md: IN-02 fixed, live-tested (2 events) | PASS |
| CR-02 intact: field exchange still rejects person codes | 05.1-01-SUMMARY: `argus_exchange_code` → "Pro-Person-Code — nur für die Leitungs-Seite gültig" | PASS |
| Append-only audit_log | 05.1-01-SUMMARY: anon INSERT → HTTP 401 | PASS |
| Backward-compat: no p_scope → echt | 05.1-03-SUMMARY: "master-person-Login-Regression intakt" | PASS |

Per orchestrator instruction: these live/browser tests were orchestrator-driven and are treated as satisfied.

---

### Probe Execution

Step 7c: No conventional `scripts/*/tests/probe-*.sh` files exist for this project. No phase-declared probes. SKIPPED — no probe files found.

---

### Requirements Coverage

No formal REQUIREMENTS.md IDs declared for this phase. Acceptance is derived from CONTEXT D5.1-01…04 + 05.1-REVIEW.md (all resolved). All four decisions verified:

| Decision | Status | Evidence |
|----------|--------|----------|
| D5.1-01: 'normal' unblocked for individual issuance; mass-provisioning SSO-gated | SATISFIED | 0018: whitelist includes 'normal'; leitung page issue+role-change UI. SELF-HOSTING hint "D-10" preserved. |
| D5.1-02: One-field auto-detect (shared code first, then person-code fallback) | SATISFIED | `doUnlock()` → `exchangeCode()` first, `exchangePersonCode()` fallback on non-'gesperrt' error. |
| D5.1-03: Two-tab Echt/Schulung picker, default Echt, unmissable banner, twin via schulungs_zwilling_id | SATISFIED | Two pickertab buttons, `pickerMode()` defaults 'echt', permanent yellow banner, `confirmModal` on switch, twin re-exchange via 0019/0020. |
| D5.1-04: Server-side triggers on patients/ccps write to audit_log via argus_usbnk() | SATISFIED | 0018 trigger functions + 0020 fixes. Both triggers AFTER INSERT/UPDATE, security-definer, argus_usbnk() called. |

---

### Anti-Patterns Found

| File | Finding | Severity | Impact |
|------|---------|----------|--------|
| `docs/SELF-HOSTING.md` | Migration 0020 absent from file list (line 91-93) and sequence (line 172: ends at "→ 0019"). The file exists in the repo and is live on the backend, but SELF-HOSTING.md doesn't document it for a self-hoster who follows the guide. | INFO | A self-hoster following SELF-HOSTING.md would miss applying 0020 (p_scope whitelist, normal-praesidium guard, photo-replace audit). They would get a functional deployment but missing three security/correctness hardening fixes from REVIEW.md. Not a blocker for the phase goal (engineering is complete + tested); is a documentation gap. |

No `TBD`/`FIXME`/`XXX` debt markers found in any phase-modified files. The pattern `XXXX-XXXX` in index.html is a code format placeholder, not a debt marker.

No stub patterns (empty implementations, `return null`, `return {}`, hardcoded empty arrays that feed rendering) found in phase-modified files.

---

### Human Verification Required

None. All must-haves are verifiable statically or were live-tested orchestrator-driven (per instruction: treat as satisfied). No visual-only, real-time-only, or external-service-only items remain unverified.

---

### Gaps Summary

**No blocking gaps.** All 27 must-haves are VERIFIED.

**Documentation note (non-blocking):** `docs/SELF-HOSTING.md` documents migrations 0018 and 0019 but omits 0020. Since 0020 contains correctness and security hardening fixes (CR-02 scope whitelist, WR-01 praesidium guard, IN-02 photo-replace audit) that are live on the backend, a self-hoster following SELF-HOSTING.md would miss these three fixes. This is a documentation gap, not a functional gap in the engineering deliverable. Recommended follow-up: add 0020 to SELF-HOSTING.md (file list, dependency prose, sequence line) in the next documentation pass.

---

_Verified: 2026-06-28T10:30:00Z_
_Verifier: Claude (gsd-verifier)_
