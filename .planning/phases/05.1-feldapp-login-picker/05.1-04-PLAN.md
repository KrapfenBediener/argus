---
phase: 05.1-feldapp-login-picker
plan: 04
type: execute
wave: 3
depends_on:
  - 05.1-03
files_modified:
  - index.html
  - sw.js
  - version.json
  - CHANGELOG.md
  - WHATS_NEW.md
  - docs/UPDATE_v0.32.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "APP_VERSION ist auf 0.32.0 gebumpt; version.json trägt dieselbe Version; sw.js CACHE_NAME ist auf v59 erhöht (erzwingt das Update)"
    - "CHANGELOG.md und WHATS_NEW (in-App) beschreiben Pro-Person-Login + Echt/Schulung-Picker; UPDATE_SHEETS verweist auf docs/UPDATE_v0.32.html"
    - "docs/UPDATE_v0.32.html existiert als visuelles Update-Sheet im ARGUS-Design (IBM Plex, Schiefer #1f2530, noindex)"
    - "Das Trainings-Drehbuch (TRAIN_LESSONS) spiegelt das neue Login/Picker-Verhalten (DAUERREGEL)"
    - "Die Release-Dateien sind nur gebaut/verifiziert — der Deploy/Push bleibt dem Owner-Gate vorbehalten (nicht in diesem Plan)"
  artifacts:
    - path: "docs/UPDATE_v0.32.html"
      provides: "Visuelles Update-Sheet v0.32 (ARGUS-Design, noindex)"
      contains: "0.32"
    - path: "version.json"
      provides: "Update-Erkennung (Network-only)"
      contains: "0.32.0"
  key_links:
    - from: "index.html APP_VERSION + UPDATE_SHEETS"
      to: "docs/UPDATE_v0.32.html"
      via: "UPDATE_SHEETS['0.32.0'] = 'docs/UPDATE_v0.32.html'"
      pattern: "UPDATE_v0.32"
    - from: "sw.js CACHE_NAME v59"
      to: "version.json 0.32.0"
      via: "Update-Banner-Erkennung bei Versionsdiff"
      pattern: "ccp-shell-v59"
---

<objective>
Das App-Release zu Phase 5.1 bauen und verifizieren (nicht deployen): APP_VERSION-Bump auf v0.32.0, sw.js CACHE_NAME auf v59 (erzwingt Update-Auslieferung), version.json gleichziehen, CHANGELOG-Eintrag, In-App-„Was ist neu" (WHATS_NEW), visuelles `docs/UPDATE_v0.32.html`-Sheet pro [[release-convention]], und das Trainings-Drehbuch (TRAIN_LESSONS) gemäß DAUERREGEL an das neue Login/Picker-Verhalten anpassen.

Purpose: Phase 5.1 IST ein App-Release (anders als Phase 5). Nutzer müssen die Pro-Person-Login-/Picker-Änderung zuverlässig ausgeliefert bekommen (SW-Cache-Bump + version.json) und das Training muss zum geänderten Einstieg passen. Der eigentliche Go-Live/Push bleibt bewusst beim Owner.

Output: gebumpte/ergänzte Release-Dateien + neues UPDATE-Sheet + aktualisiertes Drehbuch — bereit, aber NICHT deployt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-feldapp-login-picker/05.1-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Versions-Bump (APP_VERSION/sw.js/version.json) + CHANGELOG + WHATS_NEW + UPDATE-Sheet</name>
  <files>index.html, sw.js, version.json, CHANGELOG.md, WHATS_NEW.md, docs/UPDATE_v0.32.html</files>
  <read_first>
    - index.html — APP_VERSION (~Z.112: aktuell '0.31.0'), UPDATE_SHEETS (~Z.129–139: Map Version→Sheet-Pfad) und WHATS_NEW (~Z.140ff: Map Version→Bullet-Array; whatsNewModal nutzt beide).
    - sw.js — CACHE_NAME (~Z.10: 'ccp-shell-v58'), version.json-Network-only-Logik (~Z.79–87).
    - version.json — aktueller Inhalt { "version": "0.31.0" }.
    - CHANGELOG.md — Kopf + die letzten Einträge (v0.31.0/v0.30.0) als Stil-/Formatvorlage.
    - docs/UPDATE_v0.30.html — als Layout-/Design-Vorlage für ein neues Update-Sheet (ARGUS-Design, noindex-Meta, IBM Plex, Schiefer #1f2530). docs/UI-AUSBLICK.html ist die verbindliche Referenz-Optik (NICHT ändern).
  </read_first>
  <action>
    Setze APP_VERSION in index.html auf '0.32.0' und sw.js CACHE_NAME auf 'ccp-shell-v59'; setze version.json auf { "version": "0.32.0" }. Ergänze in index.html UPDATE_SHEETS den Eintrag `'0.32.0': 'docs/UPDATE_v0.32.html'` und in WHATS_NEW einen '0.32.0'-Eintrag mit 1–3 knappen, einsatztauglichen deutschen Bullets, die das Pro-Person-Login (ein Freischalt-Feld, erkennt persönlichen Code automatisch) und den Echt/Schulung-Reiter (Default Echtbetrieb, unübersehbarer Schulungsmodus, kein Vermischen) beschreiben — ohne interne Begriffe (USBNK/argus_role) nach außen zu kehren; formuliere nutzerverständlich.

    Füge CHANGELOG.md oben einen `## v0.32.0 — 2026-06-27`-Block hinzu (Stil der bestehenden Einträge): Pro-Person-Login mit Auto-Erkennung, Echt/Schulung-Zwei-Reiter-Picker + verstärkter Schulungs-Banner + bewusste Moduswechsel-Bestätigung, server-seitige Feld-Audit-Trigger (Migration 0018, personenscharf), 'normal'-Rolle für Einzel-Ausgabe entsperrt, SW-Cache v59. Falls eine separate WHATS_NEW.md im Repo gepflegt wird, dort denselben nutzerorientierten Eintrag spiegeln (sonst genügt die In-App-WHATS_NEW-Map; in dem Fall WHATS_NEW.md aus files_modified streichen ist nicht nötig — nur ergänzen wenn vorhanden).

    Erstelle `docs/UPDATE_v0.32.html` als eigenständiges visuelles Sheet im ARGUS-Design (Vorlage UPDATE_v0.30.html): noindex-Meta, IBM Plex, heller Hintergrund, Schiefer #1f2530 als Chrome, Triage-Farben nur funktional, kein Apple-/Royal-Stil. Inhalt: was neu ist (persönliche Anmeldung, Echt/Schulung-Reiter), wie es bedient wird, und der Hinweis „bitte App aktualisieren". Keine geheimen URLs/Leitungs-Dateinamen (D-06).
  </action>
  <verify>
    <automated>grep -q "APP_VERSION = '0.32.0'" index.html && grep -q "ccp-shell-v59" sw.js && grep -q '"version": "0.32.0"' version.json && grep -q "UPDATE_v0.32.html" index.html && grep -q "0.32.0" CHANGELOG.md && test -f docs/UPDATE_v0.32.html && grep -qi "noindex" docs/UPDATE_v0.32.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - APP_VERSION=0.32.0, CACHE_NAME=ccp-shell-v59, version.json=0.32.0 — konsistent (Update-Erkennung greift).
    - UPDATE_SHEETS + WHATS_NEW haben 0.32.0-Einträge; CHANGELOG-Block vorhanden; nutzerverständliche Sprache (keine internen Claims).
    - docs/UPDATE_v0.32.html im ARGUS-Design mit noindex; kein Leitungs-Dateiname/keine geheime URL (D-06).
  </acceptance_criteria>
  <done>Grep-Gate grün; Release-Dateien konsistent gebumpt; Update-Sheet existiert.</done>
</task>

<task type="auto">
  <name>Task 2: Trainings-Drehbuch (TRAIN_LESSONS) an Login/Picker anpassen (DAUERREGEL)</name>
  <files>index.html</files>
  <read_first>
    - index.html — TRAIN_LESSONS (~Z.2438–2530: DAUERREGEL-Kommentar, Lektions-Array, 3-Modus-Modell explain/do/choice, Spotlight-Selektoren, ctx/ctxBack-Kontext-Wächter), startTraining()/trainStep-Logik. Insbesondere die Lektionen rund um Einstieg/Präsidiumsauswahl und den Schulungs-Sandbox (train-ccp, training:true synct nie).
    - .planning/phases/05.1-feldapp-login-picker/05.1-03-SUMMARY.md (sofern vorhanden) — was an vPraesidium()/Picker/Banner konkret geändert wurde, damit die Lektionen/Spotlight-Selektoren passen.
  </read_first>
  <action>
    Gleiche TRAIN_LESSONS mit dem in Plan 03 geänderten Einstieg ab (DAUERREGEL): Wenn der Picker jetzt zwei Reiter Echt/Schulung zeigt bzw. das Freischalt-Feld die persönliche Anmeldung mit erklärt, müssen die betroffenen Lektionen (und ihre Spotlight-Selektoren/ctx-Wächter) das spiegeln — z. B. ein kurzer Hinweis, dass das Training im Schulungs-Reiter/Schulungsumgebung läuft und der SCHULUNG-Banner permanent ist. Keine neue Funktionalität erfinden; nur die Lektions-Texte/Selektoren konsistent halten, damit kein Spotlight ins Leere zeigt. Der geräte-lokale Trainings-Sandbox (train-ccp, training:true synct nie) bleibt unverändert; Person-Login/Echt-Scope wird im Training NICHT erzwungen (Training bleibt single-user, lokal, paralleltauglich). tacSTART-Lektionen NICHT inhaltlich verändern (mdr-tacstart-accepted).
  </action>
  <verify>
    <automated>grep -q "TRAIN_LESSONS" index.html && node --check index.html 2>/dev/null; if [ $? -ne 0 ]; then BODY=$(awk '/<script>/{f=1;next}/<\/script>/{f=0}f' index.html); echo "$BODY" > /tmp/argus_app4.js && node --check /tmp/argus_app4.js; fi && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TRAIN_LESSONS spiegelt den geänderten Einstieg/Picker; keine ins Leere zeigenden Spotlight-Selektoren.
    - Trainings-Sandbox unverändert (lokal, synct nie); Person-Login im Training nicht erzwungen.
    - tacSTART-Lektionen inhaltlich unverändert; JS-Syntax valide.
  </acceptance_criteria>
  <done>Drehbuch konsistent zum neuen Login/Picker; Syntax-Check grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW-Cache → ausgelieferte App-Version | veralteter Cache darf nicht die neue Login-/Picker-Logik verdecken |
| Update-Sheet (öffentlich) → keine Geheimnisse | docs/ ist öffentlich — keine Leitungs-URLs/Dateinamen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-054-01 | Tampering | veralteter SW-Cache liefert alte Login-Logik | mitigate | CACHE_NAME-Bump v59 invalidiert den alten Cache; version.json (Network-only) löst das Update-Banner aus — Muster aller bisherigen Releases |
| T-054-02 | Information Disclosure | geheime URL/Leitungs-Dateiname im öffentlichen Update-Sheet | mitigate | docs/UPDATE_v0.32.html mit noindex; D-06-Gate: keine Leitungs-Dateinamen/Lage-URLs im Sheet oder CHANGELOG |
| T-054-03 | Spoofing | irreführende Update-Notiz | accept | Nutzerorientierte, korrekte Beschreibung; keine internen Claims (USBNK/argus_role) nach außen — niedriges Risiko |
| T-054-SC | — | keine Package-Installs in diesem Plan | accept | Reine Datei-Edits + HTML-Sheet, keine npm/pip/cargo-Installs |
</threat_model>

<verification>
- APP_VERSION/CACHE_NAME/version.json konsistent auf 0.32.0/v59.
- UPDATE_SHEETS+WHATS_NEW+CHANGELOG+UPDATE_v0.32.html vorhanden und stimmig.
- TRAIN_LESSONS spiegelt den neuen Einstieg; tacSTART unverändert; Syntax grün.
- KEIN Deploy/Push (Owner-Gate).
</verification>

<success_criteria>
- Vollständiges, konsistentes v0.32.0-Release gebaut (Version/SW/version.json/CHANGELOG/WHATS_NEW/UPDATE-Sheet) + Drehbuch nachgezogen.
- ARGUS-Design + noindex im Sheet; D-06 gewahrt; tacSTART unangetastet.
- Deploy/Go-Live bleibt ausdrücklich dem Owner überlassen (nicht Teil dieses Plans).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04)
- `index.html` — APP_VERSION 0.32.0, UPDATE_SHEETS+WHATS_NEW 0.32.0-Einträge, TRAIN_LESSONS an Login/Picker angepasst.
- `sw.js` — CACHE_NAME ccp-shell-v59.
- `version.json` — 0.32.0.
- `CHANGELOG.md` — v0.32.0-Block.
- `docs/UPDATE_v0.32.html` — visuelles Update-Sheet (ARGUS-Design, noindex).
</artifacts>

<output>
Create `.planning/phases/05.1-feldapp-login-picker/05.1-04-SUMMARY.md` when done
</output>
