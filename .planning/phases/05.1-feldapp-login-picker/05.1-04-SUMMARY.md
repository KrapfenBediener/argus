---
phase: 05.1-feldapp-login-picker
plan: 04
subsystem: release
tags: [release, pwa, sw-cache, changelog, update-sheet, drehbuch, argus-design, self-hosting]

# Dependency graph
requires:
  - phase: 05.1-03
    provides: Person-Login, Echt/Schulung-Picker, SCHULUNG-Banner, Migration 0019
provides:
  - "v0.32.0-Release: APP_VERSION/sw.js/version.json konsistent gebumpt"
  - "UPDATE_SHEETS + WHATS_NEW 0.32.0-Einträge (nutzerverständlich, keine internen Begriffe)"
  - "CHANGELOG.md v0.32.0-Block (0018/0019 Live, Person-Login, Picker, SW-Cache v59)"
  - "docs/UPDATE_v0.32.html (ARGUS-Design, noindex, IBM Plex, Schiefer #1f2530)"
  - "TRAIN_LESSONS Willkommen-Lektion: Schulungsumgebung-Kontext + SCHULUNG-Banner-Hinweis"
  - "SELF-HOSTING.md: 0019-Eintrag (Migrationsliste + Reihenfolge + Prose)"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "UPDATE_SHEETS-Eintrag + WHATS_NEW-Bullets nutzerverständlich ohne interne Claims (USBNK/argus_role)"
    - "ARGUS-Design für Update-Sheets: IBM Plex, heller Hintergrund, Schiefer #1f2530, noindex"
    - "DAUERREGEL: TRAIN_LESSONS-Willkommen spiegelt Schulungsumgebung-Picker-Kontext"

key-files:
  created:
    - "docs/UPDATE_v0.32.html"
  modified:
    - "index.html"
    - "sw.js"
    - "version.json"
    - "CHANGELOG.md"
    - "docs/SELF-HOSTING.md"

key-decisions:
  - "WHATS_NEW.md als separate Datei existiert nicht — nur die In-App-WHATS_NEW-Map befüllt"
  - "TRAIN_LESSONS: nur Willkommen-Lektion angepasst (minimaler DAUERREGEL-Eingriff); alle anderen Selektoren weiterhin gültig"
  - "D-06: pre-existing Treffer in CHANGELOG/MDR-Dateien (leitung-<suffix>-Platzhalter) sind Out-of-scope; neue Dateien sauber"

# Metrics
duration: ~5min
completed: 2026-06-28
---

# Phase 5.1 Plan 04: v0.32.0 Release-Build Summary

**v0.32.0-Release gebaut: APP_VERSION/SW-Cache/version.json konsistent gebumpt, CHANGELOG + WHATS_NEW + UPDATE-Sheet erstellt, TRAIN_LESSONS DAUERREGEL nachgezogen, SELF-HOSTING.md um Migration 0019 ergänzt — bereit zum Deploy, nicht gepusht (Owner-Gate).**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-06-27T22:26:49Z
- **Completed:** 2026-06-28
- **Tasks:** 2 Auto-Tasks + 1 Extra-Task
- **Files modified:** 5 (`index.html`, `sw.js`, `version.json`, `CHANGELOG.md`, `docs/SELF-HOSTING.md`) + 1 created (`docs/UPDATE_v0.32.html`)

## Accomplishments

- **Versions-Bump konsistent:** `APP_VERSION = '0.32.0'` (index.html), `CACHE_NAME = 'ccp-shell-v59'` (sw.js), `{ "version": "0.32.0" }` (version.json). SW-Cache-Bump v58→v59 erzwingt Update-Auslieferung an alle Geräte; version.json (Network-only) löst das Update-Banner aus.
- **UPDATE_SHEETS + WHATS_NEW:** `UPDATE_SHEETS['0.32.0'] = 'docs/UPDATE_v0.32.html'` und WHATS_NEW `'0.32.0'`-Eintrag mit 3 nutzerzentrierten deutschen Bullets (persönliche Anmeldung, Echt/Schulung-Reiter, strikte Trennung) — keine internen Begriffe (kein USBNK/argus_role/RPC-Namen).
- **CHANGELOG.md v0.32.0-Block:** Enthält Person-Login mit Auto-Erkennung, Echt/Schulung-Picker + SCHULUNG-Banner + bewusste Bestätigung, Migration 0018 (Feld-Audit-Trigger + 'normal'-Freigabe, LIVE), Migration 0019 (twin-scoped JWT für Schulungs-Reiter, LIVE), SW-Cache v59.
- **docs/UPDATE_v0.32.html:** Eigenständiges visuelles Update-Sheet im ARGUS-Design (IBM Plex, heller Hintergrund, Schiefer `#1f2530` als Chrome, Triage-Farben nur funktional, noindex-Meta). Inhalt: Anmelden (ein Freischalt-Feld), Echtbetrieb (Standard), Schulungsumgebung (gelber Banner, strikte Trennung), Update-Hinweis. Keine geheimen URLs, keine Leitungs-Dateinamen (D-06).
- **TRAIN_LESSONS DAUERREGEL:** Willkommen-Lektion aktualisiert — erwähnt die Schulungsumgebung-Kontext und den gelben SCHULUNG-Banner, den der Übende beim Eintritt sieht. DAUERREGEL-Kommentar ergänzt (Person-Login nicht erzwungen, tacSTART unberührt). Alle übrigen Spotlight-Selektoren geprüft: `[data-action="joinccp"]`, `[data-action="newccp"]`, `.countbar`, `.banner`, alle navbtn-Ziele — weiterhin gültig, kein Spotlight ins Leere. Syntax: `node --check` OK.
- **SELF-HOSTING.md (Extra):** `0019_phase51_twin_scope.sql` in die Migrationsliste (Section 3) aufgenommen; Reihenfolge-Hinweis auf `… → 0018 → 0019` erweitert (Section 5); Prose-Notiz: `argus_exchange_person_code` erhält `p_scope` für den Schulungs-Twin per Re-Exchange — keine RLS-Änderung, keine neuen Server-Abhängigkeiten. D-06 gewahrt.

## Task Commits

1. **Task 1: Versions-Bump + CHANGELOG + WHATS_NEW + UPDATE-Sheet** — `dbaea60` (feat)
2. **Task 2: TRAIN_LESSONS Drehbuch anpassen (DAUERREGEL)** — `4178f04` (feat)
3. **Extra: SELF-HOSTING.md Migration 0019** — `5f29cc1` (docs)

**Plan-Metadaten:** dieser SUMMARY-Commit (docs).

## Files Created/Modified

- `index.html` — `APP_VERSION='0.32.0'`, `UPDATE_SHEETS['0.32.0']` + `WHATS_NEW['0.32.0']`-Eintrag, TRAIN_LESSONS Willkommen-Lektion + DAUERREGEL-Kommentar
- `sw.js` — `CACHE_NAME='ccp-shell-v59'`
- `version.json` — `{ "version": "0.32.0" }`
- `CHANGELOG.md` — v0.32.0-Block oben eingefügt
- `docs/UPDATE_v0.32.html` — neu erstellt (ARGUS-Design, noindex)
- `docs/SELF-HOSTING.md` — 0019-Eintrag + Reihenfolge-Erweiterung + Prose-Notiz

## Decisions Made

- **WHATS_NEW.md nicht als separate Datei angelegt:** Die Datei existiert nicht im Repo — der WHATS_NEW-Eintrag lebt ausschließlich in der In-App-Map (plan-konform).
- **TRAIN_LESSONS minimal angepasst:** Nur die Willkommen-Lektion erhielt einen Schulungsumgebung-Kontexthinweis. Alle anderen Lektionen und Selektoren blieben unverändert — kein Spotlight zeigt ins Leere; die Person-Login-Strecke ist kein Teil des Trainingsablaufs (Training ist single-user, lokal, ohne erzwungenen Person-Login).
- **D-06:** Pre-existing Treffer (`leitung-<suffix>`-Platzhalter in alten CHANGELOG-Einträgen/MDR-Doku) sind Out-of-scope für diesen Plan. Neue Dateien (UPDATE_v0.32.html, CHANGELOG v0.32.0-Block) sind vollständig sauber.

## Deviations from Plan

None — plan executed exactly as written (+ one EXTRA task: SELF-HOSTING.md 0019, explizit im Auftrag enthalten).

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes. Update-Sheet ist statisches HTML (noindex). Keine neuen Trust-Boundary-Übergänge.

## Known Stubs

None — keine Platzhalter oder leeren Datenpfade in den geänderten Dateien.

## User Setup Required

None — kein Deploy in diesem Plan (Owner-Gate). Migration 0018 + 0019 sind bereits LIVE (aus Plan 05.1-01 bzw. 05.1-03). Der Push/Deploy obliegt dem Owner.

## Next Phase Readiness

- **05.1 vollständig abgeschlossen:** Alle drei Pläne (01–04) committed, verifiziert, bereit.
- **Deploy/Go-Live:** Owner-Gate — `git push` + ggf. CDN-Purge nach Freigabe.
- Keine offenen Blocker.

## Self-Check: PASSED

- FOUND: `docs/UPDATE_v0.32.html`
- FOUND: `APP_VERSION = '0.32.0'` in index.html
- FOUND: `ccp-shell-v59` in sw.js
- FOUND: `"version": "0.32.0"` in version.json
- FOUND: `UPDATE_v0.32.html` in UPDATE_SHEETS
- FOUND: `noindex` in docs/UPDATE_v0.32.html
- FOUND: `0019_phase51_twin_scope.sql` in docs/SELF-HOSTING.md
- FOUND commit `dbaea60` (Task 1), `4178f04` (Task 2), `5f29cc1` (Extra)
- SYNTAX CHECK: node --check OK

---
*Phase: 05.1-feldapp-login-picker*
*Completed: 2026-06-28*
