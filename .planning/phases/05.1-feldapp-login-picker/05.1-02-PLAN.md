---
phase: 05.1-feldapp-login-picker
plan: 02
type: execute
wave: 2
depends_on:
  - 05.1-01
files_modified:
  - docs/leitung-*.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "Im Identitäten-Bereich der Leitungs-Seite kann der Master die Rolle 'Normal' aus dem Ausgabe-Dropdown wählen und ein Pro-Person-Token mit role='normal' ausgeben"
    - "Beim Rollenwechsel (doPersonRole) wird 'normal' als gültige Eingabe akzeptiert und an argus_master_role_change_person übergeben"
    - "Für role='normal' wird ein Präsidium verlangt (Feld-Scope), genau wie für role='admin' — die UI fordert es ein"
    - "Die Rollen-Beschriftung 'Normal' und ihre Pill-Farbe erscheinen korrekt in der USBNK-Suchergebnis-Tabelle"
    - "Keine Token-Secrets in der UI; nur In-App-Modals (kein natives alert/confirm/prompt); D-02 (nur USBNK, keine Klarnamen) gewahrt"
  artifacts:
    - path: "docs/leitung-*.html"
      provides: "'normal'-Option im Ausgabe-Dropdown + Rollenwechsel-Annahme + Präsidiums-Pflicht für normal"
      contains: "normal"
  key_links:
    - from: "secIdentitaeten()-Dropdown (pers-rolle)"
      to: "argus_master_issue_person"
      via: "doMkPerson() RPC-Aufruf mit p_role='normal' + p_praesidium_id"
      pattern: "argus_master_issue_person"
    - from: "doPersonRole()"
      to: "argus_master_role_change_person"
      via: "Prompt akzeptiert 'normal'"
      pattern: "argus_master_role_change_person"
---

<objective>
Die Leitungs-Seite (Glob `docs/leitung-*.html`) so erweitern, dass der Master Pro-Person-Tokens mit der Rolle `'normal'` ausgeben und bestehende Person-Rollen auf `'normal'` ändern kann — die Server-Freigabe dafür kam in Plan 01 (0018). Es ist die individuelle, mengenarme Einzel-Ausgabe pro Präsidium (D5.1-01); Massen-Provisionierung bleibt ausgeschlossen (D-10, kein Batch-Pfad).

Purpose: Ohne diese UI-Freigabe könnte der Master zwar serverseitig 'normal'-Tokens erzeugen, aber nicht über die Leitungs-Seite — und PPF/Pilot könnte nicht personenscharf ins Feld gehen. Bewusst KLEIN gehalten: nur die fehlende Rollen-Option, kein neuer Workflow.

Output: aktualisierte Leitungs-Seite (`docs/leitung-*.html`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-feldapp-login-picker/05.1-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 'normal' in das Ausgabe-Dropdown + Präsidiums-Pflicht + Rollenwechsel-Annahme</name>
  <files>docs/leitung-*.html</files>
  <read_first>
    - docs/leitung-*.html (Glob auflösen) — secIdentitaeten() (Block 1 „Pro-Person-Token ausgeben": das `pers-rolle`-select mit den Optionen master/flz/admin; der `pers-praes-wrap`-Block, der per JS-Sichtbarkeit für die Admin-Rolle eingeblendet wird), doMkPerson() (liest pers-rolle/pers-praes; Validierung `if(rolle==='admin' && !praesId)`), doPersonRole() (promptModal „Neue Rolle eingeben (master/flz/admin)"), rolleLabel()/rolleColor() (kennen 'normal' bereits — prüfen, ob ein Tweak nötig ist), und den Handler/Listener, der `pers-praes-wrap` bei Rollen-Auswahl ein-/ausblendet (data-action o. change-Event auf pers-rolle).
  </read_first>
  <action>
    Im `pers-rolle`-Dropdown von secIdentitaeten() eine vierte Option `<option value="normal">Normal</option>` ergänzen (nach Admin oder vor FLZ — Reihenfolge nach Augenmaß; FLZ bleibt `selected` als Default). Ergänze in doMkPerson() die Präsidiums-Pflicht für 'normal': erweitere die bestehende Bedingung zu „wenn rolle 'admin' ODER 'normal' und kein praesId → Toast: für diese Rolle ist ein Präsidium erforderlich" (normal trägt im Feld den praesidium_id-Scope — D5.1-01). Sorge dafür, dass der `pers-praes-wrap`-Sichtbarkeits-Mechanismus auch bei Auswahl von 'normal' das Präsidiums-Select einblendet (nicht nur bei 'admin') — passe die entsprechende Bedingung an (Label ggf. von „(für Admin-Rolle)" zu „(für Admin-/Normal-Rolle)").

    In doPersonRole() den Prompt-Text auf „Neue Rolle eingeben (master/flz/admin/normal)" erweitern und die Annahme von 'normal' sicherstellen (die eigentliche Whitelist-Prüfung macht der Server in 0018 — die UI darf 'normal' nicht clientseitig abweisen). Falls rolleLabel('normal') bereits „Normal" liefert (Bestand), keine Änderung nötig; nur prüfen, dass die Pill-Farbe (rolleColor) für 'normal' lesbar bleibt.

    Ausschließlich In-App-Modals/Toasts verwenden (kein natives alert/confirm/prompt). D-02 wahren: weiterhin nur USBNK, keine Klarnamen-Felder. Den konkreten Dateinamen NICHT in committete Texte/Commits schreiben (D-06).
  </action>
  <verify>
    <automated>F=$(ls docs/leitung-*.html | head -1); grep -q '<option value="normal">' "$F" && grep -q "argus_master_issue_person" "$F" && grep -q "argus_master_role_change_person" "$F" && grep -qi "master/flz/admin/normal" "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 'normal' als Option im Ausgabe-Dropdown; Präsidiums-Select wird auch bei 'normal' eingeblendet und in doMkPerson() für 'normal' verpflichtend geprüft.
    - doPersonRole akzeptiert 'normal' (Prompt-Text aktualisiert); kein clientseitiges Abweisen von 'normal'.
    - rolleLabel/rolleColor für 'normal' korrekt; keine Token-Secrets; nur In-App-Modals; D-02 gewahrt.
    - Kein Leitungs-Seiten-Dateiname in committetem Text (D-06).
  </acceptance_criteria>
  <done>Grep-Gate grün; das Dropdown bietet 'Normal', die Validierung verlangt ein Präsidium für 'normal', der Rollenwechsel akzeptiert 'normal'.</done>
</task>

<task type="auto">
  <name>Task 2: Hinweistext + D-06-Gate</name>
  <files>docs/leitung-*.html</files>
  <read_first>
    - docs/leitung-*.html — der `hint`-Text in Block 1 von secIdentitaeten() („Einzel-Ausgabe für privilegierte Rollen (Master/FLZ/Admin). … keine Massen-Provisionierung (D-10).").
  </read_first>
  <action>
    Den Hinweistext in secIdentitaeten() so anpassen, dass er die jetzt zusätzliche 'normal'-Einzel-Ausgabe korrekt beschreibt: „Einzel-Ausgabe (Master/FLZ/Admin/Normal) — pro Präsidium, geringe Menge. Massen-Provisionierung normaler Nutzer bleibt SSO-gated (D-10)." Keine weiteren funktionalen Änderungen.

    D-06-Gate vor Abschluss: `git grep` über alle getrackten Dateien nach dem konkreten Leitungs-Seiten-Dateinamen-Muster (z. B. dem Hex-Suffix) → muss 0 Treffer liefern. Der Dateiname steht ausschließlich in der untracked/gitignorten LEITUNG-URL.md.
  </action>
  <verify>
    <automated>F=$(ls docs/leitung-*.html | head -1); grep -qi "D-10" "$F" && BN=$(basename "$F" .html); test "$(git grep -l "$BN" -- ':!*.gitignore' 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Hinweistext nennt die 'normal'-Einzel-Ausgabe und hält die Massen-Provisionierung als SSO-gated fest (D-10).
    - D-06-Gate grün: der Leitungs-Dateiname taucht in keiner getrackten Datei auf.
  </acceptance_criteria>
  <done>Hinweistext aktualisiert; D-06-Gate liefert 0 Treffer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Leitungs-Seite (Master-Session) → DB-RPC | Master gibt 'normal'-Tokens aus; Server prüft argus_is_master + Whitelist |
| UI-Auswahl → Token-Privilegien | Rollen-Auswahl darf keine versteckten Privilegien setzen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-052-01 | Elevation of Privilege | 'normal' clientseitig zu mehr Scope verbiegen | mitigate | Die UI übergibt nur p_role/p_praesidium_id; der Server (0018) leitet alle Claims aus role ab und setzt für 'normal' alle Boolean-Flags false — die UI kann keine Privilegien injizieren |
| T-052-02 | Spoofing | 'normal'-Issue ohne Master | mitigate | argus_master_issue_person/role_change_person prüfen argus_is_master() serverseitig (0018); die UI ist nur Komfort, RLS/Guard ist die Grenze |
| T-052-03 | Information Disclosure | Token-Secret in der UI | mitigate | argus_master_search_usbnk gibt kein Secret zurück; UI zeigt nur short_code; doMkPerson zeigt den Code per In-App-promptModal, kein persistentes Logging im DOM |
| T-052-04 | Information Disclosure | Leitungs-Dateiname leakt | mitigate | D-06-Gate (git grep = 0) vor Abschluss; Glob `docs/leitung-*.html` in allen Plan-Texten |
| T-052-SC | — | keine Package-Installs in diesem Plan | accept | Reine HTML/JS-Edits, keine npm/pip/cargo-Installs |
</threat_model>

<verification>
- 'normal' wählbar; Präsidium für 'normal' verpflichtend; Rollenwechsel akzeptiert 'normal'.
- Server lehnt 'normal'-Issue ohne Master ab (Guard aus 0018) — durch Plan 01 belegt.
- D-06-Gate grün.
</verification>

<success_criteria>
- Master kann über die Leitungs-Seite 'normal'-Person-Tokens ausgeben + Rollen auf 'normal' wechseln, jeweils präsidiumsgebunden.
- Nur In-App-Modals; D-02/D-06 gewahrt; kein App-Release (index.html/sw.js unverändert).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)
- `docs/leitung-*.html` — 'normal'-Option im Ausgabe-Dropdown, Präsidiums-Pflicht für 'normal', Rollenwechsel-Annahme von 'normal', angepasster Hinweistext.
</artifacts>

<output>
Create `.planning/phases/05.1-feldapp-login-picker/05.1-02-SUMMARY.md` when done
</output>
