---
phase: 04.13-paket3-schulung
verified: 2026-06-13T10:30:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Orchestrator: Push nach origin/main + Live-Smoke GitHub Pages (ausgeliefertes version.json = 0.28.1; Marker ccp-shell-v52 in sw.js, argus_schulung_reset in index.html, argus_provision_schulung in der Leitungs-Seite)"
    expected: "Live-Instanz liefert exakt den lokalen Stand v0.28.1 / SW v52 aus"
    why_human: "main ist 18 Commits ahead of origin/main — die gesamte Phase (inkl. Review-Fixes) ist noch nicht gepusht; Live-Smoke ist erst nach dem Push möglich (Executor-Regel: pusht nicht, Verifier committet/pusht nicht)"
  - test: "AT-MIST-Druck real testen: Leitungs-Seite öffnen, Einsatzprotokoll-Detail eines abgeschlossenen Einsatzes, Knopf '🖨 AT-MIST drucken' — auf Desktop UND iPad/Safari"
    expected: "Druckvorschau zeigt NUR die AT-MIST-Karten (nicht die normale Protokollansicht); je Patient eine Karte ohne Foto; bei Patienten mit Tourniquet erscheint 'TQ angelegt HH:MM Uhr · X min bis Einsatzende'; nach dem Druck ist die normale Ansicht wieder da"
    why_human: "Print-CSS-Umschaltung (body[data-atmist-print]) und afterprint-Timing (WR-02-Fix, iOS-nicht-blockierendes window.print) sind nur im echten Browser-Druckdialog prüfbar; Preview-HTML (04.13-02-atmist-preview.html) belegt nur das Karten-Markup"
  - test: "Provisionierungs-Knopf mit Master-Token: Leitungs-Seite, Zugänge-Bereich, 'Schulungs-Präsidien anlegen/prüfen' klicken (2×)"
    expected: "1. Klick: Toast nennt neu angelegte '<Name> — Schulung'-Präsidien ODER 'Keine neuen Schulungs-Präsidien nötig.' (PP Karlsruhe — Schulung existiert seit Plan 01); 2. Klick: 'Keine neuen Schulungs-Präsidien nötig.'; Zugänge-Liste lädt neu und zeigt die Schulungs-Karten"
    why_human: "Master-Operation — dem Verifier untersagt; nur Anon-Negativprobe (P0001 'Nur mit MasterUser-Token') war zulässig und ist grün"
  - test: "Schulungs-Reset end-to-end mit zwei Geräten: Gerät B im Schulungs-Präsidium offline nehmen, auf Gerät A (Trainer/Master) 'Zurücksetzen' ausführen, Gerät B wieder online bringen"
    expected: "Gerät A: Toast 'Schulung zurückgesetzt — 6 Demo-CCPs angelegt', frische CCPs A–F; Gerät B bereinigt sich beim Reconnect über die Abschluss-Erkennung (alter CCP wird als beendet erkannt, lokale Übungsdaten verschwinden)"
    why_human: "Schreibende Operation auf der Live-Instanz — dem Verifier untersagt; REST-Positivtests sind in SUMMARY 01 und der WR-03-Fix-Notiz dokumentiert (7 offene CCPs → 7 Tombstones, 0 Rest-Patienten), aber nicht vom Verifier reproduziert; das Offline-Reconnect-Verhalten ist ohnehin nur am Gerät prüfbar"
---

# Phase 04.13: Paket 3 (Schulung) — Verification Report

**Phase Goal:** Extern unblockierter Teil von Paket 3 — (1) Schulungs-Reset über Tombstones statt hartem DELETE (RPC `argus_schulung_reset` mit 7-Tage-Hygiene), (2) Schulungs-Provisionierung je Präsidium (RPC `argus_provision_schulung`, master-only, idempotent; Knopf in der Leitungs-Seite), (3) AT-MIST-Druckansicht im Protokoll-Detail der Leitungs-Seite. Release v0.28.x / SW v51+.
**Verified:** 2026-06-13T10:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

Geprüft wurde der AKTUELLE Code-Stand inkl. der 8 Review-Fixes nach den SUMMARYs (Commits `5272537`…`06c1661`, Release inzwischen v0.28.1 / SW v52). SUMMARY-Behauptungen wurden nicht übernommen, sondern gegen Code, Git-Historie und (read-only, Anon-Key) gegen die Live-Instanz geprüft.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Master-/Trainer-Token kann Schulungs-Präsidium per RPC zurücksetzen; offene CCPs werden Tombstones (closed_at, ort='', master_medic='', join_token=null), patients+checklists gelöscht | ✓ VERIFIED | `supabase/migrations/0011_paket3_schulung.sql:87-102` — Schleife je offenem CCP: UPDATE mit exakter Tombstone-Signatur (Z. 93-98), danach DELETE patients+checklists (Z. 99-100). Reihenfolge UPDATE→DELETE ist der WR-03-Fix (`f3461f0`) — schließt das Race-Fenster, das die ursprüngliche Plan-Reihenfolge DELETE→UPDATE offen ließ. Signatur matcht `isCcpClosedRow` der Feld-App. Live-Beleg: RPC existiert auf der Instanz (Probe 1 unten) |
| 2 | Reset verweigert echte Präsidien mit jsonb-Fehler; ein echtes Präsidium kann NIE getombstonet werden | ✓ VERIFIED | 0011 Z. 71-78: `if not coalesce(v_praesidium.schulung,false)` → `{'error':'Nur für Schulungs-Präsidien'}` VOR jeder Mutation; davor Berechtigungs-Gate (Z. 63-67, WR-04-Fix `33c15ee`). Live-Probe (Anon-Key, Zufalls-UUID): `{"error":"Keine Berechtigung"}` HTTP 200 — beweist zugleich, dass die GEFIXTE Gate-Reihenfolge deployt ist (alte Version hätte 'Präsidium nicht gefunden' geliefert) |
| 3 | Schulungs-Tombstones > 7 Tage werden beim Reset hart gelöscht — nur im eigenen Präsidium (praesidium_id-Filter + schulung-Guard) | ✓ VERIFIED | 0011 Z. 109-115: `delete from public.ccps c where c.praesidium_id = p_praesidium_id and c.closed_at < v_now - 7::bigint * 86400000 and exists(... pp.schulung)` — Filter + redundanter Guard vorhanden; Operatorpräzedenz korrekt (Multiplikation bindet stärker). Kein purge_log/governance_log: grep = 0/0 in 0011 |
| 4 | argus_provision_schulung legt je Produktiv-Präsidium '<Name> — Schulung' (schulung=true) an, idempotent, master-only | ✓ VERIFIED | 0011 Z. 133-175: `raise exception 'Nur mit MasterUser-Token'` (Z. 143), `pg_advisory_xact_lock` (WR-05-Fix `aa151f9`, Z. 155), not-exists-INSERT mit `p.name \|\| ' — Schulung'` + schulung=true (Z. 159-171), Rückgabe `{angelegte:[...]}` mit `coalesce(...,'[]'::jsonb)`. Live-Probe (Anon-Key): HTTP 400 `P0001 "Nur mit MasterUser-Token"` — Funktion live, Gate greift |
| 5 | Migration 0011 zweimal anwendbar (create or replace); SELF-HOSTING.md führt 0011 in Liste + Betriebs-Absatz | ✓ VERIFIED | Beide Funktionen `create or replace` (Z. 45, 133), INSERT not-exists-geschützt → strukturell idempotent; Doppel-Anwendung in SUMMARY 01 und nochmals in der WR-03-Fix-Notiz dokumentiert. `docs/SELF-HOSTING.md:84` (Migrationsliste) + Z. 252-268 (Betriebs-Absatz: einmaliger Provisionierungs-Aufruf, Reset-Beschreibung, 7-Tage-Hygiene); `grep -ci provision` = 5 |
| 6 | schulreset ruft argus_schulung_reset per RPC (kein Client-DELETE der ccps); Demo-Neubefüllung clientseitig (genDemoRows) | ✓ VERIFIED | `index.html:3143`: `rr=await scx.rpc('argus_schulung_reset',{p_praesidium_id:_praesidiumId})` in try/catch mit beiden Fehlerpfaden (Transport `rr.error` + jsonb `rr.data.error` → Toast + return). Im schulreset-Block (3132-3169) KEIN `from('ccps').delete` (verbleibende delete-Treffer gehören zu 'CCP löschen'/'Verbund löschen'-Bestandscode). Neubefüllung: 6 CCP-Inserts + `genDemoRows(...)` clientseitig (3155-3163) |
| 7 | 4 Gates prüfen _praesidiumSchulung statt _praesidiumName==='Schulungsumgebung' | ✓ VERIFIED | Gates: 1278 (loadSchulDirty `!_praesidiumSchulung \|\| !_isMaster`), 2368 (Training), 2373 (Reset-Knopf `&& _isMaster && _schulDirty!==0`), 3133 (Handler-Guard `if(!_praesidiumSchulung)return`), plus 5. Vorkommen 3065 (Demo-Direkt-Merge, Discretion umgestellt). Der EINE verbleibende `_praesidiumName.*Schulungsumgebung`-Treffer (3136) ist KEIN Gate, sondern der IN-03-Fix-Confirm-Text ('dieser Schulungsumgebung' nur als Wortlaut-FALLBACK wenn _praesidiumName leer) — das Plan-Gate '=0' wurde durch den späteren IN-03-Fix wortlautbedingt überholt, die Bedingung selbst ist flagbasiert. Zusatz: loadSchulDirty zählt nur OFFENE CCPs (Rule-1-Fix, 1283-1288) — konsistent mit Tombstone-Semantik |
| 8 | Leitungs-Seite: master-only Knopf 'Schulungs-Präsidien anlegen/prüfen' ruft argus_provision_schulung + loadZugaenge | ✓ VERIFIED | `docs/leitung-<suffix>.html` (D-06: Glob): Knopf Z. 479 (`data-action="provision"` im Zugänge-Kopf), `makeProvision()` Z. 566-577 (sb()-Guard, `cl.rpc('argus_provision_schulung')`, Fehler-Toast, Namen-Toast bzw. 'Keine neuen Schulungs-Präsidien nötig.', abschließend `loadZugaenge()`), Dispatcher Z. 1248. Master-only serverseitig erzwungen (Live-Probe 2) |
| 9 | Protokoll-Detail: 'AT-MIST drucken' rendert Karten aus _detail.data.patients und druckt — kein API-Call, keine Fotos | ✓ VERIFIED | Knopf Z. 886 (`data-action="atmistprint"`), Dispatcher Z. 1251, `vAtmistPrint()` Z. 955-1070: liest `_detail.data.patients` (Z. 956-958), Print-CSS `body[data-atmist-print]` (Z. 74-78), Overlay `#atmist-print-overlay`. Im gesamten Funktionskörper KEIN `fetch(`/`.rpc(`/`<img` (grep über Z. 955-1070 = 0 Treffer). Review-Fixes drin: `tq_start` snake_case (CR-01, Z. 999-1003 — einziger verbliebener `tqStart`-Treffer ist ein Kommentar), TQ-Dauer gegen Einsatzende (WR-01, Z. 1003/1025), afterprint-Cleanup + 60s-Fallback + try/catch (WR-02, Z. 1054-1061), `vlog.filter(Boolean)` (IN-01, Z. 1017). XSS: dynamische Werte laufen durch esc() |
| 10 | Release konsistent (nach Review-Fixes: v0.28.1 / SW v52), CHANGELOG v0.28.0 + v0.28.1, kein UPDATE-Sheet, Drehbuch geprüft | ✓ VERIFIED | `index.html:112` APP_VERSION '0.28.1' = `version.json` 0.28.1; `sw.js:10` CACHE_NAME 'ccp-shell-v52'; CHANGELOG.md führt v0.28.1 (Review-Korrekturen, SW v52) UND v0.28.0; WHATS_NEW['0.28.1'] + ['0.28.0'] befüllt; UPDATE_SHEETS endet bei 0.26.0 (kein 0.28.x-Eintrag — CONTEXT-Entscheid). Drehbuch-Check in SUMMARY 02 + CHANGELOG v0.28.1 dokumentiert (Reset-Knopf in keiner Lektion). Hinweis: Plan-Soll 'v0.28.0/SW v51' ist durch die Review-Fix-Runde regulär auf v0.28.1/v52 fortgeschrieben — vom Orchestrator als Erwartung vorgegeben, keine Abweichung |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `supabase/migrations/0011_paket3_schulung.sql` | Beide RPCs, grants, comments | ✓ VERIFIED | 183 Zeilen, substantiv; beide `create or replace`-Signaturen, `grant execute ... to anon` ×2, `comment on function` ×2; inkl. WR-03/WR-04/WR-05-Fixes |
| `docs/SELF-HOSTING.md` | 0011 in Liste + Betriebs-Absatz | ✓ VERIFIED | Z. 84 (Liste) + Z. 252-268 (Provisionierung, Reset, 7-Tage-Restrisiko) |
| `index.html` | schulreset-RPC + 4 Gates + APP_VERSION + WHATS_NEW | ✓ VERIFIED | RPC Z. 3143, Gates 1278/2368/2373/3133 (+3065), APP_VERSION 0.28.1, WHATS_NEW 0.28.0+0.28.1; JSC-Syntax beider Script-Blöcke grün |
| `docs/leitung-<suffix>.html` (D-06) | makeProvision + vAtmistPrint + Print-CSS | ✓ VERIFIED | Alle drei vorhanden + verdrahtet (Knöpfe, Dispatcher); JSC-Syntax grün |
| `sw.js` | CACHE_NAME v51+ | ✓ VERIFIED | `ccp-shell-v52` (Z. 10); 9 PRECACHE_URLS unverändert plausibel |
| `version.json` | 0.28.x | ✓ VERIFIED | `{ "version": "0.28.1" }` |
| `CHANGELOG.md` | v0.28.0-Abschnitt | ✓ VERIFIED | v0.28.0 UND v0.28.1 vorhanden |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| argus_schulung_reset | ccps/patients/checklists | Tombstone-UPDATE + DELETE je offenem CCP | ✓ WIRED | 0011 Z. 87-102 (Reihenfolge per WR-03 auf UPDATE→DELETE korrigiert, Begründung im Code) |
| argus_schulung_reset | argus_is_master()/argus_praesidium_id() | Berechtigungs-Gate (jetzt ZUERST, WR-04) | ✓ WIRED | 0011 Z. 63-67; live bestätigt (Probe: Anon → 'Keine Berechtigung' auch bei unbekannter UUID) |
| argus_provision_schulung | public.praesidien | not-exists-INSERT, Namenskonvention | ✓ WIRED | 0011 Z. 159-171; Advisory-Lock Z. 155 |
| schulreset-Handler | argus_schulung_reset | `scx.rpc(...)` | ✓ WIRED | index.html:3143 mit beiden Fehlerpfaden |
| makeProvision() | argus_provision_schulung | `cl.rpc(...)` + loadZugaenge() | ✓ WIRED | Leitungs-Seite Z. 566-577; Knopf 479, Dispatcher 1248 |
| vAtmistPrint() | _detail.data.patients | Render + window.print() | ✓ WIRED | Z. 955-1070; Knopf 886, Dispatcher 1251; Print-CSS Z. 74-78 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| vAtmistPrint | `_detail.data.patients` / `d.ccps` | bereits geladenes Protokoll (argus_governance_protokoll, snake_case-Rows) | Ja — Feldnamen nach CR-01-Fix korrekt (tq_start); leere Felder werden als '— nicht erfasst' angezeigt (datengetrieben, kein Stub) | ✓ FLOWING |
| makeProvision-Toast | `r.data.angelegte` | RPC-jsonb-Rückgabe | Ja — Array-Guard + beide Toast-Pfade | ✓ FLOWING |
| schulreset | `rr.data.error` / genDemoRows | RPC-Rückgabe + clientseitige Demo-Generierung | Ja | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| JS-Syntax index.html (2 Blöcke) + Leitungs-Seite (1 Block) | osascript-JSC `new Function(...)` | 3× SYNTAX OK | ✓ PASS |
| RPC argus_schulung_reset live + Gate-Reihenfolge (WR-04) | REST POST, Anon-Key, Zufalls-UUID (read-only — Gate verweigert vor jeder Mutation) | `{"error":"Keine Berechtigung"}` HTTP 200 | ✓ PASS |
| RPC argus_provision_schulung live + master-only | REST POST, Anon-Key (read-only — Exception vor jeder Mutation) | HTTP 400 `P0001 "Nur mit MasterUser-Token"` | ✓ PASS |
| Alle 14 Phasen-Commits existieren (c0db693…06c1661) | `git cat-file -t` | 14/14 ok | ✓ PASS |
| D-06-Gate | `git grep -E '(leitung\|lage)-[0-9a-f]{6,}' -- ':!docs/'` | leer | ✓ PASS |
| Schreibende Positivpfade (Reset/Provision mit Master-/Trainer-Token) | — | — | ? SKIP → Human (untersagte Master-/Schreib-Operationen) |

### Probe Execution

Keine projektkonventionellen `scripts/*/tests/probe-*.sh` vorhanden und keine in PLAN/SUMMARY deklariert — entfällt.

### Requirements Coverage

`.planning/REQUIREMENTS.md` existiert nicht (manuell geführtes GSD). Stattdessen Plan-Frontmatter-IDs gegen Code geprüft:

| Requirement | Source Plan | Status | Evidence |
| --- | --- | --- | --- |
| F2-Tombstone | 01 | ✓ SATISFIED | Truth 1 |
| F2-Schutz | 01 | ✓ SATISFIED | Truth 2 (+ Live-Negativprobe) |
| F2-Hygiene | 01 | ✓ SATISFIED | Truth 3 |
| F2-Idempotenz | 01 | ✓ SATISFIED | Truth 5 (strukturell + dokumentierte Doppel-Anwendung) |
| F3-Provision | 01+02 | ✓ SATISFIED | Truths 4+8 (+ Live-Negativprobe) |
| F1-Reset | 02 | ✓ SATISFIED | Truth 6 |
| F1-Gate | 02 | ✓ SATISFIED | Truth 7 |
| F4-AT-MIST | 02 | ✓ SATISFIED | Truth 9 |
| F5-Release | 02 | ✓ SATISFIED | Truth 10 (lokal konsistent; LIVE-Auslieferung offen → Human/Orchestrator) |
| D-06 | 02 | ✓ SATISFIED | git-grep-Gate leer; Dateiname nirgends in committeten Nicht-docs-Texten, auch nicht in diesem Report |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| — | — | Keine TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER in den hinzugefügten Zeilen des Phasen-Diffs (c0db693^..HEAD, ohne .planning) | — | — |
| index.html | 3136 | `_praesidiumName.*Schulungsumgebung` (1 Treffer, Plan-Gate verlangte 0) | ℹ️ Info | Kein Gate — Wortlaut-Fallback des IN-03-Fix-Confirm-Texts; Bedingung selbst ist flagbasiert |

### CONTEXT-Entscheide (gesperrt) — Einhaltung

| Entscheid | Eingehalten | Beleg |
| --- | --- | --- |
| Tombstone statt DELETE, Signatur = Einsatz-Abschluss | Ja | 0011 Z. 93-98 |
| 7-Tage-Hygiene, Restrisiko dokumentiert | Ja | 0011 Z. 109-115; SELF-HOSTING Z. 266-268 |
| Kein purge_log/governance_log-Eintrag | Ja | grep 0/0 in 0011 |
| Demo-Neubefüllung clientseitig, Reset-Knopf Feld-App, Flag-Gate | Ja | index.html 3133-3163 |
| Provisionierung über Namenskonvention, master-only, idempotent; Knopf Zugänge-Bereich + Reload | Ja | 0011 + Leitungs-Seite 479/566-577 |
| Bestehende 'Schulungsumgebung' unverändert | Ja | Flag-Umbau bricht Bestand nicht (0009 setzte schulung-Flag; REVIEW bestätigt) |
| AT-MIST aus geladenem Protokoll, ohne Fotos, kein neuer RPC | Ja | Truth 9 |
| Kein UPDATE-Sheet; Drehbuch geprüft | Ja | UPDATE_SHEETS endet 0.26.0; SUMMARY 02 + CHANGELOG |
| SW-Version: CONTEXT sagte v50 — Plan korrigierte regulär auf v51 (v50 durch v0.27.2 verbraucht), Review-Fixes auf v52 | Ja (fortgeschrieben) | sw.js v52 |

### Human Verification Required

#### 1. Push + Live-Smoke (Orchestrator)

**Test:** `main` nach `origin/main` pushen (aktuell 18 Commits ahead), dann auf GitHub Pages: ausgeliefertes `version.json`, `sw.js`-Marker `ccp-shell-v52`, `argus_schulung_reset` in index.html, `argus_provision_schulung` in der Leitungs-Seite prüfen.
**Expected:** Live-Stand = lokaler Stand v0.28.1 / SW v52.
**Why human:** Push ist Orchestrator-Aufgabe (Verifier committet/pusht nicht); Live-Smoke erst danach möglich. Dies ist der EINZIGE Grund, warum 'live' aus den Success Criteria noch nicht erfüllt ist — der Code selbst ist komplett.

#### 2. AT-MIST-Druck real (Desktop + iPad/Safari)

**Test:** Protokoll-Detail eines abgeschlossenen Einsatzes → '🖨 AT-MIST drucken'.
**Expected:** Druckvorschau zeigt nur die Karten (keine Fotos, TQ-Zeile mit Anlagezeit + Dauer bis Einsatzende); danach normale Ansicht wieder sichtbar.
**Why human:** Print-CSS-Umschaltung und afterprint-Timing (WR-02) nur im echten Druckdialog prüfbar.

#### 3. Provisionierungs-Knopf mit Master-Token (2× klicken)

**Test:** Leitungs-Seite, Zugänge-Bereich.
**Expected:** Idempotentes Verhalten; erwartbar bereits beim 1. Klick 'Keine neuen Schulungs-Präsidien nötig.' (PP Karlsruhe — Schulung existiert seit Plan 01).
**Why human:** Master-Operation dem Verifier untersagt.

#### 4. Schulungs-Reset end-to-end mit Offline-Gerät

**Test:** Zwei Geräte im Schulungs-Präsidium; eines offline, Reset auf dem anderen, Reconnect.
**Expected:** Offline-Gerät bereinigt sich über die Abschluss-Erkennung; 6 frische Demo-CCPs.
**Why human:** Schreibende Live-Operation untersagt; Offline-Reconnect nur am Gerät prüfbar.

### Gaps Summary

Keine Code-Gaps. Alle 10 Must-have-Truths beider Pläne sind im aktuellen Code-Stand (inkl. der 8 Review-Fixes 5272537…06c1661) verifiziert; die Migration ist live deployt — beide RPCs antworten auf der Instanz, und die Anon-Negativproben belegen zusätzlich, dass die GEFIXTE Fassung (WR-04-Gate-Reihenfolge) live ist. Offen ist ausschließlich der Auslieferungs-/Geräte-Teil: Push + Live-Smoke (main 18 Commits ahead) sowie drei Geräte-/Master-Tests, die programmatisch nicht bzw. dem Verifier nicht erlaubt prüfbar sind.

---

_Verified: 2026-06-13T10:30:00Z_
_Verifier: Claude (gsd-verifier)_
