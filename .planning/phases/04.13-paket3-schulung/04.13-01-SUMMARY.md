---
phase: 04.13-paket3-schulung
plan: 01
subsystem: database
tags: [supabase, postgres, plpgsql, rpc, security-definer, tombstone, management-api]

# Dependency graph
requires:
  - phase: 04.8-datenschutz-haertung
    provides: "argus_close_einsatz/argus_run_purge (Tombstone- und Gate-Muster, Migration 0002)"
  - phase: 04.12-flz-lageansicht (Paket 2, Migration 0009)
    provides: "schulung-Flag auf praesidien, master-only-RPC-Muster, jti-Sofortsperre (0007) in den Claim-Helfern"
provides:
  - "public.argus_schulung_reset(p_praesidium_id uuid) — Tombstone-Reset für Schulungs-Präsidien (live, REST-verifiziert)"
  - "public.argus_provision_schulung() — idempotentes, master-only Anlegen von Schulungs-Schwester-Präsidien (live, REST-verifiziert)"
  - "supabase/migrations/0011_paket3_schulung.sql (2x angewendet = Idempotenz-Beleg)"
  - "docs/SELF-HOSTING.md mit 0011 + Provisionierungs-Betriebs-Absatz + 7-Tage-Restrisiko"
affects: [04.13-02 (Feld-App schulreset-RPC-Umbau + Leitungs-Seite Provision-Knopf), self-hosting]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Tombstone-Hygiene mit praesidium_id-Filter + redundantem schulung-Guard (Defense in depth)", "Provisionierung über Namenskonvention '<Name> — Schulung' statt FK-Modell"]

key-files:
  created: [supabase/migrations/0011_paket3_schulung.sql]
  modified: [docs/SELF-HOSTING.md]

key-decisions:
  - "Provisionierte Schwester-Präsidien ('PP Karlsruhe — Schulung') auf der Live-Instanz BELASSEN — gewollter Endzustand der Provisionierung, idempotent; nur die ZZTEST-Artefakte wurden aufgeräumt"
  - "Kommentare in 0011 ohne die Strings purge_log/governance_log formuliert, damit das wörtliche Acceptance-Gate (grep -c purge_log → 0) sauber greift"
  - "Rückgabe des Resets als {ok,reset,hygiene} (Zähler) — Plan stellte das frei"

patterns-established:
  - "Schulungs-Reset erzeugt EXAKT die Tombstone-Signatur des Einsatz-Abschlusses (closed_at, ort='', master_medic='', join_token=null) — Client-Erkennung isCcpClosedRow bleibt unverändert"

requirements-completed: [F2-Tombstone, F2-Schutz, F2-Hygiene, F2-Idempotenz, F3-Provision]

# Metrics
duration: ~25min
completed: 2026-06-13
---

# Phase 4.13 Plan 01: Migration 0011 — Schulungs-Reset + Provisionierung Summary

**Migration 0011 live deployt (2x = Idempotenz-Beleg): argus_schulung_reset tombstont Schulungs-CCPs mit der Abschluss-Signatur inkl. 7-Tage-Hygiene; argus_provision_schulung legt master-only und idempotent Schwester-Präsidien „<Name> — Schulung" an — alle Positiv-/Negativfälle per REST belegt.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-06-12T23:33:54Z
- **Completed:** 2026-06-12T23:58:00Z (UTC; lokal 2026-06-13)
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- **Migration 0011** mit beiden RPCs nach Projektmuster (0002/0009): schulung-Pflicht-Check ZUERST, Gate Master ODER eigenes Präsidium, DELETE patients/checklists VOR Tombstone-UPDATE, 7-Tage-Hygiene mit `praesidium_id`-Filter + redundantem schulung-Guard, bewusst KEIN Lösch-/Governance-Log-Eintrag; Provisionierung master-only, not-exists-INSERT, Rückgabe `{angelegte:[...]}`.
- **Live deployt und idempotent belegt:** Management API (ephemerer PAT, nie persistiert), zwei Läufe, beide HTTP 201 ohne Fehler.
- **Vollständige REST-Testmatrix grün** (Details unten), Testdaten restlos aufgeräumt.
- **SELF-HOSTING.md** führt 0011 in Liste/Apply-Reihenfolge und beschreibt den einmaligen Provisionierungs-Aufruf nach dem Anlegen der echten Präsidien inkl. dokumentiertem 7-Tage-Restrisiko.

## Task Commits

Each task was committed atomically:

1. **Task 1: Migration 0011 schreiben** - `c0db693` (feat)
2. **Task 2: Deploy + REST-Tests + SELF-HOSTING.md** - `65286ce` (docs)

## Files Created/Modified

- `supabase/migrations/0011_paket3_schulung.sql` - argus_schulung_reset(uuid) + argus_provision_schulung(), grants, comments
- `docs/SELF-HOSTING.md` - 0011 in Migrationsliste (Abschnitt 3) + Apply-Reihenfolge (Abschnitt 5, Hinweis 0009/0011 ersetzt), Betriebs-Absatz Provisionierung mit Restrisiko-Hinweis (Abschnitt 8)

## Testbelege (REST + Management API)

**Idempotenz (F2-Idempotenz):**
- Migration 0011 zweimal via Management API angewendet — Lauf 1: HTTP 201, Lauf 2: HTTP 201, beide ohne Fehler (`create or replace`).

**Reset-Positivtest (F2-Tombstone):**
- Setup: Test-Präsidium `ZZTEST-0011 — Schulung` (per Provision angelegt) mit offenem CCP (kennung Z, ort, master_medic, join_token gesetzt) + 1 Patient + 1 Checkliste.
- RPC mit **Präsidiums-JWT (Nicht-Master, eigenes Präsidium = Trainer-Pfad)** → `{"ok": true, "reset": 1, "hygiene": 0}`.
- Verifikation per SQL: `closed=true, ort='', master_medic='', join_token=null, patients=0, checklists=0` — exakt die `isCcpClosedRow`-Signatur.
- Governance-Filter-Probe (Filter aus 0003/0009 wörtlich nachgestellt): 0 Treffer → vollständiger Tombstone erscheint NICHT in der Governance-Liste (kein Protokoll-Spam).

**Reset-Negativtests (F2-Schutz):**
- (a) Echtes Präsidium (`ZZTEST-0011`, schulung=false) mit Master-JWT → `{"error": "Nur für Schulungs-Präsidien"}` (HTTP 200, jsonb-Fehler).
- (b) Fremdes Präsidium (Prod-JWT auf Schulungs-Präsidium, Nicht-Master) → `{"error": "Keine Berechtigung"}`.
- (c) Unbekannte UUID → `{"error": "Präsidium nicht gefunden"}`.

**Hygiene-Test (F2-Hygiene):**
- Künstlicher Tombstone mit `closed_at` = jetzt − 8 Tage angelegt → erneuter Reset-Aufruf → `{"ok": true, "reset": 0, "hygiene": 1}`; Row hart gelöscht, der frische Tombstone (jünger als 7 Tage) blieb erhalten.

**Provision-Tests (F3-Provision):**
- Aufruf 1 (Master-JWT) → `{"angelegte": ["PP Karlsruhe — Schulung", "ZZTEST-0011 — Schulung"]}` (HTTP 200).
- Aufruf 2 (Master-JWT) → `{"angelegte": []}` — idempotent.
- Nicht-Master-JWT → HTTP 400, `P0001 "Nur mit MasterUser-Token"`.

**Log-Freiheit:** Seit Teststart 0 neue `purge_log`- und 0 neue `governance_log`-Einträge (CONTEXT-Entscheid bestätigt).

**Aufräumen:** Beide ZZTEST-Präsidien samt CCPs gelöscht; Restdaten-Check (praesidien/ccps/patients mit ZZTEST-Mustern) = 0/0/0. Test-JWTs (1 h Laufzeit) und tmp-Dateien gelöscht; PAT nur in der Shell-Variablen, nie in Dateien/Commits (`git grep sbp_` = 0 Treffer).

## Decisions Made

1. **Diskretionsentscheid:** Das beim Provision-Test entstandene Schwester-Präsidium **„PP Karlsruhe — Schulung" bleibt auf der Instanz** — es ist der gewollte Endzustand der Provisionierung (Plan 02 / Leitungs-Knopf würde es identisch anlegen; Löschen+Neuanlegen wäre Churn ohne Nutzen). Die bestehende „Schulungsumgebung" (globale Eigentümer-/Dev-Schulung) ist unberührt.
2. **Gate-Hygiene:** Kommentare in 0011 nennen die Log-Tabellen umschreibend („Lösch-/Governance-Protokoll"), damit das wörtliche Acceptance-Gate `grep -c "purge_log" → 0` greift.
3. **Reset-Rückgabe mit Zählern** `{ok, reset, hygiene}` (Plan stellte „ok oder Anzahl" frei) — hilfreich für Toast/Debugging in Plan 02.

## Deviations from Plan

None - plan executed exactly as written. (Die drei Punkte oben sind im Plan freigestellte Diskretionsentscheide, keine Abweichungen.)

## Issues Encountered

None. Cloudflare-403-Falle vermieden (curl statt python-urllib, wie im Betriebswissen dokumentiert).

## User Setup Required

None — kein neuer externer Dienst. **Owner-Punkt (wie Vorphasen):** den für den Deploy genutzten ephemeren PAT im Supabase-Dashboard widerrufen.

## Next Phase Readiness

- Backend für Plan 02 steht bereit: die Feld-App kann `argus_schulung_reset` aufrufen (Tombstone-Mechanik greift über vorhandenes `isCcpClosedRow`/`handleRemoteClose`), die Leitungs-Seite kann `argus_provision_schulung` aufrufen.
- „PP Karlsruhe — Schulung" existiert bereits live — der Leitungs-Knopf wird beim ersten Klick „Keine neuen Schulungs-Präsidien nötig" melden (erwartetes idempotentes Verhalten).
- Keine Blocker.

## Self-Check: PASSED

- FOUND: supabase/migrations/0011_paket3_schulung.sql
- FOUND: docs/SELF-HOSTING.md
- FOUND: .planning/phases/04.13-paket3-schulung/04.13-01-SUMMARY.md
- FOUND: Commit c0db693 (Task 1)
- FOUND: Commit 65286ce (Task 2)

---
*Phase: 04.13-paket3-schulung*
*Completed: 2026-06-13*
