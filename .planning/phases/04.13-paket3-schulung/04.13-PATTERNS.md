# Phase 4.13: Paket 3 (Schulungs-Provisionierung, Tombstone-Reset, AT-MIST-Druck) — Pattern Map

**Mapped:** 2026-06-12
**Files analyzed:** 7 (new/modified)
**Analogs found:** 7 / 7

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `supabase/migrations/0011_paket3_schulung.sql` (`argus_schulung_reset`) | migration / RPC | request-response | `supabase/migrations/0002_phase48_datenschutz.sql` (`argus_close_einsatz`) | exact — same gate: Master ODER eigenes Präsidium; DELETE-vor-UPDATE-Muster |
| `supabase/migrations/0011_paket3_schulung.sql` (`argus_provision_schulung`) | migration / RPC | CRUD | `supabase/migrations/0009_paket2_gastcode_schulung.sql` (`argus_mark_export`, `argus_governance_einsaetze`) | exact — master-only, `raise exception`, idempotent, `create or replace`, grant |
| `index.html` — schulreset-Handler (Zeile 3120–3150) | utility / event-driven | request-response | `index.html` selbst — Handler direkt daneben, Zeile 3120 | self-analog — Umbau bestehender Logik |
| `index.html` — Flag-Gates (Zeilen 1270, 2358, 2363, 3121) | utility / config | — | `index.html` selbst — `_praesidiumSchulung` Deklaration, Zeile 296 | self-analog — 4 mechanische Textersetzungen |
| `docs/leitung-<suffix>.html` — `makeProvision()` + Knopf im mh-Header | service / component | request-response | `docs/leitung-<suffix>.html` — `makeGast()` / `makeObserver()` (Zeilen 542–579) | exact — selbes RPC-call-Muster, toast, loadZugaenge-Reload |
| `docs/leitung-<suffix>.html` — AT-MIST-Druckfunktion `vAtmistPrint()` | component | request-response | `index.html` — `exportPrintCard(p)` (Zeilen 2198–2246) | role-match — AT-MIST-Struktur übernehmen; Helper-Portierung nötig |
| `sw.js` / `version.json` / `CHANGELOG.md` | config | — | `sw.js` Zeile 10 / `version.json` Zeile 1 / letzter CHANGELOG-Eintrag | exact — mechanische Wertänderung |

---

## Pattern Assignments

### `supabase/migrations/0011_paket3_schulung.sql` — `argus_schulung_reset` (RPC, request-response)

**Analog:** `supabase/migrations/0002_phase48_datenschutz.sql`, Funktion `argus_close_einsatz` (Zeilen 78–126)

**Kopfkommentar-Stil** (Zeilen 1–36 von 0009, repräsentativ):
```sql
-- © 2026 Gabor Szeman – Alle Rechte vorbehalten. Proprietär, Nutzung nur mit Genehmigung.
-- ============================================================================
-- Paket 3 (Schulungs-Reset, Tombstone, Provisionierung)
-- ============================================================================
-- Beschreibungstext mit Owner-Entscheiden, Reihenfolge-Hinweis,
-- Zeitkonvention (bigint JS-ms), Idempotenz-Hinweis.
-- ============================================================================
```

**RPC-Grundgerüst** (0009, `argus_mark_export`, Zeilen 295–331):
```sql
create or replace function public.argus_schulung_reset(p_praesidium_id uuid)
  returns jsonb
  language plpgsql
  security definer
  set search_path to 'public'
as $function$
declare
  v_now bigint := (extract(epoch from now())*1000)::bigint;
  v_praesidium record;
begin
  -- ...
end;
$function$;
grant execute on function public.argus_schulung_reset(uuid) to anon;
comment on function public.argus_schulung_reset(uuid) is '...';
```

**Berechtigungs-Gate: Master ODER eigenes Präsidium** (0002, `argus_close_einsatz`, Zeilen 102–106):
```sql
if not public.argus_is_master()
   and ( public.argus_praesidium_id() is null
         or v_praesidium.id is distinct from public.argus_praesidium_id() ) then
  return jsonb_build_object('error', 'Keine Berechtigung');
end if;
```

**Schulung=true-Pflicht-Check** (Muster analog 0009, `schulung`-Flag, Zeile 41):
```sql
select id, schulung into v_praesidium
from public.praesidien where id = p_praesidium_id;
if not found then return jsonb_build_object('error', 'Präsidium nicht gefunden'); end if;
if not coalesce(v_praesidium.schulung, false) then
  return jsonb_build_object('error', 'Nur für Schulungs-Präsidien');
end if;
```

**Tombstone-Muster: DELETE vor UPDATE** (0002, `argus_run_purge`, Zeilen 183–187):
```sql
-- Reihenfolge ZWINGEND: erst DELETE, dann UPDATE (Pitfall 3: kein Tombstone mit Patienten)
delete from public.patients   where ccp_id = v_ccp.id;
delete from public.checklists where ccp_id = v_ccp.id;
update public.ccps set
  closed_at    = v_now,
  ort          = '',
  master_medic = '',
  join_token   = null
where id = v_ccp.id;
```

**Tombstone-Hygiene: 7-Tage-Hartlöschung** (kein direktes Analog — Muster aus 0002 `argus_run_purge` Zeile 185 adaptiert):
```sql
-- Hygiene: Schulungs-Tombstones > 7 Tage hart löschen (kein purge_log-Eintrag)
-- praesidium_id-Filter + schulung=true-Guard ZWINGEND (Pitfall 2)
delete from public.ccps
where praesidium_id = p_praesidium_id
  and closed_at is not null
  and closed_at < v_now - 7::bigint * 86400000;
```

**jsonb-Fehler-Rückgabe** (0008, `argus_claim_nums`, Zeilen 53–55):
```sql
if p_count is null or p_count < 1 or p_count > 50 then
  return jsonb_build_object('error', 'Ungültige Anzahl');
end if;
```

**Zeitkonvention** (0002, Zeile 85; 0008, Zeile 37; 0009, Zeile 78):
```sql
v_now bigint := (extract(epoch from now())*1000)::bigint;
```

---

### `supabase/migrations/0011_paket3_schulung.sql` — `argus_provision_schulung` (RPC, CRUD)

**Analog:** `supabase/migrations/0009_paket2_gastcode_schulung.sql`, `argus_mark_export` (Zeilen 295–331) und `argus_governance_einsaetze` (Zeilen 255–290)

**Master-only-Gate mit `raise exception`** (0009, `argus_mark_export`, Zeilen 305–307; 0009, `argus_governance_einsaetze`, Zeilen 264–266):
```sql
if not public.argus_is_master() then
  raise exception 'Nur mit MasterUser-Token';
end if;
```

**Idempotenz-Pattern** (0009, Zeile 49 — idempotenter UPDATE; 0008, Zeile 28 — `add column if not exists`):
```sql
-- Schulungs-Schwester-Präsidium anlegen, falls noch nicht vorhanden:
insert into public.praesidien (name, schulung)
select p.name || ' — Schulung', true
from public.praesidien p
where p.schulung = false
  and not exists (
    select 1 from public.praesidien s
    where s.schulung = true
      and s.name = p.name || ' — Schulung'
  );
```

**Rückgabe: angelegte Namen** (Muster analog `argus_mark_export` Zeile 328):
```sql
return jsonb_build_object('angelegte', array_agg(neu_name));
-- oder bei leerer Ergebnismenge:
return jsonb_build_object('angelegte', '[]'::jsonb);
```

**Grant + Comment** (0009, Zeile 331; 0008, Zeilen 84, 86–90):
```sql
grant execute on function public.argus_provision_schulung() to anon;
comment on function public.argus_provision_schulung() is
  'Idempotentes Anlegen von Schulungs-Schwester-Präsidien (0011): ...';
```

---

### `index.html` — schulreset-Handler (Zeilen 3120–3150, Umbau)

**Analog:** `index.html` selbst — bestehender Handler (Zeilen 3120–3150) und `checkClosedEinsatz`/`handleRemoteClose` (Zeilen 1352–1360)

**Heutiger Handler — umzubauende Struktur** (Zeilen 3120–3150):
```js
if(a==="schulreset"){
  if(_praesidiumName!=='Schulungsumgebung')return;          // ERSETZEN durch: if(!_praesidiumSchulung)return;
  var oks2=await confirmModal("...", "Zurücksetzen");
  if(!oks2)return;
  var scx=sb(); if(!scx||!_praesidiumId){toast("Keine Backend-Verbindung");return;}
  toast("Setze zurück…");
  /* 1) ERSETZEN: statt scx.from('ccps').delete()... → RPC-Aufruf: */
  try{await scx.from('ccps').delete().eq('praesidium_id',_praesidiumId);}catch(e){}   // ENTFERNEN
  /* NEU:
  var rr=await scx.rpc('argus_schulung_reset',{p_praesidium_id:_praesidiumId});
  if(rr.error){toast('Reset fehlgeschlagen: '+(rr.error.message||'unbekannt'));return;}
  */
  // Lokaler Speicher bereinigen (bleibt unverändert):
  _demoMerged=false; await sSet("ccp_merged",false);
  var slk=await sList("ccp_pat_"); for(var si=0;si<slk.length;si++){await sDel(slk[si]);}
  /* 2) Demo-Neubefüllung bleibt clientseitig (UNVERÄNDERT, Zeilen 3132–3141) */
  // ...genDemoRows()-Loop...
}
```

**RPC-Aufruf-Muster** (analog `makeGast` in `docs/leitung-<suffix>.html`, Zeilen 542–554):
```js
var rr; try{ rr=await scx.rpc('argus_schulung_reset',{p_praesidium_id:_praesidiumId}); }
catch(e){ rr={error:{message:'keine Verbindung'}}; }
if(rr && rr.error){ toast('Reset fehlgeschlagen: '+(rr.error.message||'unbekannt')); return; }
```

**Tombstone-Erkennung unverändert** (index.html, Zeilen 1302–1306 — NICHT anfassen):
```js
function isCcpClosedRow(row){
  if(!row) return false;
  if(row.closed_at) return true;
  return ('join_token' in row) && !row.join_token && !(row.ort||'') && !(row.master_medic||'');
}
```

---

### `index.html` — Flag-Gates (4 Stellen, mechanische Textersetzung)

**Analog:** `index.html` selbst — `_praesidiumSchulung`-Deklaration (Zeile 296)

Alle vier Stellen — Ist-Zustand → Soll-Zustand:

| Zeile | Jetzt | Nach Umbau |
|-------|-------|------------|
| 1270 | `if(_praesidiumName!=='Schulungsumgebung' \|\| !_isMaster) return;` | `if(!_praesidiumSchulung \|\| !_isMaster) return;` |
| 2358 | `if(_praesidiumName==='Schulungsumgebung'){` | `if(_praesidiumSchulung){` |
| 2363 | `if(_praesidiumName==='Schulungsumgebung' && _isMaster && _schulDirty!==0){` | `if(_praesidiumSchulung && _isMaster && _schulDirty!==0){` |
| 3121 | `if(_praesidiumName!=='Schulungsumgebung')return;` | `if(!_praesidiumSchulung)return;` |

**`_praesidiumSchulung` wird gesetzt bei Code-Einlösung** (index.html, Zeile 296):
```js
var _praesidiumSchulung = false;   // schulung-Flag aus praesidien.schulung (Migration 0009)
```
Und beim Login (Zeile ~406): aus `praesidien.schulung` gelesen und in `localStorage('argus_praesidium_schulung')` gecacht.

---

### `docs/leitung-<suffix>.html` — `makeProvision()` + Knopf (Zugänge-Bereich)

**Analog:** `docs/leitung-<suffix>.html` — `makeGast()` (Zeilen 542–554) und `makeObserver()` (Zeilen 560–580)

**Async-Funktion mit sb()-Guard, RPC-Aufruf, toast, loadZugaenge-Reload** (Muster aus `makeGast`, Zeilen 542–554):
```js
async function makeProvision(){
  var cl=sb(); if(!cl){ toast('Sitzung abgelaufen — bitte neu anmelden.'); return; }
  var r; try{ r=await cl.rpc('argus_provision_schulung'); }
  catch(e){ r={error:{message:'keine Verbindung'}}; }
  if(r.error){ toast('Provisionierung fehlgeschlagen: '+(r.error.message||'unbekannt')); return; }
  var neu=(r.data && Array.isArray(r.data.angelegte)) ? r.data.angelegte : [];
  toast(neu.length
    ? 'Schulungs-Präsidien angelegt: '+neu.map(function(n){return esc(n);}).join(', ')
    : 'Keine neuen Schulungs-Präsidien nötig.');
  loadZugaenge();
}
```

**Knopf im `.mh`-Header** (Muster aus `secZugaenge`, Zeile 473):
```js
// In secZugaenge(), in der .mh-Zeile nach dem Aktualisieren-Knopf:
h+='<button class="btn ghost" data-action="provision">Schulungs-Präsidien anlegen/prüfen</button>';
```

**Dispatcher-Eintrag** (Muster aus Dispatcher, Zeilen 1092–1103):
```js
if(a==='provision'){ await makeProvision(); return; }
```

---

### `docs/leitung-<suffix>.html` — AT-MIST-Druckfunktion `vAtmistPrint()` + zweiter Druckknopf

**Analog:** `index.html` — `exportPrintCard(p)` (Zeilen 2198–2246) + bestehender `protoprint`-Dispatcher (Zeile 1105)

**Zweiter Knopf im vProtokollDetail-Header** (Zeilen 860–863 — bestehende Knopfzeile):
```js
// Bestehend (Zeile 860–863):
var h='<div class="mh"><h2>Einsatzprotokoll</h2><span style="display:flex;gap:8px;flex-wrap:wrap">'+
  '<button class="btn" data-action="protoexport">⬇ Übergabe-Export (JSON)</button>'+
  '<button class="btn ghost" data-action="protoprint">🖨 Drucken</button>'+
  // NEU EINFÜGEN:
  '<button class="btn ghost" data-action="atmistprint">🖨 AT-MIST drucken</button>'+
  '<button class="btn ghost" data-action="closeproto">← Zurück zur Liste</button></span></div>';
```

**Dispatcher-Eintrag** (Muster aus Zeile 1105):
```js
if(a==='protoprint'){ window.print(); return; }
if(a==='atmistprint'){ vAtmistPrint(); return; }   // NEU
```

**AT-MIST-Druckfunktion — Vorlage `exportPrintCard`** (index.html, Zeilen 2198–2246):
```js
// Benötigte Helper, die in der Leitungs-Seite NICHT vorhanden sind (Pitfall 5):
// fmtDur, ageStr, pno-Äquivalent, TXG-Array — als minimale Inline-Varianten portieren.
// nowMs() ist bereits vorhanden (Zeile 150).
// esc() ist bereits vorhanden (Zeile 153–156).
// fmtMs() ist bereits vorhanden (Zeile 158–161).

function vAtmistPrint(){
  var d=_detail && _detail.data;
  if(!d) return;
  var pats=Array.isArray(d.patients)?d.patients:[];
  var ccps=Array.isArray(d.ccps)?d.ccps:[];
  var kenn=ccps.map(function(c){return c.kennung||'?';}).join(', ')||'CCP';
  // row()-Helper — 1:1 aus exportPrintCard (index.html Zeile 2202–2207):
  function row(letter,label,val){
    return '<div style="display:flex;gap:9px;padding:7px 0;border-top:1px solid var(--line)">'
      +'<span class="mono" style="width:22px;height:22px;border-radius:6px;background:var(--acc);'
      +'color:#fff;font-weight:700;font-size:12px;display:flex;align-items:center;justify-content:center;flex-shrink:0">'
      +letter+'</span>'
      +'<div style="flex:1;min-width:0"><div style="font-size:9.5px;color:var(--mut);text-transform:uppercase;'
      +'letter-spacing:.4px;font-weight:700">'+label+'</div>'
      +'<div style="font-size:13.5px;line-height:1.4">'+val+'</div></div></div>';
  }
  // Inline-Variante pno (Leitungs-Seite kennt kein pno — Pitfall 5):
  function pno(p){ return esc(String(p.num!=null?p.num:'?'))+(p.ccp?' ('+esc(String(p.ccp))+')':''); }
  // Kein fmtDur in der Leitungs-Seite — Inline: ms-Dauer in hh:mm formatieren
  function fmtDur(ms){
    if(!ms||ms<0) return '—';
    var m=Math.floor(ms/60000); return (m>=60?Math.floor(m/60)+'h ':'')+(m%60)+'min';
  }
  // Kein ageStr — Inline: Zeitpunkt als "vor X min" oder Uhrzeit
  function ageStr(ms){
    if(!ms) return '—';
    return new Date(+ms).toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'});
  }
  // Print-Overlay aufbauen + window.print() auslösen
  var overlay=document.getElementById('atmist-print-overlay');
  if(!overlay){ overlay=document.createElement('div'); overlay.id='atmist-print-overlay'; document.body.appendChild(overlay); }
  var h='<div style="padding:12px"><div style="font-weight:700;font-size:15px;margin-bottom:10px">AT-MIST · CCP '+esc(kenn)+'</div>';
  pats.sort(function(a,b){return (+a.num||0)-(+b.num||0);}).forEach(function(p){
    var c=CATS[p.cat]||{label:esc(p.cat||'—'),bg:'#888',fg:'#fff'};
    var dim='<span style="color:#9aa0a6;font-style:italic">— nicht erfasst</span>';
    var ap=[]; if(p.age)ap.push(esc(p.age)+' J');
    var g=p.gender==='m'?'männl.':(p.gender==='w'?'weibl.':''); if(g)ap.push(g);
    var tp=[]; if(p.tqStart)tp.push('<b style="color:var(--t1)">TQ seit '+fmtDur(nowMs()-p.tqStart)+'</b>');
    if(p.created)tp.push('erfasst '+ageStr(p.created));
    var il=[]; if(Array.isArray(p.inj))p.inj.forEach(function(it){il.push(esc((it.r||'?'))+' – '+esc((it.a||'?')));});
    if(p.injury&&String(p.injury).trim())il.push(esc(String(p.injury)));
    var v=p.vit||{};
    var vp=[['WASB',v.avpu],['AF',v.af],['SpO₂',v.spo2],['HF',v.hf],['RR',v.rr],['Schmerz',v.pain]]
      .filter(function(x){return x[1]!=null&&String(x[1]).trim()!=='';});
    var sVal=vp.length?('<span class="mono">'+vp.map(function(x){return x[0]+' '+esc(String(x[1]));}).join(' · ')+'</span>'):dim;
    if(p.allergies)sVal+='<br><b style="color:var(--t1)">⚠ Allergien: '+esc(p.allergies)+'</b>';
    var tl=[]; if(p.tqStart)tl.push('Tourniquet seit '+fmtDur(nowMs()-p.tqStart));
    if(Array.isArray(p.tx))p.tx.forEach(function(t){if(t)tl.push(esc(String(t)));});
    // Als Fallback: p.tx als Freitext, falls kein Array (Leitungs-Seite kennt kein TXG — Pitfall 5)
    h+='<div class="card" style="padding:10px 13px;margin-bottom:10px">';
    h+='<div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap">';
    h+='<span style="font-family:var(--mono);font-weight:700;font-size:18px">'+pno(p)+'</span>';
    h+='<span class="pill" style="background:'+c.bg+';color:'+c.fg+'">'+c.label+'</span>';
    if(p.prio)h+='<span class="pill" style="background:var(--prio);color:#fff">PRIO</span>';
    h+='</div>';
    h+=row('A','Alter / Geschlecht',ap.length?ap.join(' · '):dim);
    h+=row('T','Zeiten',tp.length?tp.join(' · '):dim);
    h+=row('M','Mechanismus',p.moi?esc(p.moi):dim);
    h+=row('I','Verletzungen',il.length?il.join('<br>'):dim);
    h+=row('S','Befund / Vitalwerte',sVal);
    h+=row('T','Maßnahmen',tl.length?tl.map(function(x){return '▸ '+x;}).join('<br>'):dim);
    h+='</div>';
  });
  h+='</div>';
  overlay.innerHTML=h;
  document.body.setAttribute('data-atmist-print','1');
  window.print();
  document.body.removeAttribute('data-atmist-print');
  overlay.innerHTML='';
}
```

**Druck-CSS-Erweiterung** (bestehender `@media print`-Block, Zeilen 69–73 der Leitungs-Seite — erweitern):
```css
/* Bestehend (Zeilen 69–73): */
@media print{
  .side,.topbar,.btn,.mh button{display:none!important}
  body{background:#fff}
  .layout{display:block}
  /* NEU: AT-MIST-Print-Modus (body[data-atmist-print] gesetzt) */
  body[data-atmist-print] .layout { display:none !important }
  body[data-atmist-print] #atmist-print-overlay { display:block !important }
}
/* Overlay standardmäßig unsichtbar: */
#atmist-print-overlay { display:none }
```

---

### `sw.js` / `version.json` / `CHANGELOG.md` (Release v0.28.0)

**Analog:** eigene Dateien — mechanische Wertänderungen

**`sw.js` Zeile 10** — aktuell `'ccp-shell-v50'` (v50 ist in v0.27.2 verbraucht):
```js
const CACHE_NAME = 'ccp-shell-v51';   // v50 war v0.27.2 — ZWINGEND v51 (Pitfall 1)
```

**`index.html` Zeile 112** — APP_VERSION:
```js
var APP_VERSION = '0.28.0';
```

**`version.json`** — aktuell `{"version":"0.27.2"}`:
```json
{ "version": "0.28.0" }
```

**WHATS_NEW-Eintrag** (index.html, Zeilen 139–143 als Muster — KEIN UPDATE_SHEETS-Eintrag für 0.28.0):
```js
'0.28.0': [
  'Schulungs-Reset über Tombstones: Offline-Geräte bereinigen sich beim Reconnect automatisch.',
  'Jedes Präsidium kann eine eigene Schulungsumgebung erhalten (Leitungs-Seite).',
  'AT-MIST-Druckansicht im Einsatzprotokoll der Leitungs-Seite.'
],
```

**`CHANGELOG.md`** — Muster analog zum letzten Eintrag (neuer Abschnitt oben):
```markdown
## v0.28.0 — 2026-06-xx

### Neu
- Schulungs-Reset als Tombstone statt Hard-DELETE: Offline-Geräte erkennen den Reset
  beim Reconnect über die bestehende checkClosedEinsatz/handleRemoteClose-Mechanik.
- Schulungs-Präsidien-Provisionierung (Leitungs-Seite, master-only): je Produktiv-Präsidium
  ein Schulungs-Schwester-Präsidium anlegen/prüfen.
- AT-MIST-Druckansicht in der Leitungs-Seite: zweiter Druckknopf im Protokoll-Detail.

### Geändert
- Flag-Gate für Training-/Reset-Knopf: `_praesidiumSchulung` statt `_praesidiumName==='Schulungsumgebung'`
  → Reset und Training stehen in jedem Schulungs-Präsidium zur Verfügung.
- SW v51 (v50 war v0.27.2).
```

---

## Shared Patterns

### Berechtigungs-Gate (RPCs)
**Source:** `supabase/migrations/0002_phase48_datenschutz.sql`, `argus_close_einsatz`, Zeilen 102–106
**Source (master-only):** `supabase/migrations/0009_paket2_gastcode_schulung.sql`, Zeilen 264–266 und 305–307
**Anwenden auf:** beide RPCs in Migration 0011

Master ODER eigenes Präsidium (`argus_schulung_reset`):
```sql
if not public.argus_is_master()
   and ( public.argus_praesidium_id() is null
         or v_praesidium.id is distinct from public.argus_praesidium_id() ) then
  return jsonb_build_object('error', 'Keine Berechtigung');
end if;
```

Master-only (`argus_provision_schulung`):
```sql
if not public.argus_is_master() then
  raise exception 'Nur mit MasterUser-Token';
end if;
```

### In-App-Modals (keine nativen Dialoge)
**Source:** `docs/leitung-<suffix>.html`, `confirmModal`/`promptModal` (Zeilen 181–208)
**Source:** `index.html`, `confirmModal`/`promptModal` (analog)
**Anwenden auf:** schulreset-Handler in index.html (bereits `confirmModal` genutzt), `makeProvision` in Leitungs-Seite

```js
// Bestätigungs-Abfrage (Projektkonvention — kein native confirm()):
var ok=await confirmModal("Schulungs-Präsidien anlegen/prüfen?", "Anlegen");
if(!ok) return;
```

### RPC-Fehlerbehandlung (Client)
**Source:** `docs/leitung-<suffix>.html`, `makeGast` Zeilen 542–554; `extendFrist` Zeilen 964–966
**Anwenden auf:** `makeProvision()` in Leitungs-Seite, schulreset-Handler in index.html

```js
var r; try{ r=await cl.rpc('argus_xxx', {...}); }
catch(e){ r={error:{message:'keine Verbindung'}}; }
if(r.error){ toast('Fehlermeldung: '+(r.error.message||'unbekannt')); return; }
```

### HTML-Escaping
**Source:** `docs/leitung-<suffix>.html`, `esc()` (Zeilen 153–156) — identisch in index.html
**Anwenden auf:** alle dynamischen Werte in `vAtmistPrint()` und `makeProvision()`

```js
function esc(s){
  if(s==null) return '';
  return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')
    .replace(/"/g,'&quot;').replace(/'/g,'&#39;');
}
```

### `var`+`function`-Stil (kein ES6-Klassen/Arrow, keine Module)
**Source:** Gesamte index.html und docs/leitung-<suffix>.html
**Anwenden auf:** alle neuen Funktionen in index.html und Leitungs-Seite

```js
// Projektkonvention:
var _variable = false;
async function makeSomething(){ ... }
function helper(x){ return ...; }
```

---

## No Analog Found

Keine Dateien ohne Analog — alle Muster sind im Repo direkt vorhanden.

---

## Pitfall-Register (für Planer)

| # | Pitfall | Betroffene Datei | Schutzmaßnahme |
|---|---------|-----------------|----------------|
| 1 | SW bleibt auf v50 | `sw.js` | `CACHE_NAME = 'ccp-shell-v51'` — v50 ist verbraucht |
| 2 | Hygiene-DELETE ohne Präsidiums-Guard | Migration 0011 | `praesidium_id = p_praesidium_id` + `schulung=true`-Gate zwingend |
| 3 | Tombstone mit noch vorhandenen Patienten | Migration 0011 | DELETE vor UPDATE — Reihenfolge einhalten |
| 4 | `loadSchulDirty` nicht umgestellt | `index.html` Zeile 1270 | 4. Umbau-Stelle: `!_praesidiumSchulung` |
| 5 | `fmtDur`/`pno`/TXG fehlen in Leitungs-Seite | Leitungs-Seite | Inline-Varianten als Teil der `vAtmistPrint`-Funktion portieren |
| 6 | Tombstone-Hygiene löscht zu spät | Migration 0011 | Akzeptiertes Restrisiko (>7 Tage offline) — in SELF-HOSTING.md dokumentieren |

---

## Metadata

**Analog-Suchbereich:** `supabase/migrations/`, `index.html`, `docs/leitung-<suffix>.html`, `sw.js`, `version.json`
**Dateien gelesen:** 0002, 0008, 0009 (Migrations-SQL), index.html (vollständig), docs/leitung-<suffix>.html (vollständig), sw.js (Header), version.json
**Muster-Extraktionsdatum:** 2026-06-12
**Gültig bis:** nächste Schema-Änderung (keine externen Abhängigkeiten)
