---
phase: 04.13-paket3-schulung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0011_paket3_schulung.sql
  - docs/SELF-HOSTING.md
autonomous: true
requirements: [F2-Tombstone, F2-Schutz, F2-Hygiene, F2-Idempotenz, F3-Provision]
must_haves:
  truths:
    - "Ein Master-/Trainer-Token kann ein Schulungs-Präsidium per RPC zurücksetzen; offene CCPs werden zu Tombstones (closed_at gesetzt, ort='', master_medic='', join_token=null), patients+checklists gelöscht."
    - "Der Reset verweigert echte (Nicht-Schulungs-)Präsidien mit jsonb-Fehler; ein echtes Präsidium kann NIE getombstonet werden."
    - "Schulungs-Tombstones älter als 7 Tage werden beim Reset hart gelöscht — nur innerhalb des eigenen Präsidiums (praesidium_id-Filter + schulung=true-Guard)."
    - "argus_provision_schulung legt je Produktiv-Präsidium ein Schwester-Präsidium '<Name> — Schulung' (schulung=true) an, ist idempotent (2. Aufruf legt nichts an), master-only."
    - "Migration 0011 ist zweimal anwendbar ohne Fehler (create or replace); SELF-HOSTING.md führt 0011 in Liste/Reihenfolge + Betriebs-Absatz."
  artifacts:
    - path: "supabase/migrations/0011_paket3_schulung.sql"
      provides: "argus_schulung_reset(uuid) + argus_provision_schulung()"
      contains: "create or replace function public.argus_schulung_reset"
    - path: "supabase/migrations/0011_paket3_schulung.sql"
      provides: "argus_provision_schulung idempotentes Anlegen"
      contains: "create or replace function public.argus_provision_schulung"
    - path: "docs/SELF-HOSTING.md"
      provides: "0011 in Migrationsliste + Betriebs-Absatz Provisionierung"
      contains: "0011_paket3_schulung.sql"
  key_links:
    - from: "argus_schulung_reset"
      to: "public.ccps / public.patients / public.checklists"
      via: "DELETE patients/checklists VOR UPDATE-Tombstone je offenem CCP"
      pattern: "update public.ccps set\\s+closed_at"
    - from: "argus_schulung_reset"
      to: "argus_is_master() / argus_praesidium_id()"
      via: "Berechtigungs-Gate Master ODER eigenes Präsidium"
      pattern: "argus_is_master\\(\\)"
    - from: "argus_provision_schulung"
      to: "public.praesidien"
      via: "insert ... where not exists (Namenskonvention)"
      pattern: "not exists"
---

<objective>
Migration 0011 erstellen und deployen: die zwei serverseitigen RPCs für Paket 3.

1. `argus_schulung_reset(p_praesidium_id uuid)` — Tombstone-Reset (statt Hard-DELETE)
   für Schulungs-Präsidien, inkl. 7-Tage-Tombstone-Hygiene, KEIN purge_log-Eintrag.
2. `argus_provision_schulung()` — idempotentes, master-only Anlegen von
   Schulungs-Schwester-Präsidien („<Name> — Schulung", schulung=true).

Danach SELF-HOSTING.md nachführen (0011 in Liste + Betriebs-Absatz).

Purpose: Beide geräteübergreifenden Backend-Bausteine müssen existieren und live
deployt sein, BEVOR die Feld-App (Plan 02) den Reset-RPC aufruft und die
Leitungs-Seite den Provisionierungs-RPC aufruft. Backend vor Frontend.
Output: Migration 0011 live auf der Instanz, idempotent verifiziert; SELF-HOSTING.md aktuell.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04.13-paket3-schulung/04.13-CONTEXT.md
@.planning/phases/04.13-paket3-schulung/04.13-RESEARCH.md
@.planning/phases/04.13-paket3-schulung/04.13-PATTERNS.md
@.planning/phases/04.13-paket3-schulung/04.13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0011 schreiben — argus_schulung_reset + argus_provision_schulung</name>
  <files>supabase/migrations/0011_paket3_schulung.sql</files>
  <read_first>
    - supabase/migrations/0002_phase48_datenschutz.sql — `argus_close_einsatz` (Berechtigungs-Gate Master ODER eigenes Präsidium) UND `argus_run_purge` (Tombstone-Muster `update ccps set ort='', master_medic='', join_token=null`, DELETE-vor-UPDATE). Diese Datei ist die EXAKTE Vorlage für beide Muster.
    - supabase/migrations/0009_paket2_gastcode_schulung.sql — `argus_mark_export` und `argus_governance_einsaetze` (master-only `raise exception 'Nur mit MasterUser-Token'`, `create or replace`, `grant execute ... to anon`, comment-Stil); `schulung`-Flag-Definition auf `praesidien`.
    - supabase/migrations/0003_phase49_einsatzprotokoll.sql — `argus_governance_einsaetze`-Filter (`closed_at is not null and (exists patients or ... or ort<>'' or master_medic<>'' or join_token is not null)`) — bestätigt, dass ein VOLLSTÄNDIGER Tombstone NICHT in der Governance-Liste erscheint (kein Protokoll-Spam).
    - supabase/migrations/0007_jti_sofortsperre.sql — aktuelle Definitionen `argus_is_master()` / `argus_praesidium_id()` (die Gates konsumieren diese).
    - supabase/migrations/0008_nummernvergabe.sql — `argus_claim_nums` (jsonb-Fehler-Rückgabe-Muster, Zeitkonvention `(extract(epoch from now())*1000)::bigint`).
    - 04.13-PATTERNS.md Zeilen 25–152 — fertige Muster-Snippets für beide RPCs (Kopfkommentar, Gates, DELETE-vor-UPDATE, Hygiene, idempotenter INSERT, Rückgabe `angelegte`).
    - 04.13-RESEARCH.md Abschnitte F2/F3 (Zeilen 194–278) und Common Pitfalls 2/3 (Zeilen 420–426).
  </read_first>
  <action>
    Neue Datei `supabase/migrations/0011_paket3_schulung.sql` mit Copyright-Kopfkommentar
    (Stil wie 0009) und ZWEI Funktionen.

    **(A) `create or replace function public.argus_schulung_reset(p_praesidium_id uuid)
    returns jsonb language plpgsql security definer set search_path to 'public'`:**
    - Deklaration: `v_now bigint := (extract(epoch from now())*1000)::bigint;` und ein record/row-Variablen-Set für die Präsidiums-Prüfung sowie zum Iterieren über offene CCPs.
    - Schulung-Pflicht-Check ZUERST: Präsidium per `select id, schulung ... where id = p_praesidium_id` laden; `if not found` → `return jsonb_build_object('error', 'Präsidium nicht gefunden');`; `if not coalesce(schulung,false)` → `return jsonb_build_object('error', 'Nur für Schulungs-Präsidien');`.
    - Berechtigungs-Gate Master ODER eigenes Präsidium (Muster `argus_close_einsatz`): `if not public.argus_is_master() and (public.argus_praesidium_id() is null or p_praesidium_id is distinct from public.argus_praesidium_id()) then return jsonb_build_object('error', 'Keine Berechtigung'); end if;`.
    - Reset-Schleife je OFFENEM CCP (`closed_at is null` im Präsidium): Reihenfolge ZWINGEND DELETE-vor-UPDATE (Pitfall 3): `delete from public.patients where ccp_id = ...;` `delete from public.checklists where ccp_id = ...;` dann `update public.ccps set closed_at=v_now, ort='', master_medic='', join_token=null where id = ...;` — exakt diese Tombstone-Signatur (Pitfall: muss `isCcpClosedRow` am Client matchen).
    - Tombstone-Hygiene NACH der Reset-Schleife: `delete from public.ccps where praesidium_id = p_praesidium_id and closed_at is not null and closed_at < v_now - 7::bigint * 86400000;` — praesidium_id-Filter ZWINGEND (Pitfall 2). KEIN purge_log-/governance_log-Eintrag (CONTEXT-Entscheid, als Kommentar vermerken).
    - Rückgabe `jsonb_build_object('ok', true)` (oder Anzahl getombsteter CCPs, frei).
    - `grant execute on function public.argus_schulung_reset(uuid) to anon;` + `comment on function ... is '...';`.

    **(B) `create or replace function public.argus_provision_schulung() returns jsonb
    language plpgsql security definer set search_path to 'public'`:**
    - Master-only-Gate STRENG: `if not public.argus_is_master() then raise exception 'Nur mit MasterUser-Token'; end if;`.
    - Idempotentes Anlegen über Namenskonvention (kein neues Spalten-/FK-Modell): für jedes `praesidien` mit `schulung=false`, dem KEIN Schwester-Präsidium `<Name> — Schulung` (schulung=true) existiert, ein neues `insert into public.praesidien(name, schulung) ... select p.name||' — Schulung', true ... where not exists(...)`. Die angelegten Namen sammeln und als `jsonb_build_object('angelegte', <array oder []>)` zurückgeben (bei leerer Menge `'[]'::jsonb`).
    - `grant execute on function public.argus_provision_schulung() to anon;` + comment.

    Deutsche Kommentare. KEINE View-/Speicher-Naht-Änderungen. Keine neuen Spalten.
  </action>
  <verify>
    <automated>cd "/Users/gaborszeman/Desktop/Projekt CCP_APP" && grep -c "create or replace function public.argus_schulung_reset" supabase/migrations/0011_paket3_schulung.sql && grep -c "create or replace function public.argus_provision_schulung" supabase/migrations/0011_paket3_schulung.sql && grep -c "grant execute on function public.argus_schulung_reset(uuid) to anon" supabase/migrations/0011_paket3_schulung.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0011_paket3_schulung.sql` existiert.
    - Enthält genau die beiden Funktions-Signaturen `argus_schulung_reset(p_praesidium_id uuid)` und `argus_provision_schulung()`.
    - `argus_schulung_reset` enthält in dieser Reihenfolge: schulung-Pflicht-Check (`'Nur für Schulungs-Präsidien'`), Master-ODER-eigenes-Präsidium-Gate, DELETE patients+checklists VOR `update public.ccps set closed_at=...`, 7-Tage-Hygiene mit `praesidium_id = p_praesidium_id`-Filter (`grep` für `86400000` findet die Hygiene-Zeile).
    - KEIN `purge_log`/`governance_log`-Insert im Reset (`grep -c "purge_log" 0011...` → 0).
    - `argus_provision_schulung` enthält `raise exception 'Nur mit MasterUser-Token'` und einen `not exists`-geschützten INSERT mit `' — Schulung'`.
    - Beide Funktionen haben `grant execute ... to anon` und `comment on function`.
  </acceptance_criteria>
  <done>Migration 0011 SQL-Datei vollständig, beide RPCs nach Projektmuster, alle grep-Gates positiv.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0011 deployen (idempotent 2×) + REST-Positiv/Negativtests + SELF-HOSTING.md nachführen</name>
  <files>docs/SELF-HOSTING.md</files>
  <read_first>
    - docs/SELF-HOSTING.md Zeilen 69–160 — Migrationsliste/Reihenfolge (zuletzt 0009/0010), Apply-Abschnitt 5, „Hinweis 0009". Hier wird 0011 eingereiht.
    - Betriebswissen MEMORY: Supabase-Admin-Workflow (DDL via Management API + ephemerer PAT; KEIN CLI). Den PAT NICHT in eine Datei schreiben — Verfahren wie Vorphasen (PAT aus Session-Log extrahieren). Repo ist PUBLIC.
    - 04.13-VALIDATION.md Zeilen 41–50 (F2/F3-Testfälle: Positiv = schulung=true tombstont; Negativ = echtes Präsidium / fremdes Präsidium verweigert; Provision 2. Aufruf leer; Nicht-Master verweigert).
    - 04.13-RESEARCH.md F7/Validation (Zeilen 385–397, 569–595) — Migration 2× = Idempotenz, REST-Test-Pattern.
  </read_first>
  <action>
    1. **Deploy:** Migration 0011 über die Supabase Management API anwenden (PAT-Verfahren
       wie Vorphasen; Token NIE in Dateien/Commits schreiben). Anschließend EIN ZWEITES MAL
       anwenden → muss fehlerfrei durchlaufen (Idempotenz via `create or replace`).
    2. **REST-Positivtest `argus_schulung_reset`:** mit einem Token eines Präsidiums mit
       `schulung=true` (oder Master) RPC aufrufen; per REST `ccps` prüfen: vormals offene
       CCPs haben jetzt `closed_at` gesetzt, `ort=''`, `master_medic=''`, `join_token=null`;
       zugehörige `patients` gelöscht.
    3. **REST-Negativtests:** (a) RPC für ein Präsidium OHNE schulung=true → jsonb
       `{"error":"Nur für Schulungs-Präsidien"}`; (b) RPC mit Präsidiums-Token für ein
       FREMDES Präsidium (Nicht-Master) → `{"error":"Keine Berechtigung"}`.
    4. **REST-Hygiene-Test:** künstlich einen Schulungs-CCP mit `closed_at` älter als
       7 Tage anlegen → Reset aufrufen → Row ist hart gelöscht.
    5. **REST-Provision-Test:** `argus_provision_schulung()` mit Master-Token → legt für
       Produktiv-Präsidien Schwester-Präsidien an, Rückgabe `angelegte`-Liste; ZWEITER
       Aufruf → `angelegte` leer (idempotent). Mit Nicht-Master-Token → Exception/Fehler.
    6. **SELF-HOSTING.md:** `0011_paket3_schulung.sql` in die Migrationsliste (nach 0010)
       und in die Apply-Reihenfolge aufnehmen; kurzen Betriebs-Absatz ergänzen: nach dem
       Anlegen der echten Präsidien einmalig `argus_provision_schulung()` aufrufen
       (Provisionierung der Schulungs-Schwester-Präsidien), plus den dokumentierten
       Restrisiko-Hinweis aus CONTEXT (Gerät >7 Tage offline in Schulung verpasst
       Tombstone → behält lokalen Übungsmüll bis manuellem Aufräumen; kein Blocker).

    Testergebnisse (Positiv-/Negativfälle, Idempotenz) im SUMMARY festhalten.
  </action>
  <verify>
    <automated>cd "/Users/gaborszeman/Desktop/Projekt CCP_APP" && grep -c "0011_paket3_schulung.sql" docs/SELF-HOSTING.md && grep -ci "provision" docs/SELF-HOSTING.md</automated>
  </verify>
  <acceptance_criteria>
    - Migration 0011 ist zweimal angewendet worden, der zweite Lauf liefert keinen Fehler (im SUMMARY belegt).
    - REST-Positivtest belegt Tombstone-Signatur (closed_at gesetzt, ort='', master_medic='', join_token=null) und gelöschte patients (im SUMMARY).
    - REST-Negativtests belegen: Nicht-Schulungs-Präsidium → `'Nur für Schulungs-Präsidien'`; fremdes Präsidium (Nicht-Master) → `'Keine Berechtigung'` (im SUMMARY).
    - Provision idempotent belegt: 2. Aufruf liefert leere `angelegte`-Liste; Nicht-Master verweigert (im SUMMARY).
    - `docs/SELF-HOSTING.md` listet `0011_paket3_schulung.sql` und enthält einen Provisionierungs-Betriebs-Absatz inkl. 7-Tage-Restrisiko-Hinweis.
    - Kein PAT/Token in irgendeiner committeten Datei.
  </acceptance_criteria>
  <done>0011 live + idempotent, alle REST-Positiv/Negativfälle grün, SELF-HOSTING.md aktuell.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Plan 01 — neue/aktualisierte Symbole

| Symbol / Pfad | Art | Quelle |
|---|---|---|
| `public.argus_schulung_reset(p_praesidium_id uuid)` | SQL-RPC (security definer) | Migration 0011 |
| `public.argus_provision_schulung()` | SQL-RPC (security definer, master-only) | Migration 0011 |
| `supabase/migrations/0011_paket3_schulung.sql` | neue Migrationsdatei | — |
| `docs/SELF-HOSTING.md` (0011-Eintrag + Provision-Absatz) | Doku-Nachtrag | — |

Keine neuen Spalten, keine Schema-Änderung an Tabellen (bewusst Namenskonvention statt FK).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-JWT → RPC (Supabase) | Untrusted Caller-Token; Berechtigung NUR über `argus_is_master()`/`argus_praesidium_id()` (jti-Sofortsperre 0007 greift) |
| Management API → DB (Deploy) | PAT ephemerer Admin-Zugang; NIE persistiert (PUBLIC Repo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.13-01 | Tampering | argus_schulung_reset auf echtem Präsidium | mitigate | `schulung=true`-Pflicht-Check ZUERST → echte Präsidien werden nie getombstonet |
| T-04.13-02 | Elevation of Privilege | argus_schulung_reset durch fremdes Präsidium | mitigate | Gate Master ODER eigenes Präsidium (`p_praesidium_id is distinct from argus_praesidium_id()`) |
| T-04.13-03 | Elevation of Privilege | argus_provision_schulung durch Nicht-Master | mitigate | `raise exception 'Nur mit MasterUser-Token'` |
| T-04.13-04 | Tampering | 7-Tage-Hygiene löscht fremde Tombstones | mitigate | `praesidium_id = p_praesidium_id`-Filter + schulung=true-Guard zwingend |
| T-04.13-05 | Information Disclosure | PAT-Leak im Public Repo | mitigate | PAT-Verfahren wie Vorphasen, Token nie in Datei/Commit |
| T-04.13-SC | Tampering | npm/pip/cargo installs | n/a | Keine Paket-Installs in dieser Phase (RESEARCH: Package-Audit entfällt) |
</threat_model>

<verification>
- `grep` beide Funktions-Signaturen + grant in 0011 → vorhanden.
- Migration 2× anwenden → kein Fehler (Idempotenz).
- REST-Positiv-/Negativtests (schulung-Gate, Präsidiums-Gate, Hygiene, Provision-Idempotenz, master-only).
- `git grep -E '(leitung|lage)-[0-9a-f]{6,}' -- ':!docs/'` → leer (D-06).
- Kein PAT/Token in committeten Dateien.
</verification>

<success_criteria>
- Beide RPCs live und per REST positiv/negativ verifiziert.
- Migration idempotent (2× ohne Fehler).
- SELF-HOSTING.md führt 0011 + Provisionierungs-Absatz.
- Backend steht bereit für Plan 02 (Feld-App + Leitungs-Seite).
</success_criteria>

<output>
Create `.planning/phases/04.13-paket3-schulung/04.13-01-SUMMARY.md` when done.
Im SUMMARY belegen: Idempotenz-Lauf, REST-Positiv/Negativ-Resultate, Hygiene-Test, Provision-Idempotenz.
</output>
