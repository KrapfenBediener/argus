---
phase: 04.13
slug: paket3-schulung
status: verified
threats_open: 0
asvs_level: 1
created: 2026-06-13
---

# Phase 04.13 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Audit-Stand: NACH den Review-Fixes (04.13-REVIEW.md, status: fixed) und Release v0.28.1 / SW v52 — der aktuelle Code zählt, nicht der SUMMARY-Stand.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Client-JWT → RPC (Supabase) | Untrusted Caller-Token; Autorisierung NUR serverseitig über `argus_is_master()` / `argus_praesidium_id()` (jti-Sofortsperre 0007 greift) | Präsidiums-UUID, Reset-/Provision-Kommandos |
| Management API → DB (Deploy) | PAT als ephemerer Admin-Zugang; nie persistiert (Repo ist PUBLIC) | DDL (Migration 0011) |
| Client-UI → RPC | schulreset / makeProvision rufen die 0011-RPCs; Client-Gates sind reine UX | RPC-Aufrufe mit Anon-Key + JWT |
| Public Repo / GitHub Pages | D-06: kein leitung-/lage-Dateiname in committeten Texten außerhalb `docs/` | Dateinamen / URLs |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-04.13-01 | Tampering | `argus_schulung_reset` auf echtem Präsidium | mitigate | schulung=true-Pflicht-Check vor jeder Mutation: `supabase/migrations/0011_paket3_schulung.sql:69-78` (`if not coalesce(v_praesidium.schulung,false) → 'Nur für Schulungs-Präsidien'`); Reset-Schleife beginnt erst Zeile 87. Nach WR-04-Umstellung (Gate vor schulung-Prüfung) liegen BEIDE Checks weiterhin vor der ersten Mutation — Schutzwirkung erhalten. REST-Negativbeleg im 01-SUMMARY (echtes Präsidium + Master-JWT → jsonb-Fehler). | closed |
| T-04.13-02 | EoP | `argus_schulung_reset` durch fremdes Präsidium | mitigate | Gate Master ODER eigenes Präsidium: `0011_paket3_schulung.sql:63-67` (`argus_is_master()` / `p_praesidium_id is distinct from argus_praesidium_id()`). Live-Probe 2026-06-13 (read-only, Anon-Key ohne Claims, Zufalls-UUID) → `{"error":"Keine Berechtigung"}` — Gate live wirksam, zugleich Beleg, dass der WR-04-Fix (kein Existenz-Orakel: nicht-existente UUID liefert NICHT 'Präsidium nicht gefunden') deployt ist. | closed |
| T-04.13-03 | EoP | `argus_provision_schulung` durch Nicht-Master | mitigate | `0011_paket3_schulung.sql:142-144`: `if not public.argus_is_master() then raise exception 'Nur mit MasterUser-Token'`. Live-Probe 2026-06-13 (Anon-Key) → HTTP-Fehler `P0001 "Nur mit MasterUser-Token"` — Gate live wirksam, Exception vor jedem INSERT (keine Mutation). | closed |
| T-04.13-04 | Tampering | 7-Tage-Hygiene löscht fremde Tombstones | mitigate | `0011_paket3_schulung.sql:109-115`: DELETE mit `c.praesidium_id = p_praesidium_id` UND redundantem `exists (… pp.schulung)`-Guard (Defense in depth); erreichbar ohnehin nur nach bestandenem Berechtigungs- und schulung-Gate. Hygiene-Positivbeleg (8-Tage-Tombstone gelöscht, frischer blieb) im 01-SUMMARY. | closed |
| T-04.13-05 | Information Disclosure | PAT-Leak im Public Repo | mitigate | `git grep -oE 'sbp_[A-Za-z0-9]{0,50}'` über Worktree: alle 9 Treffer sind das nackte Präfix `sbp_` in Plan-/Summary-Anweisungstexten („git grep sbp_"), nie gefolgt von Token-Zeichen. Pickaxe `git log --all -S'sbp_'` + Voll-Diff der Phasen-Commits (`c0db693^..HEAD`): einziger Treffer ebenfalls nur das Präfix (01-SUMMARY-Beleg-Text). Kein PAT in Datei, Commit oder History. | closed |
| T-04.13-06 | Spoofing/EoP | Client-Flag `_praesidiumSchulung` manipuliert | accept | Client-Gates (index.html 1278, 2368, 2373, 3065, 3133) sind reine UX; echte Autorisierung ausschließlich serverseitig in den 0011-RPC-Gates (T-01/T-02/T-03, alle closed + live verifiziert). Siehe Accepted Risks Log AR-01. | closed (accepted) |
| T-04.13-07 | Information Disclosure | AT-MIST-Druck enthält Fotos | mitigate | Quelle der Druckdaten ist `argus_governance_protokoll`: `0003_phase49_einsatzprotokoll.sql:321` strippt `to_jsonb(p) - 'photo' - 'locked_by' - 'locked_at'` (nur `has_photo`-Flag). `vAtmistPrint()` (Leitungs-Seite Zeilen 955–1070): 0 Treffer für `<img`, `fetch(`, `.rpc(` — kein API-Call, kein Bild im Druckpfad. Das `<img>` bei Zeile 1084 gehört zum separaten, master-only, protokollierten Einzelfoto-Viewer (`argus_governance_photo`), nicht zum Druckpfad. | closed |
| T-04.13-08 | Information Disclosure | leitung-/lage-Dateiname leakt (D-06) | mitigate | `git grep -E '(leitung|lage)-[0-9a-f]{6,}' -- ':!docs/'` → leer; zusätzlich Voll-Diff aller Phasen-Commits (`git log -p c0db693^..HEAD -- ':!docs/'`) → 0 Treffer; alle 20 Phasen-Commit-Messages geprüft → kein Dateiname. SECURITY.md selbst nennt nur das Muster `docs/leitung-<suffix>.html`. | closed |
| T-04.13-09 | DoS | Veraltetes SW-Cache → Update kommt nicht | mitigate | `sw.js:10`: `CACHE_NAME = 'ccp-shell-v52'` (Stand nach v0.28.1, wie Register fordert; v50/v51 verbraucht); `PRECACHE_URLS` (sw.js:12–21) — alle 9 Einträge (`./` + 8 Dateien) existieren im Worktree; alter Cache wird im activate-Handler verworfen (sw.js:44). `version.json` = 0.28.1 konsistent. | closed |
| T-04.13-SC | Tampering | npm/pip/cargo installs (Supply Chain) | n/a | `git log --name-only c0db693^..HEAD`: keine package.json/Lockfile/Cargo/requirements-Änderungen in den Phasen-Commits — keine Paket-Installs in dieser Phase. | closed (n/a) |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-01 | T-04.13-06 | Client-Flag `_praesidiumSchulung` (localStorage `argus_praesidium_schulung`) ist manipulierbar; die Client-Gates steuern nur Sichtbarkeit/UX. Ein Angreifer mit manipuliertem Flag erreicht maximal den RPC-Aufruf — dort verweigern die serverseitigen Gates (schulung=true-Pflicht, Master-ODER-eigenes-Präsidium, master-only-Provision), live verifiziert 2026-06-13. Kein zusätzlicher Schaden möglich. | Plan 02 Threat Model (Owner-Entscheid) | 2026-06-13 |
| AR-02 | T-04.13-04 (Restrisiko, dokumentiert) | Gerät >7 Tage offline in einer Schulung verpasst den Tombstone (Hygiene-Hard-Delete) und behält lokalen Übungsmüll bis zum manuellen Aufräumen. Dokumentiert in Migration 0011 (Kopfkommentar) und `docs/SELF-HOSTING.md`; Review IN-02 nennt einen optionalen Client-Fix — Owner-Entscheid steht aus, kein Blocker (reine Übungsdaten ohne Personenbezug). | CONTEXT-/Owner-Entscheid | 2026-06-13 |

*Accepted risks do not resurface in future audit runs.*

---

## Unregistered Flags / Hinweise (keine Blocker)

- **Keine `## Threat Flags`-Sektion** in 04.13-01-SUMMARY.md oder 04.13-02-SUMMARY.md — der Executor hat keine neue Angriffsfläche gemeldet.
- **5. Flag-Gate (Demo-Direkt-Merge, index.html:3065):** im Plan als Discretion gekennzeichnet, im 02-SUMMARY dokumentiert; reine UX-Logik, durch T-04.13-06/AR-01 abgedeckt — kein neues Server-Privileg.
- **IN-04 (Review, informativ):** UI zeigt den Reset-Knopf nur Mastern (index.html:2373), der Server erlaubt bewusst auch das eigene Schulungs-Präsidium (Trainer-Pfad). Bewusste, aber bisher unkommentierte Diskrepanz — kein Sicherheitsloch (Server enger zu machen wäre Policy-, nicht Security-Frage); Owner-Entscheid offen.
- **Acceptance-Grep-Abweichung (kosmetisch):** Plan 02 forderte `grep -c "_praesidiumName.*Schulungsumgebung" index.html` = 0; aktuell 1 Treffer (index.html:3136) — das ist seit Review-Fix IN-03 (`8dea193`) reiner Dialog-Anzeigetext (Fallback „dieser Schulungsumgebung"), KEIN Gate. Alle Berechtigungs-relevanten Stellen nutzen `_praesidiumSchulung`.

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-06-13 | 10 | 10 (davon 1 accepted, 1 n/a) | 0 | gsd-security-auditor (Claude) |

Audit-Methodik: Code-Grep gegen Migration 0011 (Stand nach Review-Fixes `33c15ee`/`f3461f0`/`aa151f9`), index.html, Leitungs-Seite (`docs/leitung-<suffix>.html`), sw.js, 0003; History-Prüfung (Pickaxe + Phasen-Diffs) für PAT/D-06; zwei read-only Live-Proben gegen die Instanz (Anon-Key ohne Claims, Zufalls-UUID — beweisbar mutationsfrei): beide RPC-Gates verweigern korrekt.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-06-13
