---
phase: 04.13-paket3-schulung
plan: 02
type: execute
wave: 2
depends_on: ["04.13-01"]
files_modified: [index.html, docs/leitung-*.html, sw.js, version.json, CHANGELOG.md]
autonomous: true
requirements: [F1-Reset, F1-Gate, F4-AT-MIST, F3-Provision, F5-Release, D-06]
must_haves:
  truths:
    - "schulreset ruft argus_schulung_reset per RPC (kein Client-DELETE der ccps); Demo-Neubefuellung clientseitig (genDemoRows)."
    - "4 Gates (1270, 2358, 2363, 3121) pruefen _praesidiumSchulung statt _praesidiumName==='Schulungsumgebung'."
    - "Leitungs-Seite: master-only Knopf 'Schulungs-Praesidien anlegen/pruefen' ruft argus_provision_schulung + loadZugaenge."
    - "Protokoll-Detail: Knopf 'AT-MIST drucken' rendert Karten aus _detail.data.patients und druckt - kein API-Call, keine Fotos."
    - "Release v0.28.0 konsistent (APP_VERSION/version.json/CHANGELOG/WHATS_NEW), SW ccp-shell-v51, kein UPDATE-Sheet, Drehbuch geprueft."
  artifacts:
    - {path: index.html, provides: "schulreset RPC + 4 Gates + APP_VERSION 0.28.0 + WHATS_NEW", contains: argus_schulung_reset}
    - {path: docs/leitung-*.html, provides: "makeProvision + vAtmistPrint + print-CSS", contains: argus_provision_schulung}
    - {path: sw.js, provides: "CACHE_NAME ccp-shell-v51", contains: ccp-shell-v51}
    - {path: version.json, provides: "0.28.0", contains: "0.28.0"}
    - {path: CHANGELOG.md, provides: "v0.28.0", contains: "0.28.0"}
  key_links:
    - {from: "schulreset-Handler", to: "argus_schulung_reset", via: "scx.rpc(...)", pattern: "rpc\\(['\"]argus_schulung_reset"}
    - {from: "makeProvision()", to: "argus_provision_schulung", via: "cl.rpc + loadZugaenge", pattern: "rpc\\(['\"]argus_provision_schulung"}
    - {from: "vAtmistPrint()", to: "_detail.data.patients", via: "AT-MIST + window.print()", pattern: "data-action=['\"]atmistprint"}
---

<objective>
Feld-App-Umbau + Leitungs-Seiten-Erweiterungen + Release v0.28.0 / SW v51.

1. index.html: schulreset von Hard-DELETE auf argus_schulung_reset-RPC; 4 Gates auf _praesidiumSchulung (1270/2358/2363/3121); Demo-Neubefuellung clientseitig.
2. Leitungs-Seite: master-only Provisionierungs-Knopf (argus_provision_schulung); AT-MIST-Druckknopf (vAtmistPrint, kein Foto/kein API-Call).
3. Release v0.28.0; SW ccp-shell-v51 (NICHT v50 - durch v0.27.2 verbraucht); kein UPDATE-Sheet; Drehbuch pruefen.

Purpose: Backend-RPCs aus Plan 01 erhalten ihre UI-Naht.
Output: v0.28.0 live, Live-Smoke gruen.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04.13-paket3-schulung/04.13-CONTEXT.md
@.planning/phases/04.13-paket3-schulung/04.13-RESEARCH.md
@.planning/phases/04.13-paket3-schulung/04.13-PATTERNS.md
@.planning/phases/04.13-paket3-schulung/04.13-VALIDATION.md
@.planning/phases/04.13-paket3-schulung/04.13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html - schulreset auf RPC + 4 Flag-Gates auf _praesidiumSchulung</name>
  <files>index.html</files>
  <read_first>
    - index.html 3120-3150 (schulreset: Gate 3121, Hard-DELETE `scx.from('ccps').delete().eq('praesidium_id',_praesidiumId)` ~3126, Speicher-Bereinigung, genDemoRows-Loop) - nur Gate+DELETE umbauen.
    - index.html 1270 (loadSchulDirty: `if(_praesidiumName!=='Schulungsumgebung' || !_isMaster) return;` - 4. Umbaustelle, Pitfall 4).
    - index.html 2357-2364 (Training-Gate 2358, Reset-Gate 2363); 296/406/411 (_praesidiumSchulung-Deklaration + Login-Set).
    - index.html 1302-1306 (isCcpClosedRow) + ~1352-1360 (checkClosedEinsatz/handleRemoteClose): NICHT anfassen, nur Tombstone-Signatur aus Plan 01 bestaetigen.
    - 04.13-PATTERNS.md 156-217; 04.13-RESEARCH.md F1 155-190.
    - HINWEIS 3055 (`if(_praesidiumName==='Schulungsumgebung'){`, Demo-Direkt-Merge): 5. Vorkommen, NICHT Pflicht-Gate. Pruefen ob im Schulungs-Praesidium gewuenscht -> ggf. auf _praesidiumSchulung umstellen + im SUMMARY vermerken, sonst unveraendert + Discretion notieren. Die 4 Pflicht-Gates sind nicht optional.
  </read_first>
  <action>
    (a) schulreset (~3120-3150): Guard 3121 -> `if(!_praesidiumSchulung)return;`. Hard-DELETE-Zeile ERSETZEN durch RPC argus_schulung_reset {p_praesidium_id:_praesidiumId} in try/catch (Muster makeGast); bei error `toast('Reset fehlgeschlagen: '+(message||'unbekannt'))`+return; confirmModal davor bleibt. Speicher-Bereinigung + genDemoRows-Loop UNVERAENDERT.
    (b) Gates: 1270 `!_praesidiumSchulung || !_isMaster`; 2358 `_praesidiumSchulung`; 2363 `_praesidiumSchulung && _isMaster && _schulDirty!==0`; 3121 siehe (a).
    Speicher-Naht nicht umbauen. var+function-Stil, deutsche Kommentare, keine nativen Dialoge.
  </action>
  <verify>
    <automated>cd "/Users/gaborszeman/Desktop/Projekt CCP_APP" && grep -c "argus_schulung_reset" index.html && test "$(grep -c "_praesidiumName.*Schulungsumgebung" index.html)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - awk 'NR>=3115&&NR<=3160' index.html | grep -c "from('ccps').delete" -> 0.
    - grep -c "argus_schulung_reset" index.html >= 1; Guard lautet `if(!_praesidiumSchulung)return;`.
    - Gates 1270/2358/2363 nutzen _praesidiumSchulung; genDemoRows-Loop + Speicher-Bereinigung unveraendert.
    - JSC-Syntaxcheck der index.html-Script-Bloecke fehlerfrei.
  </acceptance_criteria>
  <done>schulreset ruft RPC, kein Client-DELETE; 4 Pflicht-Gates migriert; Syntax gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Leitungs-Seite - makeProvision + AT-MIST-Druck (vAtmistPrint)</name>
  <files>docs/leitung-*.html</files>
  <read_first>
    - docs/leitung-*.html makeGast/makeObserver/makeLink (~542-580): RPC-Call-Muster (sb()-Guard, try/catch, toast, loadZugaenge) - Vorlage fuer makeProvision.
    - docs/leitung-*.html secZugaenge (~472-534) + .mh-Header (~473): Platz fuer globalen Knopf.
    - docs/leitung-*.html vProtokollDetail-Knopfzeile (~860-863), protoprint-Dispatcher (~1105).
    - docs/leitung-*.html @media print (~69-73); Helper esc/fmtMs/nowMs (~150-161) vorhanden; fmtDur/ageStr/pno/TXG NICHT vorhanden -> inline portieren (Pitfall 5).
    - index.html exportPrintCard(p) (2198-2246): AT-MIST A/T/M/I/S/T, row()-Helper, KEIN <img>.
    - 04.13-PATTERNS.md 221-367 (fertige Snippets); RESEARCH F4/F5 280-356, Pitfall 5.
  </read_first>
  <action>
    (a) Provisionierung: `async function makeProvision()` nach makeGast: sb()-Guard; `cl.rpc('argus_provision_schulung')` try/catch; error -> toast 'Provisionierung fehlgeschlagen: '+message; sonst neu=r.data.angelegte (esc); toast 'Schulungs-Praesidien angelegt: '+neu.join(', ') bzw. 'Keine neuen Schulungs-Praesidien noetig.'; danach loadZugaenge(). Knopf in .mh: `<button class="btn ghost" data-action="provision">Schulungs-Praesidien anlegen/pruefen</button>`. Dispatcher: `if(a==='provision'){ await makeProvision(); return; }`.
    (b) AT-MIST: Knopf in vProtokollDetail: `<button class="btn ghost" data-action="atmistprint">AT-MIST drucken</button>`. Dispatcher: `if(a==='atmistprint'){ vAtmistPrint(); return; }`. `function vAtmistPrint()` liest _detail.data.patients (kein API-Call), rendert je Patient AT-MIST-Karte (A Alter/Geschlecht, T Zeiten, M Mechanismus, I Verletzungen, S Befund/Vitalwerte+Allergien, T Massnahmen) in `#atmist-print-overlay`; Inline-Helper fmtDur/ageStr/pno (esc/fmtMs/nowMs vorhanden); KEIN <img>/Foto; setzt `data-atmist-print` am body, window.print(), entfernt Attribut + leert Overlay. @media print erweitern: bei body[data-atmist-print] .layout aus, #atmist-print-overlay ein; Overlay default display:none.
    Keine neuen RPCs/Daten. D-06: echter Dateiname NICHT in PLAN/SUMMARY/Commit (Glob docs/leitung-*.html).
  </action>
  <verify>
    <automated>cd "/Users/gaborszeman/Desktop/Projekt CCP_APP" && F=$(ls docs/leitung-*.html|head -1); grep -c "argus_provision_schulung" "$F"; grep -c "atmistprint" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - Leitungs-Seite enthaelt makeProvision + rpc('argus_provision_schulung') + data-action="provision".
    - Enthaelt vAtmistPrint + data-action="atmistprint" + atmist-print-overlay + data-atmist-print-print-CSS.
    - Kein neuer fetch/RPC im AT-MIST-Druckpfad; kein `<img` im AT-MIST-Render.
    - JSC-Syntaxcheck fehlerfrei; Preview-Screenshot der AT-MIST-Ansicht im SUMMARY.
  </acceptance_criteria>
  <done>Provisionierung ruft RPC + Reload; AT-MIST druckt ohne API/Foto; Syntax gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Release v0.28.0 / SW v51 + Drehbuch-Check + Commit + Push + Live-Smoke</name>
  <files>sw.js, version.json, CHANGELOG.md, index.html</files>
  <read_first>
    - sw.js 10 `const CACHE_NAME = 'ccp-shell-v50';` (Soll v51 - Pitfall 1).
    - index.html 112 `var APP_VERSION = '0.27.2';`; WHATS_NEW/UPDATE_SHEETS (~139-143) - KEIN UPDATE_SHEETS-Eintrag fuer 0.28.0.
    - version.json `{ "version": "0.27.2" }`; CHANGELOG.md letzter Eintrag (Stil).
    - 04.13-PATTERNS.md 371-414; RESEARCH F6 359-380; MEMORY Release-Konvention + Drehbuch-Dauerregel.
  </read_first>
  <action>
    1. sw.js: CACHE_NAME = 'ccp-shell-v51'. 2. index.html: APP_VERSION = '0.28.0'. 3. version.json: { "version": "0.28.0" }.
    4. WHATS_NEW['0.28.0'] befuellen (Tombstone-Reset offline-sicher; eigene Schulungsumgebung je Praesidium; AT-MIST-Druck Leitungs-Seite). KEIN UPDATE_SHEETS-Eintrag.
    5. CHANGELOG.md: v0.28.0 (Neu: Tombstone-Reset, Provisionierung, AT-MIST-Druck; Geaendert: Flag-Gate _praesidiumSchulung, SW v51).
    6. Drehbuch-Check: bestaetigen dass Reset-Knopf in KEINER Lektion vorkommt -> keine Anpassung, im SUMMARY vermerken; Diskretionsentscheid 'kein UPDATE-Sheet' im SUMMARY dokumentieren.
    7. Precache-/Asset-Konsistenz pruefen (python3 -m http.server, Asset-200).
    8. Commit (D-06: kein leitung-/lage-Dateiname in Message) + Push. Live-Smoke GitHub Pages: version.json zeigt 0.28.0, Marker-Grep in ausgeliefertem index.html/sw.js.
  </action>
  <verify>
    <automated>cd "/Users/gaborszeman/Desktop/Projekt CCP_APP" && grep -c "ccp-shell-v51" sw.js; grep -c "0.28.0" version.json</automated>
  </verify>
  <acceptance_criteria>
    - sw.js enthaelt ccp-shell-v51; version.json 0.28.0; index.html APP_VERSION 0.28.0; WHATS_NEW['0.28.0'] befuellt; KEIN UPDATE_SHEETS['0.28.0'].
    - CHANGELOG.md hat v0.28.0-Abschnitt.
    - git grep -E '(leitung|lage)-[0-9a-f]{6,}' -- ':!docs/' leer; Commit-Message ohne leitung-/lage-Dateiname.
    - Live-Smoke: ausgeliefertes version.json = 0.28.0 (im SUMMARY belegt); Drehbuch-Check + UPDATE-Sheet-Entscheid im SUMMARY.
  </acceptance_criteria>
  <done>v0.28.0 konsistent + live, SW v51, D-06 sauber, Drehbuch geprueft.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Symbol / Pfad | Art |
|---|---|
| index.html schulreset-Handler (RPC-Aufruf) | geaenderter Handler |
| index.html 4 Flag-Gates (_praesidiumSchulung) | geaenderte Gates 1270/2358/2363/3121 |
| `makeProvision()` (Leitungs-Seite) | neue JS-Funktion |
| `vAtmistPrint()` (Leitungs-Seite) | neue JS-Funktion |
| data-action "provision" / "atmistprint" + #atmist-print-overlay + @media print-Erweiterung | neue UI/CSS |
| APP_VERSION 0.28.0 / version.json 0.28.0 / CACHE_NAME ccp-shell-v51 / WHATS_NEW['0.28.0'] / CHANGELOG v0.28.0 | Release-Bumps |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Client-UI -> RPC | schulreset/makeProvision rufen Plan-01-RPCs; serverseitige Gates entscheiden (Client-Gate ist nur UX) |
| Public Repo / GitHub Pages | D-06: kein leitung-/lage-Dateiname in committeten Texten |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-04.13-06 | Spoofing/EoP | Client-Flag _praesidiumSchulung manipuliert | accept | Client-Gate ist nur UX; echte Autorisierung im RPC (Plan 01, schulung=true + Master/eigenes Praesidium) |
| T-04.13-07 | Information Disclosure | AT-MIST-Druck enthaelt Fotos | mitigate | argus_governance_protokoll liefert kein photo; vAtmistPrint ohne <img>-Tag |
| T-04.13-08 | Information Disclosure | leitung-/lage-Dateiname leakt | mitigate | D-06: Glob docs/leitung-*.html in Plan/Summary/Commit; git grep-Gate |
| T-04.13-09 | DoS | Veraltetes SW-Cache -> Update kommt nicht | mitigate | CACHE_NAME ccp-shell-v51 (v50 verbraucht); Precache-Check |
| T-04.13-SC | Tampering | npm/pip/cargo installs | n/a | keine Paket-Installs |
</threat_model>

<verification>
- index.html: schulreset ruft RPC, kein Client-DELETE; 4 Gates migriert; JSC-Syntax gruen.
- Leitungs-Seite: makeProvision + vAtmistPrint vorhanden; kein API/Foto im AT-MIST-Druck; JSC-Syntax gruen.
- Release: ccp-shell-v51, version.json/APP_VERSION/CHANGELOG/WHATS_NEW = 0.28.0; kein UPDATE_SHEETS-Eintrag.
- D-06: git grep -E '(leitung|lage)-[0-9a-f]{6,}' -- ':!docs/' leer; Commit-Message sauber.
- Live-Smoke nach Push (version.json + Marker).
</verification>

<success_criteria>
- schulreset offline-sicher ueber Tombstone-RPC; Schulungs-Knoepfe in jedem Schulungs-Praesidium.
- Provisionierungs-Knopf + AT-MIST-Druck auf der Leitungs-Seite funktionsfaehig.
- v0.28.0 / SW v51 konsistent und live; D-06 eingehalten; Drehbuch geprueft.
</success_criteria>

<output>
Create `.planning/phases/04.13-paket3-schulung/04.13-02-SUMMARY.md` when done.
Im SUMMARY belegen: 3055-Discretion, AT-MIST-Preview, Live-Smoke, Drehbuch-Check, UPDATE-Sheet-Entscheid.
</output>
