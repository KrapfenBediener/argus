---
phase: 04.12-flz-lageansicht
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_phase412_lageansicht.sql
autonomous: true
requirements: [FLZ-A-BACKEND]
must_haves:
  truths:
    - "Ein Beobachter-Code lässt sich gegen ein Observer-JWT tauschen, das is_observer:true + observer_praesidium_id trägt und NIEMALS praesidium_id"
    - "Ein Observer-JWT erhält per REST auf patients/ccps/checklists/access_tokens 0 Zeilen und kann keine Governance-RPCs aufrufen (Negativtests belegt)"
    - "argus_lage liefert dem Observer-JWT je aktivem (nicht abgeschlossenem) CCP seines Präsidiums Zähler je Kategorie (T1/T2/T3/T5), gPA, Prio, transportfertig — KEINE Patientenrows, keine Namen, keine Fotos, keine Bediener-Kürzel"
    - "Beobachter-Codes funktionieren NICHT im Feld-App-Exchange (argus_exchange_code weist sie ab); MasterToken/normale Codes funktionieren NICHT im Beobachter-Exchange"
    - "Jeder Beobachter-Login (Exchange-Erfolg) erzeugt genau einen governance_log-Eintrag action='lage_view' — nicht je Poll"
    - "revoked sperrt auch Beobachter-Codes ('Code wurde gesperrt')"
    - "Der DB-Stand ist als idempotente Migration 0005 versioniert (zweifaches Anwenden gefahrlos)"
  artifacts:
    - path: "supabase/migrations/0005_phase412_lageansicht.sql"
      provides: "observer-Spalte, Claim-Helfer, Observer-Exchange, argus_lage, lage_view-Log, Feld-Exchange-Abweisung"
      contains: "argus_exchange_observer_code"
  key_links:
    - from: "argus_lage"
      to: "request.jwt.claims->observer_praesidium_id"
      via: "argus_observer_praesidium_id() (security definer, stable)"
      pattern: "observer_praesidium_id"
    - from: "argus_exchange_observer_code"
      to: "governance_log"
      via: "insert action='lage_view' im Erfolgspfad"
      pattern: "lage_view"
    - from: "argus_exchange_code"
      to: "access_tokens.observer"
      via: "Abweisung observer-Tokens mit eigenem Fehlertext"
      pattern: "observer"
---

<objective>
Backend für die FLZ-Lageansicht Stufe a (Phase 4.12): Migration 0005 mit
Beobachter-Token-Art, Observer-JWT-Exchange (eigene Claim-Form gem. fixiertem
Sicherheits-Design aus ROADMAP Phase 8), `security definer`-Aggregat-RPC
`argus_lage` und schlanker `lage_view`-Protokollierung. Anwendung auf das
Live-Projekt über die Management API; Beleg per REST-E2E-Positiv- und
Negativtests.

Output: `supabase/migrations/0005_phase412_lageansicht.sql` (versioniert) +
angewendeter Live-DB-Stand + Testbelege im SUMMARY.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.12-flz-lageansicht/04.12-CONTEXT.md
@.planning/ROADMAP.md
@CLAUDE.md
@supabase/migrations/0001_phase4_jwt_rls.sql
@supabase/migrations/0003_phase49_einsatzprotokoll.sql
</context>

<wichtige_regeln>
- **Sicherheits-Design (fixiert, NICHT verhandelbar):** Observer-JWT trägt
  `is_observer:true` + `observer_praesidium_id` — bewusst NICHT `praesidium_id`.
  Dadurch liefert `argus_praesidium_id()` für Beobachter `null` und ALLE
  bestehenden RLS-Policies verweigern Roh-Zugriff. Keine neue RLS-Policy für
  Beobachter anlegen!
- **Feld-App `index.html` bleibt UNANGETASTET.** Die Abweisung von
  Beobachter-Codes geschieht serverseitig in `argus_exchange_code` mit einem
  für Nutzer verständlichen Fehlertext (die Feld-App zeigt `data.error` an).
- **Kein node, kein Supabase CLI.** DDL via Management API
  (`POST https://api.supabase.com/v1/projects/sehuosjyjmrpzcqrelej/database/query`,
  Bearer = PAT). Der PAT liegt NICHT in Dateien — er stammt aus der Session
  (User-Freigabe 2026-06-12: Token weiterhin gültig, keine Rückfrage nötig).
  PAT NIEMALS in Dateien, Commits oder Logs schreiben (`git grep -i sbp_` muss
  sauber bleiben, Muster 04.9-01).
- **Zeitstempel-Konvention:** bigint in JS-Millisekunden (Date.now()), wie
  0002/0003. governance_log.at = ms.
- **Idempotenz:** Migration wie 0003 formulieren (`add column if not exists`,
  `create or replace function`, `drop policy if exists` wo nötig).
- **Keine toten RPCs, keine spekulativen Umbauten** an bestehenden Policies
  oder Funktionen außer der argus_exchange_code-Neufassung.
</wichtige_regeln>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0005 schreiben (observer-Spalte, Claim-Helfer, Exchanges, argus_lage, Log)</name>
  <files>supabase/migrations/0005_phase412_lageansicht.sql</files>
  <read_first>
    - supabase/migrations/0001_phase4_jwt_rls.sql (Claim-Helfer-Muster argus_praesidium_id/argus_is_master, JWT-Signierung via extensions.sign + Vault 'argus_jwt_secret')
    - supabase/migrations/0003_phase49_einsatzprotokoll.sql (aktuelle Fassung argus_exchange_code inkl. revoked; Idempotenz-Muster; governance_log-Inserts; Kommentar auf governance_log.action)
    - supabase/migrations/0002_phase48_datenschutz.sql Abschnitt 3 (governance_log-Spalten: at, ccp_id, patient_id, kuerzel, action, begruendung, neue_frist)
    - index.html Zeilen 811–822 und 1541–1544 (Kategorie-/Status-Semantik: cat∈T1/T2/T3/T5, status 'active'/'gpa', prio/ready nur bei active zählen)
  </read_first>
  <action>
    Neue Datei `supabase/migrations/0005_phase412_lageansicht.sql` mit Kopfkommentar
    (Muster 0003: Zweck, Owner-Entscheide/Quelle = fixiertes Phase-8-Design,
    Anwendung via Management API, Idempotenz-Hinweis, ms-Konvention). Inhalt:

    1. **Token-Art:** `alter table public.access_tokens add column if not exists
       observer boolean not null default false;` (Diskretionsentscheid: Boolean-
       Spalte, konsistent zu is_master/temporary/single_use).
    2. **Claim-Helfer (Muster 0001):**
       - `argus_is_observer()` returns boolean — liest `is_observer` aus
         `request.jwt.claims`, coalesce false.
       - `argus_observer_praesidium_id()` returns uuid — liest
         `observer_praesidium_id`, nullif ''.
       Beide `language sql stable security definer`.
    3. **argus_exchange_code-Neufassung** (identisch zur 0003-Fassung, plus):
       `observer` mitlesen; direkt nach der revoked-Prüfung:
       `if coalesce(v_token.observer,false) then return jsonb_build_object('error',
       'Beobachter-Code — nur für die Lageansicht gültig');` — Feld-App und
       Leitungs-Seite zeigen diesen Text unverändert an, kein App-Update nötig.
    4. **Neuer RPC `argus_exchange_observer_code(code text)`** (security definer,
       search_path public/extensions/vault, grant execute to anon):
       - Normalisierung identisch zu argus_exchange_code (upper, [\s\-] strippen,
         <8 → 'Code zu kurz', short_code XXXX-XXXX).
       - Token laden; not found → 'Ungültiger oder verbrauchter Code';
         revoked → 'Code wurde gesperrt'; `not coalesce(observer,false)` →
         'Kein Beobachter-Code' (MasterToken/normale Codes funktionieren hier NICHT);
         observer-Token ohne praesidium_id → 'Konfigurationsfehler' (ein
         Beobachter-Code MUSS präsidiumsgebunden sein).
       - TTL: `ttl_hours*3600` falls gesetzt, sonst 24*3600 (Diskretionsentscheid:
         Beobachter-JWT kurzlebig, Default 24 h — bewusst KEINE 30 Tage).
       - JWT via extensions.sign mit Claims: iss 'supabase', sub 'argus-observer',
         role 'anon', **is_observer true, observer_praesidium_id v_token.praesidium_id**,
         iat, exp — KEIN praesidium_id-Claim, KEIN is_master-Claim.
       - **lage_view-Log VOR der Rückgabe** (einmal je Login, nicht je Poll):
         `insert into governance_log (at, kuerzel, action) values (ms-now,
         v_short_code, 'lage_view');` — kuerzel = der eingelöste short_code
         (kein Personenbezug, keine Bediener-Kürzel; Begründung: DSFA-Hinweis
         Observer-Zugriffs-Protokollierung).
       - Rückgabe: jsonb mit jwt, exp, praesidium_id (zur Anzeige), praesidium_name,
         ttl_seconds.
    5. **Aggregat-RPC `argus_lage()`** (security definer, search_path public,
       grant execute to anon):
       - Zugang: `if not public.argus_is_observer() or
         public.argus_observer_praesidium_id() is null then raise exception
         'Nur mit Beobachter-Zugang';` — Designentscheid: argus_lage ist
         observer-exklusiv; normale JWTs und Master erhalten die Exception
         (sauber testbar; Master nutzt die Leitungs-Seite).
       - Je CCP des Beobachter-Präsidiums mit `closed_at is null` (Lage = aktuell,
         abgeschlossene Einsätze NICHT enthalten): jsonb-Objekt mit
         `kennung`, `ort`, `t1`,`t2`,`t3`,`t5` (count patients status='active'
         und cat=…), `gpa` (status='gpa'), `prio` (active und prio),
         `ready` (active und ready), `letzte_aenderung` (max(patients.updated),
         null wenn keine Patienten). Sortierung nach kennung.
       - KEINE Patientenrows, KEINE Namen/Fotos/Notizen/Vitalwerte, KEIN
         master_medic, KEINE locked_by/Bediener-Felder.
       - Top-Level: `praesidium_name`, `stand` (Server-ms), `ccps` (Array, [] wenn
         keine aktiven CCPs).
    6. **Kommentar nachziehen:** `comment on column public.governance_log.action is
       'foto_view | frist_verlaengert | protokoll_view | lage_view';`

    NICHT tun: neue RLS-Policies, Änderungen an bestehenden Policies, Realtime-
    Publikationen, Edge Functions, Änderungen an argus_check_code (Beobachter-Codes
    werden von der Lage-Seite nicht re-gecheckt; Sperre wirkt beim nächsten Login
    durch JWT-Kurzlebigkeit ≤24 h — als akzeptiertes Rest-Risiko im SUMMARY notieren).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0005_phase412_lageansicht.sql && grep -q "add column if not exists observer" supabase/migrations/0005_phase412_lageansicht.sql && grep -q "argus_exchange_observer_code" supabase/migrations/0005_phase412_lageansicht.sql && grep -q "lage_view" supabase/migrations/0005_phase412_lageansicht.sql && grep -q "argus_lage" supabase/migrations/0005_phase412_lageansicht.sql && python3 -c "
s=open('supabase/migrations/0005_phase412_lageansicht.sql').read()
# Observer-Exchange-Funktionsblock isolieren
i=s.index('argus_exchange_observer_code'); blk=s[i:s.index('\$function\$;', i)]
sign=blk[blk.index('extensions.sign'):]
assert \"'is_observer'\" in sign and \"'observer_praesidium_id'\" in sign, 'Observer-Claims fehlen im sign-Payload'
assert \"'praesidium_id',\" not in sign and \"'is_master'\" not in sign, 'VERBOTEN: praesidium_id/is_master im Observer-JWT'
assert 'sbp_' not in s.lower() or all(len(t)<10 for t in __import__('re').findall(r'sbp_[A-Za-z0-9_]*', s)), 'PAT in Datei!'
print('Claim-Form OK')"</automated>
  </verify>
  <done>Migration 0005 existiert, idempotent formuliert, enthält observer-Spalte, beide Claim-Helfer, Exchange-Neufassung mit Beobachter-Abweisung, Observer-Exchange mit korrekter Claim-Form (kein praesidium_id im signierten Payload), argus_lage und lage_view-Log; kein PAT in der Datei.</done>
</task>

<task type="auto">
  <name>Task 2: Migration via Management API anwenden (idempotent, zweifach)</name>
  <files>(Live-DB sehuosjyjmrpzcqrelej — keine Repo-Dateien)</files>
  <read_first>
    - supabase/migrations/0005_phase412_lageansicht.sql (die zu sendende Quelle)
    - .planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md (Muster: Anwendung per Management API, PAT-Hygiene)
  </read_first>
  <action>
    1. PAT aus der Session verwenden (NICHT in Dateien/Logs schreiben; in
       Shell-Kommandos als Variable halten, Ausgaben maskieren).
    2. Migration als EIN Query-Body an
       `POST https://api.supabase.com/v1/projects/sehuosjyjmrpzcqrelej/database/query`
       senden (JSON-encoded; python3 für sauberes Escaping verwenden).
    3. **Idempotenz-Beleg:** denselben Body ein ZWEITES Mal senden — beide Läufe
       müssen ohne Fehler durchlaufen.
    4. Verifikations-Queries über dieselbe API: (a) `select column_name from
       information_schema.columns where table_name='access_tokens' and
       column_name='observer'` → 1 Zeile; (b) `select proname from pg_proc where
       proname in ('argus_exchange_observer_code','argus_lage','argus_is_observer',
       'argus_observer_praesidium_id')` → 4 Zeilen; (c) `select prosecdef from
       pg_proc where proname='argus_lage'` → true.
  </action>
  <verify>
    <automated>echo "Beleg erfolgt im Ausführungsprotokoll: 2× HTTP 2xx auf database/query + Verifikations-Queries (observer-Spalte vorhanden, 4 Funktionen, argus_lage security definer)"</automated>
  </verify>
  <done>Migration 0005 zweifach fehlerfrei angewendet; observer-Spalte, alle 4 neuen/geänderten Funktionen live; argus_lage ist security definer; kein PAT in Dateien oder Commits.</done>
</task>

<task type="auto">
  <name>Task 3: REST-E2E: Positiv- und Negativtests des Sicherheits-Designs</name>
  <files>(Live-DB — Testdaten im Schulungs-Präsidium; keine Repo-Dateien)</files>
  <read_first>
    - config.js (SUPA_URL + Anon-Key für REST-Aufrufe)
    - supabase/migrations/0001_phase4_jwt_rls.sql (RLS-Policies, die verweigern müssen)
  </read_first>
  <action>
    Testaufbau über Management API: einen Test-Beobachter-Code für das
    Schulungs-Präsidium anlegen (`insert into access_tokens (token, praesidium_id,
    observer, label, short_code) values (md5(random()::text), <schulungs-id>,
    true, 'TEST 4.12 Beobachter', 'ZZTE-ST01')`). Präsidiums-ID per Query ermitteln.

    Dann per REST (curl, Anon-Key als apikey):
    1. **Observer-Exchange positiv:** rpc/argus_exchange_observer_code mit
       'ZZTEST01' → jwt vorhanden; JWT-Payload base64-dekodieren und belegen:
       `is_observer:true`, `observer_praesidium_id` gesetzt, **kein** `praesidium_id`,
       **kein** `is_master`.
    2. **Roh-Tabellen-Negativtests mit Observer-JWT (Bearer):**
       GET /rest/v1/patients?select=id → `[]`; ebenso ccps, checklists,
       access_tokens → jeweils `[]` (RLS verweigert via argus_praesidium_id()=null).
    3. **Governance-Negativtest:** rpc/argus_governance_einsaetze mit Observer-JWT
       → Fehler (Nur mit MasterUser-Token).
    4. **argus_lage positiv:** mit Observer-JWT → 200, JSON mit praesidium_name,
       stand, ccps-Array; Stichprobe: kein Schlüssel `name`, `photo`, `master_medic`,
       `locked_by` in der Antwort (python3-Assertion über das ganze JSON).
    5. **argus_lage negativ:** (a) mit blankem Anon-Key (ohne JWT) → Fehler;
       (b) mit normalem Feld-JWT (via argus_exchange_code mit existierendem
       Schulungs-Code aus access_tokens holen) → Fehler 'Nur mit Beobachter-Zugang'.
    6. **Cross-Exchange-Negativtests:** (a) Beobachter-Code in
       rpc/argus_exchange_code → `{error:'Beobachter-Code — nur für die Lageansicht
       gültig'}`; (b) normaler Schulungs-Code in rpc/argus_exchange_observer_code
       → `{error:'Kein Beobachter-Code'}`.
    7. **Log-Beleg:** Management-API-Query `select count(*) from governance_log
       where action='lage_view' and kuerzel='ZZTE-ST01'` → genau die Anzahl der
       in Schritt 1 erfolgten Erfolgs-Exchanges (1×, wenn nur einmal getauscht).
    8. **revoked-Beleg:** Test-Code per Management API `revoked=true` setzen →
       Observer-Exchange → 'Code wurde gesperrt'.
    9. **Aufräumen:** Test-Code per Management API löschen (`delete from
       access_tokens where short_code='ZZTE-ST01'`); lage_view-Testeinträge im
       governance_log BELASSEN (echtes Protokoll, 12-Monats-Retention räumt auf —
       im SUMMARY vermerken).
  </action>
  <verify>
    <automated>echo "Beleg erfolgt im Ausführungsprotokoll: JWT-Claim-Dump, 4× leere Arrays, Governance-Verweigerung, argus_lage-Antwort ohne Personenfelder, beide Cross-Exchange-Abweisungen, lage_view-Count, revoked-Abweisung"</automated>
  </verify>
  <done>Alle Positiv- und Negativtests bestanden und im Ausführungsprotokoll/SUMMARY dokumentiert; Testdaten aufgeräumt (Code gelöscht), keine Klarnamen/Geheimnisse in Belegen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Beobachter (FLZ) → Supabase | Niedrigeres Vertrauensniveau als Feld/Leitung; nur Aggregat-RPC |
| Public Repo → Welt | Migration 0005 ist öffentlich — darf Design, aber keine Geheimnisse enthalten |
| Claude-Session → Management API | PAT nur flüchtig in der Session, nie in Dateien |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.12-01 | Elevation of Privilege | Observer-JWT | mitigate | Eigene Claim-Form ohne praesidium_id → bestehende RLS verweigert Roh-Zugriff; REST-Negativtests (Task 3.2/3.3) |
| T-4.12-02 | Information Disclosure | argus_lage | mitigate | Nur Zähler + kennung/ort; Assertion „keine Personenfelder im JSON" (Task 3.4); Kleinstzahlen-Re-Identifikation → DSFA-Randnotiz (Plan 02) |
| T-4.12-03 | Spoofing | Beobachter-Code in Feld-App | mitigate | Serverseitige Abweisung in argus_exchange_code (Task 1.3, Test 3.6a) |
| T-4.12-04 | Elevation of Privilege | MasterToken auf Lage-Seite | mitigate | argus_exchange_observer_code weist Nicht-Beobachter-Codes ab (Test 3.6b) |
| T-4.12-05 | Repudiation | Beobachter-Zugriff | mitigate | lage_view-Log je Login (Task 1.4, Test 3.7); 12-Monats-Retention aus 0004 greift |
| T-4.12-06 | Information Disclosure | PAT | mitigate | Nur Session-Variable, maskierte Ausgaben, Gate `git grep -i sbp_` vor Commit |
| T-4.12-07 | Denial of Service | argus_lage-Polling | accept | Free Tier, 15–30-s-Poll einzelner FLZ-Clients; kein Realtime-Abo (Design) |
| T-4.12-SC | Tampering | Paket-Installationen | accept | Keine npm/pip/cargo-Installationen (kein node verfügbar) |
</threat_model>

<verification>
- Observer-JWT-Claim-Form per Payload-Dump belegt (is_observer + observer_praesidium_id, kein praesidium_id/is_master).
- 4 Roh-Tabellen liefern dem Observer-JWT leere Arrays; Governance-RPC verweigert.
- argus_lage observer-exklusiv (Anon, Feld-JWT abgewiesen); Antwort frei von Personenfeldern.
- Cross-Exchange beidseitig abgewiesen; revoked wirkt; lage_view-Log genau 1× je Login.
- Migration idempotent (2× angewendet), versioniert, ohne Geheimnisse.
</verification>

<success_criteria>
- Fixiertes Phase-8-Sicherheits-Design vollständig umgesetzt und per REST-Tests belegt.
- Feld-App und Leitungs-Seite unangetastet (kein Diff an index.html / docs/leitung-*.html in diesem Plan).
- DB-Stand als 0005 versioniert; PAT nirgends persistiert.
</success_criteria>

<output>
Create `.planning/phases/04.12-flz-lageansicht/04.12-01-SUMMARY.md` when done
(Diskretionsentscheide dokumentieren: observer-Boolean, eigener Exchange-RPC,
24-h-Default-TTL, argus_lage observer-exklusiv, kein argus_check_code für
Beobachter; Testbelege zusammenfassen).
</output>
