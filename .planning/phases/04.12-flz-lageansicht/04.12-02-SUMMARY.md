# Plan 04.12-02 — SUMMARY

**Phase:** 04.12 FLZ-Lageansicht Stufe a · **Plan:** 02 (Beobachter-Seite, Leitungs-Erweiterung, Doku)
**Ausgeführt:** 2026-06-12 · **Status:** ✅ abgeschlossen — **Phase 4.12 damit komplett**

## Was gebaut wurde

1. **Beobachter-Seite `docs/lage-<suffix>.html`** (konkreter Name NUR in der
   untracked `LAGE-URL.md`; `.gitignore`-Eintrag `/LAGE-URL.md` wurde VOR der
   Dateierstellung gesetzt und per `git check-ignore` belegt):
   - Login mit Beobachter-Code → `rpc/argus_exchange_observer_code` →
     sessionStorage (`arguslg_jwt/_exp/_pname`, flüchtig), Abmelden-Knopf.
   - Lage-Ansicht: Summenzeile („Gesamt · n CCPs") + Karte je aktivem CCP
     mit großen Zahlen (Mono, 36 px) in Triage-Farben T1/T2/T3/T5 + gPA,
     Zeile „Prio / transportfertig", „letzte Änderung HH:MM" je Karte.
   - Polling `rpc/argus_lage` alle 20 s + sofort bei `online`/`visibilitychange`;
     Status in der Topbar („Stand: HH:MM:SS" + grüner Punkt); Fehlerzustand:
     roter Punkt + Banner „Keine Verbindung — letzte Zahlen von HH:MM" (alte
     Zahlen bleiben stehen); 401/abgelaufene Sitzung → zurück zum Login.
   - Footer-Hinweis „Anonyme Übersichtszahlen — keine Personendaten."
   - ARGUS-Optik (Leitungs-Palette, IBM Plex lokal, Schiefer-Chrome), noindex/
     nofollow/noarchive, no-referrer, `../config.js` als einzige Endpunkt-Quelle,
     esc() für alle dynamischen Werte (kennung/ort), keine PWA-/SW-Einbindung.
2. **Leitungs-Seite (Zugänge-Bereich):** Knopf „Beobachter-Zugang (FLZ)" je
   Präsidium (`makeObserver`: vorhandenen aktiven observer-Code wiederverwenden,
   sonst Insert mit `observer:true`); Art-Anzeige „Beobachter (FLZ)";
   Hinweistext Verteilungs-Disziplin; `observer` in der select-Spaltenliste;
   `makeLink`-Wiederverwendungs-Filter um `!t.observer` ergänzt; Sperren läuft
   über bestehendes toggleRevoke. **Bewusst NUR der Code in die Zwischenablage —
   die Lage-URL wird in der committeten Datei nirgends konstruiert (D-06).**
3. **Interne Doku (gitignored, lokal, NICHT committet):** DSB-BRIEFING Punkt 5
   um Status „Stufe a umgesetzt" ergänzt; DSFA (03) R7-Randnotiz Kleinstzahlen
   (auch für Stufe a, k-Schwelle = DSB-Frage, in argus_lage nachrüstbar);
   DATENSCHUTZ-SPEC „Ergänzung Phase 4.12"; Maßnahmenplan (08) — Lageübersicht
   Stufe a [~], Beobachter-Zugriffsprotokoll [x]; ZEITLOG-Zeile.
4. **Statusführung:** ROADMAP 4.12 ✅ (mit Abschluss-Vermerk), Phase-8-Vermerk
   „Stufe a vorgezogen"; STATE Kopf + Abschluss-Tabelle.

## Diskretionsentscheide

- **fetch statt vendor/supabase.js** auf der Lage-Seite: nur 2 RPC-Endpunkte,
  kein Tabellen-Read — Intent der Vorgabe („keine externen Hosts") bleibt
  vollständig erfüllt; weniger Angriffs-/Wartungsfläche.
- **Polling-Intervall 20 s** (Vorgabe 15–30 s).
- **Code-only-Ausgabe** in der Leitung (keine URL) — zwingende Folge von D-06.
- **Kein App-Release / kein CHANGELOG-Eintrag:** Feld-App (index.html, sw.js,
  version.json) unangetastet; CHANGELOG dokumentiert App-Releases. Lage-/
  Leitungs-Seite sind nicht versionierte Desktop-Begleitseiten.
- **Drehbuch-DAUERREGEL geprüft:** keine Feld-App-/Trainings-UI-Änderung →
  keine Lektionsanpassung nötig.

## Testbelege

- JSC-Syntax: Lage-Seite (10 198 Z.) und Leitungs-Seite (43 090 Z.) SYNTAX OK.
- Task-Gates Plan 02 Task 1 + 2: alle bestanden (inkl. „kein Projekt-Ref-
  Literal", „kein vendor/supabase.js", 20-s-Intervall, observer in select,
  `!t.observer`-Filter, kein `lage-<hex>` in der Leitungs-Seite).
- **E2E Leitungs-Flow per REST nachgestellt:** Master-JWT → Insert
  `observer:true` (Test-Code) → frischer Code per `argus_exchange_observer_code`
  eingelöst (JWT erhalten) → Testdaten gelöscht.
- Lokaler Smoke-Test: Lage-Seite + config.js je HTTP 200.
- D-06-Gates: `git grep lage-<hex>` außerhalb docs/ leer; keine gitignorte
  Doku staged; Commit-Messages nur Muster-Schreibweise.
- Live-Smoke nach Push: siehe Abschluss-Vermerk unten.

## Abweichungen vom Plan

- Verify-Gate „kein supabase.js" präzisiert auf `vendor/supabase.js` (der
  erklärende Kommentar in der Seite erwähnt das Wort legitim).
- Zusätzlicher E2E-Test des Leitungs-Flows per REST (im Plan nur als
  Code-Inspektion vorgesehen) — Mehrwert, kein Scope-Bruch.

## Phase-Abschluss

Phase 4.12 vollständig: Backend (Plan 01) + Frontend/Ausgabe/Doku (Plan 02).
Nächster realer Schritt laut STATE: DSB-Gespräch → bestimmt Phase 5/6/7.
