---
phase: 04.12-flz-lageansicht
plan: 02
type: execute
wave: 2
depends_on: [04.12-01]
files_modified:
  - .gitignore
  - "docs/lage-<suffix>.html (NEU; konkreter Name nur in untracked LAGE-URL.md — D-06 analog)"
  - "docs/leitung-<suffix>.html (Name via Glob docs/leitung-*.html — D-06)"
  - "LAGE-URL.md (untracked, NIE committen)"
  - "docs/datenschutz/* + docs/DSB-BRIEFING.md (gitignored, lokal)"
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [FLZ-A-FRONTEND]
must_haves:
  truths:
    - "Es gibt eine eigene read-only Desktop-Seite docs/lage-<10hex>.html: Beobachter-Code-Login → Aggregat-Kacheln je aktivem CCP mit Zahlen je Kategorie, Summenzeile, Stand-Zeitstempel, Auto-Polling mit sichtbarem Status und klarem Offline-Zustand"
    - "Der konkrete Dateiname der Lage-Seite steht in KEINER committeten Datei und KEINEM Commit-Text (D-06 analog); .gitignore-Eintrag /LAGE-URL.md existiert VOR der Notiz-Datei"
    - "Die Leitungs-Seite kann je Präsidium einen Beobachter-Zugang (FLZ) erstellen und sperren wie bestehende Codes — mit Hinweis auf Verteilungs-Disziplin; sie verlinkt die Lage-Seite NICHT"
    - "Die Lage-Seite nutzt ../config.js als einzige Endpunkt-Quelle, lokale Fonts, keine externen Hosts, noindex, sessionStorage (flüchtig)"
    - "Feld-App index.html ist unangetastet (kein App-Release in dieser Phase)"
    - "ROADMAP/STATE führen Phase 4.12 als abgeschlossen; interne Datenschutz-Doku (DSB-BRIEFING, DATENSCHUTZ-SPEC/Maßnahmenplan, DSFA-Randnotiz Kleinstzahlen) ist lokal nachgezogen"
  artifacts:
    - path: "docs/lage-<suffix>.html (Glob docs/lage-*.html)"
      provides: "Beobachter-Seite: Login, Polling, Kacheln, Offline-Zustand, Datentiefe-Hinweis"
    - path: ".gitignore"
      provides: "Schutz der URL-Notiz"
      contains: "/LAGE-URL.md"
    - path: "LAGE-URL.md"
      provides: "Untracked Notiz mit konkretem Dateinamen + URL + Betriebs-Hinweisen"
  key_links:
    - from: "docs/lage-<suffix>.html"
      to: "../config.js"
      via: "script-Tag, window.ARGUS_CONFIG"
      pattern: "src=\"\\.\\./config\\.js\""
    - from: "docs/lage-<suffix>.html"
      to: "rpc/argus_exchange_observer_code + rpc/argus_lage"
      via: "fetch (Anon-Key bzw. Bearer-JWT), Polling"
      pattern: "argus_lage"
    - from: "docs/leitung-<suffix>.html"
      to: "access_tokens.observer"
      via: "Insert observer:true + Spalte im select + tokenArt"
      pattern: "observer"
---

<objective>
Frontend + Ausgabe + Doku der FLZ-Lageansicht Stufe a: die eigentliche
Beobachter-Seite (unguessable Name, ARGUS-Optik, Polling), die
Beobachter-Code-Ausgabe im Zugänge-Bereich der Leitungs-Seite, interne
Datenschutz-Doku und Statusführung. Abschluss der Phase 4.12 — OHNE Release
der Feld-App (index.html unangetastet).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.12-flz-lageansicht/04.12-CONTEXT.md
@.planning/phases/04.12-flz-lageansicht/04.12-01-SUMMARY.md
@CLAUDE.md
@docs/UI-AUSBLICK.html
</context>

<wichtige_regeln>
- **D-06 analog (LAGE-Geheimhaltung):** Konkreter Dateiname `docs/lage-<10hex>.html`
  NIE in committete Dateien oder Commit-Messages — überall nur Muster-Schreibweise
  `docs/lage-<suffix>.html`. Gate vor JEDEM Commit. Die Leitungs-Seite (committet!)
  darf die Lage-URL daher NICHT erzeugen/verlinken — nur den CODE ausgeben.
- **ARGUS-Optik verbindlich** (`docs/UI-AUSBLICK.html`, NICHT ändern): IBM Plex
  (lokale Fonts wie Leitungs-Seite), hell, Schiefer `#1f2530` als Chrome,
  Triage-Farben nur funktional. Muster/CSS-Variablen der Leitungs-Seite
  wiederverwenden.
- **Keine nativen alert/confirm/prompt**; keine externen Hosts; `noindex,
  nofollow, noarchive` + `referrer no-referrer`.
- **Kein node:** JS-Syntaxprüfung via `osascript -l JavaScript` (Inline-Script
  per python3 extrahieren, JSC parsen — Muster 04.9-03). Statischer Smoke-Test
  via `python3 -m http.server`.
- **Feld-App index.html, sw.js, version.json, CHANGELOG bleiben unangetastet**
  — kein App-Release (Diskretionsentscheid: Lage-/Leitungs-Seite sind nicht
  Teil der versionierten Feld-App; im SUMMARY begründen). Drehbuch-DAUERREGEL:
  keine Feld-App-UI-Änderung → keine Lektionsanpassung, explizit dokumentieren.
- **Interne Doku ist gitignored** (docs/datenschutz/, docs/DSB-BRIEFING.md):
  lokal editieren, NIE committen — vor dem Commit per `git status` prüfen,
  dass keine dieser Dateien staged ist.
</wichtige_regeln>

<tasks>

<task type="auto">
  <name>Task 1: .gitignore + LAGE-URL.md + Beobachter-Seite docs/lage-&lt;10hex&gt;.html</name>
  <files>.gitignore, LAGE-URL.md (untracked), docs/lage-&lt;suffix&gt;.html</files>
  <read_first>
    - docs/leitung-*.html (per Glob; Zeilen 1–130: head/noindex/Fonts/CSS-Variablen/Topbar/Login-CSS; Zeilen 193–260: sessionStorage-Sitzung, doLogin-fetch-Muster, _configError-Behandlung)
    - config.js (window.ARGUS_CONFIG-Form)
    - LEITUNG-URL.md (Struktur der untracked Notiz als Vorlage)
    - .planning/phases/04.12-flz-lageansicht/04.12-01-SUMMARY.md (finale RPC-Namen/Antwortformen aus Plan 01)
  </read_first>
  <action>
    1. **Reihenfolge zwingend:** zuerst `.gitignore` um Eintrag `/LAGE-URL.md`
       erweitern (im Abschnitt der Leitungs-URL-Zeile, analog `/LEITUNG-URL.md`),
       DANN erst Dateien anlegen.
    2. **Namen generieren:** 10 Hex-Zeichen via /dev/urandom → `docs/lage-<10hex>.html`.
       `LAGE-URL.md` (untracked) nach Muster LEITUNG-URL.md: Dateiname, GitHub-
       Pages-URL, Zugang (nur Beobachter-Code), Hinweis Verteilungs-Disziplin
       (URL + Code getrennt übermitteln, Adressat FLZ), D-06-Regel.
    3. **Beobachter-Seite bauen** (eigene HTML-Datei, Desktop-Webansicht,
       KEINE PWA/Manifest/SW-Einbindung):
       - Head wie Leitungs-Seite: charset, viewport, Titel „ARGUS · Lage",
         noindex/nofollow/noarchive, no-referrer, Icon, lokale @font-face
         (fonts/… relativ — Seite liegt unter docs/), CSS-Variablen-Palette
         der Leitungs-Seite (Triage-Farben --t1/--t2/--t3/--t5, --acc Schiefer).
       - `<script src="../config.js"></script>`; _cfg/_configError-Muster der
         Leitungs-Seite übernehmen (kein supabase.js nötig — Diskretionsentscheid:
         die Seite spricht nur 2 RPCs über fetch, wie doLogin; „keine externen
         Hosts" bleibt erfüllt, im SUMMARY begründen).
       - **Login-Ansicht:** Loginbox-Muster („Beobachter-Code", XXXX-XXXX-Input,
         Format-tolerant); fetch POST `rpc/argus_exchange_observer_code`
         (apikey + Bearer Anon-Key); `{error}` → Fehlertext anzeigen; Erfolg →
         sessionStorage `arguslg_jwt`, `arguslg_jwt_exp` (exp*1000),
         `arguslg_pname`; flüchtig, Abmelden-Knopf = sessionStorage.clear()+reload.
         Abgelaufene Sitzung → zurück zum Login (Muster sessionJwt() der
         Leitungs-Seite).
       - **Lage-Ansicht:** Topbar „ARGUS · Lage" + Präsidiums-Name + Abmelden.
         Polling: fetch POST `rpc/argus_lage` (Bearer = Observer-JWT) sofort nach
         Login, dann alle **20 s** (Diskretionsentscheid, innerhalb 15–30 s);
         zusätzlich bei `online`-Event und `visibilitychange→visible`.
         Rendering je CCP eine Karte: Kopf „CCP <kennung>" + ort; große Zahlen
         (mono, ≥34px) je Kategorie in Triage-Farben: T1 rot, T2 gelb, T3 grün,
         T5 schwarz, gPA grau/muted; darunter Zeile „Prio: n · transportfertig: n";
         „letzte Änderung HH:MM" je Karte (aus letzte_aenderung, fmtMs-Muster).
         **Summenzeile** über alle CCPs (gleiche Kategorien, fett).
         Leerzustand: „Keine aktiven CCPs in diesem Präsidium."
       - **Status/Offline:** sichtbarer Status in der Topbar oder Kopfzeile:
         „Stand: HH:MM:SS" (Zeit des letzten erfolgreichen Polls) + grüner Punkt;
         bei Fetch-Fehler: roter Punkt + „Keine Verbindung — letzte Zahlen von
         HH:MM" (letzte Daten WEITER anzeigen, nicht leeren); Polling läuft weiter.
         401/Exception (JWT abgelaufen) → Logout zum Login mit Hinweis.
       - **Hinweiszeile** (Footer, wie Leitungs-Datenschutzzeile): „Anonyme
         Übersichtszahlen — keine Personendaten." Kein Bediener-/MasterMedic-Name
         irgendwo im Markup.
       - HTML-Escaping ALLER dynamischen Werte (esc()-Muster — kennung/ort kommen
         von Feld-Geräten).
       - Copyright-Kopfkommentar wie übrige Dateien.
    4. **JSC-Syntaxbeleg:** Inline-Script per python3 extrahieren, `osascript -l
       JavaScript` parsen (Muster 04.9-03).
  </action>
  <verify>
    <automated>grep -q "^/LAGE-URL.md$" .gitignore && test -f LAGE-URL.md && sh -c 'set -- docs/lage-*.html; test -f "$1"' && grep -q 'noindex' docs/lage-*.html && grep -q 'src="../config.js"' docs/lage-*.html && grep -q 'argus_exchange_observer_code' docs/lage-*.html && grep -q 'argus_lage' docs/lage-*.html && grep -q 'Anonyme Übersichtszahlen' docs/lage-*.html && grep -q 'arguslg_jwt' docs/lage-*.html && ! grep -q 'sehuosjyjmrpzcqrelej' docs/lage-*.html && ! grep -qE 'supabase\.js' docs/lage-*.html && python3 -c "import glob,re; p=glob.glob('docs/lage-*.html')[0]; s=open(p).read(); assert '20000' in s or '20 s' in s or '20*1000' in s, 'Polling-Intervall 20s nicht gefunden'" && git check-ignore -q LAGE-URL.md</automated>
  </verify>
  <done>.gitignore schützt /LAGE-URL.md (Eintrag VOR Dateierstellung); Lage-Seite existiert mit Login, 20-s-Polling, Kategorie-Kacheln, Summenzeile, Stand-/Offline-Anzeige, Datentiefe-Hinweis, noindex, config.js-Anbindung, ohne URL/Key-Literale; JSC-Syntax bestanden; LAGE-URL.md untracked mit Name+URL+Verteilungs-Disziplin.</done>
</task>

<task type="auto">
  <name>Task 2: Leitungs-Seite — Beobachter-Zugang (FLZ) erstellen/sperren</name>
  <files>docs/leitung-&lt;suffix&gt;.html (via Glob docs/leitung-*.html)</files>
  <read_first>
    - docs/leitung-*.html Zeilen 317–472 (Zugänge-Bereich: loadZugaenge-select, tokenArt/tokenStatus/tokenRows, secZugaenge-Buttons, makeLink-Wiederverwendungs-Filter, toggleRevoke)
    - docs/leitung-*.html Zeilen 870–895 (Click-Handler data-action-Routing)
  </read_first>
  <action>
    1. `loadZugaenge()`: `observer` in die select-Spaltenliste von access_tokens
       aufnehmen.
    2. `tokenArt(t)`: vor den bestehenden Fällen `if(t.observer) return
       'Beobachter (FLZ)';`.
    3. `secZugaenge()`: je Präsidium einen vierten Knopf „Beobachter-Zugang (FLZ)"
       (data-action="mkobserver"). Hinweistext unter der Tabelle ergänzen:
       Beobachter-Codes zeigen NUR anonyme Übersichtszahlen (Lageansicht) und
       funktionieren nicht in der Feld-App; Verteilungs-Disziplin: Code nur an
       die zuständige FLZ, URL der Lageansicht separat übermitteln.
    4. Neue Funktion `makeObserver(pid,pname)` (Muster makeLink/Dauerhaft):
       vorhandenen nicht gesperrten observer-Code des Präsidiums wiederverwenden,
       sonst Insert `{token:genToken(), praesidium_id:pid, is_master:false,
       observer:true, label:pname+' FLZ-Beobachter', short_code:genShortCode(),
       temporary:false, ttl_hours:null, single_use:false, used_at:null}`.
       **NUR den CODE in die Zwischenablage** (KEINE URL — D-06: die Lage-URL darf
       nicht in dieser committeten Datei konstruiert werden); Toast
       „Beobachter-Code kopiert ✓ — URL der Lageansicht separat übermitteln";
       Fallback promptModal mit dem Code.
    5. `makeLink()`-Wiederverwendungs-Filter um `&& !t.observer` ergänzen
       (sonst könnte ein Beobachter-Code fälschlich als Dauerhaft-Link
       ausgegeben werden — der Exchange würde ihn abweisen, aber die UX wäre
       falsch).
    6. Click-Handler: `mkobserver` → makeObserver routen.
    7. Sperren/Entsperren: funktioniert über bestehendes toggleRevoke
       unverändert (observer-Zeilen haben short_code) — nicht anfassen.
    8. JSC-Syntaxbeleg (Muster 04.9-03).
  </action>
  <verify>
    <automated>grep -q "Beobachter (FLZ)" docs/leitung-*.html && grep -q "mkobserver" docs/leitung-*.html && grep -q "observer:true" docs/leitung-*.html && ! grep -qE "lage-[0-9a-f]{6,}" docs/leitung-*.html && python3 -c "
import glob,re
s=open(glob.glob('docs/leitung-*.html')[0]).read()
assert '!t.observer' in s, 'makeLink-Wiederverwendungs-Filter fehlt'
sel=re.search(r\"from\('access_tokens'\)\.select\('([^']*)'\", s)
assert sel and 'observer' in sel.group(1), 'observer fehlt in der select-Spaltenliste'
print('Leitung OK')"</automated>
  </verify>
  <done>Leitungs-Seite erstellt/kopiert Beobachter-Codes je Präsidium (nur Code, keine URL), zeigt Art „Beobachter (FLZ)" und Verteilungs-Hinweis; Dauerhaft/24-h-Wiederverwendung schließt observer aus; Sperren wirkt; kein Lage-Dateiname in der Datei; JSC bestanden.</done>
</task>

<task type="auto">
  <name>Task 3: Interne Doku + Statusführung + Gesamt-Gates + Deploy</name>
  <files>docs/DSB-BRIEFING.md (lokal), docs/datenschutz/DATENSCHUTZ-SPEC.md u. a. (lokal), .planning/ROADMAP.md, .planning/STATE.md, ZEITLOG.md (lokal)</files>
  <read_first>
    - docs/datenschutz/DATENSCHUTZ-SPEC.md (Struktur Maßnahmenplan/Tasks)
    - docs/DSB-BRIEFING.md (Struktur; Abschnitt zu FLZ/Phase 8 falls vorhanden)
    - .planning/ROADMAP.md Abschnitt 4.12 + Phase 8
    - .planning/STATE.md Kopfbereich
  </read_first>
  <action>
    1. **DSB-BRIEFING (lokal, gitignored):** kurzer Abschnitt „FLZ-Lageansicht
       Stufe a umgesetzt": anonyme Aggregat-Zahlen, eigener Beobachter-Zugang,
       Roh-Zugriff per RLS verweigert (Negativtests), Zugriff protokolliert
       (lage_view, je Login), Stufe b wartet auf DSB-Votum. **Re-Identifikations-
       Hinweis bei Kleinstzahlen** als DSFA-Randnotiz ergänzen (in der DSFA-Datei
       unter docs/datenschutz/ an passender Stelle, sonst im Briefing).
    2. **DATENSCHUTZ-SPEC/Maßnahmenplan (lokal):** Eintrag „Lageansicht Stufe a
       (4.12)" mit Datum und Verweis auf Migration 0005 + Negativtests.
    3. **ROADMAP.md:** Phase 4.12 auf ✅ mit Einzeiler (Stufe a live: Migration
       0005, Beobachter-Seite, Code-Ausgabe Leitung); Phase-8-Abschnitt um
       Vermerk „Stufe a vorgezogen in 4.12 umgesetzt (✅); hier verbleiben
       Stufe b (DSB-gated), Landes-Observer, Einzel-Freigabe" ergänzen.
    4. **STATE.md:** Kopf aktualisieren (Phase 4.12 abgeschlossen, Datum,
       nächster Schritt: DSB-Gespräch → Phase 5/6/7; Hinweis: kein App-Release,
       Feld-App weiter v0.24.0); Phase-Abschluss-Tabelle ergänzen.
    5. **ZEITLOG.md (lokal, NICHT committen):** Zeile für den Arbeitsblock 4.12
       ergänzen, Summen aktualisieren.
    6. **Gesamt-Gates vor dem Commit:**
       - D-06-Gate: `git grep -E "lage-[0-9a-f]{6,}" -- ':!docs/'` → leer; und
         `git diff --cached --name-only` enthält keine gitignorten Doku-Pfade.
         (Der Dateiname docs/lage-<hex>.html selbst ist als TRACKED FILE sichtbar
         — bewusst akzeptiert wie bei der Leitungs-Seite: Schutz liegt in
         JWT/RLS, nicht im Namen; Commit-MESSAGES nur Muster-Schreibweise.)
       - PAT-Gate: `git grep -i "sbp_"` über die zu committenden Dateien → kein
         Token (nur zitierte grep-Kommandos erlaubt).
       - Statischer Smoke-Test: `python3 -m http.server`, Lage-Seite per curl 200.
       - Drehbuch-DAUERREGEL: keine Feld-App-/Trainings-UI-Änderung → keine
         Lektionsanpassung; im SUMMARY festhalten.
    7. **Commit + Push** (getrennte, saubere Commits: Backend-Plan-Dateien sind
       aus Plan 01 bereits committet; hier: .gitignore + Lage-Seite,
       Leitungs-Seite, Statusführung; Muster-Schreibweise in Messages) →
       GitHub Pages deployt automatisch. Nach Push: Live-Smoke-Test
       `curl -s -o /dev/null -w "%{http_code}" https://krapfenbediener.github.io/argus/docs/lage-<konkret>.html`
       (Name NUR im Kommando, nicht in committete Dateien) — 200 ggf. nach
       Pages-Build-Wartezeit (bis ~2 min, wiederholen).
  </action>
  <verify>
    <automated>grep -q "4.12" .planning/STATE.md && grep -A3 "Phase 4.12" .planning/ROADMAP.md | grep -q "✅" && git grep -qE "lage-[0-9a-f]{6,}" -- ':!docs/' && echo "D-06-VERLETZUNG" || true; test -z "$(git status --porcelain docs/datenschutz docs/DSB-BRIEFING.md 2>/dev/null | grep -v '^??')" && ! git log --oneline -10 | grep -qE "lage-[0-9a-f]{6,}"</automated>
  </verify>
  <done>Interne Doku lokal nachgezogen (nichts davon committet); ROADMAP/STATE führen 4.12 als ✅; Commits ohne konkrete Lage-/Leitungs-Namen und ohne PAT; Push erfolgt, Live-URL antwortet 200; Drehbuch-Prüfung dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Public Repo/Pages → Welt | Lage-Seite ist öffentlich abrufbar — ohne Login zeigt sie keinerlei Daten |
| FLZ-Beobachter → Backend | Nur 2 RPCs; Vertrauensniveau unter Feld/Leitung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.12-08 | Information Disclosure | Lage-Seiten-Name | mitigate | Unguessable 10hex, noindex, nirgends verlinkt, D-06-Gates; Restsichtbarkeit als tracked Filename bewusst akzeptiert (Schutz = JWT/RLS) |
| T-4.12-09 | Information Disclosure | URL-Notiz | mitigate | .gitignore VOR Dateierstellung; Gate in Task 3 |
| T-4.12-10 | Spoofing/XSS | kennung/ort aus Feld-Geräten | mitigate | esc() für alle dynamischen Werte im Lage-Rendering |
| T-4.12-11 | Information Disclosure | Beobachter-Code-Ausgabe | mitigate | Leitungs-Seite gibt nur Code (keine URL) aus + Verteilungs-Hinweis; Sperre via revoked |
| T-4.12-12 | Tampering | sessionStorage-JWT | accept | Flüchtig, ≤24 h TTL, read-only Aggregat-Scope |
| T-4.12-SC | Tampering | Paket-Installationen | accept | Keine npm/pip/cargo-Installationen (kein node verfügbar) |
</threat_model>

<verification>
- Lage-Seite: Login → Kacheln → Polling → Offline-Zustand (Code-Inspektion + JSC + statischer Smoke-Test; Live-Funktionstest gegen Schulungs-Präsidium mit temporärem Beobachter-Code, danach Code sperren/löschen).
- Leitungs-Seite: Beobachter-Knopf, Art-Anzeige, Code-only-Ausgabe, Filter-Fix.
- D-06-Gates beidseitig (lage-Name nicht außerhalb docs/, nicht in Messages); gitignorte Doku nicht staged.
- ROADMAP/STATE konsistent; kein Diff an index.html/sw.js/version.json/CHANGELOG.
</verification>

<success_criteria>
- FLZ kann mit Beobachter-Code die Lage eines Präsidiums anonym mitlesen (Stufe a komplett).
- Leitung gibt Beobachter-Zugänge kontrolliert aus und kann sie sperren.
- Keine Feld-App-Änderung, kein Release; Phase 4.12 sauber abgeschlossen und dokumentiert.
</success_criteria>

<output>
Create `.planning/phases/04.12-flz-lageansicht/04.12-02-SUMMARY.md` when done
(Diskretionsentscheide: fetch statt supabase.js, 20-s-Intervall, Code-only-Ausgabe,
kein App-Release/CHANGELOG; Live-Testbelege; D-06-Gate-Ergebnisse).
</output>
