# Plan 04.8-01 — SUMMARY (T8: Externe Laufzeit-Abhängigkeiten entfernt)

**Status:** ✅ abgeschlossen 2026-06-10 · **Release:** v0.19.9 (deployt)
**Tasks:** 3/3 · **Commits:** `a08c393`, `6cb3508`, `756015c`

## Was gebaut wurde

- **Supabase-JS v2.108.1 lokal gevendort** (`vendor/supabase.js`, UMD, 203 kB,
  Versionskommentar) — jsdelivr-Einbindung aus `index.html` entfernt, Precache erweitert.
- **IBM-Plex-Fonts self-hosted:** 5 woff2 (`fonts/`) — Sans 400/500/600 aus
  `docs/fonts` übernommen, Mono 600 vorhanden, Mono 500 einmalig von Google Fonts
  bezogen (latin-Subset) zur exakten Optik-Erhaltung. 5 `@font-face`-Regeln,
  Google-Fonts-Link/preconnect entfernt, SW-Sonderfall googleapis/gstatic gestrichen.
- **THIRD_PARTY_NOTICES.md** auf lokale Einbindung umgeschrieben (Version 2.108.1
  benannt). Release: APP_VERSION 0.19.9 = version.json, CACHE_NAME v35→v36,
  CHANGELOG + WHATS_NEW. Kein UPDATE-Sheet (keine sichtbare UI-Änderung — plangemäß).

## Verifikation

- `grep -RnE "jsdelivr|googleapis|gstatic" index.html sw.js` → 0 Treffer
  (SPEC-T8-Akzeptanz: keine Requests an Nicht-Backend-Domains).
- Precache-Konsistenz: jede PRECACHE_URL und jede `./fonts/`-/`./vendor/`-Referenz
  existiert; Smoke-Test via `python3 -m http.server`: alle Assets HTTP 200.
- Syntax-Checks (vendor, sw.js, Haupt-Script) via JavaScriptCore: OK.
- gitignore-Schutz verifiziert (`git check-ignore` für CONTEXT/SPEC, keine Duplikate).
- Drehbuch-Check: TRAIN_LESSONS referenziert weder CDN noch Schriftquellen —
  keine Lektionsänderung nötig.

## Abweichungen

1. Mono 500 nachgeladen statt Stellen auf 600 umzustellen (Optik-Erhalt, dokumentiert).
2. Push erfolgte nachgelagert durch den Orchestrator (GitHub-Credential-Gate,
   inzwischen behoben) — v0.19.9 ist live.
