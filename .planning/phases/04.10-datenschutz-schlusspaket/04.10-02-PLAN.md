---
phase: 04.10-datenschutz-schlusspaket
plan: 02
type: execute
wave: 2
depends_on: [04.10-01]
files_modified:
  - index.html
  - version.json
  - sw.js
  - CHANGELOG.md
  - docs/UPDATE_v0.9_zu_v0.12.html
  - docs/UPDATE_v0.17.html
  - docs/UPDATE_v0.18.html
  - docs/UPDATE_v0.20.html
  - docs/UPDATE_v0.21.html
  - "docs/(Leitungs-Seite — Dateiname AUSSCHLIESSLICH aus untracked LEITUNG-URL.md, D-06)"
autonomous: true
requirements: [T5, NOINDEX]
must_haves:
  truths:
    - "Aus den Hilfestellungen ist eine Ansicht „Datenschutz" erreichbar (Art.-13-Inhalte: Verantwortlicher, Zwecke, Datenarten, Fristen, Rechte, DSB-Kontakt)"
    - "Verantwortlicher und DSB-Kontakt sind über EINE zentrale Konstante (DS_KONTAKT) pflegbar, Default [Platzhalter]"
    - "Der Datenschutz-Text enthält keine internen Begriffe (kein § 82, keine Vertragsstrategie)"
    - "Alle Alt-UPDATE-Sheets tragen ein noindex-Meta"
    - "App meldet v0.23.1; version.json und SW-Cache sind synchron gebumpt"
  artifacts:
    - path: "index.html"
      provides: "Konstante DS_KONTAKT, View vDatenschutz(), Einstieg in vHelp(), Drehbuch-Anpassung, Release v0.23.1"
      contains: "DS_KONTAKT"
    - path: "version.json"
      provides: "Update-Erkennung"
      contains: "0.23.1"
  key_links:
    - from: "index.html vHelp()"
      to: "View datenschutz"
      via: "Button data-action=nav data-view=datenschutz"
      pattern: "data-view=\"datenschutz\""
    - from: "index.html render()"
      to: "vDatenschutz()"
      via: "Dispatch-Zweig"
      pattern: "vDatenschutz\\(\\)"
    - from: "vDatenschutz()"
      to: "DS_KONTAKT"
      via: "Platzhalter-Ausgabe"
      pattern: "DS_KONTAKT"
---

<objective>
T5 Transparenz & Betroffenenrechte (D-02): In-App-Datenschutzhinweis als statische
Ansicht, aus den Hilfestellungen erreichbar, mit zentral pflegbaren Platzhaltern —
öffentlich-tauglich formuliert (Repo ist public). Dazu Kleinkram (D-05): noindex
für die Alt-UPDATE-Sheets. Drehbuch-DAUERREGEL und Release-Konvention einhalten.

Purpose: Art.-13-Informationspflicht in der App erfüllbar machen, bevor der DSB sie einfordert.
Output: index.html v0.23.1 mit Datenschutz-Ansicht, 5 nachgerüstete UPDATE-Sheets, Fußzeilen-Hinweis auf der Leitungs-Seite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.10-datenschutz-schlusspaket/04.10-CONTEXT.md
@CLAUDE.md
</context>

<wichtige_fakten>
- **Abhängigkeit Welle 1:** Der T5-Text nennt die 12-Monats-Log-Frist — die muss
  serverseitig live sein (Plan 04.10-01), bevor die App sie behauptet.
- **Repo public:** Quellen für die Inhalte sind die INTERNEN Doks
  `docs/datenschutz/01-VVT-EINTRAG.md` und `05-LOESCHKONZEPT.md` — Inhalte NEUTRAL
  übertragen. VERBOTEN im öffentlichen Text: „§ 82", „Auftragsverarbeitung",
  Vertrags-/Benennungsstrategie, Klarnamen als Verantwortlicher/DSB (Platzhalter!).
  Das bestehende „© 2026 Gabor Szeman" im App-Footer ist davon unberührt (Bestand).
- **D-06 (aus Phase 4.9, gilt weiter):** Der Dateiname der Leitungs-Seite darf in
  keiner committeten Datei auftauchen — auch nicht in diesem Plan oder der SUMMARY.
  Dateiname NUR aus der untracked `LEITUNG-URL.md` lesen; Verifikation per Muster-Grep.
- **Diskretions-Entscheide (dokumentieren in SUMMARY):** Version **v0.23.1**
  (kleines additives Feature, kein Verhaltenswechsel) → KEIN eigenes UPDATE-Sheet,
  WHATS_NEW-Eintrag genügt (per D-02 „klein"). SW-Cache **v42**.
- Fristen-Wortlaut für den T5-Text (Quelle 04.9-Stand): Einsatzprotokoll 72 h nach
  Abschluss, Fotos hart 72 h (keine Verlängerung), Protokoll-Frist-Verlängerung nur
  begründet durch die Leitung, Protokoll-Logs 12 Monate; Auskunft (Art. 15) über den
  Einzelpatient-/Einsatz-Export.
- Konventionen: deutsche UI, In-App-Modals, bestehende Komponenten (`card`, `sech`,
  `helpa`, `hint`), Optik nicht „aufhübschen", keine nativen alert/confirm/prompt.
</wichtige_fakten>

<tasks>

<task type="auto">
  <name>Task 1: Datenschutz-Ansicht in index.html (DS_KONTAKT + vDatenschutz + Einstieg)</name>
  <files>index.html</files>
  <read_first>index.html Zeilen ~100–230 (APP_VERSION/WHATS_NEW-Block), ~1518–1545 (render()-Dispatch + titles), ~2067–2095 (vHelp-Muster mit kb()/card); INTERN: docs/datenschutz/01-VVT-EINTRAG.md + 05-LOESCHKONZEPT.md (Inhalte neutral übertragen, NICHT zitieren)</read_first>
  <action>
    Per D-02: (1) Im Konstanten-Bereich (nahe APP_VERSION) eine zentrale Konstante
    `var DS_KONTAKT = { verantwortlicher: '[Platzhalter — wird vor dem Echtbetrieb benannt]',
    dsb: '[Platzhalter — Kontakt des/der Datenschutzbeauftragten]' };` mit kurzem
    Pflege-Kommentar anlegen. (2) Neue View-Funktion `vDatenschutz()` neben `vHelp()`:
    statische Karten im bestehenden card/sech/helpa-Muster mit den Abschnitten
    Verantwortlicher (aus DS_KONTAKT), Zwecke (Sichtung, Versorgung, Übergabe am CCP),
    gespeicherte Datenarten (pseudonyme Patientendaten über laufende Nummer, Name optional,
    Gesundheitsdaten zur Versorgung, Foto optional zur Wiedererkennung, Bediener-Kürzel),
    Speicher- und Löschfristen (72 h nach Einsatz-Abschluss automatisch; Fotos endgültig
    nach 72 h ohne Verlängerung; Frist-Verlängerung des Protokolls nur begründet durch
    die Leitung; technische Protokolle nach 12 Monaten), Betroffenenrechte (Auskunft,
    Berichtigung, Löschung, Einschränkung — Hinweis: Auskunft nach Art. 15 wird über den
    Einzelpatient-/Einsatz-Export bedient) und DSB-Kontakt (aus DS_KONTAKT).
    Formulierungen knapp, einsatztauglich, laienverständlich, öffentlich-tauglich.
    (3) Verdrahtung: `titles`-Eintrag `datenschutz:"Datenschutz"`, Dispatch-Zweig
    `else if(v==="datenschutz")h=vDatenschutz();` in render(), und in `vHelp()` oben
    einen ghost-Button „🛡 Datenschutzhinweis" mit `data-action="nav" data-view="datenschutz"`
    (Muster wie „Einführung erneut ansehen"). Keine weiteren Einstiege.
  </action>
  <verify>
    <automated>grep -c 'DS_KONTAKT' index.html | awk '$1>=3{exit 0}{exit 1}' && grep -q 'data-view="datenschutz"' index.html && grep -q 'vDatenschutz()' index.html && ! grep -q '§ *82' index.html && ! grep -qi 'Auftragsverarbeit' index.html</automated>
  </verify>
  <done>Ansicht „Datenschutz" aus den Hilfestellungen erreichbar; alle sechs Art.-13-Abschnitte vorhanden; Platzhalter ausschließlich über DS_KONTAKT; Negativ-Greps (interne Begriffe) sauber.</done>
</task>

<task type="auto">
  <name>Task 2: noindex für Alt-UPDATE-Sheets + Fußzeilen-Hinweis Leitungs-Seite</name>
  <files>docs/UPDATE_v0.9_zu_v0.12.html, docs/UPDATE_v0.17.html, docs/UPDATE_v0.18.html, docs/UPDATE_v0.20.html, docs/UPDATE_v0.21.html, Leitungs-Seite (Dateiname aus LEITUNG-URL.md)</files>
  <read_first>docs/UPDATE_v0.23.html Zeile ~7 (Referenz-Meta), LEITUNG-URL.md (untracked — einzige Fundstelle des Dateinamens), Fußzeile der Leitungs-Seite</read_first>
  <action>
    Per D-05: In den fünf Alt-Sheets (v0.9_zu_v0.12, v0.17, v0.18, v0.20, v0.21) im
    head jeweils `<meta name="robots" content="noindex">` ergänzen — exakt das Muster
    aus UPDATE_v0.23.html; sonst nichts ändern. Per D-02 (Diskretion bestätigt):
    Auf der Leitungs-Seite (Dateiname NUR aus `LEITUNG-URL.md` lesen, D-06!) in der
    Fußzeile einen kleinen Hinweis ergänzen: „Datenschutzhinweis: in der ARGUS-App
    unter Hilfestellungen → Datenschutz." — KEIN Duplikat des Textes, keine
    DS_KONTAKT-Kopie. In SUMMARY/Commit-Text die Seite nur als „Leitungs-Seite"
    bezeichnen, nie mit Dateinamen.
  </action>
  <verify>
    <automated>test -z "$(grep -L 'name="robots" content="noindex"' docs/UPDATE_*.html)" && test "$(git grep -l 'Datenschutzhinweis' -- 'docs/*.html' | grep -v UPDATE_ | wc -l | tr -d ' ')" = "1" && test "$(git grep -cE 'leitung-[0-9a-f]{6,}' -- '.planning' ':!*.html' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>Alle 6 UPDATE-Sheets tragen noindex; Leitungs-Seite verweist in der Fußzeile auf den App-Datenschutzhinweis; kein Leitungs-Dateiname in Planungs-/Textdateien (Muster-Grep 0 Treffer).</done>
</task>

<task type="auto">
  <name>Task 3: Drehbuch-Check + Release v0.23.1</name>
  <files>index.html, version.json, sw.js, CHANGELOG.md</files>
  <read_first>index.html ~2203–2260 (TRAIN_LESSONS, Lektion „Knopf: Hilfestellungen" ~2213), ~106–135 (APP_VERSION/UPDATE_SHEETS/WHATS_NEW), sw.js Zeile 10 (CACHE_NAME), CHANGELOG.md Kopf</read_first>
  <action>
    DAUERREGEL Drehbuch: Lektion „Knopf: Hilfestellungen" (TRAIN_LESSONS) textlich
    ergänzen, sodass der Datenschutzhinweis als Inhalt der Hilfestellungen genannt ist
    (z. B. „… Rollen, Merksätze und der Datenschutzhinweis"); restliche Lektionen prüfen —
    keine weitere betroffen (neue View ist rein statisch, kein Workflow). Release gemäß
    Konvention: `APP_VERSION = '0.23.1'`, `version.json` auf `0.23.1`, `sw.js`
    CACHE_NAME auf `ccp-shell-v42`, CHANGELOG.md-Eintrag (Datenschutz-Ansicht, noindex
    Alt-Sheets, serverseitige 12-Monats-Log-Löschung aus Plan 01 miterwähnen),
    WHATS_NEW-Eintrag '0.23.1' (ein Punkt: Datenschutzhinweis in den Hilfestellungen —
    was gespeichert wird, wie lange, welche Rechte). KEIN UPDATE-Sheet, KEIN
    UPDATE_SHEETS-Eintrag (Diskretions-Entscheid, in SUMMARY begründen).
    Syntax-Beleg per JSC: Inline-Script-Blöcke aus index.html extrahieren und mit
    `osascript -l JavaScript` auf Syntaxfehler prüfen (Muster aus 04.9-03).
  </action>
  <verify>
    <automated>grep -q "APP_VERSION = '0.23.1'" index.html && grep -q '"0.23.1"' version.json && grep -q "ccp-shell-v42" sw.js && grep -q "0.23.1" CHANGELOG.md && grep -q "'0.23.1'" index.html && grep -A2 "Knopf: Hilfestellungen" index.html | grep -qi "atenschutz"</automated>
  </verify>
  <done>Version/version.json/SW-Cache/CHANGELOG/WHATS_NEW synchron auf v0.23.1; Drehbuch-Lektion nennt den Datenschutzhinweis; JSC-Syntaxprüfung bestanden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public GitHub-Repo → Welt | jeder committete Text ist öffentlich lesbar |
| Suchmaschinen → docs/*.html | GitHub-Pages-Inhalte sind crawlbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0410-05 | Information Disclosure | T5-Text in public index.html | mitigate | Negativ-Greps (§ 82, Auftragsverarbeitung), Platzhalter statt Namen (Task 1) |
| T-0410-06 | Information Disclosure | Leitungs-Seiten-Dateiname | mitigate | D-06: Name nur aus LEITUNG-URL.md, Muster-Grep-Gate in Task 2 |
| T-0410-07 | Information Disclosure | Alt-UPDATE-Sheets im Suchindex | mitigate | noindex-Meta (Task 2) |
| T-0410-08 | Spoofing | Falsche Verantwortlichen-Angabe | accept | Platzhalter sind als solche gekennzeichnet; Benennung ist Owner-/DSB-Punkt |
</threat_model>

<verification>
- Hilfestellungen → „Datenschutzhinweis" → Ansicht rendert alle Abschnitte (Greps + JSC-Syntax).
- `grep -L noindex docs/UPDATE_*.html` leer.
- Release-Kette vollständig (APP_VERSION = version.json, SW v42, CHANGELOG, WHATS_NEW).
- D-06-Gate: Muster-Grep auf den Leitungs-Dateinamen in .planning/Textdateien = 0 Treffer.
</verification>

<success_criteria>
Ein Betroffener/Bediener findet in der App ohne Netz alle Art.-13-Pflichtangaben;
die Angaben sind wahr (Fristen entsprechen dem Live-Serverstand inkl. 12-Monats-Logs);
nichts Internes ist öffentlich geworden; Release-Konvention eingehalten.
</success_criteria>

<output>
Create `.planning/phases/04.10-datenschutz-schlusspaket/04.10-02-SUMMARY.md` when done
</output>
