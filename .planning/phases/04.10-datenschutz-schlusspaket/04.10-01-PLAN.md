---
phase: 04.10-datenschutz-schlusspaket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/0004_phase410_log_retention.sql]
autonomous: false
requirements: [LOG-12M]
must_haves:
  truths:
    - "governance_log-Einträge älter als 12 Monate werden vom bestehenden stündlichen Purge-Lauf gelöscht"
    - "purge_log-Einträge älter als 12 Monate werden vom selben Lauf gelöscht"
    - "Der Log-Lösch-Schritt erzeugt selbst KEINEN purge_log-/governance_log-Eintrag (keine Rekursion, kein Spam bei leerem Ergebnis)"
    - "Die Migration ist zweimal hintereinander anwendbar ohne Fehler (Idempotenz-Beleg)"
  artifacts:
    - path: "supabase/migrations/0004_phase410_log_retention.sql"
      provides: "Erweiterte argus_run_purge mit Schritt (d) Log-Retention 12 Monate"
      contains: "create or replace function public.argus_run_purge"
  key_links:
    - from: "supabase/migrations/0004_phase410_log_retention.sql"
      to: "pg_cron-Job argus_purge (0002, '17 * * * *')"
      via: "create or replace derselben Funktion — Job-Definition bleibt unangetastet"
      pattern: "argus_run_purge"
---

<objective>
Log-Löschfrist umsetzen (D-01): `governance_log` und `purge_log` wachsen bisher
unbegrenzt und enthalten Bediener-Kürzel (pseudonyme Daten). Einträge älter als
**12 Monate** (Modell § 73 Abs. 5 PolG BW) werden künftig automatisch im
bestehenden stündlichen Purge-Lauf gelöscht — keine zweite Cron-Infrastruktur.

Purpose: Letzte serverseitige Datenschutz-Lücke vor dem DSB-Gespräch schließen.
Output: Migration `0004_phase410_log_retention.sql`, live angewendet (2×) und verifiziert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.10-datenschutz-schlusspaket/04.10-CONTEXT.md
@supabase/migrations/0002_phase48_datenschutz.sql
@supabase/migrations/0003_phase49_einsatzprotokoll.sql
@.planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md
</context>

<wichtige_fakten>
- **Diskretions-Entscheid (per CONTEXT „Claude's Discretion"):** Die 12-Monats-Löschung
  wird als Schritt **(d) direkt in `argus_run_purge` integriert** (create or replace der
  bestehenden Funktion), KEINE eigene Funktion/kein zweiter Cron-Schritt — der Job
  `argus_purge` (0002, stündlich `17 * * * *`) bleibt wörtlich unverändert.
- **Zeitbasis — ABWEICHUNG vom CONTEXT-Wortlaut (verifiziert in 0002):**
  - `governance_log.at` ist **bigint (JS-Millisekunden)** → Vergleich gegen
    `v_now - 365::bigint * 86400000` (12 Monate als 365 Tage in ms).
  - `purge_log.at` ist **timestamptz default now()** → Vergleich gegen
    `now() - interval '12 months'`. NICHT in bigint umrechnen wollen — Spaltentyp
    respektieren. Diese Abweichung im Migrations-Kommentar UND in der SUMMARY festhalten.
- **Keine Rekursion:** Der Log-Lösch-Schritt schreibt NICHTS in purge_log/governance_log.
  Wiederholungs-Guard ist damit inhärent (delete betrifft nur alte Rows; leeres Ergebnis
  erzeugt keinerlei Eintrag).
- Die aktuell live gültige Definition von `argus_run_purge` steht in 0002 (Zeilen ~140–226);
  0003 hat sie NICHT verändert (nur Kommentar-Verweis). Schritte (a) Foto, (b) Frist,
  (c) Inaktiv unverändert übernehmen.
- DDL ausschließlich via Supabase Management API + ephemerem PAT (kein CLI, kein node).
  Projekt-Ref: `sehuosjyjmrpzcqrelej`. Vorgehen wie in 04.9-01 (siehe SUMMARY).
</wichtige_fakten>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0004 schreiben (argus_run_purge + Schritt (d) Log-Retention)</name>
  <files>supabase/migrations/0004_phase410_log_retention.sql</files>
  <read_first>supabase/migrations/0002_phase48_datenschutz.sql (komplette argus_run_purge-Definition + grant), supabase/migrations/0003_phase49_einsatzprotokoll.sql (Kopf-Konventionen, Abschnitt 11)</read_first>
  <action>
    Migration `supabase/migrations/0004_phase410_log_retention.sql` anlegen (D-01),
    Kopf-Kommentar im Stil von 0003 (Zweck, Datum, Idempotenz-Hinweis, Abweichungs-Vermerk
    zur Zeitbasis von purge_log.at). Inhalt: `create or replace function
    public.argus_run_purge()` — Schritte (a)/(b)/(c) wörtlich aus 0002 übernehmen
    (KEINE inhaltliche Änderung daran), danach neuer Schritt (d) „Log-Retention 12 Monate":
    zwei Zähler-Variablen (z. B. `n_glog`, `n_plog`), dann
    `delete from public.governance_log where at is not null and at <= v_now - 365::bigint * 86400000`
    mit `get diagnostics n_glog = row_count`, und
    `delete from public.purge_log where at is not null and at <= now() - interval '12 months'`
    mit `get diagnostics n_plog = row_count`. KEIN purge_log-/governance_log-Eintrag über
    diesen Schritt. Return-JSON um einen Schlüssel erweitern:
    `'log_retention', jsonb_build_object('governance_log', n_glog, 'purge_log', n_plog)`
    (rein additiv — die App wertet das Ergebnis des opportunistischen Aufrufs nicht aus).
    Zum Abschluss `grant execute on function public.argus_run_purge() to anon;` erneut
    setzen (Idempotenz-/Sicherheitsnetz) und einen `comment on function` mit der
    12-Monats-Regel. Die gesamte Datei muss bei Zweitanwendung fehlerfrei durchlaufen
    (nur create-or-replace/grant/comment — kein create table, kein alter).
  </action>
  <verify>
    <automated>grep -c "interval '12 months'" supabase/migrations/0004_phase410_log_retention.sql | grep -qx 1 && grep -c "365::bigint \* 86400000" supabase/migrations/0004_phase410_log_retention.sql | grep -qx 1 && ! grep -E "insert into public\.(purge_log|governance_log)" supabase/migrations/0004_phase410_log_retention.sql | grep -qi "retention\|12" && grep -q "log_retention" supabase/migrations/0004_phase410_log_retention.sql</automated>
  </verify>
  <done>Datei existiert; Schritt (d) löscht governance_log per bigint-ms-Vergleich und purge_log per timestamptz-Intervall; Schritt (d) enthält keine eigenen Log-Inserts; Return-JSON um log_retention erweitert; Schritte (a)–(c) byte-gleich zur 0002-Logik.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Migration 0004 liegt geschrieben vor; zum Anwenden via Management API wird ein ephemerer Supabase-PAT benötigt.</what-built>
  <how-to-verify>
    Frage den Nutzer (NICHT annehmen): „Ist der ephemere PAT von gestern (Phase 4.9)
    noch gültig, oder soll ein neuer erzeugt werden? (Supabase-Dashboard → Account →
    Access Tokens)". Token entgegennehmen, NUR im Speicher/als Shell-Variable halten,
    nie in Dateien oder Commits schreiben.
  </how-to-verify>
  <resume-signal>Nutzer liefert gültigen PAT (alt oder neu)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Migration live anwenden (2×) und verifizieren</name>
  <files>supabase/migrations/0004_phase410_log_retention.sql</files>
  <read_first>.planning/phases/04.9-governance-oberflaeche/04.9-01-SUMMARY.md (Management-API-Aufruf-Muster, Verifikations-Stil)</read_first>
  <action>
    Wie in 04.9-01: Migration via Management API (`POST /v1/projects/sehuosjyjmrpzcqrelej/database/query`)
    **zweimal** anwenden (zweiter Lauf = Idempotenz-Beleg, muss fehlerfrei sein).
    Funktionstest auf der Live-DB, vollständig rückstandsfrei: (1) je einen synthetischen
    Alt-Eintrag einfügen — `governance_log` mit `at = (extract(epoch from now())*1000)::bigint - 400::bigint*86400000`
    (400 Tage alt, action z. B. 'retention_test') und `purge_log` mit
    `at = now() - interval '13 months'` (reason 'retention_test'); zusätzlich je einen
    JUNGEN Test-Eintrag (jetzt) als Negativ-Probe. (2) `select public.argus_run_purge();`
    ausführen, Return-JSON prüfen: `log_retention.governance_log >= 1` und
    `log_retention.purge_log >= 1`. (3) Per select belegen: alte Test-Einträge weg,
    junge Test-Einträge noch da, und es existiert KEIN neuer purge_log-Eintrag über den
    Lösch-Lauf selbst. (4) Junge Test-Einträge löschen (Aufräumen belegen: select count
    auf 'retention_test' = 0). (5) `select jobname, schedule, command from cron.job` —
    unverändert genau ein Job `argus_purge`, stündlich, ruft `argus_run_purge()`.
    Ergebnisse in der SUMMARY protokollieren. Abschließend den Nutzer erinnern, den PAT
    im Dashboard zu widerrufen (Owner-Punkt, in SUMMARY/STATE-Hinweis aufnehmen).
  </action>
  <verify>
    <automated>REST-Probe ohne JWT: curl auf /rest/v1/rpc/argus_run_purge mit Anon-Key liefert HTTP 200 und JSON mit Schlüssel log_retention (Funktion ist security definer, grant to anon besteht — wie 0002)</automated>
  </verify>
  <done>Migration 2× fehlerfrei angewendet; synthetische Alt-Logs nachweislich gelöscht, junge behalten; kein Rekursions-Eintrag; Test-Daten restlos entfernt; Cron-Job unverändert; PAT-Widerruf als Owner-Punkt notiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon-REST → argus_run_purge | öffentlich aufrufbarer RPC (security definer, war schon so) |
| Entwickler-Maschine → Management API | DDL mit ephemerem PAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0410-01 | Information Disclosure | governance_log/purge_log (Bediener-Kürzel) | mitigate | genau dieser Plan: automatische 12-Monats-Löschung |
| T-0410-02 | Tampering | argus_run_purge-Erweiterung könnte (a)–(c) beschädigen | mitigate | (a)–(c) wörtlich aus 0002 übernehmen; Funktionstest + Cron-Check in Task 3 |
| T-0410-03 | Elevation of Privilege | PAT-Leck | mitigate | PAT nur im Speicher, nie in Dateien/Commits; Widerruf als Owner-Punkt |
| T-0410-04 | Denial of Service | anon kann argus_run_purge triggern | accept | Bestandsverhalten seit 0002; Funktion ist idempotent und löscht nur Fristabgelaufenes |
</threat_model>

<verification>
- Return-JSON von `argus_run_purge` enthält `log_retention` mit beiden Zählern.
- Idempotenz: zweite Anwendung der Migration fehlerfrei.
- `cron.job` unverändert (ein Job, stündlich).
- Keine Test-Rückstände in den Log-Tabellen.
</verification>

<success_criteria>
Logs älter als 12 Monate werden serverseitig automatisch gelöscht, ohne neue
Cron-Infrastruktur, ohne Log-über-Log-Rekursion, mit korrekt typisierten
Zeitvergleichen (bigint ms vs. timestamptz) — live verifiziert.
</success_criteria>

<output>
Create `.planning/phases/04.10-datenschutz-schlusspaket/04.10-01-SUMMARY.md` when done
</output>
