---
phase: "4.10"
plan: 1
subsystem: database
tags: [supabase, purge, log-retention, datenschutz, governance_log, purge_log]
requires: ["0002_phase48_datenschutz.sql", "0003_phase49_einsatzprotokoll.sql"]
provides:
  - "argus_run_purge Schritt (d): Log-Retention 12 Monate für governance_log + purge_log"
  - "Return-JSON additiv erweitert um log_retention {governance_log, purge_log}"
affects: ["04.10-03 (interne Doku-Sync: Löschkonzept-Eintrag Log-Frist)"]
key-files:
  created:
    - supabase/migrations/0004_phase410_log_retention.sql
decisions:
  - "12-Monats-Löschung als Schritt (d) DIREKT in argus_run_purge (create or replace) — keine eigene Funktion, kein zweiter Cron-Schritt; Job argus_purge wörtlich unverändert (Diskretions-Entscheid laut CONTEXT)"
  - "Zeitbasis-Trennung nach Spaltentyp: governance_log.at (bigint-ms) gegen v_now minus 365 Tage in ms; purge_log.at (timestamptz) gegen now() minus kalendarisches Zwölf-Monats-Intervall — Abweichung vom CONTEXT-Wortlaut, im Migrations-Kopf vermerkt"
  - "Schritt (d) schreibt bewusst NICHTS in purge_log/governance_log — Rekursions-/Spam-Guard inhärent"
requirements: [LOG-12M]
metrics:
  duration: "~15 min"
  completed: "2026-06-11"
---

# Phase 4.10 Plan 01: Migration 0004 — Log-Retention 12 Monate — Summary

**Einzeiler:** Migration 0004 erweitert `argus_run_purge` um Schritt (d):
`governance_log`- und `purge_log`-Einträge älter als 12 Monate (Modell
§ 73 Abs. 5 PolG BW) werden im bestehenden stündlichen Purge-Lauf gelöscht —
typkorrekt (bigint-ms vs. timestamptz), ohne Log-über-Log-Rekursion, live
angewendet (2× = Idempotenz-Beleg) und mit synthetischen Alt-/Jung-Logs
rückstandsfrei verifiziert.

## Erledigte Tasks

| Task | Ergebnis | Commit |
|---|---|---|
| T1 Migration 0004 schreiben | `supabase/migrations/0004_phase410_log_retention.sql` (159 Zeilen): (a)–(c) byte-gleich aus 0002 (diff-Beleg über 63 extrahierte Zeilen), neu Schritt (d) + `log_retention` im Return-JSON, grant + comment | 2eb27df |
| T2 PAT bereitstellen (Checkpoint) | PAT kam über den Execution-Context (Nutzer: noch gültig) — Checkpoint vorab erfüllt | — |
| T3 Anwenden (2×) + verifizieren | Management API, beide Läufe HTTP 201 (Idempotenz ✓), Funktionstest + REST-Probe grün, Testdaten restlos entfernt | — |

## Verifikationsergebnisse (Live-Projekt sehuosjyjmrpzcqrelej)

**Idempotenz:** Migration 2× hintereinander via Management API angewendet —
beide Läufe HTTP 201, leeres Ergebnis, kein Fehler (Datei enthält nur
create or replace / grant / comment — kein create table, kein alter).

**Plan-Verify-Gates (Datei):** `interval '12 months'` exakt 1× ✓ ·
`365::bigint * 86400000` exakt 1× ✓ (Kopf-Kommentar beschreibt die Zeitbasis
nur in Worten — Checker-Hinweis umgesetzt) · keine Retention-Inserts in
purge_log/governance_log ✓ · `log_retention` vorhanden ✓.

**Funktionstest (synthetische Logs, danach restlos entfernt):**
1. Eingefügt: governance_log 2 Einträge (`action='retention_test'`: 400 Tage
   alt als bigint-ms + jetzt) und purge_log 2 Einträge (`reason='retention_test'`:
   13 Monate alt als timestamptz + jetzt). Stand davor: governance_log
   gesamt 4 (2 echte + 2 Test), purge_log gesamt 2 (0 echte + 2 Test).
2. `select public.argus_run_purge()` → Return-JSON:
   `log_retention = {"governance_log": 1, "purge_log": 1}`;
   foto/frist/inaktiv alle 0 (keine fälligen CCPs).
3. Nachweis per select: alte Test-Einträge in beiden Tabellen 0 verbleibend ✓;
   junge Test-Einträge je 1 noch da (Negativ-Probe ✓); purge_log gesamt = 1
   (vorher 2, ein alter gelöscht, **KEIN neuer Eintrag durch den Lauf
   selbst** — keine Rekursion ✓).
4. Aufräumen: beide junge Test-Einträge gelöscht; Abschluss-Query
   `retention_test` = 0 in beiden Tabellen ✓; governance_log zurück auf 2
   echte Einträge, purge_log leer (Ausgangsstand) ✓.
5. `cron.job`: unverändert genau **1** Job — `argus_purge`, `17 * * * *`,
   `select public.argus_run_purge()` ✓.

**REST-Probe (automated verify, Anon-Key aus index.html, OHNE Master-JWT):**
`POST /rest/v1/rpc/argus_run_purge` → HTTP 200, Response enthält den
Schlüssel `log_retention` (`{"purge_log": 0, "governance_log": 0}`) ✓ —
security definer + grant to anon bestehen wie seit 0002.

## Abweichungen vom Plan

1. **[Checkpoint vorgezogen] T2 (PAT)** — der ephemere PAT kam über den
   Execution-Context (vom Nutzer als noch gültig bestätigt); kein Stopp nötig.
   PAT nur als Shell-Variable verwendet, in keiner Datei, keinem Commit.
2. **[Konvention] Ein Commit für T1, T3 ohne Datei-Änderung** — T3 verändert
   keine Repo-Dateien (reine Live-Anwendung + Verifikation); die Migration
   wurde nach bestandenen Datei-Gates in T1 committet (2eb27df), die
   Live-Belege stehen in dieser SUMMARY.
3. **[Werkzeug] SQL-Übergabe als Temp-Datei statt Inline-Shell-String** —
   Shell-Quoting zerlegte die SQL-Single-Quotes (Syntaxfehler bei
   `interval '13 months'`); Test-Queries daher als Temp-Dateien geschrieben
   und mit python3 JSON-encodiert (Temp-Dateien ohne Secrets, danach gelöscht).

Sonst: Plan exakt wie geschrieben ausgeführt; die im Plan bereits
spezifizierte Zeitbasis-Abweichung (bigint-ms vs. timestamptz) ist im
Migrations-Kopf und oben unter „decisions" festgehalten.

## PAT-Widerruf (ERINNERUNG — Owner-Punkt)

Der für diesen Lauf genutzte ephemere PAT ist noch aktiv → im
Supabase-Dashboard (Account → Access Tokens) **widerrufen** — zusammen mit
ggf. noch offenen Alt-PATs (siehe STATE.md, offene Owner-Punkte 4.9).

## Hinweise für Folgepläne

- **04.10-02 (App v0.23.1):** keine App-Anpassung nötig — der
  opportunistische `argus_run_purge`-Aufruf beim App-Start wertet das
  Return-JSON nicht aus; `log_retention` ist rein additiv.
- **04.10-03 (Doku-Sync):** Löschkonzept/VVT um die Log-Frist ergänzen:
  governance_log + purge_log werden nach 12 Monaten automatisch gelöscht
  (Schritt (d) im stündlichen Purge-Lauf, Migration 0004).

## Self-Check: PASSED

- `supabase/migrations/0004_phase410_log_retention.sql` existiert ✓
- Commit 2eb27df existiert und enthält ausschließlich die Migrationsdatei ✓
- Schritte (a)–(c) byte-gleich zu 0002 (diff leer) ✓
- Live-DB entspricht der Datei (2× angewendet, REST-Probe liefert log_retention) ✓
- Test-Daten restlos entfernt (Abschluss-Query: 0 retention_test-Rows) ✓
