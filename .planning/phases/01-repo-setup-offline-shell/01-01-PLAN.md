---
phase: 01-repo-setup-offline-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - .gitignore
autonomous: true
requirements:
  - PHASE0-COPY
  - PHASE0-GITIGNORE
must_haves:
  truths:
    - "index.html existiert im Projekt-Root und ist eine 1:1-Kopie von CCP_App_Closed_Beta_V1.html"
    - "Das Supabase-CDN-Script ist in index.html eingebunden (vor dem App-Script)"
    - "window.supabaseLib = window.supabase; steht unmittelbar nach dem CDN-Script"
    - "Der Service-Worker-Registrierungscode ist am Ende des <script>-Blocks vorhanden"
    - "Supabase wird NICHT initialisiert — kein createClient()-Aufruf in index.html"
    - ".gitignore existiert mit projektrelevanten Einträgen"
  artifacts:
    - path: "index.html"
      provides: "Aktive Entwicklungsversion der CCP-App"
      contains: "cdn.jsdelivr.net/npm/@supabase/supabase-js@2"
    - path: ".gitignore"
      provides: "Git-Ignore-Konfiguration"
      contains: ".DS_Store"
  key_links:
    - from: "index.html"
      to: "sw.js"
      via: "navigator.serviceWorker.register('/sw.js')"
      pattern: "serviceWorker.*register.*sw\\.js"
    - from: "index.html"
      to: "window.supabaseLib"
      via: "Zuweisung nach CDN-Script"
      pattern: "window\\.supabaseLib\\s*="
---

<objective>
index.html als aktive Entwicklungsversion anlegen: 1:1-Kopie der eingefrorenen Referenz-HTML, ergänzt um drei genau definierte Einfügungen. Dazu .gitignore für das Projekt erstellen.

Purpose: Die Legacy-Datei bleibt unberührt. index.html ist die einzige Datei, die in Phase 1 ff. verändert wird. Die drei Ergänzungen sind Vorbereitung: CDN-Script (Phase 1 nutzt es), SW-Registrierung (Phase 0 / sw.js), window.supabaseLib-Alias (stabiler Zugriffspunkt für spätere Phasen).

Output: index.html (aktive Version), .gitignore
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gaborszeman/Desktop/Projekt CCP_APP/.planning/PROJECT.md
@/Users/gaborszeman/Desktop/Projekt CCP_APP/.planning/ROADMAP.md
@/Users/gaborszeman/Desktop/Projekt CCP_APP/CLAUDE.md
@/Users/gaborszeman/Desktop/Projekt CCP_APP/.planning/phases/01-repo-setup-offline-shell/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html als modifizierte Kopie der Legacy-HTML anlegen</name>
  <files>/Users/gaborszeman/Desktop/Projekt CCP_APP/index.html</files>
  <read_first>
    - /Users/gaborszeman/Desktop/Projekt CCP_APP/CCP_App_Closed_Beta_V1.html — VOLLSTÄNDIG lesen. Jede Zeile der Quelldatei wird 1:1 übernommen, nur an den drei unten beschriebenen Stellen wird ergänzt.
    - /Users/gaborszeman/Desktop/Projekt CCP_APP/.planning/phases/01-repo-setup-offline-shell/01-CONTEXT.md — Entscheidungen und Phase-Abgrenzung beachten.
  </read_first>
  <action>
Kopiere den gesamten Inhalt von CCP_App_Closed_Beta_V1.html nach index.html. Führe danach exakt drei Änderungen durch — nichts anderes darf verändert werden:

EINFÜGUNG 1 — Supabase CDN-Script + Alias (nach dem Google-Fonts-Link, vor dem `<style>`-Tag):

Füge diese beiden Zeilen direkt nach der Zeile
  `<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@500;600&display=swap" rel="stylesheet">`
und vor dem `<style>`-Tag ein:

  `<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js"></script>`
  `<script>window.supabaseLib = window.supabase;</script>`

Begründung: Das CDN-Script stellt window.supabase bereit. Der Alias window.supabaseLib ist der stabile Zugriffspunkt für Phase 1 — unabhängig davon, ob das CDN den globalen Namen ändert. Supabase wird hier NICHT initialisiert (kein createClient-Aufruf).

EINFÜGUNG 2 — Service-Worker-Registrierung (am Ende des `<script>`-Blocks, nach der letzten Zeile `})();` und vor `</script>`):

Füge diese Zeilen nach `})();` ein:

  `if ('serviceWorker' in navigator) {`
  `  navigator.serviceWorker.register('/sw.js').catch(function(e) {});`
  `}`

Begründung: Der SW wird registriert, sobald die App lädt. Der catch verhindert, dass fehlende sw.js einen sichtbaren Fehler erzeugt. Der Pfad `/sw.js` ist absolut — sw.js liegt im Root.

SONSTIGE REGELN:
- Keine einzige weitere Zeile ändern. Kein CSS, keine Funktionen, keine Kommentare entfernen.
- Die Speicher-Schicht-Funktionen (sList, sGet, sSet, sDel, getOperator, setOperatorStore, loadMeta, setMasterMedic, loadPatients, savePatient, addPatient) bleiben unverändert.
- Kein createClient(), kein supabase.from(), kein initSupabase() oder ähnliches einfügen.
  </action>
  <verify>
    <automated>grep -c 'cdn.jsdelivr.net/npm/@supabase/supabase-js@2' "/Users/gaborszeman/Desktop/Projekt CCP_APP/index.html" && grep -c 'window.supabaseLib = window.supabase' "/Users/gaborszeman/Desktop/Projekt CCP_APP/index.html" && grep -c "serviceWorker.*register.*sw\.js" "/Users/gaborszeman/Desktop/Projekt CCP_APP/index.html"</automated>
    Alle drei Befehle müssen `1` ausgeben. Zusätzlich: grep -c 'createClient' index.html muss `0` ausgeben.
  </verify>
  <acceptance_criteria>
    - index.html existiert unter /Users/gaborszeman/Desktop/Projekt CCP_APP/index.html
    - Datei enthält genau 1 Vorkommen von `cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js`
    - Datei enthält genau 1 Vorkommen von `window.supabaseLib = window.supabase;`
    - Datei enthält genau 1 Vorkommen von `navigator.serviceWorker.register('/sw.js')`
    - Das CDN-Script steht vor dem `<style>`-Tag (nach dem Google-Fonts-Link)
    - Die SW-Registrierung steht nach `})();` und vor `</script>` am Ende der Datei
    - Das Wort `createClient` kommt NICHT vor (grep -c 'createClient' gibt 0)
    - Zeilenanzahl von index.html entspricht CCP_App_Closed_Beta_V1.html + 5 Zeilen (2 CDN-Zeilen + 3 SW-Zeilen)
    - Die Speicher-Schicht-Funktionssignaturen (sList, sGet, sSet, sDel) sind identisch mit der Originaldatei
  </acceptance_criteria>
  <done>index.html ist eine funktionale 1:1-Kopie der Legacy-App mit den drei definierten Ergänzungen. Browser-Öffnen zeigt identisches Aussehen und Verhalten zur Legacy-Datei.</done>
</task>

<task type="auto">
  <name>Task 2: .gitignore anlegen</name>
  <files>/Users/gaborszeman/Desktop/Projekt CCP_APP/.gitignore</files>
  <read_first>
    - /Users/gaborszeman/Desktop/Projekt CCP_APP/CLAUDE.md — Stack: Vanilla JS, keine Build-Chain, keine node_modules
    - Projektstruktur: Single-File-HTML, docs/, .planning/, legacy/ — diese alle NICHT ignorieren
  </read_first>
  <action>
Erstelle .gitignore im Projekt-Root mit folgenden Einträgen. Jeder Abschnitt ist kommentiert:

```
# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V8
.Trashes
ehthumbs.db
Thumbs.db

# Editor & IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Entwicklungsserver-Logs (python -m http.server)
*.log

# Umgebungsvariablen (kommen in Phase 1)
.env
.env.local
.env.*.local

# Temporäre Dateien
*.tmp
*.bak

# Node (kein npm in Phase 0, aber für Phase 1+ vorbereiten)
node_modules/
npm-debug.log*

# Sicherheitskritisch — nie einchecken
*.pem
*.key
supabase-config.js
```

Regel: .planning/, docs/, legacy/, index.html, sw.js, CLAUDE.md, README.md — alles NICHT in .gitignore, diese Dateien sollen versioniert werden.
  </action>
  <verify>
    <automated>grep -c '\.DS_Store' "/Users/gaborszeman/Desktop/Projekt CCP_APP/.gitignore" && grep -c '\.env' "/Users/gaborszeman/Desktop/Projekt CCP_APP/.gitignore" && grep -c 'node_modules' "/Users/gaborszeman/Desktop/Projekt CCP_APP/.gitignore"</automated>
    Alle drei Befehle müssen >= 1 ausgeben.
  </verify>
  <acceptance_criteria>
    - .gitignore existiert unter /Users/gaborszeman/Desktop/Projekt CCP_APP/.gitignore
    - Enthält mindestens: .DS_Store, .env, node_modules/
    - Enthält NICHT: index.html, sw.js, .planning, docs, legacy (diese sind versionierungswürdig)
    - Datei ist UTF-8, Unix-Zeilenenden
  </acceptance_criteria>
  <done>.gitignore liegt im Root und schützt vor versehentlichem Einchecken von Systemdateien, Secrets und Entwicklungsartefakten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| CDN → index.html | Supabase-Script wird von cdn.jsdelivr.net geladen (externer Ursprung) |
| index.html → sw.js | SW-Registrierung lädt sw.js vom gleichen Ursprung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | cdn.jsdelivr.net/supabase-js | accept | Phase 0 initialisiert Supabase nicht; CDN-Script wird geladen aber nicht ausgeführt (kein createClient). SRI-Hash kann in Phase 1 ergänzt werden wenn Supabase verdrahtet wird. |
| T-01-02 | Information Disclosure | .gitignore | mitigate | .env und supabase-config.js explizit in .gitignore aufnehmen — verhindert versehentliches Einchecken von Credentials in Phase 1+ |
| T-01-SC | Tampering | CDN-Script-Install | accept | Kein npm-Install in Phase 0; CDN-URL ist exakt fixiert (@2 = major pin). Kein [ASSUMED]-Package-Problem. |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. `grep -c 'cdn.jsdelivr.net/npm/@supabase/supabase-js@2' index.html` → 1
2. `grep -c 'window.supabaseLib = window.supabase' index.html` → 1
3. `grep -c "serviceWorker.*register" index.html` → 1
4. `grep -c 'createClient' index.html` → 0
5. `grep -c '\.DS_Store' .gitignore` → >= 1
6. `grep -c '\.env' .gitignore` → >= 1
7. Datei CCP_App_Closed_Beta_V1.html ist unverändert: `diff CCP_App_Closed_Beta_V1.html index.html` zeigt nur die 5 eingefügten Zeilen
</verification>

<success_criteria>
- index.html öffnet im Browser identisch zur Legacy-HTML (visuell und funktional)
- Supabase-CDN-Script ist eingebunden, aber window.supabase wird nur als Alias zugewiesen, kein createClient
- SW-Registrierung ist im Script-Block vorhanden
- .gitignore schützt vor Secrets und macOS-Artefakten
- CCP_App_Closed_Beta_V1.html ist unverändert (diff bestätigt)
</success_criteria>

<output>
Erstelle `.planning/phases/01-repo-setup-offline-shell/01-01-SUMMARY.md` wenn fertig
</output>
