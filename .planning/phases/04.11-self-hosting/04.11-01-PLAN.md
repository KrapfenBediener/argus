---
phase: 04.11-self-hosting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.js
  - index.html
  - sw.js
  - "docs/leitung-<suffix>.html (Name via Glob docs/leitung-*.html ermitteln — D-06: Name NIE in committete Texte/Commits)"
  - version.json
  - CHANGELOG.md
autonomous: true
requirements: [T7]
must_haves:
  truths:
    - "Feld-App UND Leitungs-Seite initialisieren den Supabase-Client ausschließlich aus der zentralen config.js — Projekt-URL/Anon-Key stehen in keiner anderen getrackten html/js-Datei mehr"
    - "Die App startet offline weiterhin vollständig (config.js liegt im SW-Precache und ist vor dem Hauptscript verfügbar)"
    - "Bei fehlender/kaputter config.js zeigt die App einen klaren Hinweis (Banner/Toast) statt still in den lokalen Modus zu fallen"
    - "Release v0.24.0 ist konsistent (APP_VERSION, version.json, CACHE_NAME v43, CHANGELOG, WHATS_NEW knapp)"
  artifacts:
    - path: "config.js"
      provides: "Zentrale build-freie Backend-Konfiguration (window.ARGUS_CONFIG mit url + anonKey)"
      contains: "window.ARGUS_CONFIG"
    - path: "index.html"
      provides: "Feld-App konsumiert ARGUS_CONFIG; Fehlerpfad bei fehlender Config"
    - path: "sw.js"
      provides: "Precache inkl. ./config.js, CACHE_NAME ccp-shell-v43"
      contains: "ccp-shell-v43"
    - path: "version.json"
      provides: "Update-Erkennung auf 0.24.0"
  key_links:
    - from: "index.html"
      to: "config.js"
      via: "script-Tag VOR dem Hauptscript"
      pattern: "src=\"\\./config\\.js\""
    - from: "index.html sb()/RPC-Aufrufe"
      to: "window.ARGUS_CONFIG"
      via: "SUPA_URL/SUPA_KEY werden aus ARGUS_CONFIG abgeleitet (Aufrufstellen unverändert)"
      pattern: "ARGUS_CONFIG"
    - from: "sw.js PRECACHE_URLS"
      to: "config.js"
      via: "Precache-Eintrag"
      pattern: "'\\./config\\.js'"
    - from: "docs/leitung-<suffix>.html"
      to: "config.js"
      via: "script-Tag mit RELATIVEM Pfad ../config.js"
      pattern: "src=\"\\.\\./config\\.js\""
---

<objective>
Config-Auslagerung für Self-Hosting (Phase 4.11, T7, Entscheid D-01): Supabase-URL
und Anon-Key aus `index.html` und der Leitungs-Seite in EINE zentrale, build-freie
Datei `config.js` im Projektroot ziehen. PWA-/Offline-Verhalten bleibt unverändert
(Precache, Ladereihenfolge), fehlende Config bricht nicht still. Abschluss als
Release v0.24.0 gemäß Konvention (D-04).

Purpose: Produktweg zu Betriebsmodell M1 — eine Polizei-/BITBW-Instanz bindet ARGUS
ohne Codeänderung an, nur durch Austausch der config.js.
Output: `config.js` (neu), angepasste `index.html`, `sw.js`, Leitungs-Seite,
`version.json`, `CHANGELOG.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.11-self-hosting/04.11-CONTEXT.md
@.planning/ROADMAP.md
@CLAUDE.md
@sw.js
</context>

<wichtige_regeln>
- **Speicher-Naht und View-Funktionen NICHT umbauen** (CLAUDE.md): `sList/sGet/sSet/sDel`,
  `loadPatients/savePatient/...` sowie alle Aufrufstellen von `SUPA_URL`/`SUPA_KEY`
  bleiben unangetastet — nur die KONSTANTEN-DEFINITION wird umgestellt.
- **D-06:** Der konkrete Dateiname der Leitungs-Seite (`docs/leitung-<suffix>.html`)
  darf in KEINEM committeten Text und KEINER Commit-Message auftauchen. Datei per
  Glob `docs/leitung-*.html` finden; in SUMMARY/CHANGELOG nur Muster-Schreibweise.
- **Kein node/npm.** JS-Syntaxprüfung via `osascript -l JavaScript` (Muster aus
  04.9-03: Inline-Script-Blöcke per python3 extrahieren, JSC-parsen). Statischer
  Smoke-Test via `python3 -m http.server` erlaubt.
- **Keine nativen alert/confirm/prompt** — Fehlerhinweise über vorhandene
  Banner-/Toast-/Modal-Mechanik.
- config.js enthält NUR die heute schon öffentlichen Werte (Projekt-URL + Anon-Key).
  KEINE Service-Keys, keine neuen Geheimnisse.
</wichtige_regeln>

<tasks>

<task type="auto">
  <name>Task 1: config.js anlegen und index.html auf ARGUS_CONFIG umstellen (inkl. Fehlerpfad)</name>
  <files>config.js, index.html</files>
  <read_first>
    - index.html Zeilen 1–20 (head, vendor/supabase.js-Einbindung Z. 13–14)
    - index.html Zeilen 95–110 (Beginn Hauptscript, Marker „PHASE 1 — SUPABASE BACKEND", APP_VERSION Z. 106)
    - index.html Zeilen 230–300 (SUPA_URL/SUPA_KEY Z. 237–238, sb() ab Z. 270, createClient Z. 280)
    - index.html Zeile ~1573 (Statusbanner „🟢 Echtzeit-Modus / 🔴 Lokaler Modus")
    - index.html Zeilen 470–485 und 570–585 (fetch-RPCs nutzen SUPA_URL/SUPA_KEY direkt — NICHT umbauen)
  </read_first>
  <action>
    Umsetzung von D-01 (zentrale Konfiguration), Format gem. Claude's Discretion als
    globales Script (kein JSON+fetch — synchron, offline-tauglich, build-frei):

    1. **`config.js` im Projektroot anlegen:** setzt `window.ARGUS_CONFIG = { url: '…', anonKey: '…' }`
       mit den heutigen Werten aus index.html Z. 237–238. Kopfkommentar (deutsch):
       Zweck (zentrale Backend-Konfiguration, einzige Stelle für Instanz-Tausch beim
       Self-Hosting, siehe docs/SELF-HOSTING.md), Hinweis „nur öffentliche Werte
       (Anon-Key), niemals Service-Keys". Keine weiteren Schlüssel erfinden.
    2. **index.html einbinden:** `<script src="./config.js"></script>` direkt nach der
       vendor/supabase.js-Zeile (Z. 13–14) — damit garantiert VOR dem Hauptscript (Z. 98).
    3. **Konstanten ableiten statt definieren (Minimal-Diff):** Z. 237–238 ersetzen durch
       Ableitung aus der Config, z. B. `var _cfg = window.ARGUS_CONFIG || null;`,
       `var SUPA_URL = _cfg ? _cfg.url : '';`, `var SUPA_KEY = _cfg ? _cfg.anonKey : '';`.
       ALLE Aufrufstellen (sb(), fetch-RPCs Z. 478/576 u. a.) bleiben dadurch unverändert.
    4. **Fehlerpfad (D-01):** Wenn `_cfg` fehlt oder `url`/`anonKey` leer/kein https —
       Flag setzen (z. B. `_configError=true`); sb() liefert dann weiterhin null.
       Im Statusbanner (~Z. 1573) bei gesetztem Flag statt „🔴 Lokaler Modus" einen
       eindeutigen Hinweis zeigen, z. B. „⚠️ Konfiguration fehlt (config.js) — kein
       Backend verbunden", zusätzlich einmaliger Toast beim Start. Kein natives alert.
    5. **JSC-Syntaxbeleg:** Inline-Script aus index.html extrahieren und mit
       `osascript -l JavaScript` auf Syntaxfehler prüfen (Muster aus 04.9-03);
       config.js ebenfalls per JSC evaluieren (mit Stub `var window={}`).

    NICHT tun: Versionsbump (Task 3), sw.js (Task 2), Leitungs-Seite (Task 2),
    keine Umbenennung von SUPA_URL/SUPA_KEY an den Aufrufstellen.
  </action>
  <verify>
    <automated>test -f config.js && grep -q "window.ARGUS_CONFIG" config.js && grep -q 'src="./config.js"' index.html && ! grep -qE "var SUPA_URL *= *'https" index.html && ! grep -q 'sehuosjyjmrpzcqrelej' index.html && grep -q 'ARGUS_CONFIG' index.html && python3 -c "s=open('index.html').read(); assert s.index('config.js') < s.index('PHASE 1'), 'config.js muss vor dem Hauptscript geladen werden'" && osascript -l JavaScript -e 'ObjC.import("Foundation"); var s=ObjC.unwrap($.NSString.stringWithContentsOfFileEncodingError("config.js",4,$())); var window={}; eval(s); var c=window.ARGUS_CONFIG; if(!c||c.url.indexOf("https://")!==0||!c.anonKey) throw new Error("config invalid"); "OK"'</automated>
  </verify>
  <done>config.js existiert mit ARGUS_CONFIG (url+anonKey); index.html lädt config.js vor dem Hauptscript, enthält keine Projekt-URL-Literale mehr, leitet SUPA_URL/SUPA_KEY aus der Config ab; Fehlerpfad mit sichtbarem Hinweis implementiert; JSC-Syntaxprüfung (index-Inline-Script + config.js) bestanden.</done>
</task>

<task type="auto">
  <name>Task 2: Leitungs-Seite auf ../config.js umstellen + SW-Precache (v43) erweitern</name>
  <files>docs/leitung-<suffix>.html (via Glob docs/leitung-*.html), sw.js</files>
  <read_first>
    - Leitungs-Seite (Glob `docs/leitung-*.html`) Zeilen 100–130 (vendor-Einbindung Z. 108, SUPA_URL/SUPA_KEY Z. 119–120) und 195–230 (createClient Z. ~205, fetch-RPC Z. ~222–224)
    - sw.js Zeilen 10–21 (CACHE_NAME, PRECACHE_URLS)
  </read_first>
  <action>
    1. **Leitungs-Seite (D-01, zweiter Konsument):** Datei per Glob `docs/leitung-*.html`
       ermitteln (Name aus untracked LEITUNG-URL.md; D-06: Name nirgends in committete
       Texte/Commit-Messages schreiben). `<script src="../config.js"></script>` direkt
       nach der `../vendor/supabase.js`-Zeile einfügen (RELATIVER Pfad — Seite liegt
       unter docs/). Z. 119–120 wie in Task 1 durch Ableitung aus `window.ARGUS_CONFIG`
       ersetzen; Aufrufstellen (createClient, fetch-RPC) unverändert. Fehlerpfad: bei
       fehlender Config klaren roten Hinweistext in der Login-Ansicht zeigen
       („Konfiguration fehlt (config.js)"), Login-Aktion abfangen.
    2. **sw.js:** `'./config.js'` in PRECACHE_URLS aufnehmen (nach `'./vendor/supabase.js'`),
       `CACHE_NAME` auf `ccp-shell-v43` bumpen (D-04: SW-Cache +1). Sonst NICHTS an der
       fetch-Strategie ändern — insbesondere die `.supabase.co`-Network-only-Regel
       unangetastet lassen (Befund dazu wird in Plan 04.11-02 nur dokumentiert,
       keine spekulativen Umbauten).
    3. **Precache-Konsistenz-Gate (wie 04.8-01):** per python3 alle PRECACHE_URLS aus
       sw.js parsen und Existenz jeder Datei prüfen.
    4. JSC-Syntaxbeleg für das Inline-Script der Leitungs-Seite und sw.js (Muster 04.9-03).
  </action>
  <verify>
    <automated>grep -q 'src="../config.js"' docs/leitung-*.html && ! grep -q 'sehuosjyjmrpzcqrelej' docs/leitung-*.html && ! grep -qE "var SUPA_URL *= *'https" docs/leitung-*.html && grep -q "'./config.js'" sw.js && grep -q 'ccp-shell-v43' sw.js && python3 -c "import re,os; sw=open('sw.js').read(); urls=re.findall(r\"'(\./[^']*)'\", sw.split('PRECACHE_URLS')[1].split(']')[0]); missing=[u for u in urls if not os.path.exists(u)]; assert urls and not missing, ('Precache-Eintraege fehlen auf Platte:', missing)"</automated>
  </verify>
  <done>Leitungs-Seite lädt ../config.js und enthält keine URL/Key-Literale mehr (mit Fehlerpfad); sw.js precacht config.js unter CACHE_NAME v43; jeder Precache-Eintrag existiert als Datei; D-06 gewahrt (kein Klarname in Texten/Commits).</done>
</task>

<task type="auto">
  <name>Task 3: Release v0.24.0 + Gesamt-Gates (URL/Key nur noch in config.js, Offline-Smoke-Test, Drehbuch-Prüfung)</name>
  <files>index.html, version.json, CHANGELOG.md</files>
  <read_first>
    - index.html Zeilen 104–145 (APP_VERSION Z. 106, WHATS_NEW ab Z. 131)
    - CHANGELOG.md Kopf (Format der v0.23.1-Eintragung)
  </read_first>
  <action>
    Release gemäß Konvention (D-04):
    1. `APP_VERSION = '0.24.0'` in index.html; `version.json` auf `0.24.0`.
       (CACHE_NAME v43 bereits in Task 2.)
    2. CHANGELOG.md: Eintrag `## v0.24.0 — <Datum>` — Titel sinngemäß
       „Self-Hosting-Vorbereitung: zentrale Backend-Konfiguration (config.js)";
       Inhalt: Config-Auslagerung (Feld-App + Leitungs-Seite, nur Muster-Schreibweise
       `docs/leitung-<suffix>.html`!), SW-Precache v43, Fehlerhinweis bei fehlender
       Config. KEIN UPDATE-Sheet (für Nutzer unsichtbare Änderung — Diskretions-Entscheid
       in SUMMARY vermerken).
    3. WHATS_NEW['0.24.0'] in index.html: EIN knapper Eintrag, z. B. „Technische
       Vorbereitung für den Betrieb auf eigener Infrastruktur — für die Bedienung
       ändert sich nichts."
    4. **Drehbuch-DAUERREGEL dokumentieren:** keine UI-/Bedienungsänderung → keine
       Lektionsanpassung nötig; Prüfung explizit im SUMMARY und im CHANGELOG-Eintrag
       („Drehbuch geprüft: keine Änderung") festhalten.
    5. **Gesamt-Gate URL/Key:** Über alle getrackten html/js-Dateien belegen, dass
       Projekt-URL und Anon-Key NUR noch in config.js stehen (git grep, siehe verify).
    6. **Statischer Smoke-Test:** `python3 -m http.server` starten, config.js und
       index.html per curl abrufen (200), Server beenden.
  </action>
  <verify>
    <automated>grep -q "APP_VERSION = '0.24.0'" index.html && grep -q '"0.24.0"' version.json && grep -q 'ccp-shell-v43' sw.js && grep -q '## v0.24.0' CHANGELOG.md && grep -q "'0.24.0'" index.html && test "$(git grep --untracked -l 'sehuosjyjmrpzcqrelej' -- '*.html' '*.js' | tr -d '\n')" = "config.js" && test "$(git grep --untracked -l 'G0DuJmeJBQ' -- '*.html' '*.js' | tr -d '\n')" = "config.js" && (python3 -m http.server 8123 >/dev/null 2>&1 & SRV=$!; sleep 1; curl -fsS http://localhost:8123/config.js >/dev/null && curl -fsS http://localhost:8123/index.html >/dev/null; RC=$?; kill $SRV 2>/dev/null; exit $RC)</automated>
  </verify>
  <done>v0.24.0 konsistent über APP_VERSION/version.json/CACHE_NAME v43/CHANGELOG/WHATS_NEW; Projekt-URL und Anon-Key stehen in genau einer getrackten html/js-Datei (config.js); statischer Smoke-Test bestanden; Drehbuch-Prüfung („keine Lektionsänderung") dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Public Repo → Welt | Alles Committete ist öffentlich lesbar (config.js, CHANGELOG, Pläne) |
| Browser → Supabase-API | Anon-Key + JWT; unverändert durch diese Phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.11-01 | Information Disclosure | config.js (public) | accept | Enthält ausschließlich heute schon öffentliche Werte (URL + Anon-Key); Negativ-Gate gegen Service-Keys/Interna in Task 1 (nur url+anonKey-Schlüssel) |
| T-4.11-02 | Information Disclosure | CHANGELOG/SUMMARY/Commits | mitigate | D-06-Muster-Schreibweise; Gate `! git grep -qE 'leitung-[0-9a-f]{6,}' -- ':!docs/'` in Plan 02 |
| T-4.11-03 | Tampering | config.js auf dem Host | accept | Statisches HTTPS-Hosting wie bisher; kein neuer Angriffsvektor ggü. Inline-Konstanten |
| T-4.11-SC | Tampering | Paket-Installationen | accept | Keine npm/pip/cargo-Installationen in dieser Phase (kein node verfügbar) |
</threat_model>

<verification>
- URL/Anon-Key-Isolation: `git grep -l` über `*.html`/`*.js` liefert exakt `config.js`.
- Precache-Konsistenz: alle PRECACHE_URLS existieren; config.js enthalten; CACHE_NAME v43.
- Ladereihenfolge: config.js-Script-Tag vor dem Hauptscript (python3-Index-Gate).
- JSC: config.js evaluierbar (url https, anonKey gesetzt); Inline-Scripts syntaxfehlerfrei.
- Release-Konsistenz v0.24.0 über alle vier Stellen.
</verification>

<success_criteria>
- Feld-App + Leitungs-Seite konsumieren ausschließlich window.ARGUS_CONFIG (D-01).
- Offline-Verhalten unverändert (Precache v43 inkl. config.js, Reihenfolge gesichert).
- Fehlende/kaputte Config → sichtbarer Hinweis, kein stiller Bruch.
- Release v0.24.0 gemäß Konvention, kein UPDATE-Sheet, Drehbuch-Prüfung dokumentiert (D-04).
- Keine Änderungen an Speicher-Naht, View-Funktionen oder SW-fetch-Strategie.
</success_criteria>

<output>
Create `.planning/phases/04.11-self-hosting/04.11-01-SUMMARY.md` when done
(Diskretions-Entscheide dokumentieren: config.js-Format, Platzierung des Fehlerhinweises, kein UPDATE-Sheet).
</output>
