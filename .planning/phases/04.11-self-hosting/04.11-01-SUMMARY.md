---
phase: 04.11-self-hosting
plan: 01
subsystem: infra
tags: [supabase, config, pwa, service-worker, self-hosting]

# Dependency graph
requires:
  - phase: 04.9-governance-oberflaeche
    provides: Leitungs-Seite (docs/leitung-<suffix>.html) als zweiter Supabase-Konsument
  - phase: 04.10-datenschutz-schlusspaket
    provides: Stand v0.23.1 / SW-Cache v42 als Ausgangsbasis
provides:
  - config.js (window.ARGUS_CONFIG mit url + anonKey) als EINZIGE Stelle für den Instanz-Tausch beim Self-Hosting (D-01)
  - Feld-App + Leitungs-Seite konsumieren ausschließlich ARGUS_CONFIG; keine URL/Key-Literale mehr außerhalb config.js
  - Fehlerpfad bei fehlender/kaputter Config (Banner, Start-Toast, Login-Hinweis, abgefangene Code-Eingabe/Login)
  - Release v0.24.0 (SW-Cache v43, config.js im Precache)
affects: [04.11-02 (SELF-HOSTING.md verweist auf config.js), 05-uebergabe-paket]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Zentrale build-freie Backend-Konfiguration als globales Script (config.js), synchron VOR dem Hauptscript geladen, im SW-Precache"]

key-files:
  created: [config.js]
  modified: [index.html, sw.js, "docs/leitung-<suffix>.html", version.json, CHANGELOG.md]

key-decisions:
  - "Config-Format: globales Script (window.ARGUS_CONFIG) statt JSON+fetch — synchron, offline-tauglich, build-frei (Claude's Discretion lt. CONTEXT)"
  - "Fehlerhinweis-Platzierung Feld-App: Statusbanner (ersetzt 🔴 Lokaler Modus) + einmaliger Start-Toast; Leitungs-Seite: roter Hinweis (.lerr) in der Login-Box + Login-Abfang"
  - "Kein UPDATE-Sheet für v0.24.0 — für Nutzer unsichtbare Änderung (D-04)"
  - "exchangeCode/checkRevokedCode/doUnlock fangen _configError hart ab — verhindert relative fetch-RPCs gegen den eigenen Origin (kryptischer 404)"

patterns-established:
  - "ARGUS_CONFIG-Ableitung: var _cfg = window.ARGUS_CONFIG || null; _configError-Flag; SUPA_URL/SUPA_KEY abgeleitet, Aufrufstellen unverändert (Speicher-Naht-Regel gewahrt)"

requirements-completed: [T7]

# Metrics
duration: 12min
completed: 2026-06-11
---

# Phase 4.11 Plan 01: Config-Auslagerung für Self-Hosting Summary

**Supabase-URL + Anon-Key in zentrale build-freie config.js (window.ARGUS_CONFIG) ausgelagert — Feld-App und Leitungs-Seite konsumieren nur noch die Config, mit sichtbarem Fehlerpfad und Release v0.24.0 (SW-Cache v43)**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-06-11T13:17:28Z
- **Completed:** 2026-06-11T13:29:00Z
- **Tasks:** 3
- **Files modified:** 6 (1 neu: config.js)

## Accomplishments
- `config.js` (neu, Projektroot): `window.ARGUS_CONFIG` mit `url` + `anonKey` — einzige Stelle für den Instanz-Tausch beim Betriebsmodell M1; enthält nur die ohnehin öffentlichen Werte, deutscher Kopfkommentar mit „niemals Service-Keys"-Warnung
- Feld-App: lädt `./config.js` synchron direkt nach `vendor/supabase.js` (vor dem Hauptscript); `SUPA_URL`/`SUPA_KEY` werden abgeleitet statt definiert — ALLE Aufrufstellen (sb(), fetch-RPCs) unverändert (Speicher-Naht-Regel)
- Leitungs-Seite: lädt `../config.js` (relativer Pfad, Seite liegt unter docs/); gleiche Ableitung; Fehlerpfad in der Login-Ansicht
- Fehlerpfad ohne native Dialoge: `_configError`-Flag → Statusbanner „⚠️ Konfiguration fehlt (config.js) — kein Backend verbunden", einmaliger Start-Toast, sb() liefert null; Code-Eingabe (doUnlock) und Leitungs-Login (doLogin) mit Klartext-Meldung abgefangen
- sw.js: `'./config.js'` im Precache, `CACHE_NAME` ccp-shell-v43; fetch-Strategie (inkl. `.supabase.co`-Network-only) unangetastet
- Release v0.24.0 konsistent: APP_VERSION, version.json, CACHE_NAME v43, CHANGELOG-Eintrag, WHATS_NEW knapp

## Task Commits

Each task was committed atomically:

1. **Task 1: config.js anlegen + index.html auf ARGUS_CONFIG umstellen (inkl. Fehlerpfad)** - `d99c048` (feat)
2. **Task 2: Leitungs-Seite auf ../config.js + SW-Precache v43** - `8481428` (feat)
3. **Task 3: Release v0.24.0 + Gesamt-Gates** - `5e9ac55` (feat)

## Files Created/Modified
- `config.js` - NEU: zentrale Backend-Konfiguration (window.ARGUS_CONFIG), nur öffentliche Werte
- `index.html` - Script-Tag ./config.js, Konstanten-Ableitung, _configError-Fehlerpfad (Banner/Toast/Guards), APP_VERSION 0.24.0, WHATS_NEW
- `docs/leitung-<suffix>.html` - Script-Tag ../config.js, Konstanten-Ableitung, Login-Hinweis + Login-Abfang (D-06: Muster-Schreibweise)
- `sw.js` - Precache + ./config.js, CACHE_NAME ccp-shell-v43
- `version.json` - 0.24.0
- `CHANGELOG.md` - v0.24.0-Eintrag inkl. „Drehbuch geprüft: keine Änderung"

## Decisions Made (Diskretions-Entscheide)
- **config.js-Format:** globales Script statt JSON+fetch — synchron vor dem Hauptscript verfügbar, offline-tauglich (Precache), build-frei, EIN Ort für beide Konsumenten (relativer Pfad ../config.js für docs/)
- **Platzierung Fehlerhinweis:** Feld-App im Statusbanner (ersetzt den „🔴 Lokaler Modus"-Text bei _configError) plus einmaliger Toast beim Start; Leitungs-Seite als roter .lerr-Hinweis direkt in der Login-Box
- **Kein UPDATE-Sheet:** für Nutzer unsichtbare Änderung — nur knapper WHATS_NEW-Eintrag (D-04)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Code-Eingabe/RPC-Aufrufe bei _configError hart abgefangen**
- **Found during:** Task 1 (Fehlerpfad)
- **Issue:** Bei leerer SUPA_URL liefen die fetch-RPCs (exchangeCode, checkRevokedCode) relativ gegen den eigenen Origin → kryptischer 404 statt klarer Meldung (Checker-Hinweis)
- **Fix:** Guards in exchangeCode (setzt _lastExchangeError-Klartext), checkRevokedCode (früher return) und doUnlock (Toast vor „Prüfe Code…"); Leitungs-Seite analog in doLogin
- **Files modified:** index.html, docs/leitung-<suffix>.html
- **Verification:** JSC-Syntaxprüfung bestanden; Meldungstexte eindeutig („Konfiguration fehlt (config.js) — kein Backend verbunden")
- **Committed in:** d99c048 (Task 1), 8481428 (Task 2)

---

**Total deviations:** 1 auto-fixed (Rule 2, vom Checker als INFO vorgegeben)
**Impact on plan:** Notwendig für einen sauberen Fehlerpfad — kein Scope Creep.

## Zusätzliche Gates (über Plan-Verify hinaus)
- `! git check-ignore -q config.js` — PASS (.gitignore ignoriert nur den Altnamen `supabase-config.js`; config.js ist committet, enthält nur öffentliche Werte)
- WHATS_NEW-Gate spezifisch: `grep -q "'0.24.0':"` — PASS
- Precache-Konsistenz: alle 9 PRECACHE_URLS existieren auf Platte — PASS
- URL/Key-Isolation: `git grep --untracked -l` über *.html/*.js liefert exakt `config.js` (für Projekt-Ref UND Anon-Key-Fragment) — PASS
- JSC (osascript, Parse ohne Ausführung via `new Function`): index-Inline-Script (217 kB), Leitungs-Inline-Script (41 kB), sw.js, config.js (eval mit window-Stub, url/anonKey validiert) — alle PASS
- Statischer Smoke-Test (`python3 -m http.server`): config.js + index.html → HTTP 200 — PASS

## Drehbuch-DAUERREGEL (geprüft)
Keine UI-/Bedienungsänderung: Der Fehlerhinweis erscheint nur bei kaputtem Deployment (config.js fehlt), nie im Normalbetrieb oder Training; die Trainings-Lektion „Status & Bediener" (🟢/🔴) bleibt korrekt. **Keine Lektionsanpassung nötig** — auch im CHANGELOG-Eintrag festgehalten.

## Threat-Model-Abdeckung
| Threat | Status |
|---|---|
| T-4.11-01 config.js public | accept — nur url+anonKey, keine weiteren Schlüssel; Warnkommentar gegen Service-Keys |
| T-4.11-02 D-06 Leitungs-Name | gewahrt — Klarname weder in Commits noch in committeten Texten (nur Muster-Schreibweise) |
| T-4.11-03 Tampering config.js | accept — statisches HTTPS-Hosting unverändert |

## Issues Encountered
None

## User Setup Required
None — keine externe Konfiguration nötig. (Self-Hosting-Anleitung folgt in Plan 04.11-02.)

## Next Phase Readiness
- Plan 04.11-02 (Kompatibilitäts-Audit + docs/SELF-HOSTING.md) kann direkt aufsetzen: config.js als dokumentierter Tauschpunkt existiert
- Live-Test gegen Zweitinstanz weiterhin offen (kein Zugang zu Polizei-Infra) — Akzeptanz über Grep-Isolation + JSC-Konsumtest erbracht

---
*Phase: 04.11-self-hosting*
*Completed: 2026-06-11*

## Self-Check: PASSED
Alle erzeugten/geänderten Dateien vorhanden; Commits d99c048 / 8481428 / 5e9ac55 in der Historie; D-06-Gates (Tracked-Files, Commit-Messages, SUMMARY) grün.
