---
phase: 04.11-self-hosting
plan: 02
type: execute
wave: 2
depends_on: [04.11-01]
files_modified:
  - docs/SELF-HOSTING.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - "docs/PROD-SETUP.md (intern/gitignored — NICHT committen)"
  - "docs/datenschutz/DATENSCHUTZ-SPEC.md + 08-MASSNAHMENPLAN.md (intern/gitignored — NICHT committen)"
autonomous: true
requirements: [T7]
must_haves:
  truths:
    - "docs/SELF-HOSTING.md befähigt eine fachkundige Person ohne Projektkenntnis, ARGUS auf eigener Supabase-/Postgres-Infrastruktur aufzubauen (Varianten, Migrationen, Abhängigkeiten, frische Codes, config-Tausch, Smoke-Test)"
    - "Das Kompatibilitäts-Audit deckt vault.*, cron.*/pg_cron, pgjwt und Realtime über ALLE Migrationen 0000–0004 ab — je Abhängigkeit: Pflicht / optional / Ersatzweg"
    - "docs/SELF-HOSTING.md enthält keine Secrets, keine internen Begriffe/Dateinamen und nicht den Leitungs-Dateinamen (Muster-Schreibweise)"
    - "Betriebsvorgaben für Lizenznehmer (MDM-Dienstgeräte, Kürzel-Liste, Codes nur mündlich/Funk) stehen als eigener Abschnitt in der Anleitung"
    - "ROADMAP/STATE führen Phase 4.11 als abgeschlossen; SPEC T7 ist lokal abgehakt"
  artifacts:
    - path: "docs/SELF-HOSTING.md"
      provides: "Öffentliche Self-Hosting-Anleitung inkl. Audit-Tabelle, Smoke-Test-Drehbuch, Betriebsvorgaben"
      min_lines: 120
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4.11 ✅ mit abgehakten Plänen"
    - path: ".planning/STATE.md"
      provides: "Projektstand v0.24.0, Phase 4.11 abgeschlossen"
  key_links:
    - from: "docs/SELF-HOSTING.md"
      to: "supabase/migrations/0000–0004"
      via: "Ein-Schritt-Apply in Reihenfolge"
      pattern: "0000.*0004|0004"
    - from: "docs/SELF-HOSTING.md"
      to: "config.js"
      via: "Abschnitt „App auf die neue Instanz zeigen""
      pattern: "config\\.js"
---

<objective>
Kompatibilitäts-Audit der Migrationen 0000–0004 auf Supabase-Spezifika (D-03) und
darauf aufbauend die öffentliche Aufbau-Anleitung `docs/SELF-HOSTING.md` (D-02)
schreiben — Zielgruppe: fachkundige BITBW-Admin-Person ohne Projektkenntnis.
Abschließend Statusführung (ROADMAP/STATE, SPEC T7 lokal abhaken).

Purpose: Übergabefähigkeit für Betriebsmodell M1 — ohne Zugang zur Polizei-Infra
ist die Akzeptanz „läuft gegen zweite Instanz" über Verifizierbarkeit der Anleitung
plus Config-Isolation (Plan 04.11-01) abgedeckt.
Output: `docs/SELF-HOSTING.md` (committbar), aktualisierte Statusdateien,
lokal nachgeführte interne Dokumente.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.11-self-hosting/04.11-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/0000_base_schema.sql
@supabase/migrations/0001_phase4_jwt_rls.sql
@supabase/migrations/0002_phase48_datenschutz.sql
@supabase/migrations/0003_phase49_einsatzprotokoll.sql
@supabase/migrations/0004_phase410_log_retention.sql
</context>

<wichtige_regeln>
- **Repo ist public:** docs/SELF-HOSTING.md wird committet → KEINE Secrets, keine
  internen Begriffe/Strategie-Details, kein Verweis auf gitignorte interne Dateien
  (PROD-SETUP, LEITUNG-URL, Datenschutz-Dossier-Interna), kein Leitungs-Dateiname —
  nur Muster-Schreibweise `docs/leitung-<zufallssuffix>.html` (D-06).
- **KEINE spekulativen Code-Umbauten** für self-hosted-Kompatibilität: Befunde, die
  sich nicht rein dokumentarisch lösen lassen, als klare Voraussetzungen in
  SELF-HOSTING.md festhalten (D-03). Migrationen/sw.js/index.html werden in diesem
  Plan NICHT verändert.
- **Kein PAT, keine Migration-Ausführung** — Phase ist voll autonom, reine
  Analyse-/Doku-/Statusarbeit.
- Interne Dateien (PROD-SETUP.md, DATENSCHUTZ-SPEC.md, 08-MASSNAHMENPLAN.md) nur
  lokal bearbeiten, per `git check-ignore` absichern, NIE committen.
</wichtige_regeln>

<tasks>

<task type="auto">
  <name>Task 1: Kompatibilitäts-Audit 0000–0004 durchführen und als Abschnitte „Varianten" + „Server-Abhängigkeiten" in docs/SELF-HOSTING.md anlegen</name>
  <files>docs/SELF-HOSTING.md</files>
  <read_first>
    - supabase/migrations/0000_base_schema.sql (Extensions Z. 16–19: uuid-ossp, pgcrypto, pgjwt im Schema extensions; Grants an Rolle anon Z. 99–115; RLS)
    - supabase/migrations/0001_phase4_jwt_rls.sql (vault.decrypted_secrets Z. ~62 — Secret `argus_jwt_secret`; extensions.sign/pgjwt Z. ~70; search_path public/extensions/vault Z. 24)
    - supabase/migrations/0002_phase48_datenschutz.sql Z. 384–404 (pg_cron fehlertolerant gekapselt, Job `argus_purge` 17 * * * *, dokumentierter Fallback: App-Start-Aufruf)
    - supabase/migrations/0003_phase49_einsatzprotokoll.sql Z. 45, 86, 94 (zweiter RPC mit vault + pgjwt)
    - supabase/migrations/0004_phase410_log_retention.sql (nur Verweis auf bestehenden Cron-Job, keine neue Infrastruktur)
    - sw.js Z. 73 (Network-only-Regel matcht NUR Hostnames auf `.supabase.co`)
    - index.html Z. 364–393 (subscribeRealtime: postgres_changes auf public.patients + public.ccps — Supabase-Realtime-Publikation nötig)
  </read_first>
  <action>
    Umsetzung von D-03 (ehrliches Audit) und Beginn von D-02. `docs/SELF-HOSTING.md`
    anlegen (Sprache: Deutsch, Zielgruppe fachkundige Admin-Person ohne
    Projektkenntnis) mit zunächst diesen Abschnitten:

    1. **Zielbild & Geltungsbereich:** Betriebsmodell M1 (Lizenznehmer hostet selbst,
       stellt den Verantwortlichen); Eigentümer-Instanz bleibt Entwicklungs-/
       Schulungsumgebung mit Fiktivdaten; Verweis auf Lizenz-Rahmen OHNE
       Vertragsdetails. Kurz halten.
    2. **Varianten (ehrlich):** (A) Supabase self-hosted via Docker Compose —
       voller Funktionsumfang (Vault, Realtime, pg_cron je nach Image prüfen);
       (B) verwaltetes Postgres + PostgREST-kompatible Schicht — ehrlich
       dokumentieren, was dann fehlt/ersetzt werden muss (Vault → Ersatzweg,
       Realtime → Polling-Einschränkung: Live-Sync zwischen Geräten entfällt,
       Daten kommen erst bei Neuladen; pg_cron → externer Scheduler ODER der in
       Migration 0002 bereits eingebaute Fallback über den App-Start-Aufruf des
       Purge). Empfehlung klar aussprechen (A).
    3. **Server-Abhängigkeiten (Audit-Tabelle)** — pro Zeile: Abhängigkeit, wo sie
       vorkommt (Migration), wofür, Einstufung Pflicht/optional/Ersatzweg, Prüf-SQL:
       - `pgjwt` + Schema `extensions` (0000, 0001, 0003 — JWT-Signierung der
         Code-Exchange-RPCs): **Pflicht**; Prüfung z. B. `select extensions.sign(...)`.
       - `vault.decrypted_secrets` mit Secret **`argus_jwt_secret`** (0001, 0003):
         **Pflicht in Variante A**; Wert MUSS dem JWT-Secret entsprechen, das
         PostgREST/GoTrue der Instanz verifiziert — sonst lehnt die API alle vom
         RPC signierten JWTs ab. Variante B: Ersatzweg dokumentieren (Befund
         festhalten, KEIN Code-Umbau in dieser Phase).
       - `pg_cron` / Job `argus_purge` (0002, referenziert in 0004): **optional
         mit Ersatzweg** — Migration ist fehlertolerant gekapselt (bricht ohne
         pg_cron nicht ab), Fallback App-Start-Purge existiert; trotzdem
         Einrichtung + Prüfung (`select * from cron.job;`) beschreiben.
       - **Realtime-Publikation** für `public.patients` + `public.ccps`: nötig für
         den Mehrgeräte-Live-Sync (postgres_changes); steht NICHT in den
         Migrationen (wurde im Dashboard konfiguriert) → als expliziten
         Einrichtungsschritt dokumentieren (SQL `alter publication supabase_realtime
         add table …` bzw. Dashboard) inkl. Funktionsprüfung.
       - Rolle `anon` + PostgREST-Konventionen (Grants in 0000): bei Supabase
         vorhanden, bei Variante B selbst anzulegen.
    4. **Hinweise/Grenzen (Befunde ohne Code-Änderung):** (a) Der Service Worker
       behandelt nur `*.supabase.co`-Hosts als Network-only; bei eigener Domain
       laufen API-Aufrufe über die Network-first-Regel — funktional unkritisch
       (API-Antworten werden dort nie gecacht), aber: **App-Hosting und API dürfen
       nicht auf demselben Origin liegen**, sonst würde die Shell-Cache-Regel
       greifen. Als Betriebs-Voraussetzung formulieren. (b) App erwartet HTTPS.

    Audit-Belege (welche Migration welche Spezifika enthält, mit Zeilenangaben)
    zusätzlich knapp ins SUMMARY — die öffentliche Datei bleibt anleitungsförmig.
  </action>
  <verify>
    <automated>test -f docs/SELF-HOSTING.md && for t in pgjwt vault pg_cron argus_jwt_secret argus_purge Realtime anon; do grep -qi "$t" docs/SELF-HOSTING.md || exit 1; done && for m in 0000 0001 0002 0003 0004; do grep -q "$m" docs/SELF-HOSTING.md || exit 1; done && grep -qi 'Pflicht' docs/SELF-HOSTING.md && grep -qi 'Ersatz' docs/SELF-HOSTING.md && grep -qi 'Docker' docs/SELF-HOSTING.md && grep -qi 'publication\|Publikation' docs/SELF-HOSTING.md</automated>
  </verify>
  <done>docs/SELF-HOSTING.md existiert mit Zielbild, beiden Varianten (inkl. ehrlicher Einschränkungen) und vollständiger Abhängigkeits-Tabelle (pgjwt/vault/pg_cron/Realtime/anon-Rolle) über alle Migrationen 0000–0004, je mit Pflicht/optional/Ersatzweg und Prüfschritt; SW-Origin-Befund als Voraussetzung dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 2: SELF-HOSTING.md vervollständigen — Apply, frische Codes, config-Tausch, Smoke-Test-Drehbuch, Betriebsvorgaben + Negativ-Gates</name>
  <files>docs/SELF-HOSTING.md</files>
  <read_first>
    - docs/PROD-SETUP.md (intern/gitignored — als inhaltliche Quelle: Vault-Schritt, Codes/MasterToken-Hinweis „keine alten übernehmen", Verifikationsliste; Formulierungen für die öffentliche Datei NEU schreiben, interne Bezüge weglassen)
    - config.js (Stand nach Plan 04.11-01 — Schlüsselnamen url/anonKey für den Tausch-Abschnitt)
    - supabase/migrations/0000_base_schema.sql Z. 99–115 (Tabellen für Code-/Token-Beispiele: praesidien, access_tokens)
  </read_first>
  <action>
    Restliche Abschnitte gem. D-02 ergänzen:

    1. **Migrationen anwenden (Ein-Schritt-Apply):** `supabase/migrations/0000…0004`
       in Reihenfolge, z. B. `for f in supabase/migrations/*.sql; do psql "$DB_URL" -f "$f"; done`
       (alphabetische Reihenfolge = korrekte Reihenfolge); alternativ SQL-Editor.
       Hinweis auf Idempotenz-Grenzen ehrlich halten (frische Instanz empfohlen).
    2. **Frische Zugangsdaten erzeugen (NIEMALS alte übernehmen):** SQL-Beispiele für
       Präsidium anlegen, Einsatz-Codes (dauerhaft/24 h/einmalig) und einen NEUEN
       MasterToken — generisch formuliert (Spalten aus 0000 ableiten), Platzhalter
       statt echter Werte.
    3. **App auf die neue Instanz zeigen (config-Tausch):** einziger Eingriff =
       `config.js` editieren (`url`, `anonKey` der neuen Instanz); statisches
       HTTPS-Hosting der App-Dateien (beliebiger statischer Webserver/CDN); Hinweis:
       Leitungs-Seite liegt unter `docs/leitung-<zufallssuffix>.html` — Betreiber
       soll einen eigenen, zufälligen Dateinamen vergeben (Muster-Schreibweise,
       D-06: konkreten Bestandsnamen NICHT nennen).
    4. **Smoke-Test-Drehbuch** (nummeriert, mit Erwartungsergebnis je Schritt):
       Code einlösen → CCP anlegen → Patient erfassen (Kategorie, Vitalwerte) →
       zweites Gerät: Live-Sync sichtbar (Realtime-Prüfung) → Einsatz-Abschluss
       (72-h-Frist) → Leitungs-Seite: MasterToken-Login + Protokoll → Purge-Lauf
       prüfen (`select public.argus_run_purge();` bzw. cron.job-Eintrag) →
       Negativ-Prüfungen: Anon-Key ohne JWT liefert leere Listen; access_tokens
       ohne JWT nicht lesbar.
    5. **Betriebsvorgaben für Lizenznehmer** (eigener Abschnitt, D-02): nur
       verwaltete Dienstgeräte (MDM, Gerätesperrcode), Kürzel-Liste je Einsatz beim
       Einsatzleiter (Zuordnung Kürzel → Person), Einsatz-Codes nur mündlich/Funk
       weitergeben.
    6. **Negativ-Gates laufen lassen** (siehe verify): keine Projekt-URL, kein
       Anon-Key, kein Leitungs-Dateiname, keine Verweise auf interne gitignorte
       Dateien in der öffentlichen Datei.
  </action>
  <verify>
    <automated>grep -qi 'Smoke-Test' docs/SELF-HOSTING.md && grep -qi 'MDM' docs/SELF-HOSTING.md && grep -qi 'mündlich' docs/SELF-HOSTING.md && grep -qi 'Kürzel' docs/SELF-HOSTING.md && grep -q 'config.js' docs/SELF-HOSTING.md && grep -qi 'MasterToken' docs/SELF-HOSTING.md && grep -qi 'argus_run_purge\|Purge' docs/SELF-HOSTING.md && ! grep -q 'sehuosjyjmrpzcqrelej' docs/SELF-HOSTING.md && ! grep -qE 'leitung-[0-9a-f]{6,}' docs/SELF-HOSTING.md && ! grep -q 'eyJhbGciOiJIUzI1Ni' docs/SELF-HOSTING.md && ! grep -qiE 'PROD-SETUP|LEITUNG-URL|DATENSCHUTZ-SPEC' docs/SELF-HOSTING.md && ! git check-ignore -q docs/SELF-HOSTING.md && python3 -c "assert sum(1 for _ in open('docs/SELF-HOSTING.md')) >= 120"</automated>
  </verify>
  <done>SELF-HOSTING.md ist vollständig (Varianten, Abhängigkeiten, Apply, frische Codes, config-Tausch, vollständiges Smoke-Test-Drehbuch mit Erwartungsergebnissen, Betriebsvorgaben, Zielbild) und besteht alle Negativ-Gates (keine Secrets/Interna/Leitungs-Name); Datei ist committbar (nicht gitignored).</done>
</task>

<task type="auto">
  <name>Task 3: Statusführung — ROADMAP/STATE, SPEC T7 + Maßnahmenplan lokal, PROD-SETUP-Verweis, D-06-Gesamt-Gate</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, docs/PROD-SETUP.md (lokal), docs/datenschutz/DATENSCHUTZ-SPEC.md (lokal), docs/datenschutz/08-MASSNAHMENPLAN.md (lokal)</files>
  <read_first>
    - .planning/ROADMAP.md Abschnitt Phase 4.11 (Z. ~251–270) und Plan-Checkbox-Format von 4.10 (Z. 233–237)
    - .planning/STATE.md (Schluss-Abschnitte: Projektstand, offene Owner-Punkte)
    - docs/datenschutz/DATENSCHUTZ-SPEC.md Z. 216–231 (T7) und Abschnitt „Statusführung" Z. 240–244
  </read_first>
  <action>
    1. **ROADMAP.md:** Phase 4.11 auf ✅ setzen, Plan-Checkboxen `04.11-01` / `04.11-02`
       abhaken (Format wie 4.10, mit Datum); Akzeptanz-Vermerk ergänzen: „läuft gegen
       zweite Instanz" ersatzweise belegt über Config-Isolation (Grep-/JSC-Gates) +
       extern nachvollziehbare Anleitung — echte Zweitinstanz-Erprobung bleibt offen
       (deferred, sobald Zielumgebung existiert).
    2. **STATE.md:** Projektstand auf v0.24.0 / SW v43, Phase 4.11 abgeschlossen,
       nächster Schritt Phase 4.12 (FLZ Stufe a); unter „Offene Owner-Punkte"
       ergänzen: Live-Test der Config-Umstellung auf dem iPhone nach Deploy
       (Update-Banner v0.24.0, Offline-Start) — kein neuer PAT-Punkt (keine
       Migration in dieser Phase).
    3. **DATENSCHUTZ-SPEC.md (NUR lokal, gitignored):** T7-Abschnitt als umgesetzt
       markieren (Vermerk „✅ umgesetzt 2026-06-… (Phase 4.11): config.js +
       docs/SELF-HOSTING.md" unter der T7-Überschrift); Akzeptanz-Einschränkung
       notieren (Zweitinstanz-Test ausstehend). **08-MASSNAHMENPLAN.md** (NUR lokal):
       falls T7 dort geführt wird synchronisieren, sonst kurzen T7-Eintrag ergänzen.
    4. **PROD-SETUP.md (NUR lokal, Diskretions-Entscheid aus CONTEXT):** Kopfvermerk
       ergänzen: für den M1-Weg ist `docs/SELF-HOSTING.md` maßgeblich; PROD-SETUP
       bleibt internes Runbook für Eigentümer-Instanz/Phase-5-Restthemen. Datei NICHT
       committen (gitignored).
    5. **Gates:** D-06-Gesamt-Gate über das Repo; check-ignore-Gates für alle vier
       internen Dateien; sicherstellen, dass `git status` keine der internen Dateien
       als zu committende Änderung zeigt.
  </action>
  <verify>
    <automated>grep -q 'Phase 4.11 — Self-Hosting-Fähigkeit (T7, Übergabefähigkeit) ✅' .planning/ROADMAP.md && grep -q '\[x\] 04.11-01-PLAN.md' .planning/ROADMAP.md && grep -q '\[x\] 04.11-02-PLAN.md' .planning/ROADMAP.md && grep -q '0.24.0' .planning/STATE.md && grep -qi 'SELF-HOSTING' .planning/STATE.md && grep -A3 '### T7' docs/datenschutz/DATENSCHUTZ-SPEC.md | grep -q '✅' && git check-ignore -q docs/PROD-SETUP.md && git check-ignore -q docs/datenschutz/DATENSCHUTZ-SPEC.md && git check-ignore -q docs/datenschutz/08-MASSNAHMENPLAN.md && git check-ignore -q LEITUNG-URL.md && ! git grep -qE 'leitung-[0-9a-f]{6,}' -- ':!docs/'</automated>
  </verify>
  <done>ROADMAP führt 4.11 als ✅ mit abgehakten Plänen und Akzeptanz-Vermerk; STATE auf v0.24.0/Phase-4.11-abgeschlossen mit Owner-Testpunkt; SPEC T7 + Maßnahmenplan lokal nachgeführt; PROD-SETUP verweist intern auf SELF-HOSTING.md; D-06-Gate repo-weit grün; alle internen Dateien weiterhin gitignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Public Repo → Welt | docs/SELF-HOSTING.md wird öffentlich — alles darin gilt als veröffentlicht |
| Eigentümer-Doku → Lizenznehmer | Interne Runbooks (PROD-SETUP, Dossier) dürfen nicht in die öffentliche Anleitung durchsickern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.11-04 | Information Disclosure | docs/SELF-HOSTING.md | mitigate | Negativ-Greps in Task 2 (Projekt-URL, Anon-Key-Präfix, leitung-Pattern, interne Dateinamen) |
| T-4.11-05 | Information Disclosure | Leitungs-Seiten-Name (D-06) | mitigate | Muster-Schreibweise + repo-weites Gate `! git grep -qE 'leitung-[0-9a-f]{6,}' -- ':!docs/'` in Task 3 |
| T-4.11-06 | Elevation of Privilege | Übernahme alter Codes/MasterToken auf neuer Instanz | mitigate | Anleitung schreibt FRISCHE Codes + neuen MasterToken zwingend vor (Task 2, Schritt 2) |
| T-4.11-SC | Tampering | Paket-Installationen | accept | Keine npm/pip/cargo-Installationen in dieser Phase |
</threat_model>

<verification>
- Audit-Abdeckung: pgjwt/vault/pg_cron/Realtime/anon + Migrationen 0000–0004 alle in SELF-HOSTING.md benannt, je mit Pflicht/optional/Ersatzweg.
- Anleitung vollständig: Varianten, Apply, Abhängigkeiten, frische Codes, config-Tausch, Smoke-Test-Drehbuch, Betriebsvorgaben, Zielbild.
- Negativ-Gates: keine Secrets/Interna/Leitungs-Name in der öffentlichen Datei; D-06 repo-weit.
- Statusführung: ROADMAP ✅, STATE v0.24.0, SPEC T7 lokal abgehakt, interne Dateien gitignored.
</verification>

<success_criteria>
- Eine fachkundige Person ohne Projektkenntnis kann mit docs/SELF-HOSTING.md eine eigene Instanz aufbauen und per Smoke-Test-Drehbuch abnehmen (T7-Akzeptanz, ersatzweise belegt).
- Alle Supabase-Spezifika ehrlich eingeordnet; nicht-dokumentarisch lösbare Punkte als Voraussetzungen festgehalten — keine Code-Änderungen in diesem Plan.
- Phase 4.11 in ROADMAP/STATE/SPEC konsistent abgeschlossen; D-06 gewahrt.
</success_criteria>

<output>
Create `.planning/phases/04.11-self-hosting/04.11-02-SUMMARY.md` when done
(Audit-Belege mit Zeilenangaben, Diskretions-Entscheide: SELF-HOSTING-Struktur, PROD-SETUP-Verweis statt Ablösung).
</output>
