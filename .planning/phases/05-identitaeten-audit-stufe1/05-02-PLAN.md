---
phase: 05-identitaeten-audit-stufe1
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - supabase/migrations/0016_phase5_t4_audit.sql
  - docs/SELF-HOSTING.md
autonomous: false   # PAT-Checkpoint (Management-API-Apply) + Retention-Funktionstest (blocking-human)
requirements: []    # Phase 5 hat keine REQ-IDs — Akzeptanz aus CONTEXT D-07 + 05-VALIDATION.md

must_haves:
  truths:
    - "argus_run_purge() löscht audit_log-Einträge älter als 12 Monate (365 Tage in ms) — über einen neuen Schritt (e) in der bestehenden Funktion, ohne zweiten Cron-Job (D-07)."
    - "Der Purge-Lauf gibt die Anzahl gelöschter audit_log-Rows additiv im Return-JSON zurück; Schritt (e) schreibt selbst KEINEN audit_log-/purge_log-Eintrag (keine Rekursion)."
    - "Synthetische audit_log-Rows > 365 Tage werden gelöscht, Rows innerhalb der Frist bleiben erhalten."
  artifacts:
    - path: "supabase/migrations/0016_phase5_t4_audit.sql"
      provides: "argus_run_purge() um audit_log-Retention (Schritt e, 365 Tage) erweitert; idempotent (create or replace)"
      contains: "audit_log"
      min_lines: 40
    - path: "docs/SELF-HOSTING.md"
      provides: "Migration 0016 in Dateiliste/Reihenfolge nachgeführt"
      contains: "0016_phase5_t4_audit.sql"
  key_links:
    - from: "argus_run_purge"
      to: "public.audit_log"
      via: "delete > 365 Tage (ms-Cutoff) in Schritt (e)"
      pattern: "delete from public.audit_log"
    - from: "argus_run_purge"
      to: "Return-JSON"
      via: "additiver Schlüssel audit_log_retention"
      pattern: "audit_log_retention"
---

<objective>
Die 12-Monats-Retention des T4-Audit-Protokolls (D-07, § 73 PolG BW) als neue
Migration `0016_phase5_t4_audit.sql` — ein neuer Schritt (e) DIREKT in der
bestehenden `argus_run_purge()`-Funktion (create or replace), exakt nach dem
Muster der Log-Retention aus 0004 (Schritt d). Keine zweite Cron-Infrastruktur:
der bestehende stündliche Job `argus_purge` (`17 * * * *`) bleibt wörtlich
unverändert und ruft die erweiterte Funktion auf.

Purpose: audit_log wuchs sonst unbegrenzt; die Frist ist gesetzlich (Modell
§ 73 Abs. 5 PolG BW). Append-only bleibt gewahrt — das Löschen läuft
ausschließlich über den kontrollierten security-definer-Purge-Job, nie über anon.

Output: Migration 0016 (idempotent, live via Management API angewendet),
SELF-HOSTING.md nachgeführt, Retention-Funktionstest mit synthetischen Alt-Rows
belegt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-identitaeten-audit-stufe1/05-CONTEXT.md
@.planning/phases/05-identitaeten-audit-stufe1/05-RESEARCH.md
@.planning/phases/05-identitaeten-audit-stufe1/05-VALIDATION.md
@supabase/migrations/0004_phase410_log_retention.sql
@docs/SELF-HOSTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0016 — audit_log-Retention als Schritt (e) in argus_run_purge</name>
  <files>supabase/migrations/0016_phase5_t4_audit.sql</files>
  <read_first>
- supabase/migrations/0004_phase410_log_retention.sql (KOMPLETT — die aktuelle argus_run_purge-Live-Fassung mit Schritten a/b/c/d ist hier byte-genau; Schritt (e) wird analog Schritt (d) ergänzt; Return-JSON-Muster, Funktions-Kommentar)
- 05-RESEARCH.md „Code Examples" → Log-Retention-Erweiterung (delete from audit_log where at <= v_now - 365*86400000; get diagnostics; additiver Return-Schlüssel)
  </read_first>
  <action>
Lege `supabase/migrations/0016_phase5_t4_audit.sql` an, im Stil der 0004-Header
(© -Zeile zuerst, ASCII-Box-Kommentar mit Zweck, Reihenfolge-Hinweis
„0000 → … → 0015 → dieses Skript", Anwendungs-Hinweis Management API,
Idempotenz-Hinweis). Die audit_log-TABELLE selbst wurde bereits in 0015 (Plan 01)
angelegt — 0016 ergänzt NUR die Retention.

1. argus_run_purge() per create or replace neu fassen, BASIS = die komplette
   0004-Live-Fassung (Schritte a Foto, b Frist, c Inaktiv, d Log-Retention).
   Alle bestehenden Schritte a–d BYTE-GLEICH übernehmen (KEINE inhaltliche Änderung).
   Neu ist ausschließlich Schritt (e) sowie ein zusätzlicher additiver Schlüssel
   im Return-JSON:
   - Deklaration: `n_alog int := 0;` zu den bestehenden Zählern ergänzen.
   - Schritt (e) nach Schritt (d): `delete from public.audit_log where at is not null
     and at <= v_now - 365::bigint * 86400000; get diagnostics n_alog = row_count;`
     (audit_log.at ist bigint-JS-Millisekunden — derselbe Vergleich wie governance_log
     in Schritt d; 12 Monate als 365 Tage in ms). KEIN purge_log-/audit_log-Eintrag über
     diesen Schritt (keine Rekursion, kein Log-Spam bei leerem Ergebnis — wie Schritt d).
   - Return-JSON: bestehende Schlüssel unverändert lassen, additiv ergänzen:
     `'audit_log_retention', jsonb_build_object('audit_log', n_alog)` (oder den n_alog
     in den bestehenden log_retention-Block integrieren — additiv, ohne bestehende
     Schlüssel zu entfernen).
   - `set search_path to 'public'` beibehalten; `grant execute on function
     public.argus_run_purge() to anon;` erneut setzen (Idempotenz-Netz wie 0004).

2. Funktions-Kommentar (comment on function public.argus_run_purge()) nachziehen:
   die bestehende 0004-Beschreibung um Schritt (e) erweitern — „(e) audit_log-Retention:
   T4-Audit-Einträge älter als 12 Monate werden gelöscht (D-07, Modell § 73 PolG BW);
   Schritt (e) erzeugt selbst keinen Log-Eintrag".

Hinweis: KEIN zweiter pg_cron-Job. Der Job `argus_purge` aus 0002 (`17 * * * *`)
ruft argus_run_purge() auf und greift dadurch automatisch auch auf Schritt (e).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0016_phase5_t4_audit.sql | grep -c "delete from public.audit_log" | grep -qvE "^0$" && echo "RETENTION-DELETE vorhanden"</automated>
    <automated>grep -v '^--' supabase/migrations/0016_phase5_t4_audit.sql | grep -cE "365::bigint \* 86400000|365::bigint\*86400000" | grep -qvE "^0$" && echo "365-Tage-CUTOFF vorhanden"</automated>
    <automated>grep -v '^--' supabase/migrations/0016_phase5_t4_audit.sql | grep -cE "Foto|frist|inaktiv|log_retention" | grep -qE "^[3-9]|^[1-9][0-9]" && echo "Schritte a-d byte-gleich uebernommen (nicht abgespeckt)"</automated>
  </verify>
  <done>
0016 existiert; argus_run_purge neu gefasst mit Schritten a–d byte-gleich + neuem
Schritt (e) audit_log-Retention (365 Tage, ms-Cutoff, kein Eigen-Log); Return-JSON
additiv um audit_log-Zähler erweitert; Funktions-Kommentar nachgezogen; grant erneut gesetzt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
Migration 0016 ist geschrieben (Task 1) und bereit zur Anwendung über die Supabase
Management API (ephemerer PAT, kein CLI). Danach wird die audit_log-Retention mit
synthetischen Alt-Rows funktional getestet (Muster 04.10-01 für governance_log).
Projekt-Ref: `sehuosjyjmrpzcqrelej`.
  </what-built>
  <how-to-verify>
1. PAT-FREIGABE: Bitte bestätige, dass ein ephemerer Supabase-PAT für Projekt
   `sehuosjyjmrpzcqrelej` gilt/erstellt ist. Nach dem Apply den PAT wieder widerrufen.
2. APPLY (Idempotenz-Beleg): 0016 ZWEIMAL über `POST /v1/projects/{ref}/database/query`
   (Bearer <PAT>) anwenden — beide Läufe fehlerfrei (HTTP 201, reine create or replace).
3. RETENTION-FUNKTIONSTEST (synthetische Rows via Management-API-SQL):
   a. Zwei audit_log-Testzeilen einfügen — eine ALT (`at` = now-ms minus 366*86400000,
      action='person_login', usbnk='RETENTION-ALT') und eine FRISCH (`at` = now-ms,
      action='person_login', usbnk='RETENTION-NEU').
   b. `select public.argus_run_purge();` ausführen → Return-JSON enthält einen
      audit_log-Zähler ≥ 1.
   c. `select usbnk from audit_log where usbnk in ('RETENTION-ALT','RETENTION-NEU')`
      → nur 'RETENTION-NEU' bleibt; 'RETENTION-ALT' ist gelöscht.
   d. Die bestehenden Purge-Schritte (a–d) müssen weiter funktionieren: der Aufruf darf
      keinen Fehler werfen und das Return-JSON behält die bisherigen Schlüssel (foto/frist/
      inaktiv/log_retention).
4. CLEANUP: die FRISCH-Testzeile ('RETENTION-NEU') löschen.
  </how-to-verify>
  <resume-signal>Tippe „freigegeben", wenn PAT galt, beide Apply-Läufe sauber waren und der Retention-Test (Alt gelöscht, Frisch bleibt, bestehende Schritte intakt) bestanden ist — oder beschreibe die Abweichung.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: SELF-HOSTING.md nachführen (Migration 0016)</name>
  <files>docs/SELF-HOSTING.md</files>
  <read_first>
- docs/SELF-HOSTING.md (Abschnitt 3 Dateiliste + Abschnitt 5 Reihenfolge; Plan 01 hat 0015 bereits ergänzt — diese Änderungen sind beim Lesen sichtbar)
  </read_first>
  <action>
Führe `docs/SELF-HOSTING.md` weiter nach (Plan 01 hat 0015 bereits eingetragen):
1. Abschnitt 3 (Dateiliste-Codeblock): Zeile ergänzen
   `supabase/migrations/0016_phase5_t4_audit.sql   T4-Audit-Retention (argus_run_purge Schritt (e): audit_log > 12 Monate löschen)`.
2. Abschnitt 5: Reihenfolge-Hinweis auf „… → 0015 → 0016" erweitern.
3. Audit-Prosa: einen Satz ergänzen — 0016 fasst nur argus_run_purge neu (additiver
   Retention-Schritt), nutzt KEINE neuen Server-Abhängigkeiten und KEINEN zweiten Cron-Job
   (der bestehende Job argus_purge greift automatisch).
KEINE Dateinamen/URLs der Leitungs-Seite in committete Texte (D-06).
  </action>
  <verify>
    <automated>grep -c "0016_phase5_t4_audit.sql" docs/SELF-HOSTING.md | grep -qE "^[1-9]" && echo "0016 in SELF-HOSTING"</automated>
    <automated>git grep -nE 'leitung-[0-9a-f]{4,}' -- docs/SELF-HOSTING.md supabase/migrations/0016_phase5_t4_audit.sql; test $? -ne 0 && echo "D-06 OK"</automated>
  </verify>
  <done>
SELF-HOSTING.md listet 0016 in Abschnitt 3 + Reihenfolge (Abschnitt 5); Audit-Prosa
nennt 0016 als abhängigkeitsfrei (kein zweiter Cron-Job); D-06 gewahrt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron / security-definer-Purge → audit_log | Das Löschen aus dem Audit-Protokoll ist eine privilegierte Operation; sie darf NUR über den kontrollierten Purge-Job laufen, nie über anon. |
| anon/JWT → audit_log | Direktes Löschen/Ändern = Tampering; durch das fehlende anon-write-Grant (Plan 01) ausgeschlossen. |
| Self-Hoster → Migration/Doku | Falsche Reihenfolge/abgespeckte argus_run_purge würde bestehende Purge-Schritte (Foto/Frist/Inaktiv) brechen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0502-01 | Tampering | Manuelles/anon-Löschen aus audit_log statt kontrolliertem Purge | mitigate | Löschen nur in argus_run_purge (security definer); kein anon-write-Grant (Plan 01); append-only bleibt gewahrt. |
| T-0502-02 | DoS/Repudiation | Schritt (e) erzeugt rekursive Log-Einträge / Log-Spam | mitigate | Schritt (e) schreibt KEINEN audit_log-/purge_log-Eintrag (analog Schritt d, 0004). |
| T-0502-03 | Tampering | Regression: argus_run_purge byte-ungleich → Foto-/Frist-Purge bricht | mitigate | Schritte a–d byte-gleich aus 0004 übernehmen; Checkpoint-Test 3d prüft bestehende Schritte. |
| T-0502-04 | Compliance/Repudiation | Audit-Daten überschreiten 12-Monats-Frist (§ 73 PolG BW) | mitigate | Schritt (e) löscht > 365 Tage; stündlicher Job argus_purge greift automatisch. |
| T-0502-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installs (reines SQL + Doku). Kein Package-Legitimacy-Gate nötig. |
</threat_model>

<verification>
- Beide Apply-Läufe der Management API fehlerfrei (Idempotenz, HTTP 201).
- Retention-Test: Alt-Row (>365 Tage) gelöscht, Frisch-Row bleibt; bestehende Purge-Schritte intakt.
- Return-JSON enthält den audit_log-Retention-Zähler additiv.
- SELF-HOSTING.md enthält 0016; D-06 gewahrt.
</verification>

<success_criteria>
- Migration 0016 live angewendet und idempotent.
- audit_log-12-Monats-Retention wirkt über den bestehenden stündlichen Purge.
- Append-only gewahrt (Löschen nur via security-definer-Purge).
- SELF-HOSTING.md aktuell; D-06 gewahrt.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

**Geänderte Funktion:** argus_run_purge() — neuer Schritt (e) audit_log-Retention (365 Tage); additiver Return-Schlüssel audit_log-Zähler
**Migrationsdatei:** supabase/migrations/0016_phase5_t4_audit.sql
**Doku:** docs/SELF-HOSTING.md (0016 in Dateiliste/Reihenfolge)
**Kein neuer Cron-Job** (bestehender argus_purge greift automatisch)
</artifacts_produced>

<output>
Create `.planning/phases/05-identitaeten-audit-stufe1/05-02-SUMMARY.md` when done
</output>
