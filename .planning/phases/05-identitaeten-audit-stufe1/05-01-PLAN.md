---
phase: 05-identitaeten-audit-stufe1
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0015_phase5_identitaeten.sql
  - docs/SELF-HOSTING.md
autonomous: false   # PAT-Checkpoint (Management-API-Apply) + Privilege-Escalation-Negativtest-Verify (blocking-human)
requirements: []    # Phase 5 hat keine REQ-IDs — Akzeptanz aus CONTEXT D-01…D-13 + 05-VALIDATION.md
user_setup: []      # Supabase-PAT ist ephemerer Vorgang im Checkpoint, kein Dauer-Setup

must_haves:
  truths:
    - "Ein Pro-Person-Code (is_person) lässt sich über argus_exchange_person_code gegen ein JWT mit usbnk + rollen-abgeleiteten Claims (is_master/is_flz/is_admin) + jti tauschen — kein Klarname im JWT (D-02)."
    - "argus_exchange_code (Feld-Exchange) weist is_person-Codes ab; ein role='master'-Person-Code dort eingelöst → Fehler statt is_master-JWT (D-05/CR-02)."
    - "argus_exchange_person_code weist Nicht-Person-Codes (gast/observer/admin/master ohne is_person) ab (D-05/CR-02)."
    - "Ein Master-JWT kann Pro-Person-Tokens (usbnk+role) ausgeben und über short_code widerrufen (revoked=true → jti-Sofortsperre); jede Ausgabe/Widerruf schreibt einen audit_log-Eintrag mit actor-USBNK (D-13)."
    - "argus_master_search_usbnk gibt usbnk/role/short_code/revoked/praesidium — NIEMALS die token-Secret-Spalte zurück."
    - "audit_log ist append-only: anon hat nur SELECT (RLS argus_is_master); kein direkter INSERT/UPDATE/DELETE möglich (D-07)."
    - "Pro-Person-Tokens haben kein expires_at; das JWT trägt ein technisches exp (30 Tage) als Bearer-Hygiene (D-11)."
    - "FLZ-Token ist präsidienübergreifend (praesidium_id-Claim null); argus_lage liefert FLZ alle Präsidien (D-12)."
    - "Ein unprivilegiertes JWT (Gast/Geräte) auf argus_master_issue_person/revoke/search → Exception (Privilege-Escalation verweigert)."
  artifacts:
    - path: "supabase/migrations/0015_phase5_identitaeten.sql"
      provides: "Identitäts-Spalten, Claim-Helfer, CR-02-Härtung, audit_log-Tabelle, Person-Exchange-RPC, Master-Issue/Revoke/Search-RPCs, argus_lage-FLZ-Zweig"
      contains: "argus_exchange_person_code"
      min_lines: 250
    - path: "docs/SELF-HOSTING.md"
      provides: "Migration 0015 in Dateiliste/Reihenfolge nachgeführt + Master-Bootstrap-Erweiterung (Abschnitt 8: erster Stufe-1-Master-Token)"
      contains: "0015_phase5_identitaeten.sql"
  key_links:
    - from: "argus_exchange_code"
      to: "is_person-Abweisung"
      via: "CR-02-Guard vor JWT-Ausgabe"
      pattern: "is_person"
    - from: "argus_master_issue_person"
      to: "argus_is_master()"
      via: "Guard: nur Master darf ausgeben"
      pattern: "argus_is_master\\(\\)"
    - from: "argus_exchange_person_code"
      to: "audit_log"
      via: "Zwangs-Log person_login mit usbnk"
      pattern: "insert into public.audit_log"
    - from: "argus_is_flz"
      to: "argus_token_active()"
      via: "jti-Gate (Sofortsperre)"
      pattern: "argus_token_active\\(\\)"
---

<objective>
Serverseitiger Kern der Identitäts-Schicht (Stufe 1) als neue Migration
`0015_phase5_identitaeten.sql` — exakt nach dem Admin-Rolle-Muster (0013/0014),
gegated durch die jti-Sofortsperre (0007). Erweitert `access_tokens` um die drei
Identitäts-Spalten (`is_person`, `usbnk`, `role`), legt die append-only
`audit_log`-Tabelle an, härtet `argus_exchange_code` gegen Person-Codes (CR-02),
baut den eigenen `argus_exchange_person_code`-RPC (USBNK + rollen-abgeleitete
Claims, **kein** Klarname, **kein** `expires_at`, technisches 30-Tage-`exp`), die
Master-RPCs zum Ausgeben/Widerrufen/Suchen von Pro-Person-Tokens und einen
FLZ-Zweig in `argus_lage`.

Purpose: Dies ist die ECHTE Sicherheitsgrenze (RLS + RPC-Guards + CR-02-Abweisung).
Die Audit-Retention (Plan 02) und die Leitungs-Seiten-UI (Plan 03) setzen auf
diesen RPCs/dieser Tabelle auf. Ohne diese Schicht kann niemand einen
Pro-Person-Token einlösen, ausgeben oder suchen.

Output: Migration 0015 (idempotent, live via Management API angewendet),
SELF-HOSTING.md inkl. Master-Bootstrap nachgeführt, vollständige REST-Positiv- und
Privilege-Escalation-Negativtest-Batterie belegt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-identitaeten-audit-stufe1/05-CONTEXT.md
@.planning/phases/05-identitaeten-audit-stufe1/05-RESEARCH.md
@.planning/phases/05-identitaeten-audit-stufe1/05-VALIDATION.md
@.planning/PHASE7-ROLLENMODELL-DRAFT.md
@supabase/migrations/0013_phase414_admin_rolle.sql
@supabase/migrations/0014_phase414_admin_exchange_hardening.sql
@supabase/migrations/0007_jti_sofortsperre.sql
@supabase/migrations/0005_phase412_lageansicht.sql
@docs/SELF-HOSTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0015 — Identitäts-Spalten, Claim-Helfer, CR-02-Härtung, audit_log-Tabelle</name>
  <files>supabase/migrations/0015_phase5_identitaeten.sql</files>
  <read_first>
- supabase/migrations/0013_phase414_admin_rolle.sql (Header-Stil, Claim-Helfer-Muster argus_is_admin/argus_admin_praesidium_id, Spalten-ALTER)
- supabase/migrations/0014_phase414_admin_exchange_hardening.sql (CR-02-Abweisungsmuster: is_admin-Guard in argus_exchange_code — analog für is_person)
- supabase/migrations/0007_jti_sofortsperre.sql (argus_token_active — alle neuen Claim-Helfer MÜSSEN darauf gaten)
- 05-RESEARCH.md Pattern 2 (Datenmodell-Spalten, role als text nicht enum) + Pattern 3 (audit_log append-only) + „Code Examples" (CR-02-Abweisung)
  </read_first>
  <action>
Lege `supabase/migrations/0015_phase5_identitaeten.sql` an, durchgängig im Stil der
0013/0014-Header (© -Zeile zuerst, ASCII-Box-Kommentar mit Zweck, Reihenfolge-Hinweis
„0000 → … → 0014 → dieses Skript", Anwendungs-Hinweis Management API, Idempotenz-Hinweis).
Alle neuen Funktionen sind `security definer` MIT `set search_path` (RPCs ohne Vault:
`'public'` bzw. `'public','extensions'`; Exchange-RPC: `'public','extensions','vault'`) —
fehlendes search_path bei security definer ist eine kritische Lücke (05-RESEARCH Pitfall 2).

1. Identitäts-Spalten auf access_tokens (idempotent, je `add column if not exists`),
   ausschließlich USBNK — KEINE name/vorname/nachname-Spalten (D-02):
   - `is_person boolean not null default false`
   - `usbnk text`  (null für Stufe-0-Tokens)
   - `role text`   ('master'|'flz'|'admin'|'normal'|null)

2. Schema-Link Präsidium ↔ Schulungs-Zwilling (nur Datenmodell, Nutzung erst 5.1):
   `alter table public.praesidien add column if not exists schulungs_zwilling_id uuid references public.praesidien(id);`
   (D-04/Specifics-Punkt 4 — normaler Token soll später beide Scopes tragen.)

3. Claim-Helfer (Muster argus_is_admin/argus_admin_praesidium_id aus 0013, MIT jti-Gate
   wie 0007), je `language sql stable security definer`, grant execute to anon:
   - `argus_is_flz()`: `argus_token_active() and coalesce((claims->>'is_flz')::boolean,false)`
   - `argus_usbnk()`: `nullif(claims->>'usbnk','')` (gibt null für Stufe-0/Alt-JWT — das ist korrekt, 05-RESEARCH Pitfall 4; KEIN eigenes Gate).
   - `argus_argus_role()`: `nullif(claims->>'argus_role','')`.

4. CR-02-Härtung von argus_exchange_code (BEVOR irgendein is_person-Token existiert):
   argus_exchange_code BYTE-NAH zur 0014-Live-Fassung neu fassen (create or replace) und
   NUR einen Guard ergänzen — `is_person` zusätzlich in die select-Liste aufnehmen und nach
   dem bestehenden is_admin-Guard abweisen: `if coalesce(v_token.is_person,false) then return
   jsonb_build_object('error','Pro-Person-Code — nur für die Leitungs-Seite gültig'); end if;`.
   Sonst ändert sich am Verhalten von argus_exchange_code NICHTS (kein anderes Verhalten,
   keine neuen Claims). Dies ist die zentrale CR-02-Mitigation gegen Privilege-Escalation
   (05-RESEARCH Pitfall 1): ein role='master'-Person-Code dort eingelöst dürfte NIE ein
   is_master-/praesidium_id-JWT erzeugen.

5. audit_log-Tabelle (append-only, eigene Tabelle — NICHT governance_log erweitern,
   05-RESEARCH Pattern 3). Spalten: `id uuid primary key default gen_random_uuid()`,
   `at bigint not null` (JS-Millisekunden-Konvention), `usbnk text` (null bei Stufe-0),
   `action text not null`, `ccp_id uuid`, `patient_id text`, `praesidium_id uuid`,
   `detail text`. Append-only-Durchsetzung: `grant select on public.audit_log to anon;`
   — KEIN insert/update/delete-Grant an anon (deliberate omission, D-07/Pitfall 3).
   `alter table … enable row level security;` + Policy `argus_audit_log_select`
   `for select to anon using (public.argus_is_master())`. INSERT erfolgt ausschließlich
   über die security-definer-RPCs aus Task 2. Tabelle anlegen via `create table if not exists`.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0015_phase5_identitaeten.sql | grep -cE "is_person boolean not null default false|usbnk[[:space:]]+text|role[[:space:]]+text|create table if not exists public.audit_log|argus_is_flz|argus_usbnk" | grep -qE "^[5-9]|^[1-9][0-9]" && echo OK-STRUKTUR</automated>
    <automated>grep -v '^--' supabase/migrations/0015_phase5_identitaeten.sql | grep -cE "grant (insert|update|delete) on public.audit_log to anon" | grep -qE "^0$" && echo "APPEND-ONLY OK (kein anon write-grant)"</automated>
    <automated>grep -ciE "name[[:space:]]+text|vorname|nachname|klarname" supabase/migrations/0015_phase5_identitaeten.sql | grep -qE "^0$" && echo "D-02 OK (keine Klarnamen-Spalten)"</automated>
  </verify>
  <done>
0015 existiert mit is_person/usbnk/role auf access_tokens (keine Klarnamen-Spalten),
schulungs_zwilling_id auf praesidien, argus_is_flz/argus_usbnk/argus_argus_role
(gated auf argus_token_active wo nötig), CR-02-is_person-Guard in argus_exchange_code,
audit_log-Tabelle append-only (anon nur SELECT via argus_is_master).
  </done>
</task>

<task type="auto">
  <name>Task 2: Person-Exchange-RPC + Master-RPCs (Issue/Revoke/Search) + argus_lage-FLZ-Zweig</name>
  <files>supabase/migrations/0015_phase5_identitaeten.sql</files>
  <read_first>
- supabase/migrations/0013_phase414_admin_rolle.sql (argus_exchange_admin_code Aufbau; argus_admin_issue_gast Short-Code-Kollisionsschleife + Alphabet; argus_admin_revoke_gast Guard-Kaskade; argus_lage Observer/Admin-Zweige)
- 05-RESEARCH.md Pattern 1 (argus_exchange_person_code Kernlogik) + Pattern 4 (issue/revoke/search) + Open Questions 1/3/4 (FLZ-Scope=alle, role=master erlaubt+geloggt, TTL 30 Tage)
  </read_first>
  <action>
Ergänze in 0015 die RPCs, die das Person-Token-Lebenszyklus-Modell umsetzen
(Einzel-Ausgabe, kein expires_at, Widerruf-only via jti). Alle `security definer`
mit `set search_path`, grant execute to anon.

1. argus_exchange_person_code(code text) — eigener Endpunkt analog argus_exchange_admin_code,
   Normalisierung identisch (upper + regexp_replace, short_code-Split, length<8-Guard).
   Liest `praesidium_id, is_person, usbnk, role, revoked, expires_at`. Abweisungen in
   Reihenfolge: nicht gefunden → „Ungültiger oder verbrauchter Code"; revoked → „Code wurde
   gesperrt"; NICHT is_person → „Kein Pro-Person-Code" (CR-02-Spiegel: weist alle anderen
   Token-Arten ab); expires_at gesetzt und abgelaufen → „Code abgelaufen" (defensiv; Person-
   Tokens setzen normalerweise kein expires_at, D-11). JWT-Payload (extensions.sign, Secret
   aus vault.decrypted_secrets): iss('supabase'), sub('argus-person'), role('anon'),
   `usbnk`=v_token.usbnk, `argus_role`=v_token.role, `is_master`=(role='master'),
   `is_flz`=(role='flz'), `is_admin`=(role='admin'),
   `praesidium_id`= CASE WHEN role in ('master','flz') THEN null ELSE v_token.praesidium_id END
   (FLZ/Master präsidienübergreifend, D-12),
   `admin_praesidium_id`= CASE WHEN role='admin' THEN v_token.praesidium_id ELSE null END,
   `jti`=short_code, iat, exp = now + 30*24*3600 (technisches Bearer-exp, D-11 — KEIN
   expires_at-getriebenes Ablaufen). Zwangs-Log VOR Rückgabe: `insert into public.audit_log
   (at, usbnk, action, praesidium_id) values (ms-now, v_token.usbnk, 'person_login',
   v_token.praesidium_id)`. Rückgabe-JSON: jwt, exp, usbnk, argus_role, praesidium_id,
   praesidium_name (falls praesidium_id gesetzt), ttl_seconds. KEINE Änderung an den
   anderen Exchanges.

2. argus_master_issue_person(p_usbnk text, p_role text, p_praesidium_id uuid, p_label text)
   returns jsonb, security definer. Guards: `if not argus_is_master() then raise exception
   'Nur mit MasterUser-Token'`; p_usbnk leer → raise 'USBNK erforderlich'; p_role not in
   ('master','flz','admin','normal') → raise 'Ungültige Rolle'; p_role='admin' and
   p_praesidium_id is null → raise 'Admin-Token erfordert Präsidium'. role='master' ist
   ERLAUBT (D-13: Master darf weitere Master ausgeben) — wird über das audit_log-detail
   explizit protokolliert. v_actor_usbnk := argus_usbnk(). Short-Code via Kollisions-Schleife
   und Alphabet 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789' (byte-gleich zu argus_admin_issue_gast).
   token := encode(extensions.gen_random_bytes(16),'hex'). INSERT in access_tokens mit ALLEN
   Boolean-Flags explizit gesetzt (is_person=true, usbnk=btrim(p_usbnk), role=p_role,
   is_master=false, is_admin=false, observer=false, gast=false, single_use=false,
   temporary=false, ttl_hours=null, praesidium_id=p_praesidium_id, label=btrim(coalesce(p_label,'')))
   — KEIN expires_at (D-11). (Achtung 05-RESEARCH Anti-Pattern: alle Flags explizit false,
   um versehentliche Privilegien zu verhindern.) Zwangs-Log: audit_log action 'person_issue',
   usbnk=v_actor_usbnk, detail='role='||p_role||' target='||btrim(p_usbnk), praesidium_id=p_praesidium_id.
   Rückgabe {short_code}.

3. argus_master_revoke_person(p_short_code text) returns jsonb, security definer.
   Guard: argus_is_master()-Pflicht. Zielcode laden (`select is_person, usbnk, praesidium_id
   into … where short_code=p_short_code`); nicht gefunden → raise 'Code nicht gefunden';
   NICHT is_person → raise 'Nur Pro-Person-Tokens widerrufbar'. Dann `update access_tokens
   set revoked=true where short_code=p_short_code` (→ jti-Sofortsperre wirkt sofort über
   argus_token_active). Zwangs-Log: audit_log action 'person_revoke', usbnk=argus_usbnk()
   (actor), detail='target='||coalesce(v_target.usbnk,''). Rückgabe {revoked:true}.

4. argus_master_search_usbnk(p_usbnk text) returns jsonb, security definer. Guard:
   argus_is_master()-Pflicht. `select short_code, usbnk, role, revoked, praesidium_id,
   created_at, label from access_tokens where is_person=true and usbnk ILIKE '%'||p_usbnk||'%'`
   — parametrisiertes ILIKE (||-Konkatenation ist injection-sicher, Don't-Hand-Roll). SELECT-
   Liste enthält NIEMALS die token-Secret-Spalte (Pitfall 6/06-VALIDATION). Rückgabe als
   jsonb_agg (leeres Array statt null). Optional praesidium_name per Join/Subselect ergänzen.

5. argus_lage() neu fassen (create or replace, Basis = aktuelle 0013-Fassung mit Observer-
   und Admin-Zweig — Feldsatz BYTE-GLEICH, NICHT abspecken): einen FLZ-Zweig ergänzen.
   Logik: Eingangs-Guard um `argus_is_flz()` erweitern. Wenn argus_is_flz() (und nicht
   Observer/Admin): FLZ ist präsidienübergreifend (D-12) → KEIN Präsidiums-Filter, sondern
   alle offenen CCPs aller Präsidien aggregieren (analog dem bestehenden Zähler-Rumpf, aber
   ohne `where praesidium_id = v_pid`). Observer-/Admin-Zweige bleiben unverändert
   (eigenes Präsidium). grant execute to anon. (05-RESEARCH Pitfall 7: FLZ trägt
   praesidium_id-Claim null → bestehende RLS verweigert Roh-Patientenzugriff; FLZ nutzt nur
   argus_lage.)

6. comment on column public.audit_log.action mit dem Aktions-Katalog (D-06):
   'person_login | person_issue | person_revoke | person_role_change | governance_abruf |
   patient_create | patient_cat_change | patient_ready | patient_checkout | patient_photo |
   ccp_open | ccp_close | ccp_merge'. (Feld-Aktionen patient_*/ccp_* kommen erst mit
   Phase 5.1 zum Schreiben; der Katalog wird hier vollständig dokumentiert.)
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0015_phase5_identitaeten.sql | grep -cE "argus_exchange_person_code|argus_master_issue_person|argus_master_revoke_person|argus_master_search_usbnk" | grep -qE "^[4-9]|^[1-9][0-9]" && echo "ALLE 4 RPCs vorhanden"</automated>
    <automated>grep -v '^--' supabase/migrations/0015_phase5_identitaeten.sql | grep -c "argus_is_master()" | grep -qvE "^0$" && echo "MASTER-GUARDS vorhanden"</automated>
    <automated>grep -v '^--' supabase/migrations/0015_phase5_identitaeten.sql | grep -ciE "set search_path" | grep -qvE "^0$" && echo "search_path gesetzt"</automated>
    <automated>grep -v '^--' supabase/migrations/0015_phase5_identitaeten.sql | grep -A12 "function public.argus_master_search_usbnk" | grep -ciE "[^_a-z]token[^_a-z]" | grep -qE "^0$" && echo "SEARCH gibt KEIN token-Secret zurück"</automated>
  </verify>
  <done>
argus_exchange_person_code (USBNK+role-Claims, kein Klarname, 30-Tage-exp, person_login-Log,
weist Nicht-Person-Codes ab); argus_master_issue_person (Master-Guard, role-Whitelist inkl.
master, alle Flags explizit, kein expires_at, person_issue-Log mit actor-USBNK);
argus_master_revoke_person (revoked=true, person_revoke-Log); argus_master_search_usbnk
(ohne token-Spalte); argus_lage mit FLZ-Zweig (präsidienübergreifend); action-Katalog-Kommentar.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
Migration 0015 ist geschrieben (Tasks 1+2) und bereit zur Anwendung. Die Anwendung
erfolgt — wie 0001–0014 — über die Supabase Management API mit einem EPHEMEREN PAT
(kein CLI). Danach werden REST-Positiv- und die pflichtigen Privilege-Escalation-/
CR-02-/jti-Negativtests gegen die Live-/Schulungs-Instanz gefahren (Muster
04.14-01-SUMMARY, Test-Map aus 05-VALIDATION.md). Projekt-Ref: `sehuosjyjmrpzcqrelej`.
  </what-built>
  <how-to-verify>
1. PAT-FREIGABE: Bitte bestätige, dass ein ephemerer Supabase-PAT (Account → Access
   Tokens) für Projekt `sehuosjyjmrpzcqrelej` gilt/erstellt ist. Erst dann anwenden.
   Nach dem Apply den PAT wieder widerrufen.
2. APPLY (Idempotenz-Beleg): 0015 ZWEIMAL über `POST /v1/projects/{ref}/database/query`
   (Bearer <PAT>) anwenden — beide Läufe müssen fehlerfrei durchlaufen (HTTP 201).
3. BOOTSTRAP-HELFER für die Tests: einen Master-Person-Token direkt in access_tokens
   anlegen (Dashboard/Management-API-Insert: is_person=true, usbnk='TESTMASTER',
   role='master', is_master=false, short_code='MSTR-TST1', token=…) — Master-Privileg
   für den Exchange ergibt sich aus role='master' → is_master-Claim. Alternativ einen
   bestehenden is_master-Code für die Master-Guards nutzen. Alle Testdaten am Ende löschen.
4. POSITIV (D-02, D-11, D-12, D-13, USBNK-Suche, T4-Login):
   a. `rpc/argus_exchange_person_code` mit dem Master-Person-Code → jwt; JWT dekodieren:
      `usbnk`='TESTMASTER', `argus_role`='master', is_master=true, KEIN Klarname-Claim,
      exp ≈ now+30 Tage, jti=short_code.
   b. `select * from audit_log where action='person_login'` → Eintrag mit usbnk='TESTMASTER'.
   c. Mit dem Master-JWT `rpc/argus_master_issue_person` (p_usbnk='TESTFLZ', p_role='flz',
      p_praesidium_id=null) → {short_code}; `argus_master_search_usbnk('TESTFLZ')` zeigt den
      Token (role='flz') — und `jq 'has("token")'` über jedes Suchergebnis == false.
   d. audit_log enthält `person_issue` mit usbnk=actor (TESTMASTER) und detail='role=flz target=TESTFLZ' (D-13).
   e. `rpc/argus_exchange_person_code` mit dem FLZ-Code → JWT mit is_flz=true, praesidium_id=null
      (D-12); `rpc/argus_lage` mit dem FLZ-JWT → CCP-Zähler ÜBER mehrere Präsidien.
5. NEGATIV (PFLICHT — jeweils muss VERWEIGERT/leer sein), Test-JWTs als Bearer:
   a. CR-02: `rpc/argus_exchange_code` mit dem Master-PERSON-Code → Fehler „Pro-Person-Code —
      nur für die Leitungs-Seite gültig" (KEIN is_master-JWT). Dasselbe mit dem FLZ-Person-Code.
   b. CR-02 Spiegel: `rpc/argus_exchange_person_code` mit einem normalen Gast-/Observer-/Admin-Code
      (is_person=false) → Fehler „Kein Pro-Person-Code".
   c. Privilege-Escalation: `rpc/argus_master_issue_person` mit einem Gast-/Geräte-JWT (kein Master)
      → Exception „Nur mit MasterUser-Token". Ebenso argus_master_revoke_person/argus_master_search_usbnk.
   d. FLZ-RLS: mit dem FLZ-JWT `GET /rest/v1/patients?select=id` → `[]` (praesidium_id-Claim null
      → bestehende RLS verweigert Roh-Zugriff).
   e. append-only: mit irgendeinem gültigen JWT `POST /rest/v1/audit_log` (direkter Insert) → HTTP 403/RLS-verweigert;
      `PATCH`/`DELETE` auf audit_log ebenso verweigert.
   f. Suche-Disclosure: `argus_master_search_usbnk` Ergebnis enthält KEINE token-Spalte (jq-Prüfung 4c).
6. SOFORTSPERRE (jti): den FLZ-Person-Token via `rpc/argus_master_revoke_person` (oder
   update revoked=true) sperren → das zuvor ausgestellte FLZ-JWT verliert SOFORT Wirkung
   (`argus_lage` → verweigert), weil argus_is_flz auf argus_token_active gated. audit_log
   zeigt `person_revoke` mit actor-USBNK.
7. CLEANUP: alle Test-Tokens (MSTR-TST1, FLZ-Code) und audit_log-Testzeilen restlos löschen.
  </how-to-verify>
  <resume-signal>Tippe „freigegeben", wenn PAT galt, beide Apply-Läufe sauber waren und alle Positiv-/Negativ-/jti-Tests bestanden sind — oder beschreibe die Abweichung.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: SELF-HOSTING.md nachführen (Migration 0015 + Master-Bootstrap Stufe 1)</name>
  <files>docs/SELF-HOSTING.md</files>
  <read_first>
- docs/SELF-HOSTING.md (Abschnitt 3 Dateiliste endet bei 0014; Abschnitt 5 Reihenfolge „0000 → … → 0014"; Abschnitt 8 Master-Bootstrap + Admin-Code-Insert-Beispiel ~Zeile 255)
- 05-RESEARCH.md Pitfall 5 (Master-Bootstrap: erster Stufe-1-Token via Dashboard-Insert mit is_person=true/usbnk/role='master')
  </read_first>
  <action>
Führe `docs/SELF-HOSTING.md` auf den Migrationsstand nach:
1. Abschnitt 3 (Dateiliste-Codeblock): Zeile ergänzen
   `supabase/migrations/0015_phase5_identitaeten.sql   Stufe-1-Identität (is_person/usbnk/role, Person-Exchange, Master-Issue/Revoke/Search, audit_log, CR-02-Härtung, argus_lage-FLZ)`.
2. Abschnitt 5: Reihenfolge-Hinweis „0000 → … → 0014" auf „… → 0014 → 0015" erweitern.
3. Audit-Prosa (Abschnitt 3 nach dem Codeblock): einen Satz ergänzen — 0015 nutzt
   dieselben Abhängigkeiten wie 0013/0014 (pgjwt + Vault-Secret für den Person-Exchange;
   jti-Sofortsperre über argus_token_active) — KEINE neuen Server-Abhängigkeiten.
4. Abschnitt 8 (Bootstrap) um den Stufe-1-Master-Bootstrap erweitern (05-RESEARCH Pitfall 5,
   D-13): Der erste Stufe-1-Master-Token wird einmalig direkt in access_tokens eingetragen
   (Dashboard/Management-API-Insert) — wie der bestehende Master-Bootstrap, aber zusätzlich
   `is_person = true`, `usbnk = '<USBNK des Masters>'`, `role = 'master'` (kein expires_at).
   Danach kann dieser Master alle weiteren Pro-Person-Tokens über die Leitungs-Seite ausgeben.
   Beispiel-Insert analog zum bestehenden Master-/Admin-Code-Beispiel.
KEINE Dateinamen/URLs der Leitungs-Seite in committete Texte (D-06) — nur der generische
Hinweis „docs/leitung-<zufallssuffix>.html" wie bisher.
  </action>
  <verify>
    <automated>grep -c "0015_phase5_identitaeten.sql" docs/SELF-HOSTING.md | grep -qE "^[1-9]" && echo "0015 in SELF-HOSTING"</automated>
    <automated>grep -ciE "is_person|role = 'master'|role='master'" docs/SELF-HOSTING.md | grep -qvE "^0$" && echo "Stufe-1-Bootstrap dokumentiert"</automated>
    <automated>git grep -nE 'leitung-[0-9a-f]{4,}' -- docs/SELF-HOSTING.md supabase/migrations/0015_phase5_identitaeten.sql; test $? -ne 0 && echo "D-06 OK (kein konkreter Leitungs-Dateiname)"</automated>
  </verify>
  <done>
SELF-HOSTING.md listet 0015 in Abschnitt 3 + Reihenfolge (Abschnitt 5); Audit-Prosa nennt
0015 als abhängigkeitsfrei; Abschnitt 8 beschreibt den Stufe-1-Master-Bootstrap
(is_person/usbnk/role='master'); kein konkreter Leitungs-Dateiname in committeten Texten.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gerät/Browser → REST-API (anon-Key + JWT) | Untrusted: jeder mit anon-Key kann RPCs/Tabellen ansprechen; einzige echte Grenze sind RLS-Policies + RPC-Guards. |
| Pro-Person-Token → privilegierte Rollen-Claims | Ein Person-Token trägt eine Rolle; jeder Versuch, über den FALSCHEN Exchange eine höhere Wirkung (is_master/praesidium_id) zu erlangen, ist Privilege-Escalation. |
| Nicht-Master-JWT → Master-RPCs | Issue/Revoke/Search sind Master-only; jeder Aufruf ohne Master-Token ist ein Angriff. |
| anon/JWT → audit_log direkt | Direkter Write auf das Audit-Protokoll = Tampering; nur security-definer-INSERT erlaubt. |
| Self-Hoster → Migration/Doku | Ein geleakter Leitungs-Dateiname (D-06) senkt die Auffindbarkeits-Hürde. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation | role='master'-Person-Code über argus_exchange_code → is_master-JWT | mitigate | CR-02-Guard: is_person-Abweisung in argus_exchange_code (Task 1.4); Negativtest 5a. |
| T-05-02 | Elevation | Nicht-Person-Code über argus_exchange_person_code | mitigate | „Kein Pro-Person-Code"-Guard (Task 2.1); Negativtest 5b. |
| T-05-03 | Elevation | Unprivilegiertes JWT ruft Master-Issue/Revoke/Search | mitigate | argus_is_master()-Guard zuerst in jedem RPC (Task 2.2–2.4); Negativtest 5c. |
| T-05-04 | Info Disclosure | argus_master_search_usbnk gibt token-Secret zurück | mitigate | SELECT-Liste ohne token-Spalte (Task 2.4); Negativtest 5f/jq-Prüfung. |
| T-05-05 | Tampering | Direkter INSERT/UPDATE/DELETE auf audit_log | mitigate | kein anon write-Grant; nur security-definer-INSERT; RLS SELECT=argus_is_master (Task 1.5); Negativtest 5e. |
| T-05-06 | Elevation/Spoofing | Gesperrter Person-Token wirkt weiter (Replay) | mitigate | argus_is_flz/argus_is_master gated auf argus_token_active (jti); revoke→Sofortsperre; Test Schritt 6. |
| T-05-07 | Info Disclosure | FLZ-/Person-JWT liest Patientenrohdaten | mitigate | praesidium_id-Claim null für FLZ/Master → bestehende RLS liefert []; FLZ nur via argus_lage; Negativtest 5d. |
| T-05-08 | Repudiation | Token-Ausgabe/Widerruf nicht nachvollziehbar | mitigate | Zwangs-Logs person_login/person_issue/person_revoke mit actor-USBNK in audit_log (D-13). |
| T-05-09 | Info Disclosure | Klarnamen-Speicherung in ARGUS (D-02-Bruch) | mitigate | nur usbnk-Spalte; keine name/vorname/nachname-Spalten; Verify-Gate Task 1. |
| T-05-10 | Elevation | Schema-Injection via fehlendem search_path | mitigate | set search_path in ALLEN neuen security-definer-Funktionen (Task 1/2); Verify-Gate Task 2. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installs in dieser Phase (reines SQL + Doku). Kein Package-Legitimacy-Gate nötig. |
</threat_model>

<verification>
- Beide Apply-Läufe der Management API fehlerfrei (Idempotenz, HTTP 201).
- Alle Positiv-Tests (4a–4e) erfolgreich; alle Negativtests (5a–5f) VERWEIGERT/leer.
- jti-Sofortsperre (Schritt 6) wirkt für Person-Tokens.
- `git grep -nE 'leitung-[0-9a-f]{4,}' -- ':!docs/leitung-*.html'` → 0 Treffer (D-06).
- Keine Klarnamen-Spalten in 0015 (D-02). SELF-HOSTING.md enthält 0015 + Stufe-1-Bootstrap.
</verification>

<success_criteria>
- Migration 0015 live angewendet und idempotent.
- Pro-Person-Identität end-to-end serverseitig nutzbar (Exchange/Issue/Revoke/Search).
- CR-02 in beiden Richtungen verweigert; alle Privilege-Escalation-Vektoren verweigert.
- audit_log append-only belegt; person_login/issue/revoke geloggt mit actor-USBNK.
- SELF-HOSTING.md aktuell inkl. Master-Bootstrap; D-02 + D-06 gewahrt.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

**Spalten (access_tokens):** is_person (boolean), usbnk (text), role (text)
**Spalte (praesidien):** schulungs_zwilling_id (uuid)
**Tabelle:** public.audit_log (id, at, usbnk, action, ccp_id, patient_id, praesidium_id, detail) — append-only, RLS argus_audit_log_select
**Claim-Helfer (Funktionen):** argus_is_flz(), argus_usbnk(), argus_argus_role()
**Exchange-RPC:** argus_exchange_person_code(text)
**Master-RPCs:** argus_master_issue_person(text,text,uuid,text), argus_master_revoke_person(text), argus_master_search_usbnk(text)
**Geänderte Funktionen:** argus_exchange_code (CR-02 is_person-Guard), argus_lage (FLZ-Zweig)
**JWT-Claims (neu):** usbnk, argus_role, is_flz (zusätzlich zu is_master/is_admin/admin_praesidium_id/jti)
**audit_log-Aktionen (Phase 5 schreibend):** person_login, person_issue, person_revoke (Katalog vollständig kommentiert inkl. 5.1-Aktionen)
**Migrationsdatei:** supabase/migrations/0015_phase5_identitaeten.sql
**Doku:** docs/SELF-HOSTING.md (0015 + Stufe-1-Master-Bootstrap)
</artifacts_produced>

<output>
Create `.planning/phases/05-identitaeten-audit-stufe1/05-01-SUMMARY.md` when done
</output>
