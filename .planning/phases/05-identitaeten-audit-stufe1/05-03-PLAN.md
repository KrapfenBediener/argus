---
phase: 05-identitaeten-audit-stufe1
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - docs/leitung-*.html
autonomous: false   # Browser-Roundtrip-Verifikation gegen Live-Backend (human-verify)

must_haves:
  truths:
    - "Die zuvor gesperrten Sidebar-Einträge „Identitäten" und „Audit-Log" sind für Master entsperrt und führen zu echten Sektionen (keine 🔒-Locked-Zeilen mehr)."
    - "Master kann auf der Identitäten-Sektion einen Pro-Person-Token ausgeben (USBNK + Rolle Master/FLZ/Admin) — Einzel-Ausgabe, keine Massen-Provisionierung (D-03/D-10)."
    - "Master kann per USBNK suchen und für ein Treffer-Token die Rolle ändern / sperren / entsperren (jti-Sofortsperre); es werden NIE Token-Secrets angezeigt."
    - "Die Audit-Log-Sektion zeigt die T4-audit_log-Einträge mit USBNK-Spalte, Zeit, Aktion, CCP/Patient und erlaubt Filtern nach Aktion und USBNK."
    - "Alle Dialoge nutzen confirmModal()/promptModal() — keine nativen alert/confirm/prompt (CLAUDE.md)."
    - "Neue UI-Elemente folgen der bestehenden Optik (banner/navbtn/card/pill/Modals; ARGUS-Design IBM Plex, Schiefer #1f2530)."
  artifacts:
    - path: "docs/leitung-*.html"
      provides: "Identitäten-Sektion (Ausgabe/USBNK-Suche/Rollenverwaltung/Sperre) + Audit-Log-Sektion (T4-Anzeige mit USBNK-Filter); Phase-7-Sperrzone entfernt"
      contains: "argus_master_search_usbnk"
  key_links:
    - from: "secIdentitaeten (Ausgabe-Button)"
      to: "argus_master_issue_person"
      via: "cl.rpc-Aufruf mit usbnk+role+praesidium"
      pattern: "argus_master_issue_person"
    - from: "secIdentitaeten (Suche)"
      to: "argus_master_search_usbnk"
      via: "cl.rpc-Aufruf mit usbnk-Fragment"
      pattern: "argus_master_search_usbnk"
    - from: "secAudit"
      to: "public.audit_log"
      via: "cl.from('audit_log').select(...)"
      pattern: "audit_log"
---

<objective>
Die beiden seit Phase 4.9 sichtbar gesperrten Sidebar-Einträge „Identitäten" und
„Audit-Log" der Leitungs-Seite (`docs/leitung-*.html`) entsperren und mit echten
Sektionen füllen — exakt nach dem bestehenden Sektions-Muster der Seite
(SECTIONS_MASTER-Array, sideNav, render/loadSection-Dispatch, sec*()/load*()-Paar,
data-action-Event-Delegation, cl.rpc/cl.from, confirmModal/promptModal).

„Identitäten" = Master-Verwaltung der Stufe-1-Identitäten: Pro-Person-Token einzeln
ausgeben (USBNK + Rolle), per USBNK suchen, Rolle ändern, sperren/entsperren
(jti-Sofortsperre). „Audit-Log" = Anzeige des personenscharfen T4-audit_log mit
USBNK-Spalte/-Filter und Aktions-Filter.

Purpose: Die RPCs/Tabelle existieren serverseitig (Plan 01/02); diese UI macht die
Stufe-1-Identitätsverwaltung für den Master bedienbar. Die RLS/RPC-Guards bleiben
die echte Sicherheitsgrenze — die UI ist Komfort (rollen-adaptiv: nur Master sieht
diese Sektionen, Admin nicht).

Output: erweiterte `docs/leitung-*.html` (zwei neue Sektionen, Sperrzone entfernt),
per Browser-Roundtrip gegen das Live-Backend verifiziert. KEIN App-Release
(index.html/sw.js unangetastet); die Leitungs-Seite ist nicht app-versioniert →
kein Version-Bump/WHATS_NEW/Trainings-Drehbuch in dieser Phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-identitaeten-audit-stufe1/05-CONTEXT.md
@.planning/phases/05-identitaeten-audit-stufe1/05-VALIDATION.md
@.planning/phases/05-identitaeten-audit-stufe1/05-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sektion „Identitäten" — Token-Ausgabe, USBNK-Suche, Rollenverwaltung, Sperre</name>
  <files>docs/leitung-*.html</files>
  <read_first>
- docs/leitung-*.html — diese Stellen genau lesen (Glob; KEINE konkreten Dateinamen committen, D-06):
  - `var SECTIONS_MASTER=[...]` und die Phase-7-Sperrzone in `sideNav()` (die `lockedsec`/`locked`-Zeilen „Identitäten"/„Audit-Log") — hier entsperren.
  - `render()` und `loadSection()` (der section-Dispatch) — neue Zweige ergänzen.
  - Eine bestehende Master-Sektion als VORLAGE: `secZugaenge()`/`loadZugaenge()` (Karten/Buttons, _praes/_tokens-State, mklink/mkadmin/togglerevoke-data-actions, genShortCode) und `secAdminZugang()` (Code-Ausgabe-Flow mit promptModal).
  - Den zentralen data-action-Event-Delegations-Handler (wo `data-action`-Werte wie 'mkadmin','togglerevoke','adminrevoke' behandelt werden) + den supabase-Client-Getter (cl/sb mit JWT-Bearer).
- 05-01-PLAN.md <artifacts_produced> (genaue RPC-Signaturen: argus_master_issue_person(usbnk,role,praesidium,label), argus_master_revoke_person(short_code), argus_master_search_usbnk(usbnk))
- CLAUDE.md (confirmModal/promptModal Pflicht; ARGUS-Design; D-06)
  </read_first>
  <action>
Erweitere die Leitungs-Seite um die Master-Sektion „Identitäten", konsequent im
Stil von secZugaenge/secAdminZugang (gleiche Komponenten card/btn/pill/mh/hint;
gleiche cl.rpc-Mechanik; gleiche Toast/Modal-Konventionen). KEIN neues Design-System,
KEINE nativen Dialoge.

1. Sperrzone entsperren: In `sideNav()` die `lockedsec`-Block-Zeilen für „Identitäten"
   und „Audit-Log" ENTFERNEN. Stattdessen die beiden Einträge als echte Bereiche in
   `SECTIONS_MASTER` ergänzen (z. B. nach 'protokoll'): `{id:'identitaeten', label:'Identitäten'}`
   und `{id:'audit', label:'Audit-Log'}` (Audit-Sektion baut Task 2). Beide NUR für
   role==='master' (Admin sieht sie nicht — die SECTIONS_ADMIN-Liste bleibt unverändert).

2. render()/loadSection() um die Zweige `identitaeten` (→ secIdentitaeten()/loadIdentitaeten())
   und `audit` (→ secAudit()/loadAudit(), Task 2) ergänzen.

3. secIdentitaeten() rendern:
   - Block „Pro-Person-Token ausgeben" (Einzel-Ausgabe, D-03/D-10 — KEINE Batch/CSV-Eingabe):
     ein kleines Formular mit USBNK-Eingabefeld, Rollen-Auswahl (select: Master / FLZ /
     Admin — „Normal" weglassen oder deaktiviert mit Hinweis, da vor SSO inaktiv, D-04),
     für Rolle „Admin" ein Präsidiums-Select (die _praes-Liste wird bereits in der
     Zugänge-Sektion geladen; wiederverwenden oder hier laden), optional Label, Button
     „Token ausgeben" (data-action="mkperson"). Hinweis-Text: „Einzel-Ausgabe für
     privilegierte Rollen; normale Feldnutzer bleiben bis SSO Stufe 0."
   - Block „USBNK-Suche": Eingabefeld + Button (data-action="usbnksearch"), Ergebnis-Liste
     je Treffer: USBNK · Rolle (als pill) · short_code · Status (aktiv/gesperrt) ·
     Präsidium. Pro Treffer Aktionen: „Rolle ändern" (data-action="personrole"),
     „Sperren"/„Entsperren" (data-action="personrevoke"/"personunrevoke").
     NIEMALS ein Token-Secret anzeigen (die Suche liefert ohnehin keins).
   - Render-State analog _praes/_tokens: z. B. `_persResults`, `_persErr`.

4. Aktions-Handler im data-action-Delegations-Handler ergänzen (Muster wie mkadmin/adminrevoke):
   - 'mkperson': USBNK + Rolle (+ ggf. Präsidium) aus den Feldern lesen; bei leerem USBNK
     Toast-Fehler. `await cl.rpc('argus_master_issue_person', {p_usbnk, p_role, p_praesidium_id,
     p_label})`. Bei Erfolg den short_code via promptModal anzeigen (zum Kopieren/Weitergeben),
     Toast „Token ausgegeben (protokolliert)". Bei error den RPC-Fehlertext im Toast zeigen.
   - 'usbnksearch': `await cl.rpc('argus_master_search_usbnk', {p_usbnk})` → _persResults setzen,
     render().
   - 'personrole': Ziel-short_code aus data-sc; neue Rolle via promptModal/Select abfragen
     (master/flz/admin/normal); umgesetzt durch Neu-Ausgabe ist NICHT erwünscht — falls die
     Server-Seite (Plan 01) keinen dedizierten Rollen-Change-RPC hat, wird der Rollenwechsel
     als „alten Token sperren + neuen ausgeben" abgebildet ODER (falls vorhanden) per
     update über einen Master-RPC. WICHTIG: Diese UI ruft NUR vorhandene RPCs aus 05-01-PLAN.md
     auf — kein direkter access_tokens-Update aus dem Client (RLS master-only; und die
     person_role_change-Erfassung soll serverseitig geloggt werden). Falls 05-01 keinen
     Rollen-Change-RPC liefert, ist „Rolle ändern" = revoke (alt) + issue (neu, gleiche USBNK)
     mit Bestätigung via confirmModal; der person_revoke+person_issue-Doppeleintrag im
     audit_log dokumentiert den Wechsel.
   - 'personrevoke': confirmModal „USBNK-Token sperren? Wirkt sofort." →
     `await cl.rpc('argus_master_revoke_person', {p_short_code})`; Toast „Gesperrt (sofort wirksam)".
   - 'personunrevoke': falls ein Entsperren gewünscht ist und kein Unrevoke-RPC existiert,
     den Button weglassen (revoke ist der dokumentierte Kill-Switch, D-11); andernfalls den
     vorhandenen Mechanismus nutzen. (Im Zweifel: nur „Sperren" anbieten, konsistent zu D-11
     „Lebenszyklus = Widerruf".)
   Nach jeder mutierenden Aktion die USBNK-Suche neu laden (loadIdentitaeten bzw. erneute Suche).

5. Texte deutsch, knapp, einsatztauglich; keine Klarnamen-Beschriftung (immer „USBNK", D-02).
   Bestehende esc()/toast()/promptModal()/confirmModal()-Helfer verwenden.
  </action>
  <verify>
    <automated>grep -cE "argus_master_issue_person|argus_master_search_usbnk|argus_master_revoke_person" docs/leitung-*.html | grep -qE "^[3-9]|^[1-9][0-9]" && echo "Identitaeten-RPCs verdrahtet"</automated>
    <automated>grep -cE "secIdentitaeten|loadIdentitaeten|identitaeten" docs/leitung-*.html | grep -qE "^[3-9]" && echo "Sektion+Dispatch vorhanden"</automated>
    <automated>grep -cE "lockedsec|>Identitäten<.*🔒|locked.*Identit" docs/leitung-*.html | grep -qE "^0$" && echo "Sperrzone entfernt"</automated>
    <automated>grep -ciE "alert\(|confirm\(|prompt\(" docs/leitung-*.html | grep -qE "^0$" && echo "keine nativen Dialoge"</automated>
  </verify>
  <done>
„Identitäten" ist entsperrt und in SECTIONS_MASTER (nur Master); secIdentitaeten zeigt
Einzel-Ausgabe (USBNK+Rolle), USBNK-Suche (ohne Token-Secrets), Rollenwechsel und Sperre;
data-actions mkperson/usbnksearch/personrole/personrevoke rufen die Plan-01-RPCs auf;
nur confirmModal/promptModal; ARGUS-Optik beibehalten.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sektion „Audit-Log" — T4-Anzeige mit USBNK-Spalte + Aktions-/USBNK-Filter</name>
  <files>docs/leitung-*.html</files>
  <read_first>
- docs/leitung-*.html — VORLAGE: `secProtokoll()`/`loadProtokoll()` (das bestehende Protokoll/governance_log-Muster mit Filterleiste GLOG_ACTIONS, cl.from('purge_log').select(...).order(...).limit(...), Karten-Rendering, fmtMs/fmtIso, actionLabel()) — die Audit-Sektion ist das direkte Pendant für audit_log.
- 05-01-PLAN.md <artifacts_produced> (audit_log-Spalten: at, usbnk, action, ccp_id, patient_id, praesidium_id, detail; RLS argus_is_master → Master darf SELECT) + audit_log-Aktions-Katalog (person_login/person_issue/person_revoke/… + 5.1-Aktionen).
  </read_first>
  <action>
Erweitere die Leitungs-Seite um die Master-Sektion „Audit-Log", als Pendant zu
secProtokoll/loadProtokoll, aber gegen die `audit_log`-Tabelle (Plan 01). KEIN neues
Design-System; gleiche Filter-/Karten-Mechanik wie das governance_log-Protokoll.

1. loadAudit(): `await cl.from('audit_log').select('at,usbnk,action,ccp_id,patient_id,
   praesidium_id,detail').order('at',{ascending:false}).limit(200)` → _audit/_auditErr setzen,
   bei state.section==='audit' render(). (RLS lässt nur Master lesen; ein Nicht-Master-Client
   bekäme leer/Fehler — das ist korrekt.)

2. secAudit() rendern:
   - Kopf „<h2>Audit-Log</h2>" + Reload-Button (data-action="auditreload").
   - Kurzer Hinweis: personenscharfes T4-Protokoll (USBNK/Zeit/Aktion/CCP/Patient),
     append-only, 12-Monats-Frist (§ 73 PolG BW).
   - Filterleiste analog GLOG_ACTIONS: Aktions-Select (AUDIT_ACTIONS = person_login,
     person_issue, person_revoke, person_role_change, governance_abruf sowie die
     5.1-Feldaktionen patient_*/ccp_* — mit deutschen Labels) + USBNK-Text-Filter
     (data-action="auditfilter", data-field="usbnk"/"action") + Filter-Reset.
   - Tabelle/Karten je Eintrag: Zeit (fmtMs) · USBNK (fett; „—" bei null) · Aktion
     (auditActionLabel()) · Ziel (CCP/Patient gekürzt, falls vorhanden) · detail (falls vorhanden).
   - Leerzustand/Lade-/Fehler-Zustände wie secProtokoll.

3. auditActionLabel(a) ergänzen (deutsche Labels): person_login→„Anmeldung (Pro-Person)",
   person_issue→„Token ausgegeben", person_revoke→„Token gesperrt", person_role_change→
   „Rolle geändert", governance_abruf→„Governance-Abruf", patient_create→„Patient angelegt",
   patient_cat_change→„Kategorie gewechselt", patient_ready→„Transportfertig", patient_checkout
   →„Ausgecheckt", patient_photo→„Foto erfasst", ccp_open→„CCP eröffnet", ccp_close→
   „CCP geschlossen", ccp_merge→„CCPs zusammengeführt"; Fallback esc(a).

4. State `_audit`, `_auditErr`, `_auditFilter={action:'alle',usbnk:''}` und die
   data-actions auditreload/auditfilter/auditfilterreset im Delegations-Handler ergänzen
   (Muster glogfilter/glogfilterreset/protoreload). Gefilterte Liste client-seitig
   (action-Match + usbnk-ILIKE-artiges includes), wie bei _glogFilter.
  </action>
  <verify>
    <automated>grep -cE "secAudit|loadAudit|_auditFilter|audit_log" docs/leitung-*.html | grep -qE "^[4-9]|^[1-9][0-9]" && echo "Audit-Sektion vorhanden"</automated>
    <automated>grep -cE "from\('audit_log'\)|from\(\"audit_log\"\)" docs/leitung-*.html | grep -qvE "^0$" && echo "audit_log SELECT verdrahtet"</automated>
    <automated>grep -cE "auditActionLabel|person_login|person_issue" docs/leitung-*.html | grep -qE "^[2-9]" && echo "USBNK-Aktions-Labels vorhanden"</automated>
  </verify>
  <done>
„Audit-Log" zeigt audit_log (cl.from('audit_log')) mit USBNK-Spalte, Zeit, Aktion (deutsche
Labels), Ziel und detail; Filter nach Aktion und USBNK + Reset; Lade-/Leer-/Fehlerzustände;
data-actions auditreload/auditfilter im Handler verdrahtet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Die Leitungs-Seite (`docs/leitung-*.html`) hat zwei neue Master-Sektionen
„Identitäten" und „Audit-Log"; die Phase-7-Sperrzone ist entfernt. Beide setzen auf
den Plan-01-RPCs/der audit_log-Tabelle auf (live seit Plan 01/02). Verifikation per
Browser-Roundtrip gegen das Live-/Schulungs-Backend (Muster 04.14, 05-VALIDATION.md
„Manual-Only Verifications"). Lokal über statischen Server öffnen oder die deployte
HTTPS-Adresse; mit einem Master-(Person-)Token anmelden.
  </what-built>
  <how-to-verify>
1. Als Master anmelden (Master-Token bzw. Master-Person-Token). Prüfen: in der Sidebar
   erscheinen „Identitäten" und „Audit-Log" als echte Bereiche; KEINE 🔒-Locked-Zeilen mehr.
   (Als Admin angemeldet: beide Bereiche tauchen NICHT auf.)
2. IDENTITÄTEN — Ausgabe: USBNK eingeben (z. B. „TESTFLZ-UI"), Rolle „FLZ" wählen,
   „Token ausgeben" → short_code erscheint im Modal. (Admin-Rolle wählen → Präsidiums-Select
   wird verlangt.) Bestätigen: kein Token-SECRET wird angezeigt, nur der short_code.
3. IDENTITÄTEN — Suche: nach „TESTFLZ-UI" suchen → Treffer mit USBNK, Rolle (pill),
   short_code, Status „aktiv", Präsidium. Prüfen: KEINE token-Spalte/kein Secret sichtbar.
4. IDENTITÄTEN — Sperre: beim Treffer „Sperren" → confirmModal → bestätigen → Status „gesperrt";
   ein zuvor mit diesem Token gelöstes JWT verliert sofort Wirkung (jti-Sofortsperre, Plan 01).
5. IDENTITÄTEN — Rollenwechsel: „Rolle ändern" testen (gemäß der in Task 1 gewählten
   Umsetzung: dedizierter RPC ODER revoke+issue) → die neue Rolle ist in der Suche sichtbar;
   der Vorgang erscheint im Audit-Log.
6. AUDIT-LOG: Bereich öffnen → die in Schritt 2/4/5 ausgelösten Aktionen erscheinen mit
   USBNK (actor-USBNK des Masters), Zeit, deutscher Aktions-Bezeichnung (Token ausgegeben /
   Token gesperrt). Filter nach Aktion und nach USBNK testen → Liste filtert korrekt;
   Filter-Reset funktioniert.
7. DIALOGE: alle Bestätigungen/Eingaben laufen über In-App-Modals (confirmModal/promptModal) —
   kein nativer Browser-Dialog (PWA-Vollbild-tauglich).
8. OPTIK: neue Elemente fügen sich in die bestehende ARGUS-Optik ein (IBM Plex, helle Karten,
   Schiefer-Chrome) — kein Stilbruch.
9. CLEANUP: die im Test angelegten Identitäten-Tokens wieder sperren/löschen (Backend),
   Test-audit_log-Zeilen ggf. bereinigen.
  </how-to-verify>
  <resume-signal>Tippe „freigegeben", wenn beide Sektionen sichtbar sind, Ausgabe/Suche/Sperre/Rollenwechsel funktionieren, das Audit-Log die Aktionen mit USBNK + Filter zeigt, nur In-App-Modals verwendet werden und die Optik stimmt — oder beschreibe die Abweichung.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-UI (Leitungs-Seite) → REST/RPC mit Master-JWT | Die UI ist Komfort; RLS + RPC-Guards (Plan 01) sind die echte Grenze. Ein manipuliertes Frontend kann nur das, was die RPCs/RLS erlauben. |
| Angezeigte Suchergebnisse → Bildschirm | Token-Secrets dürfen nie in die UI gelangen (Information Disclosure). |
| committete HTML/Texte → öffentliches Repo | Ein geleakter Leitungs-Dateiname (D-06) senkt die Auffindbarkeits-Hürde. |
| Master-Eingaben (USBNK/Label) → DOM-Rendering | Unescaptes Rendering = XSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0503-01 | Info Disclosure | Token-Secret in USBNK-Suchergebnis sichtbar | mitigate | argus_master_search_usbnk liefert kein token (Plan 01); UI rendert nur short_code/usbnk/role/status; Checkpoint-Schritt 3. |
| T-0503-02 | Elevation | Nicht-Master sieht/nutzt Identitäten/Audit-Sektion | mitigate | Sektionen nur in SECTIONS_MASTER (role==='master'); RLS audit_log=argus_is_master + Master-Guards in den RPCs (Plan 01) als echte Grenze; Checkpoint-Schritt 1. |
| T-0503-03 | Tampering/XSS | Unescaptes Rendering von USBNK/detail/label | mitigate | bestehende esc()-Helfer auf alle dynamischen Werte anwenden (Projektkonvention). |
| T-0503-04 | Info Disclosure | Geleakter Leitungs-Dateiname in committeten Texten | mitigate | nur docs/leitung-*.html-Glob in Plänen/Doku; D-06-Gate vor Commit. |
| T-0503-05 | Repudiation | Master-Identitäts-Aktionen nicht nachvollziehbar | mitigate | Ausgabe/Widerruf werden serverseitig in audit_log geloggt (Plan 01); im Audit-Log sichtbar (Schritt 6). |
| T-0503-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installs (Single-File-HTML/JS-Erweiterung, CDN-Libs bereits vorhanden). Kein Package-Legitimacy-Gate nötig. |
</threat_model>

<verification>
- Beide Sektionen für Master sichtbar; Sperrzone entfernt; Admin sieht sie nicht.
- Ausgabe/Suche/Sperre/Rollenwechsel funktionieren gegen das Live-Backend; keine Token-Secrets in der UI.
- Audit-Log zeigt die Aktionen mit USBNK + funktionierenden Filtern.
- Nur confirmModal/promptModal (keine nativen Dialoge); ARGUS-Optik gewahrt.
- `git grep -nE 'leitung-[0-9a-f]{4,}' -- ':!docs/leitung-*.html'` → 0 Treffer (D-06).
- index.html/sw.js unverändert (kein App-Release).
</verification>

<success_criteria>
- „Identitäten" + „Audit-Log" entsperrt und funktional (Master-only).
- Stufe-1-Identitätsverwaltung end-to-end über die Leitungs-Seite bedienbar.
- T4-Audit personenscharf (USBNK) sicht- und filterbar.
- Keine nativen Dialoge; bestehende Optik; D-02/D-06 gewahrt; kein App-Release.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

**Leitungs-Seite Sektionen:** „Identitäten" (entsperrt), „Audit-Log" (entsperrt) — Phase-7-Sperrzone entfernt
**Render-Funktionen:** secIdentitaeten(), loadIdentitaeten(), secAudit(), loadAudit(), auditActionLabel()
**SECTIONS_MASTER-Einträge:** {id:'identitaeten'}, {id:'audit'}
**data-actions (neu):** mkperson, usbnksearch, personrole, personrevoke, (ggf. personunrevoke), auditreload, auditfilter, auditfilterreset
**State-Variablen:** _persResults/_persErr, _audit/_auditErr/_auditFilter
**Konsumierte Server-Artefakte (aus Plan 01/02):** argus_master_issue_person, argus_master_revoke_person, argus_master_search_usbnk, audit_log (SELECT via RLS)
**Datei:** docs/leitung-*.html (kein App-Release; index.html/sw.js unverändert)
</artifacts_produced>

<output>
Create `.planning/phases/05-identitaeten-audit-stufe1/05-03-SUMMARY.md` when done
</output>
